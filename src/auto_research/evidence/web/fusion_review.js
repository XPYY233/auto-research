(() => {
  "use strict";
  const VIEWS = Object.freeze(["paper", "search", "personal", "package", "settings"]);
  const VIEW_LABELS = Object.freeze({paper:"文献",search:"搜索",personal:"实验",package:"资料包",settings:"设置"});
  const STORAGE_KEY = "auto-research-fusion-appearance-v1";
  const ROUTES = Object.freeze({papers:"/api/search-papers",evidence:"/api/search-v2",visualAsset:"/api/visual-assets",federatedSearch:"/api/desktop/federated-search",federatedEvidence:"/api/desktop/federated-evidence",personalTable:"/api/desktop/personal-experiments/table",personalSearchStatus:"/api/desktop/personal-imports/search-status",personalSearchRefresh:"/api/desktop/personal-imports/search-refresh",tableStructures:"/api/desktop/table-structures",tableStructureReviews:"/api/desktop/table-structures/reviews",reviewQueue:"/api/desktop/review-queue",reviewActions:"/api/desktop/review-queue/actions",searchIndexRecovery:"/api/desktop/search-index/refresh",uiMode:"/api/ui-mode",settings:"/api/desktop/settings",preferences:"/api/desktop/settings/preferences",aiProviders:"/api/desktop/ai/providers",aiSettings:"/api/desktop/ai/settings",aiCustom:"/api/desktop/ai/custom-provider",librarianHistory:"/api/desktop/librarian-history",researchMemories:"/api/desktop/research-memories",evidenceChatHistory:"/api/desktop/evidence-chat-history",upload:"/api/uploads/pdf",consent:"/api/desktop/ai/consents",personalPreview:"/api/desktop/personal-imports/preview",officialPackages:"/api/desktop/evidence-packages",packageCenter:"/api/desktop/package-center",packageReceipts:"/api/desktop/package-center/receipts",packageHistory:"/api/desktop/package-center/history",literatureTaskDirectory:"/api/desktop/ai/actions/literature_extraction/task-directory"});
  const PERSONAL_IMPORT_ROWS_PATH=/^\/api\/desktop\/personal-imports\/(personal_import_[A-Za-z0-9_-]{16,96})\/sheets\/(0|[1-9][0-9]{0,2})\/rows$/;
  const AI_SCOPES = new Set(["librarian","literature_extraction","personal_suggestion","selected_evidence_chat"]);
  const AI_PROVIDERS = new Set(["deepseek","openai","custom"]);
  const AI_CALL_LIMITS = Object.freeze({librarian:8,literature_extraction:512,personal_suggestion:1,selected_evidence_chat:2});
  const AI_VERIFICATION_CALL_LIMITS = Object.freeze({librarian:8,literature_extraction:8,personal_suggestion:2,selected_evidence_chat:2});
  const AI_SCOPE_LABELS = Object.freeze({literature_extraction:"文献自动提取与核验",librarian:"图书管理员",selected_evidence_chat:"选中证据解释",personal_suggestion:"实验表格 AI 预填"});
  const AI_REASON_COPY = Object.freeze({ai_credential_required:"尚未保存当前提供商的 API 密钥。",ai_connection_verification_required:"密钥已保存，需要先验证连接。",ai_connection_ready:"提供商连接已验证。",ai_business_verification_required:"此项业务需要单独验证模型能力。",ai_business_ready:"此项业务已可以安全运行。",harness_runtime_ready:"Harness 运行环境可用。",harness_dependency_mismatch:"Harness 版本不匹配，请修复桌面运行环境。",harness_runtime_unavailable:"Harness 当前不可用，请修复桌面运行环境。",harness_runtime_invalid:"Harness 状态无效，请修复桌面运行环境。"});
  const AI_ERROR_CODE_COPY = Object.freeze({ai_provider_response_invalid:"AI 提供商返回了空内容。",ai_provider_unavailable:"AI 提供商当前不可用。",ai_runtime_verification_failed:"连接或能力验证未通过。",harness_budget_exhausted:"本次授权的 AI 调用预算已用尽。",harness_dependency_mismatch:"AI 执行环境版本不兼容。",harness_runtime_unavailable:"AI 执行环境当前不可用。",harness_runtime_failed:"AI 执行环境运行失败。",harness_provider_unavailable:"AI 提供商暂时无法完成这次请求，可稍后重试；精确检索仍可使用。",harness_provider_response_invalid:"AI 提供商返回内容未通过结构校验；请先验证连接或更换业务模型。",harness_output_invalid:"AI 回答未通过引用与结构校验。",business_action_result_invalid:"AI 返回结果未通过安全校验。",literature_pdf_too_large:"PDF 文件大小超出当前完整提取限制。",literature_pdf_page_limit_exceeded:"PDF 页数超出当前完整提取限制；系统没有截断论文。"});
  const AI_ERROR_ACTION_COPY = Object.freeze({save_credential:"请重新保存 API 密钥。",retry_connection:"请检查网络后重新验证。",verify_connection:"请先重新验证连接，再验证业务能力。",select_supported_model:"请选择受支持的模型后重试。",select_supported_provider:"请选择受支持的 AI 提供商后重试。",check_provider_configuration:"请核对提供商账户、余额和模型设置后重试。",retry_same_request:"请确认状态后重试同一操作。",retry_harness_action:"请保留当前问题并重新发送；系统不会复用失败结果。",refine_librarian_question:"请缩小问题范围或补充材料、条件与物理量后重试。",repair_harness_runtime:"请修复 AI 执行环境后重试。",reimport_pdf:"请重新导入可读取的 PDF。",replace_valid_pdf:"请更换真实、未加密且未损坏的 PDF。",replace_searchable_pdf:"请更换带可检索文字层的 PDF。",select_supported_pdf:"请选择不超过 64 页且文件不超过 128 MB 的可检索 PDF。",restart_from_current_pdf:"原始 PDF 已变化，请重新选择并新建提取任务。",confirm_rescan:"该论文已有记录，请确认重新扫描后再开始。",review_extraction_candidates:"请查看冲突与低置信候选，完成审核后再发布。",repair_workspace:"请重新验证本机文献工作区。",wait_for_task:"当前阶段仍在运行，请等待任务状态更新。",restart_extraction:"任务已过期，请从当前论文重新开始。",retry_after_queue:"任务队列繁忙，请稍后重试；已完成阶段不会重复收费。",retry_current_stage:"请按当前阶段提示重试；已有数据不受影响。"});
  const AI_APPLICATION_PHASES = Object.freeze({understand:"理解问题",retrieve:"检索证据",verify:"核验引用",organize:"组织回答",complete:"完成"});
  const AI_ACTIVITY_CODES = Object.freeze({execution_queued:"understand",execution_started:"understand",provider_acquired:"understand",harness_dependencies_verified:"understand",harness_composition_verified:"understand",harness_runtime_verified:"understand",provider_request_started:"understand",provider_response_received:"understand",harness_tool_started:"retrieve",harness_tool_completed:"retrieve",result_validating:"verify",execution_completed:"complete",execution_failed:"complete"});
  const AI_RETRIEVAL_TOOLS = new Set(["exact_search","federated_search","evidence_detail","evidence_metadata","recommend_papers"]),AI_VERIFY_TOOLS=new Set(["source_locator","source_view","citation_verify"]);
  const CSRF_HEADER = "X-Auto-Research-CSRF";
  const state = {view:"paper",settingsSection:"appearance",row:0,column:0,drawer:null,focusReturn:null,paletteOpen:false,paletteFocusReturn:null,theme:"system",density:"comfortable",settingsRevision:null,csrfToken:"",release:null,aiContext:null,aiCatalog:null,aiSettings:null,aiCredential:null,aiCustom:null,aiBusy:false,papers:[],paperCatalogQuery:"",paper:null,evidence:[],searchResults:[],searchTotal:0,searchHasMore:false,searchPage:0,searchOffset:0,selectedEvidence:null,evidenceIndex:0,selectionByView:{paper:null,search:null,personal:null,package:null,settings:null},inspectorState:{paper:null,search:null,personal:null,package:null,settings:null},evidenceTypes:new Set(["item","finding","table","figure"]),evidenceDetail:null,evidenceDetailOpen:false,evidenceDetailRequest:0,evidenceDetailReturn:null,evidenceDetailPDF:false,sourceHighlightRequest:0,sourceHighlight:null,sourceHighlightTimer:null,personalTablePage:null,reviewQueue:{items:[],total:0,loaded:false,loading:false,request:0,mode:false,pending:new Set(),retry:new Map()},reviewDetail:null,reviewDetailTabId:"",evidenceChat:{identity:"",messages:[],request:0,busy:false,threads:new Map(),requests:new Map(),pending:new Set()},literatureStatus:"loading",literatureRequest:0,literatureReceipt:null,literatureUploadAction:0,literatureTask:{jobId:"",status:"idle",stage:"prepare",job:null,watch:null,directory:null,resume:null,paperKey:""},searchRequest:0,searchMode:"precise",searchSource:"workspace",searchQuery:"",searchExportReady:false,literatureAction:0,librarianAction:0,librarianBusy:false,librarianConversationId:null,librarianMessages:[],librarianResearch:null,librarianResults:[],librarianArticles:[],librarianResultsOpen:false,librarianSessions:[],librarianHistoryQuery:"",librarianHistoryBackend:"loading",librarianHistorySave:Promise.resolve(),researchMemories:[],researchMemoryRevision:0,researchMemoryBackend:"loading",researchMemoryCandidate:null,researchMemoryMutationBusy:false,researchMemoryMessage:"",personalAction:0,personalPageRequest:0,personalSearchRequest:0,personalSearchStatus:null,personalSearchRefreshing:false,personalPreview:null,personalPreviewPage:null,personalStatus:null,personalSuggestion:null,personalSheetIndex:0,personalSeriesCounter:0,paneContainerWidth:0,paneLayoutMode:"wide",package:{loaded:false,loading:false,official:null,center:null,jobs:new Map(),receipts:[],receiptsRevision:null,receiptsLoaded:false,receiptsLoading:false,receiptsStatus:"idle",lastOfficialResult:null,literaturePlan:null,personalPlan:null,datasetPlan:null,datasetReceipt:null,userSelection:null,userInspection:null}};
  Object.assign(state.evidenceChat,{historyRevision:0,historyBackend:"loading",historyThreads:new Map(),historySave:Promise.resolve(),historyMessage:"正在读取本机加密对话…"});
  const DocumentTabStore=globalThis.AutoResearchDocumentTabs?.DocumentTabStore;
  const documentTabs=typeof DocumentTabStore==="function"?new DocumentTabStore():null;
  const tableStructureRequests=new Map();
  const PaneLayoutController=globalThis.AutoResearchPaneLayout?.PaneLayoutController;
  const paneController=typeof PaneLayoutController==="function"?new PaneLayoutController():null;
  const workspaceLayout=globalThis.AutoResearchWorkspaceLayout;
  const FusionPdfController=globalThis.AutoResearchFusionPDF?.FusionPdfController;
  const pdfController=typeof FusionPdfController==="function"?new FusionPdfController():null;
  const AIExperienceController=globalThis.AutoResearchAIExperience?.AIExperienceController;
  const aiExperience=typeof AIExperienceController==="function"?new AIExperienceController():null;
  const aiActivityAggregates=new Map();
  const q = selector => document.querySelector(selector);
  const qa = selector => [...document.querySelectorAll(selector)];
  const esc = value => String(value ?? "").replace(/[&<>"']/g, character => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[character]));
  const clampPane = (value,min,max) => {const numeric=Number(value);return Number.isFinite(numeric)?Math.min(max,Math.max(min,numeric)):min;};
  const paneLayout = paneController?.state||{context:{collapsed:false,size:244,lastExpanded:244},inspector:{collapsed:false,size:340,lastExpanded:340},editors:{primaryCollapsed:false,secondaryCollapsed:false,split:.58,lastSplit:.58}};
  const workspaceReveal={surface:null,taskId:""};
  function aiProgress(scope,stage,state="running",label="",detail=""){if(stage==="prepare"&&state==="running"){aiActivityAggregates.delete(scope);aiExperience?.begin(scope);}const safeDetail=state==="error"?"请查看对话中的原因与处理建议":detail;return aiExperience?.update(scope,{stage,state,label,detail:safeDetail})??false;}
  function renderAIActivitySummary(scope,value){const host=q(aiExperience?.definition?.(scope)?.host||""),list=host?.querySelector?.("[data-ai-activity]");if(!list)return true;list.replaceChildren();for(const phase of Object.keys(AI_APPLICATION_PHASES)){const item=value.phases.get(phase);if(!item)continue;const node=document.createElement("li"),mark=document.createElement("i"),body=document.createElement("span"),title=document.createElement("strong"),note=document.createElement("small");node.dataset.aiActivityPhase=phase;mark.setAttribute("aria-hidden","true");title.textContent=`${AI_APPLICATION_PHASES[phase]}${item.state==="failed"?" · 未完成":item.state==="done"?" · 已完成":""}`;const stats=[];if(item.callIndex&&item.callLimit)stats.push(`模型调用 ${item.callIndex}/${item.callLimit}`);if(item.toolCount)stats.push(`只读工具 ${item.toolCount} 次`);if(item.latest)stats.push(item.latest);if(Number.isFinite(item.elapsedMs))stats.push(`${Math.max(0,Math.round(item.elapsedMs/1000))} 秒`);note.textContent=stats.join(" · ");body.append(title);if(note.textContent)body.append(note);node.append(mark,body);list.append(node);}list.scrollTop=list.scrollHeight;return true;}
  function projectAIActivity(scope,raw={}){if(!AI_SCOPES.has(scope)||raw?.schema_version!=="ai-activity-event-v1"||typeof raw.job_id!=="string"||!raw.job_id||!Number.isSafeInteger(Number(raw.sequence))||Number(raw.sequence)<1)return false;const code=String(raw.code||""),basePhase=AI_ACTIVITY_CODES[code];if(!basePhase)return false;let value=aiActivityAggregates.get(scope);if(!value||value.jobId!==raw.job_id)value={jobId:raw.job_id,seen:new Set(),phases:new Map(),toolsSeen:false};if(value.seen.has(Number(raw.sequence)))return true;value.seen.add(Number(raw.sequence));const tool=typeof raw.tool==="string"?raw.tool:"";let phase=basePhase;if(code.startsWith("harness_tool_")){phase=AI_VERIFY_TOOLS.has(tool)?"verify":"retrieve";value.toolsSeen=true;}else if(["provider_request_started","provider_response_received"].includes(code)&&value.toolsSeen)phase="organize";else if(code==="execution_completed")phase="complete";else if(code==="execution_failed")phase=value.phases.has("organize")?"organize":value.phases.has("verify")?"verify":value.phases.has("retrieve")?"retrieve":"understand";const current=value.phases.get(phase)||{callIndex:0,callLimit:0,toolCount:0,latest:"",elapsedMs:0,state:"running"},callIndex=Number(raw.call_index),callLimit=Number(raw.call_limit);if(Number.isSafeInteger(callIndex)&&Number.isSafeInteger(callLimit)&&callIndex>=1&&callIndex<=callLimit){current.callIndex=Math.max(current.callIndex,callIndex);current.callLimit=Math.max(current.callLimit,callLimit);}if(code==="harness_tool_started")current.toolCount+=1;if(tool)current.latest=AI_VERIFY_TOOLS.has(tool)?"正在核对来源与引用":AI_RETRIEVAL_TOOLS.has(tool)?"正在读取相关证据":"正在使用只读证据工具";else if(code==="result_validating")current.latest="正在检查回答结构与引用";else if(code==="execution_failed")current.latest="请查看上方错误原因和处理建议";else if(code==="execution_completed")current.latest="回答与引用已就绪";if(Number.isFinite(Number(raw.elapsed_ms)))current.elapsedMs=Math.max(current.elapsedMs,Number(raw.elapsed_ms));if(code==="execution_failed")current.state="failed";else if(code==="execution_completed")current.state="done";value.phases.set(phase,current);aiActivityAggregates.set(scope,value);return renderAIActivitySummary(scope,value);}
  let paneDrag = null,paneResizeObserver=null,activeSecondaryPDFViewerId="";

  function readPaneLayout(){return paneController?.restore()??false;}
  function persistPaneLayout(){return paneController?.persist()||null;}
  function hasUsableSecondary(snapshot=documentTabs?.snapshot()){return Boolean(snapshot?.tabs?.some(tab=>tab.groupId==="secondary")&&snapshot?.groups?.some(group=>group.id==="secondary"&&group.activeTabId));}
  function repairEditorLayout(snapshot=documentTabs?.snapshot()){if(!paneController)return false;const editors=paneLayout.editors,hasSecondary=hasUsableSecondary(snapshot);if(editors.primaryCollapsed&&editors.secondaryCollapsed){paneController.reset("editor-groups");return true;}if(editors.primaryCollapsed&&!hasSecondary){paneController.expandEditor("primary");return true;}return false;}
  function paneViewport(){const shell=q("#fusion-shell"),width=Number(state.paneContainerWidth||shell?.getBoundingClientRect?.().width||shell?.clientWidth||globalThis.innerWidth||1440);return Math.max(320,width);}
  function workspaceProjection(snapshot=documentTabs?.snapshot()){
    const viewport=paneViewport(),taskId=taskLayoutIdentity();
    if(workspaceReveal.taskId&&workspaceReveal.taskId!==taskId)Object.assign(workspaceReveal,{surface:null,taskId:""});
    let hasSecondary=hasUsableSecondary(snapshot)&&!paneLayout.editors.secondaryCollapsed;
    let hasInspectorSelection=Boolean(state.selectedEvidence)||(state.view==="personal"&&Boolean(state.inspectorState.personal));
    if(workspaceReveal.taskId===taskId&&workspaceReveal.surface==="context")hasSecondary=false;
    if(workspaceReveal.taskId===taskId&&workspaceReveal.surface==="inspector"){hasSecondary=false;hasInspectorSelection=true;}
    const projected=workspaceLayout?.project?.({width:viewport,view:state.view,searchMode:state.searchMode,hasSecondary,hasInspectorSelection,librarianResultsOpen:state.librarianResultsOpen,contextExpanded:!paneLayout.context.collapsed||workspaceReveal.surface==="context",inspectorExpanded:!paneLayout.inspector.collapsed||workspaceReveal.surface==="inspector"})||{mode:viewport>=1600?"wide":viewport>=1200?"desktop":viewport>=900?"compact":"narrow",context:viewport>=1200?"docked":"drawer",secondary:hasSecondary&&viewport>=1200?"docked":hasSecondary?"single":"hidden",inspector:"hidden",librarianResults:"hidden",narrow:viewport<1200};
    const personalTable=documentTabs?.activeTab("secondary");
    if(state.view==="personal"&&viewport>=900&&hasSecondary&&personalTable?.kind==="personal-table"&&personalTable.ownerView==="personal"){
      const context=viewport>=1200&&(!paneLayout.context.collapsed||workspaceReveal.surface==="context")?"docked":viewport<1200?"drawer":"hidden";
      return Object.freeze({...projected,context,secondary:viewport>=1200?"docked":"single",inspector:"hidden",librarianResults:"hidden",dockedColumns:(viewport>=1200?2:1)+(context==="docked"?1:0),narrow:viewport<1200});
    }
    return projected;
  }
  function paneGeometry(snapshot=documentTabs?.snapshot()){const viewport=paneViewport(),projection=workspaceProjection(snapshot),hasSecondary=hasUsableSecondary(snapshot),contextDocked=projection.context==="docked",inspectorDocked=projection.inspector==="docked",contextSize=contextDocked?paneLayout.context.size:0,inspectorSize=inspectorDocked?paneLayout.inspector.size:0,outerSeparators=(contextDocked?5:0)+(inspectorDocked?5:0),activity=viewport>=640?48:0,editorWidth=Math.max(1,viewport-activity-outerSeparators-contextSize-inspectorSize);let primaryCollapsed=hasSecondary&&projection.secondary!=="hidden"&&paneLayout.editors.primaryCollapsed,secondaryCollapsed=!hasSecondary||projection.secondary==="hidden"||paneLayout.editors.secondaryCollapsed;if(projection.secondary==="single"){const activeGroup=documentTabs?.snapshot()?.activeGroupId||"primary";primaryCollapsed=activeGroup==="secondary";secondaryCollapsed=activeGroup!=="secondary";}if(primaryCollapsed&&secondaryCollapsed)primaryCollapsed=false;const split=projection.secondary==="docked"&&!primaryCollapsed&&!secondaryCollapsed,effectiveSplit=primaryCollapsed?0:secondaryCollapsed?1:clampPane(paneLayout.editors.split,.01,.99);return {viewport,mode:projection.mode,narrow:projection.narrow,projection,hasSecondary,contextDocked,inspectorDocked,contextMax:480,inspectorMax:560,effectiveContext:contextSize,effectiveInspector:inspectorSize,editorWidth,primaryCollapsed,secondaryCollapsed,split,splitBounds:[0,1],effectiveSplit};}
  function updatePaneSeparator(kind,value,min,max){const node=q(`[data-pane-separator="${kind}"]`);if(!node)return;node.setAttribute("aria-valuemin",String(Math.round(min)));node.setAttribute("aria-valuemax",String(Math.round(max)));node.setAttribute("aria-valuenow",String(Math.round(value)));}
  function applyPaneLayout(snapshot=documentTabs?.snapshot()){repairEditorLayout(snapshot);const geometry=paneGeometry(snapshot),projection=geometry.projection,style=document.documentElement?.style,context=q("#fusion-context"),inspector=q("#fusion-inspector"),contextSeparator=q("#fusion-context-separator"),inspectorSeparator=q("#fusion-inspector-separator");state.paneLayoutMode=geometry.mode;if(document.body?.dataset){document.body.dataset.layoutMode=geometry.mode;document.body.dataset.workspaceTask=projection.librarian?"librarian":state.view;document.body.dataset.workspaceContext=projection.context;document.body.dataset.workspaceSecondary=projection.secondary;document.body.dataset.workspaceInspector=projection.inspector;document.body.dataset.workspaceLibrarianResults=projection.librarianResults;}style?.setProperty?.("--fusion-context-size",`${geometry.effectiveContext}px`);style?.setProperty?.("--fusion-context-separator-size",geometry.contextDocked?"5px":"0px");style?.setProperty?.("--fusion-inspector-size",`${geometry.effectiveInspector}px`);style?.setProperty?.("--fusion-inspector-separator-size",geometry.inspectorDocked?"5px":"0px");style?.setProperty?.("--fusion-primary-fr",`${geometry.effectiveSplit}fr`);style?.setProperty?.("--fusion-secondary-fr",`${1-geometry.effectiveSplit}fr`);if(context)context.hidden=projection.context==="hidden";if(inspector)inspector.hidden=projection.inspector==="hidden";if(contextSeparator)contextSeparator.hidden=!geometry.contextDocked;if(inspectorSeparator)inspectorSeparator.hidden=!geometry.inspectorDocked;updatePaneSeparator("context",geometry.effectiveContext,0,geometry.contextMax);updatePaneSeparator("inspector",geometry.effectiveInspector,0,geometry.inspectorMax);updatePaneSeparator("editor-groups",geometry.effectiveSplit*100,0,100);qa('[data-pane-toggle="context"]').forEach(button=>button.setAttribute("aria-expanded",projection.context!=="hidden"?"true":"false"));qa('[data-pane-toggle="inspector"]').forEach(button=>button.setAttribute("aria-expanded",projection.inspector!=="hidden"?"true":"false"));qa('[data-pane-toggle="primary"]').forEach(button=>button.setAttribute("aria-expanded",geometry.primaryCollapsed?"false":"true"));qa('[data-pane-toggle="secondary"]').forEach(button=>button.setAttribute("aria-expanded",geometry.secondaryCollapsed?"false":"true"));qa("[data-search-context-mode]").forEach(node=>node.hidden=state.searchMode!==node.dataset.searchContextMode);return geometry;}
  function setPaneLayoutValue(kind,value,{persist=true}={}){if(!paneController)return false;const geometry=paneGeometry(),ok=kind==="context"||kind==="inspector"?paneController.setSide(kind,Number(value),{persist}):kind==="editor-groups"?paneController.setEditorRatio(Number(value),geometry.editorWidth,{persist}):false;if(ok){if(kind==="editor-groups")renderDocumentTabs();else applyPaneLayout();}return ok;}
  function resetPaneLayout(kind=null){if(!paneController?.reset(kind))return false;applyPaneLayout();renderDocumentTabs();return true;}
  function paneValueFromPointer(kind,startValue,delta,width){if(kind==="context")return startValue+delta;if(kind==="inspector")return startValue-delta;return startValue+delta/Math.max(1,width);}
  function startPaneDrag(event){const node=event.currentTarget,kind=node?.dataset?.paneSeparator;if(!kind||event.button!==undefined&&event.button!==0||node.hidden)return;q("#fusion-tab-more")?.removeAttribute?.("open");closeDrawers();const geometry=applyPaneLayout(),startValue=kind==="context"?geometry.effectiveContext:kind==="inspector"?geometry.effectiveInspector:geometry.effectiveSplit;paneDrag={kind,node,pointerId:event.pointerId,startX:Number(event.clientX)||0,startValue,editorWidth:geometry.editorWidth};try{node.setPointerCapture?.(event.pointerId);}catch(_error){}node.classList.add("dragging");if(document.documentElement?.dataset)document.documentElement.dataset.paneResizing="true";event.preventDefault?.();}
  function movePaneDrag(event){if(!paneDrag||event.pointerId!==paneDrag.pointerId)return;const value=paneValueFromPointer(paneDrag.kind,paneDrag.startValue,(Number(event.clientX)||0)-paneDrag.startX,paneDrag.editorWidth);if(paneDrag.kind==="context"||paneDrag.kind==="inspector")paneController?.setSide(paneDrag.kind,value,{persist:false});else paneController?.setEditorRatio(value,paneDrag.editorWidth,{persist:false});applyPaneLayout();event.preventDefault?.();}
  function endPaneDrag(event){if(!paneDrag||event.pointerId!==undefined&&event.pointerId!==paneDrag.pointerId)return;const {node,pointerId}=paneDrag;paneDrag=null;try{if(node.hasPointerCapture?.(pointerId))node.releasePointerCapture?.(pointerId);}catch(_error){}node.classList.remove("dragging");if(document.documentElement?.dataset)delete document.documentElement.dataset.paneResizing;persistPaneLayout();renderDocumentTabs();applyPaneLayout();event.preventDefault?.();}
  function handlePaneKey(event){const kind=event.currentTarget?.dataset?.paneSeparator;if(!kind||!["ArrowLeft","ArrowRight","Home","End"].includes(event.key))return;const geometry=applyPaneLayout(),large=event.shiftKey,step=kind==="editor-groups"?(large?0.08:0.02):(large?32:8),bounds=kind==="context"?[0,geometry.contextMax]:kind==="inspector"?[0,geometry.inspectorMax]:[0,1],current=kind==="context"?geometry.effectiveContext:kind==="inspector"?geometry.effectiveInspector:geometry.effectiveSplit;let next;if(event.key==="Home")next=kind==="inspector"?bounds[1]:bounds[0];else if(event.key==="End")next=kind==="inspector"?bounds[0]:bounds[1];else{const direction=event.key==="ArrowRight"?1:-1;next=current+(kind==="inspector"?-direction:direction)*step;}setPaneLayoutValue(kind,clampPane(next,...bounds));event.preventDefault();}
  function bindPaneSeparators(){qa("[data-pane-separator]").forEach(node=>{node.addEventListener("pointerdown",startPaneDrag);node.addEventListener("pointermove",movePaneDrag);node.addEventListener("pointerup",endPaneDrag);node.addEventListener("pointercancel",endPaneDrag);node.addEventListener("lostpointercapture",endPaneDrag);node.addEventListener("keydown",handlePaneKey);node.addEventListener("dblclick",()=>resetPaneLayout(node.dataset.paneSeparator));});globalThis.addEventListener?.("pointerup",endPaneDrag);globalThis.addEventListener?.("blur",endPaneDrag);}
  function taskLayoutIdentity(){const selected=state.selectedEvidence?evidenceIdentity(state.selectedEvidence):"none",paper=state.paper?.id||"none",mode=state.view==="search"?state.searchMode:"main",results=state.librarianResultsOpen?"results":"closed";return `${state.view}:${mode}:${paper}:${selected}:${results}`;}
  function coordinateTaskLayout(reason="auto"){if(workspaceReveal.taskId&&workspaceReveal.taskId!==taskLayoutIdentity())Object.assign(workspaceReveal,{surface:null,taskId:""});const geometry=applyPaneLayout();if(document.body?.dataset)document.body.dataset.taskLayout=reason;return geometry;}
  function syncResponsivePaneLayout(){let geometry=applyPaneLayout(),narrow=geometry.projection.secondary==="single"||geometry.mode==="narrow"||geometry.mode==="compact";if(documentTabs?.setNarrow(narrow))renderDocumentTabs();geometry=coordinateTaskLayout("responsive");syncDrawerAccessibility();return {narrow,...geometry};}
  function observePaneContainer(){const shell=q("#fusion-shell");if(!shell||typeof globalThis.ResizeObserver!=="function")return false;paneResizeObserver?.disconnect?.();paneResizeObserver=new globalThis.ResizeObserver(entries=>{const width=Number(entries?.[0]?.contentRect?.width||shell.getBoundingClientRect?.().width||shell.clientWidth);if(!Number.isFinite(width)||width<=0||Math.abs(width-state.paneContainerWidth)<1)return;state.paneContainerWidth=width;syncResponsivePaneLayout();});paneResizeObserver.observe(shell);return true;}

  function splitActiveTab(){const active=documentTabs?.activeTab();if(!active)return false;if(paneViewport()<900){setOperation("当前窗口以单组标签显示；放宽窗口后可同时查看两个编辑组。","warning");return false;}if(!documentTabs.split(active.tabId))return false;paneController?.expandEditor("secondary");renderDocumentTabs();coordinateTaskLayout("split");activateDocumentTab(active.tabId);return true;}
  function reopenClosedTab(){const reopened=documentTabs?.reopenClosed();if(!reopened)return false;if(reopened.groupId==="secondary")paneController?.expandEditor("secondary");else paneController?.expandEditor("primary");renderDocumentTabs();coordinateTaskLayout("reopen");activateDocumentTab(reopened.tabId);qa("[data-document-tab-id]").find(node=>node.dataset.documentTabId===reopened.tabId)?.focus({preventScroll:true});return true;}
  function togglePane(kind,trigger=null){const projection=workspaceProjection(),taskId=taskLayoutIdentity(),librarian=state.view==="search"&&state.searchMode==="librarian";if((kind==="inspector"&&(["package","settings"].includes(state.view)||librarian))||(kind==="secondary"&&["personal","package","settings"].includes(state.view)))return false;if(kind==="context"&&projection.context==="drawer"){state.drawer==="context"?closeDrawers():openDrawer("context",trigger);return true;}if(kind==="inspector"&&projection.inspector==="drawer"){state.drawer==="inspector"?closeDrawers():openDrawer("inspector",trigger);return true;}if((kind==="context"||kind==="inspector")&&projection[kind]==="hidden"&&!paneLayout[kind]?.collapsed){Object.assign(workspaceReveal,{surface:workspaceReveal.surface===kind?null:kind,taskId});renderDocumentTabs();applyPaneLayout();return true;}let changed=false;if(kind==="context"||kind==="inspector")changed=Boolean(paneController?.toggleSide(kind));else if(kind==="secondary"&&!documentTabs?.snapshot().groups.some(group=>group.id==="secondary"))changed=splitActiveTab();else if(kind==="primary"||kind==="secondary"){changed=Boolean(paneController?.toggleEditor(kind));if(changed&&paneLayout.editors[`${kind}Collapsed`]){const other=kind==="primary"?"secondary":"primary",tab=documentTabs?.activeTab(other);if(tab)documentTabs.activate(tab.tabId);}}if(changed){Object.assign(workspaceReveal,{surface:null,taskId:""});renderDocumentTabs();applyPaneLayout();}return changed;}

  function documentIdentity(row){if(!row)return null;if(row.sourceScope==="workspace")return {sourceScope:"workspace",entityType:row.type,entityUid:String(row.assetId||row.itemId||0),paperId:String(row.paperId||state.paper?.id||"")};return {sourceScope:row.sourceScope,sourceId:row.sourceId,entityType:row.type,entityUid:row.entityUid};}
  function documentTabId(kind,identity){if(!identity)return "";const stable=Object.entries(identity).map(([key,value])=>`${key}=${encodeURIComponent(String(value))}`).join("&");return stable?`${kind}:${stable}`:"";}
  function openDocumentTab(kind,title,identity,{ownerView=state.view,payload=null,activate=true,preview=false,pin=false,groupId=null}={}){if(!documentTabs||!identity)return null;const tabId=documentTabId(kind,identity),tab=tabId?documentTabs.open({tabId,kind,title,ownerView,identity,payload},{activate,preview,pin,groupId}):null;renderDocumentTabs();return tab;}
  function pinDocumentTab(tabId=documentTabs?.activeTab()?.tabId){const tab=tabId?documentTabs?.pin(tabId):null;if(tab)renderDocumentTabs();return tab;}
  function pinDocumentIdentity(kind,identity){return pinDocumentTab(documentTabId(kind,identity));}
  function evidenceChatHTML(tabId=""){return `<section class="fusion-evidence-chat" data-evidence-chat data-evidence-tab-id="${esc(tabId)}" aria-label="当前证据 AI 对话"><header><div><strong>证据 AI 对话</strong><small data-evidence-ai-reason>仅对当前打开的具体证据可用。</small><small data-evidence-history-status aria-live="polite">正在读取本机加密对话…</small></div><span data-evidence-ai-context>当前证据</span><button type="button" class="fusion-evidence-chat-clear" data-evidence-ai-clear title="只清除当前证据的本机加密对话" disabled>清空当前对话</button></header><div class="fusion-chat-thread fusion-evidence-ai-output" data-evidence-ai-output aria-live="polite"></div><div class="fusion-evidence-ai-inline-progress" data-evidence-ai-inline-progress hidden aria-live="polite">正在核对当前证据…</div><form class="fusion-chat-composer" data-evidence-ai-form><label class="sr-only">当前证据问题</label><textarea data-evidence-ai-question data-focus-token="evidence-question" rows="2" maxlength="2000" placeholder="围绕当前证据提问…">说明这个数据本身的含义，并总结该数据在文章中的具体含义</textarea><button type="submit" data-evidence-ai-send disabled aria-label="发送证据问题">发送</button><small>官方与工作区对话保存在本机加密状态区 · Enter 发送 · Shift+Enter 换行</small></form></section>`;}
  function secondaryEvidenceHTML(tab,row){if(!row?.type)return tab.payload?.status==="error"?detailState("error","标签内容无法恢复","来源已变化或当前只读接口不可用；关闭标签后可从列表重新打开。"):detailState("loading","正在读取完整证据","当前标签会在原位更新，不会抢占主编辑组。");const media=row.imageUrl?`<figure><img src="${esc(row.imageUrl)}" alt="${esc(row.caption||row.title)}"><figcaption>${esc(row.caption||"来源视觉证据")}</figcaption></figure>`:["table","figure"].includes(row.type)&&tab.payload?.status==="loading"?detailState("loading","正在读取权威原图","图片和结构化字段会一起更新到当前标签。"):"",quantity=row.type==="finding"?(row.findingText||row.value||"定性结论"):(row.value?`${row.value}${row.unit?` ${row.unit}`:""}`:row.label||EVIDENCE_LABELS[row.type]);return `<article class="fusion-secondary-document fusion-secondary-evidence" data-secondary-document-kind="evidence" data-evidence-detail-tab="${esc(tab.tabId)}"><span>${esc(EVIDENCE_LABELS[row.type]||"证据")}</span><h1>${esc(row.title||tab.title)}</h1><div class="fusion-detail-measure"><small>${row.type==="finding"?"结论正文":"证据值 / 对象"}</small><strong>${esc(quantity)}</strong></div>${media}${row.caption&&!row.imageUrl?`<section><h2>原始图注</h2><p>${esc(row.caption)}</p></section>`:""}${row.findingText?`<section><h2>论文报告的结论</h2><p>${esc(row.findingText)}</p></section>`:""}${row.context?`<section><h2>文章中的含义</h2><p>${esc(row.context)}</p></section>`:""}${row.sourceContext?`<section><h2>原文语境</h2><p>${esc(row.sourceContext)}</p></section>`:""}${row.excerpt?`<blockquote><span>原文证据</span><p>${esc(row.excerpt)}</p></blockquote>`:""}${detailDefinitionList(row)}${tableStructureHTML(tab.tabId,row,tab.payload,false)}${detailPDFURL(row)?`<button type="button" data-secondary-open-pdf="${esc(tab.tabId)}">查看原文</button>`:""}</article>${evidenceChatHTML(tab.tabId)}`;}
  function secondaryPersonalTableHTML(tab,row,page){const back='<button type="button" data-secondary-return-personal>← 返回实验核验</button>';if(!page)return `<article class="fusion-secondary-document" data-secondary-document-kind="personal-table">${back}<span>私人实验表格</span><h1>${esc(row.title||tab.title)}</h1>${detailState(tab.payload?.status==="error"?"error":"loading",tab.payload?.status==="error"?"表格暂时无法读取":"正在读取原始表格",tab.payload?.status==="error"?"现有标签没有被其他记录替换。":"每页最多读取 50 行。")}</article>`;const first=(page.page-1)*page.pageSize+1,last=Math.min(page.total,page.page*page.pageSize),columns=page.columns||[],rows=page.rows||[];return `<article class="fusion-secondary-document" data-secondary-document-kind="personal-table">${back}<span>私人实验表格</span><h1>${esc(page.title||row.title||tab.title)}</h1><p>${esc(page.sheetName||"工作表")} · ${page.total} 行 · ${columns.length} 列</p><div class="fusion-private-table-toolbar"><span>${page.total?`第 ${first}–${last} 行，共 ${page.total} 行`:"当前表格没有数据行"}</span><div><button type="button" data-secondary-table-page="${page.page-1}"${page.page<=1?" disabled":""}>上一页</button><b>第 ${page.page} 页</b><button type="button" data-secondary-table-page="${page.page+1}"${page.hasNext?"":" disabled"}>下一页</button></div></div><div class="fusion-private-table-scroll" tabindex="0"><table class="fusion-data-grid"><thead><tr><th>行</th>${columns.map(column=>`<th><strong>${esc(column.name)}</strong><small>${esc([column.meaning,column.unit].filter(Boolean).join(" · ")||column.role)}</small></th>`).join("")}</tr></thead><tbody>${rows.map((record,index)=>`<tr><th>${first+index}</th>${columns.map(column=>`<td>${esc(record[column.name])}</td>`).join("")}</tr>`).join("")}</tbody></table></div></article>`;}
  function secondaryDocumentHTML(tab){
    const payload=tab?.payload||{},row=payload.row||{};
    if(!tab)return `<div class="fusion-state-card"><strong>第二编辑组为空</strong><span>从标签栏选择“在右侧打开”即可并排查看完整内容。</span></div>`;
    if(tab.kind==="pdf"&&payload.url){const highlight=payload.highlight?.url?`<div class="fusion-source-highlight fusion-secondary-source-highlight" data-secondary-pdf-highlight><img src="${esc(payload.highlight.url)}" alt="当前证据的原文定位截图"><div><strong>原文定位</strong><span>${payload.highlight.kind==="visual"?"表格或图片在原文中的权威截图区域。":"当前证据在原文中的高亮位置。"}</span></div></div>`:"";return `<article class="fusion-secondary-document fusion-secondary-pdf" data-secondary-document-kind="pdf" data-pdf-tab-id="${esc(tab.tabId)}"><header>${payload.returnTabId?`<button type="button" data-secondary-return-tab="${esc(payload.returnTabId)}">← 返回来源标签</button>`:""}<h1>${esc(tab.title)}</h1><div class="fusion-pdf-controls" role="toolbar" aria-label="PDF 缩放">${payload.highlight?.url?`<button type="button" data-secondary-pdf-highlight-toggle>隐藏定位</button>`:""}<button type="button" data-pdf-action="fit-width" data-pdf-viewer="secondary">适合宽度</button><button type="button" data-pdf-action="fit-page" data-pdf-viewer="secondary">适合页面</button><button type="button" data-pdf-action="zoom-out" data-pdf-viewer="secondary">−</button><button type="button" data-pdf-action="reset" data-pdf-viewer="secondary">100%</button><button type="button" data-pdf-action="zoom-in" data-pdf-viewer="secondary">＋</button></div></header>${highlight}<iframe data-secondary-pdf-frame title="${esc(tab.title)}"></iframe></article>`;}
    if(tab.kind==="package-job"){const job=payload.job||{};return `<article class="fusion-secondary-document" data-secondary-document-kind="package-job"><span>资料包任务</span><h1>${esc(tab.title)}</h1><dl><div><dt>阶段</dt><dd>${esc(job.stage||"等待状态更新")}</dd></div><div><dt>进度</dt><dd>${Math.max(0,Math.min(100,Math.round(Number(job.progress||0))))}%</dd></div><div><dt>结果</dt><dd>${esc(job.outcome||job.status||"进行中")}</dd></div></dl>${job.message?`<p>${esc(job.message)}</p>`:""}</article>`;}
    if(tab.kind==="librarian")return `<article class="fusion-secondary-document fusion-secondary-librarian" data-secondary-document-kind="librarian"><span>图书管理员</span><h1>${esc(tab.title)}</h1>${payload.html||detailState("empty","回答内容尚未恢复","重新运行图书管理员后，完整回答会回到此标签。")}</article>`;
    if(tab.kind==="review-candidate")return `<article class="fusion-secondary-document" data-secondary-document-kind="review-candidate">${reviewCandidateHTML(payload.item,{interactive:false,status:payload.reviewStatus||""})}</article>`;
    if(tab.kind==="personal-table")return secondaryPersonalTableHTML(tab,row,payload.page);
    if(tab.kind==="evidence")return secondaryEvidenceHTML(tab,row);
    if(tab.kind==="paper"){const paper=payload.paper||{},evidence=Array.isArray(payload.evidence)?payload.evidence:[];return `<article class="fusion-secondary-document fusion-secondary-paper" data-secondary-document-kind="paper"><span>论文</span><h1>${esc(paper.title||tab.title)}</h1><p>${esc([paper.firstAuthor,paper.year,paper.doi,paper.material].filter(Boolean).join(" · ")||"公开元数据未提供")}</p><dl><div><dt>证据条目</dt><dd>${Number(payload.evidenceCount||evidence.length||0)}</dd></div><div><dt>处理状态</dt><dd>${esc(paper.status||"只读")}</dd></div></dl>${positiveId(paper.id)?`<button type="button" data-secondary-open-paper-pdf="${paper.id}">打开 PDF</button>`:""}<section><h2>四类证据</h2>${evidence.map((item,index)=>`<button type="button" class="fusion-secondary-evidence-row" data-secondary-paper-evidence="${index}" data-source-tab="${esc(tab.tabId)}"><strong>${esc(EVIDENCE_LABELS[item.type]||"证据")}</strong><span>${esc(item.title)}</span><small>第 ${esc(item.page)} 页</small></button>`).join("")||"<p>当前论文暂无已发布证据。</p>"}</section></article>`;}
    return detailState("empty","标签内容尚未载入","选择原始对象后可在此组恢复完整内容。");
  }
  async function loadSecondaryPersonalTablePage(tabId,row,page){const identity=privateTableIdentity(row),tab=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId);if(!identity||!tab||!Number.isInteger(page)||page<1)return false;const generation=documentTabs.beginRequest(tabId);documentTabs.update(tabId,{payload:{...tab.payload,row,page:null,status:"loading"}});renderDocumentTabs();try{const raw=await readOnlyJSON(`${ROUTES.personalTable}?source_id=${encodeURIComponent(identity.sourceId)}&entity_uid=${encodeURIComponent(identity.entityUid)}&page=${page}&page_size=50`),result=publicPersonalTablePage(raw,identity,page);if(!result)throw safeError("personal_table_invalid","个人实验表格响应无效。");const current=documentTabs.snapshot().tabs.find(value=>value.tabId===tabId);if(!current||!documentTabs.completeRequest(tabId,generation,{title:result.title||current.title,payload:{...current.payload,row,page:result,status:"ready"}}))return false;renderDocumentTabs();return true;}catch(_error){const current=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId);if(current&&documentTabs.completeRequest(tabId,generation,{payload:{...current.payload,row,page:null,status:"error"}}))renderDocumentTabs();return false;}}
  async function openSecondaryEvidence(row,ownerView,{preview=true,pin=false}={}){
    const identity=documentIdentity(row),kind=privateTableIdentity(row)?"personal-table":"evidence";paneController?.expandEditor("secondary");const tab=openDocumentTab(kind,row.title,identity,{ownerView,payload:{evidenceIdentity:evidenceIdentity(row),row,status:"loading"},groupId:"secondary",preview,pin});if(!tab)return null;coordinateTaskLayout("secondary-evidence");
    if(privateTableIdentity(row))return await loadSecondaryPersonalTablePage(tab.tabId,row,1)?tab:null;
    const generation=documentTabs.beginRequest(tab.tabId);if(["item","finding"].includes(row.type)){documentTabs.completeRequest(tab.tabId,generation,{payload:{...tab.payload,row,status:"ready"}});renderDocumentTabs();return tab;}if(row.sourceScope==="workspace"&&!positiveId(row.assetId)){documentTabs.completeRequest(tab.tabId,generation,{payload:{...tab.payload,row,status:"error"}});renderDocumentTabs();return null;}
    try{const url=row.sourceScope==="workspace"?`${ROUTES.visualAsset}/${row.assetId}`:`${ROUTES.federatedEvidence}?source_scope=${encodeURIComponent(row.sourceScope)}&source_id=${encodeURIComponent(row.sourceId)}&entity_uid=${encodeURIComponent(row.entityUid)}`,detail=publicEvidence(await readOnlyJSON(url));if(!detail||detail.type!==row.type||detail.sourceScope!==row.sourceScope||(row.sourceScope==="workspace"&&detail.assetId!==row.assetId)||(row.sourceScope!=="workspace"&&evidenceIdentity(detail)!==evidenceIdentity(row)))throw safeError("fusion_evidence_identity_changed","证据身份已变化。");const current=documentTabs.snapshot().tabs.find(value=>value.tabId===tab.tabId);if(current&&documentTabs.completeRequest(tab.tabId,generation,{payload:{...current.payload,row:detail,status:"ready"}})){renderDocumentTabs();if(detail.sourceScope==="workspace"&&detail.type==="table")void loadTableStructure(tab.tabId,detail);}return tab;}catch(_error){const current=documentTabs?.snapshot().tabs.find(value=>value.tabId===tab.tabId);if(current&&documentTabs.completeRequest(tab.tabId,generation,{payload:{...current.payload,row,status:"error"}}))renderDocumentTabs();return null;}
  }
  function returnFromSecondaryPersonalTable(){if(state.view!=="personal"||!documentTabs?.focusGroup("primary"))return false;renderDocumentTabs();coordinateTaskLayout("personal-review-return");q("#fusion-personal-confirm")?.focus?.({preventScroll:true});return true;}
  function bindSecondaryEditorActions(tab){const mount=activeMount("secondary");if(mount&&mount.dataset.actionsBound===String(tab?.revision))return;if(mount)mount.dataset.actionsBound=String(tab?.revision);q("[data-secondary-return-tab]")?.addEventListener("click",event=>activateDocumentTab(event.currentTarget.dataset.secondaryReturnTab));q("[data-secondary-return-personal]")?.addEventListener("click",()=>returnFromSecondaryPersonalTable());q("[data-secondary-pdf-highlight-toggle]")?.addEventListener("click",event=>{const panel=q("[data-secondary-pdf-highlight]");if(!panel)return;panel.hidden=!panel.hidden;event.currentTarget.textContent=panel.hidden?"重新显示定位":"隐藏定位";});q("[data-secondary-open-pdf]")?.addEventListener("click",event=>{const source=documentTabs?.snapshot().tabs.find(value=>value.tabId===event.currentTarget.dataset.secondaryOpenPdf),row=source?.payload?.row,url=detailPDFURL(row),highlight=sourceHighlightCapability(row);if(!source||!url)return;pinDocumentTab(source.tabId);openDocumentTab("pdf",row.articleTitle||"证据来源原文",{...documentIdentity(row),paperId:String(row.paperId||"")},{ownerView:source.ownerView,payload:{evidenceIdentity:evidenceIdentity(row),url,returnTabId:source.tabId,page:Number(row.page)||1,highlight},groupId:"secondary",pin:true});});q("[data-secondary-open-paper-pdf]")?.addEventListener("click",event=>{const paperId=positiveId(event.currentTarget.dataset.secondaryOpenPaperPdf);if(!paperId||!tab)return;pinDocumentTab(tab.tabId);openDocumentTab("pdf",`${tab.title} · PDF`,{paperId:String(paperId)},{ownerView:"paper",payload:{paperId,url:`/api/papers/${paperId}/pdf`,returnTabId:tab.tabId,page:1},groupId:"secondary",pin:true});});qa("[data-secondary-paper-evidence]").forEach(button=>{button.addEventListener("click",event=>{if(event.detail>1)return;const source=documentTabs?.snapshot().tabs.find(value=>value.tabId===button.dataset.sourceTab),row=source?.payload?.evidence?.[Number(button.dataset.secondaryPaperEvidence)];if(row)void openSecondaryEvidence(row,source.ownerView,{preview:true});});button.addEventListener("dblclick",()=>{const source=documentTabs?.snapshot().tabs.find(value=>value.tabId===button.dataset.sourceTab),row=source?.payload?.evidence?.[Number(button.dataset.secondaryPaperEvidence)];if(row&&!pinDocumentIdentity(privateTableIdentity(row)?"personal-table":"evidence",documentIdentity(row)))void openSecondaryEvidence(row,source.ownerView,{preview:false,pin:true});});});qa("[data-secondary-table-page]").forEach(button=>button.addEventListener("click",()=>{const page=Number(button.dataset.secondaryTablePage);if(tab?.payload?.row&&page>0)void loadSecondaryPersonalTablePage(tab.tabId,tab.payload.row,page);}));}
  function tabMountHost(groupId){return groupId==="secondary"?q("#fusion-secondary-editor-body"):null;}
  function activeMount(groupId){return tabMountHost(groupId)?.querySelector?.('[data-document-mount]:not([hidden])')||null;}
  function presentationSurface(groupId){return groupId==="secondary"?activeMount("secondary"):q("#fusion-primary-editor-surface");}
  function captureTabPresentation(groupId){const tab=documentTabs?.activeTab(groupId),surface=presentationSurface(groupId);if(!tab||!surface)return false;const active=document.activeElement,focusToken=surface.contains?.(active)?active?.dataset?.focusToken||"":"";return documentTabs.rememberPresentation(tab.tabId,{scrollTop:surface.scrollTop,focusToken});}
  function restoreTabPresentation(tab,mount){if(!tab||!mount)return false;const presentation=documentTabs?.presentation(tab.tabId);mount.scrollTop=Number(presentation?.scrollTop)||0;if(presentation?.focusToken)mount.querySelector?.(`[data-focus-token="${presentation.focusToken}"]`)?.focus?.({preventScroll:true});return true;}
  function renderStableTabMount(groupId,tab){const host=tabMountHost(groupId);if(!host||!tab||typeof document.createElement!=="function"||typeof host.append!=="function"){if(host)host.innerHTML=secondaryDocumentHTML(tab);return host;}const mounts=[...(host.querySelectorAll?.("[data-document-mount]")||[])],live=new Set(documentTabs.snapshot().tabs.filter(value=>value.groupId===groupId).map(value=>value.tabId));for(const mount of mounts)if(!live.has(mount.dataset.documentMount))mount.remove?.();let mount=[...(host.querySelectorAll?.("[data-document-mount]")||[])].find(value=>value.dataset.documentMount===tab.tabId);if(!mount){mount=document.createElement("section");mount.className="fusion-document-mount";mount.dataset.documentMount=tab.tabId;mount.tabIndex=-1;host.append(mount);}const signature=`${tab.revision}:${tab.kind}`;if(mount.dataset.renderSignature!==signature){const presentation=documentTabs.presentation(tab.tabId);mount.innerHTML=secondaryDocumentHTML(tab);mount.dataset.renderSignature=signature;mount.scrollTop=presentation.scrollTop;}for(const candidate of [...(host.querySelectorAll?.("[data-document-mount]")||[])])candidate.hidden=candidate!==mount;restoreTabPresentation(tab,mount);return mount;}

  function projectPrimaryEvidenceTab(tab){const row=tab?.payload?.row;if(!row||!["evidence","personal-table"].includes(tab.kind)||!state.evidenceDetailOpen)return false;state.evidenceDetail=row;if(tab.kind==="personal-table"&&tab.payload?.page)renderPersonalTablePage(row,tab.payload.page);else if(tab.kind==="personal-table")renderPersonalTableTerminal(row,tab.payload?.status==="error"?"error":"loading",tab.payload?.status==="error"?"原始表格暂时无法读取":"正在读取原始表格","当前主编辑组只会更新自己的标签。");else renderEvidenceDetail(row,{imageState:tab.payload?.status==="error"?"error":tab.payload?.status==="loading"?"loading":"ready"});return true;}
  function renderEditorSurfaces(snapshot=documentTabs?.snapshot()){if(!snapshot)return;let geometry=paneGeometry(snapshot);const layout=q("#fusion-editor-group-content"),primary=q("#fusion-primary-editor-surface"),secondary=q("#fusion-secondary-editor-surface"),groupSeparator=q("#fusion-editor-group-separator"),primaryTab=documentTabs.activeTab("primary"),secondaryTab=documentTabs.activeTab("secondary");if(geometry.primaryCollapsed&&(!geometry.hasSecondary||!secondaryTab)){paneController?.expandEditor("primary");geometry=paneGeometry(snapshot);}layout?.classList.toggle("split",geometry.split);if(primary){primary.hidden=geometry.narrow?geometry.hasSecondary&&snapshot.activeGroupId==="secondary":geometry.primaryCollapsed;primary.classList.toggle("active",!primary.hidden);}if(secondary){secondary.hidden=!geometry.hasSecondary||(geometry.narrow?snapshot.activeGroupId!=="secondary":geometry.secondaryCollapsed);secondary.classList.toggle("active",!secondary.hidden);}if(groupSeparator)groupSeparator.hidden=!geometry.split;if(primary&&!primary.hidden)projectPrimaryEvidenceTab(primaryTab);if(q("#fusion-secondary-editor-title"))q("#fusion-secondary-editor-title").textContent=secondaryTab?.title||"第二编辑组";renderStableTabMount("secondary",secondaryTab);const evidenceDetail=visibleEvidenceDetail();qa("[data-evidence-chat]").forEach(chat=>chat.hidden=chat.dataset.evidenceTabId!==evidenceDetail?.tab?.tabId);applyPaneLayout(snapshot);bindSecondaryEditorActions(secondaryTab);bindEvidenceChatActions();syncSelectedEvidenceAI();bindPDFControls();projectSecondaryPDF(secondaryTab);}
  function renderDocumentTabs(){if(!documentTabs)return;const snapshot=documentTabs.snapshot(),geometry=paneGeometry(snapshot),groups=q("#fusion-document-tab-groups");groups?.classList.toggle("split",geometry.split);for(const groupId of ["primary","secondary"]){const group=snapshot.groups.find(value=>value.id===groupId),host=q(`#fusion-document-tabs-${groupId}`),shell=q(`[data-document-group="${groupId}"]`);if(!host)continue;const visibleTabs=snapshot.narrow&&groupId==="primary"?snapshot.tabs:snapshot.tabs.filter(tab=>tab.groupId===groupId),collapsed=groupId==="primary"?geometry.primaryCollapsed:geometry.secondaryCollapsed;if(shell)shell.hidden=snapshot.narrow?groupId==="secondary":!group||collapsed;host.innerHTML=visibleTabs.map(tab=>{const tabGroup=snapshot.groups.find(value=>value.id===tab.groupId),groupActive=tab.tabId===tabGroup?.activeTabId,focused=groupActive&&snapshot.activeGroupId===tab.groupId,selected=groupActive&&(!snapshot.narrow||focused);return `<div class="fusion-document-tab${selected?" active":""}${focused?" focused":""}${tab.preview?" preview":""}" role="tab" aria-selected="${selected?"true":"false"}" tabindex="${focused?"0":"-1"}" data-document-group-id="${tab.groupId}" data-document-tab-id="${esc(tab.tabId)}"><span>${esc(tab.title)}</span><button type="button" class="fusion-document-tab-close" data-close-document-tab="${esc(tab.tabId)}" aria-label="关闭 ${esc(tab.title)}">×</button></div>`;}).join("");}renderEditorSurfaces(snapshot);bindDocumentTabs();bindLibrarianContentActions();}
  function bindDocumentTabs(){qa("[data-document-tab-id]").forEach(tab=>{tab.addEventListener("click",event=>{if(event.target.closest?.("[data-close-document-tab]"))return;activateDocumentTab(tab.dataset.documentTabId);});tab.addEventListener("dblclick",event=>{if(event.target.closest?.("[data-close-document-tab]"))return;pinDocumentTab(tab.dataset.documentTabId);});tab.addEventListener("keydown",event=>handleRovingTabs(event,`#${tab.parentElement.id} [data-document-tab-id]`,next=>activateDocumentTab(next.dataset.documentTabId)));});qa("[data-close-document-tab]").forEach(button=>button.addEventListener("click",event=>{event.stopPropagation();const wasActive=documentTabs?.activeTab()?.tabId===button.dataset.closeDocumentTab,closed=documentTabs?.close(button.dataset.closeDocumentTab),next=wasActive?documentTabs?.activeTab():null;renderDocumentTabs();if(next){activateDocumentTab(next.tabId);qa("[data-document-tab-id]").find(node=>node.dataset.documentTabId===next.tabId)?.focus({preventScroll:true});}else if(closed?.payload?.focusReturn?.isConnected)closed.payload.focusReturn.focus({preventScroll:true});}));}
  function restorePrimaryPresentation(tab){const surface=q("#fusion-primary-editor-surface"),presentation=documentTabs?.presentation(tab?.tabId);if(!tab||!surface||!presentation)return false;surface.scrollTop=Number(presentation.scrollTop)||0;if(presentation.focusToken)surface.querySelector?.(`[data-focus-token="${presentation.focusToken}"]`)?.focus?.({preventScroll:true});return true;}
  function activateDocumentTab(tabId){
    captureTabPresentation(documentTabs?.activeGroupId||"primary");
    const tab=documentTabs?.activate(tabId);
    if(!tab)return false;
    renderDocumentTabs();
    if(tab.payload===undefined||tab.payload?.hydration==="retry")void hydrateDocumentTab(tab);
    if(tab.groupId==="secondary")return true;
    if(tab.ownerView!==state.view)switchView(tab.ownerView,{focus:false,activateTab:false});
    if(tab.kind==="paper"){
      const paperId=Number(tab.identity.paperId);
      if(state.paper?.id!==paperId&&state.papers.some(paper=>paper.id===paperId))void selectPaper(paperId,{reveal:true,openTab:false}).then(()=>restorePrimaryPresentation(tab));
      else{closeReviewDetail({focus:false});closeEvidenceDetail({focus:false});restorePrimaryPresentation(tab);}
    }else if(tab.kind==="evidence"||tab.kind==="personal-table"){
      const rows=tab.ownerView==="search"?state.searchResults:state.evidence,persistedIdentity=tab.identity.sourceScope==="workspace"?`workspace:${tab.identity.entityType}:${tab.identity.entityUid}`:`${tab.identity.sourceScope||""}:${tab.identity.sourceId||""}:${tab.identity.entityUid||""}`,index=rows.findIndex(row=>evidenceIdentity(row)===(tab.payload?.evidenceIdentity||persistedIdentity));
      if(index>=0){const opened=openEvidenceDetail(index,null,{openTab:false});if(opened?.then)void opened.then(()=>restorePrimaryPresentation(tab));}
      else restorePrimaryPresentation(tab);
    }else if(tab.kind==="review-candidate"){showReviewDetail(tab,tab.payload?.item||null);restorePrimaryPresentation(tab);}
    else if(tab.kind==="pdf"&&tab.ownerView==="paper"){
      const paperId=Number(tab.identity.paperId);
      if(state.paper?.id!==paperId&&state.papers.some(paper=>paper.id===paperId))void selectPaper(paperId,{reveal:true,openTab:false}).then(()=>{openCurrentPDF({openTab:false});restorePrimaryPresentation(tab);});
      else{openCurrentPDF({openTab:false});restorePrimaryPresentation(tab);}
    }else if(tab.kind==="librarian"){
      setSearchMode("librarian");const selector={answer:".fusion-librarian-answer",citations:".fusion-librarian-evidence",recommendations:".fusion-librarian-recommendations"}[tab.payload?.section||tab.identity.entityType]||"#fusion-librarian-output";q(selector)?.scrollIntoView?.({block:"start"});q("#fusion-librarian-output")?.focus?.({preventScroll:true});restorePrimaryPresentation(tab);
    }
    return true;
  }

  function isAllowedRoute(url,method) {
    const path=String(url).split("?")[0];
    if(method==="GET")return [ROUTES.papers,ROUTES.evidence,ROUTES.federatedSearch,ROUTES.federatedEvidence,ROUTES.personalTable,ROUTES.personalSearchStatus,ROUTES.tableStructures,ROUTES.reviewQueue,ROUTES.uiMode,ROUTES.settings,ROUTES.aiProviders,ROUTES.aiSettings,ROUTES.aiCustom,ROUTES.librarianHistory,ROUTES.researchMemories,ROUTES.evidenceChatHistory,ROUTES.officialPackages,ROUTES.packageCenter,ROUTES.packageReceipts,ROUTES.packageHistory,ROUTES.literatureTaskDirectory].includes(path)||PERSONAL_IMPORT_ROWS_PATH.test(path)||/^\/api\/visual-assets\/[1-9][0-9]*$/.test(path)||/^\/api\/six-data\/[1-9][0-9]*\/source-view$/.test(path)||/^\/api\/desktop\/ai\/credentials\/(deepseek|openai|custom)$/.test(path)||/^\/api\/desktop\/ai\/jobs\/ai_job_[A-Za-z0-9_-]{24,160}$/.test(path)||/^\/api\/desktop\/ai\/actions\/(librarian|literature_extraction|personal_suggestion|selected_evidence_chat)\/jobs$/.test(path)||/^\/api\/desktop\/(evidence-package-jobs|package-center\/jobs)\/[A-Za-z0-9_-]{8,160}$/.test(path);
    if(method==="PATCH")return path===ROUTES.preferences||path===ROUTES.aiSettings;
    if(method==="DELETE")return /^\/api\/desktop\/ai\/credentials\/(deepseek|openai|custom)$/.test(path)||/^\/api\/desktop\/ai\/custom-provider\/[1-9][0-9]{0,8}$/.test(path);
    if(method!=="POST")return false;
    return path===ROUTES.upload||path===ROUTES.consent||path===ROUTES.personalPreview||path===ROUTES.personalSearchRefresh||path===ROUTES.reviewActions||path===ROUTES.tableStructureReviews||path===ROUTES.searchIndexRecovery||path===ROUTES.aiCustom||path===ROUTES.librarianHistory||path===ROUTES.researchMemories||path===ROUTES.evidenceChatHistory||["/api/desktop/evidence-packages/import","/api/desktop/evidence-packages/rollback","/api/desktop/package-center/export-plan","/api/desktop/package-center/export","/api/desktop/package-center/inspect","/api/desktop/package-center/import","/api/desktop/package-center/dataset-plan","/api/desktop/package-center/dataset-export",ROUTES.packageReceipts,ROUTES.packageHistory].includes(path)||/^\/api\/desktop\/package-center\/jobs\/[A-Za-z0-9_-]{16,128}\/receipt-retry$/.test(path)||/^\/api\/desktop\/package-center\/history\/[0-9a-f]{64}\/receipt-retry$/.test(path)||/^\/api\/desktop\/ai\/credentials\/(deepseek|openai|custom)$/.test(path)||/^\/api\/desktop\/ai\/providers\/(deepseek|openai|custom)\/(test-actions|test)$/.test(path)||/^\/api\/desktop\/ai\/actions\/(librarian|literature_extraction|personal_suggestion|selected_evidence_chat)\/(prepare|execute|execute-jobs)$/.test(path)||/^\/api\/desktop\/personal-imports\/personal_import_[A-Za-z0-9_-]{16,96}\/reviewed-import$/.test(path);
  }
  async function request(url,{method="GET",headers={},body=null,signal=null}={}) {
    method=String(method).toUpperCase();if(!isAllowedRoute(url,method))throw safeError("fusion_route_blocked","该功能路由未获 Fusion 工作台授权。");
    const requestHeaders=new Headers(headers);requestHeaders.set("Accept","application/json");if(method!=="GET"&&state.csrfToken)requestHeaders.set(CSRF_HEADER,state.csrfToken);
    const response=await fetch(url,{method,headers:requestHeaders,body,credentials:"same-origin",cache:"no-store",signal});const csrf=response.headers?.get?.(CSRF_HEADER);if(csrf)state.csrfToken=csrf;
    const payload=await response.json().catch(()=>({}));if(!response.ok)throw safeError(String(payload.cause_code||payload.code||"fusion_request_failed"),String(payload.message||payload.error||"操作未完成，请稍后重试。"),payload);return payload;
  }
  function safeError(code,message,detail={}){const error=new Error(String(message||"操作未完成，请稍后重试。"));error.code=String(code||"fusion_request_failed");error.stage=cleanText(detail?.stage,120);error.nextAction=cleanText(detail?.next_action,500);error.retryable=detail?.retryable===true;return error;}
  async function readOnlyJSON(url, signal) {
    const path=String(url).split("?")[0],fixed=[ROUTES.papers,ROUTES.evidence,ROUTES.federatedSearch,ROUTES.federatedEvidence,ROUTES.personalTable,ROUTES.personalSearchStatus,ROUTES.tableStructures,ROUTES.reviewQueue,ROUTES.uiMode,ROUTES.settings,ROUTES.aiProviders,ROUTES.aiSettings,ROUTES.aiCustom,ROUTES.librarianHistory,ROUTES.researchMemories,ROUTES.evidenceChatHistory,ROUTES.officialPackages,ROUTES.packageCenter,ROUTES.packageReceipts,ROUTES.packageHistory,ROUTES.literatureTaskDirectory];
    if (!fixed.includes(path)&&!PERSONAL_IMPORT_ROWS_PATH.test(path)&&!/^\/api\/visual-assets\/[1-9][0-9]*$/.test(path)&&!/^\/api\/six-data\/[1-9][0-9]*\/source-view$/.test(path)&&!/^\/api\/desktop\/ai\/actions\/(librarian|literature_extraction|personal_suggestion|selected_evidence_chat)\/jobs$/.test(path)&&!/^\/api\/desktop\/(evidence-package-jobs|package-center\/jobs)\/[A-Za-z0-9_-]{8,160}$/.test(path)) throw safeError("fusion_read_route_invalid","只读请求无效。");
    return request(url,{method:"GET",signal});
  }
  async function patchPreferences() {
    if(!Number.isInteger(state.settingsRevision)||!state.csrfToken)throw new Error("fusion_settings_unavailable");
    const payload=await request(ROUTES.preferences,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({expected_revision:state.settingsRevision,preferences:{appearance:{theme:state.theme,density:state.density}}})});hydrateSettings(payload);return payload;
  }
  async function loadReleaseInfo() {
    try {const dto=await readOnlyJSON(ROUTES.uiMode),release=dto?.release;if(!release||typeof release!=="object")return false;state.release=release;const label=cleanText(release.label||release.version||"Auto Research",120),build=cleanText(release.build||release.build_number||"",40);q("#fusion-release-badge").textContent=`${label} · 本机数据 · AI 逐次授权`;q("#fusion-about-release").textContent=label;q("#fusion-about-build").textContent=build?`build ${build} · 版本由桌面发布清单管理`:"版本由桌面发布清单管理";return true;}catch(_error){return false;}
  }
  function literatureState(kind, message) {
    return `<div class="fusion-state-card large" data-literature-state="${kind}"><strong>${esc(message)}</strong><span>${kind==="error"?"请确认本机工作区可用后重试；系统不会自动发起收费 AI 请求。":"结果仅展示公开安全字段；写入与收费动作由你分别明确启动。"}</span></div>`;
  }
  function publicPaper(raw) {
    const id = Number(raw?.id);
    return Number.isSafeInteger(id) && id > 0 && String(raw?.title || "").trim() ? {id,title:String(raw.title).trim(),doi:String(raw.doi||"").trim(),year:raw.year||"",firstAuthor:String(raw.first_author||"").trim(),material:String(raw.material_focus||"").trim(),count:Math.max(0,Number(raw.six_row_count)||0),status:String(raw.six_workflow_label||raw.six_workflow_state||"只读"),requiresRescanConfirmation:raw?.requires_rescan_confirmation===true} : null;
  }
  const cleanText=(value,limit=8000)=>String(value??"").trim().slice(0,limit);
  const cleanList=(value,limit=200)=>Array.isArray(value)?value.slice(0,limit).map(item=>cleanText(item,800)).filter(Boolean):[];
  const cleanVariables=value=>value&&typeof value==="object"&&!Array.isArray(value)?Object.fromEntries(Object.entries(value).slice(0,200).map(([key,item])=>[cleanText(key,300),cleanText(item,1200)]).filter(([key])=>key)):{};
  const positiveId=value=>{const id=Number(value);return Number.isSafeInteger(id)&&id>0?id:null;};
  function setFixedDownloadLink(node,href,{visible=true}={}){if(!node)return false;const enabled=typeof href==="string"&&href.startsWith("/api/")&&!href.includes("\\")&&!href.includes("\n")&&!href.includes("\r");if(enabled){node.setAttribute("href",href);node.setAttribute("aria-disabled","false");node.setAttribute("tabindex","0");}else{node.removeAttribute?.("href");node.setAttribute("aria-disabled","true");node.setAttribute("tabindex","-1");}node.hidden=!visible;node.setAttribute("aria-hidden",visible?"false":"true");return enabled;}
  function currentPaperExportURL(format,paperId){return Number.isSafeInteger(paperId)&&paperId>0&&["csv","xlsx"].includes(format)?`/api/current-paper/export.${format}?paper_id=${paperId}`:"";}
  function workspaceSearchExportURL(kind,format,query){const base={item:"/api/six-export",finding:"/api/qualitative-export"}[kind];return base&&["csv","xlsx"].includes(format)?`${base}.${format}?q=${encodeURIComponent(String(query??""))}`:"";}
  function syncSearchExports(){const workspace=state.searchSource==="workspace",enabled=workspace&&state.searchMode==="precise"&&state.searchExportReady===true;for(const [selector,kind,format]of [["#fusion-search-item-csv","item","csv"],["#fusion-search-item-xlsx","item","xlsx"],["#fusion-search-finding-csv","finding","csv"],["#fusion-search-finding-xlsx","finding","xlsx"]])setFixedDownloadLink(q(selector),enabled?workspaceSearchExportURL(kind,format,state.searchQuery):"");const note=q("#fusion-search-export-note");if(note)note.textContent=!workspace?"当前资料源不使用本机批量导出；请在详情导出单条证据，或到资料包页导出集合。":enabled?`批量导出本机文献中的测量与结论${state.searchQuery?`：“${state.searchQuery}”`:"（全部）"}；表格和图片请在详情单独导出。`:"执行本机文献工作区精确检索后，可批量导出测量与结论；表格和图片请在详情单独导出。";}
  function evidenceExportIdentity(row){if(!row||!["item","finding","table","figure"].includes(row.type)||!["workspace","official","private"].includes(row.sourceScope))return null;if(row.sourceScope==="workspace"){const id=["item","finding"].includes(row.type)?row.itemId:row.assetId;return Number.isSafeInteger(id)&&id>0?{sourceScope:"workspace",sourceId:"workspace",entityType:row.type,entityUid:String(id)}:null;}return typeof row.sourceId==="string"&&row.sourceId&&typeof row.entityUid==="string"&&row.entityUid?{sourceScope:row.sourceScope,sourceId:row.sourceId,entityType:row.type,entityUid:row.entityUid}:null;}
  function evidenceExportURL(row,format){const identity=evidenceExportIdentity(row);if(!identity||!["csv","xlsx"].includes(format))return "";return `/api/desktop/evidence-export?source_scope=${encodeURIComponent(identity.sourceScope)}&source_id=${encodeURIComponent(identity.sourceId)}&entity_type=${encodeURIComponent(identity.entityType)}&entity_uid=${encodeURIComponent(identity.entityUid)}&format=${encodeURIComponent(format)}`;}
  function syncDetailEvidenceExports(row=null){setFixedDownloadLink(q("#fusion-detail-export-csv"),evidenceExportURL(row,"csv"));setFixedDownloadLink(q("#fusion-detail-export-xlsx"),evidenceExportURL(row,"xlsx"));}
  function syncDetailImageDownload(row=null){const visual=row&&row.sourceScope==="workspace"&&["table","figure"].includes(row.type)&&positiveId(row.assetId),node=q("#fusion-detail-download-image");return setFixedDownloadLink(node,visual?`${ROUTES.visualAsset}/${visual}/image`:"",{visible:Boolean(visual)});}
  function publicEvidence(raw) {
    const type=cleanText(raw?.entity_type||raw?.asset_type,20);if(!["item","finding","table","figure"].includes(type))return null;
    const sourceScope=["workspace","official","private"].includes(raw?.source_scope)?String(raw.source_scope):"workspace",publicWorkspace=sourceScope==="workspace"&&raw?.source_id==="workspace"&&typeof raw?.entity_uid==="string",sourceId=sourceScope==="workspace"?(publicWorkspace?"workspace":""):cleanText(raw?.source_id,500),entityUid=sourceScope==="workspace"?(publicWorkspace?cleanText(raw?.entity_uid,500):""):cleanText(raw?.entity_uid,500);
    if(sourceScope!=="workspace"&&(!sourceId||!entityUid))return null;
    const assetId=sourceScope==="workspace"&&["table","figure"].includes(type)?positiveId(raw?.id):null,itemId=sourceScope==="workspace"&&["item","finding"].includes(type)?positiveId(raw?.item_id??raw?.id):null,paperId=sourceScope==="workspace"?positiveId(raw?.paper_id):null;
    const imageCandidate=cleanText(raw?.image_url,500),imageUrl=assetId&&imageCandidate===`${ROUTES.visualAsset}/${assetId}/image`?imageCandidate:"",bboxCandidate=Array.isArray(raw?.bbox)?raw.bbox:[];
    const bbox=bboxCandidate.length===4&&bboxCandidate.every(value=>Number.isFinite(Number(value)))&&Number(bboxCandidate[2])>Number(bboxCandidate[0])&&Number(bboxCandidate[3])>Number(bboxCandidate[1])?bboxCandidate.map(Number):[];
    return {type,title:cleanText(raw?.meaning||raw?.display_title||raw?.display_name||raw?.label||raw?.caption||raw?.finding_text||"未命名证据",1200),value:cleanText(raw?.value_text||(type==="finding"?"定性结论":""),1000),findingText:cleanText(raw?.finding_text,8000),unit:cleanText(raw?.unit,300),page:raw?.source_page??raw?.page_start??"—",pageEnd:raw?.page_end??null,excerpt:cleanText(raw?.source_excerpt||raw?.original_source_excerpt||raw?.finding_text,8000),context:cleanText(raw?.context_explanation,8000),sourceContext:cleanText(raw?.source_context,8000),locator:cleanText(raw?.source_locator||raw?.original_source_locator,1000),bbox,articleTitle:cleanText(raw?.article_title,1200),doi:cleanText(raw?.doi,500),firstAuthor:cleanText(raw?.first_author,500),correspondingAuthor:cleanText(raw?.corresponding_author,500),label:cleanText(raw?.label,300),caption:cleanText(raw?.caption,8000),materials:cleanList(raw?.materials),conditions:cleanText(raw?.conditions_text,6000),methods:cleanText(raw?.methods_text,4000),quantities:cleanList(raw?.physical_quantities),variables:cleanVariables(raw?.variables),tags:cleanList(raw?.tags),linkedItemCount:Math.max(0,Math.min(100000,Number(raw?.linked_item_count)||0)),qualityStatus:cleanText(raw?.quality_gate_status,80),sourceKind:cleanText(raw?.source_kind,80),sourceScope,sourceId,entityUid,assetId,itemId,paperId,imageUrl,ref:cleanText(raw?.ref||raw?.agent_ref,80),bundleRef:cleanText(raw?.bundle_ref||raw?.bundleRef,20),bundleUid:cleanText(raw?.agent_bundle_uid||raw?.bundle_uid||raw?.bundleUid,256),paperUid:cleanText(raw?.paper_uid,500),collectionKind:sourceScope==="private"?cleanText(raw?.collection_kind,80):"",pdfAvailable:sourceScope==="private"&&raw?.pdf_available===true};
  }
  function publicTableStructure(raw,expectedEntityUid){
    if(raw?.schema_version!=="table-structure-version-v1"||raw.source_scope!=="workspace"||raw.source_id!=="workspace"||raw.entity_type!=="table"||String(raw.entity_uid)!==String(expectedEntityUid)||!Number.isSafeInteger(raw.version)||raw.version<1||!["candidate","manual_review","verified","rejected"].includes(raw.status)||!Array.isArray(raw.rows)||!Array.isArray(raw.reason_codes))return null;
    const rows=raw.rows.map(row=>Array.isArray(row)?row.map(value=>typeof value==="string"?value:null):null);if(!rows.length||rows.some(row=>!row||!row.length||row.some(value=>value===null))||new Set(rows.map(row=>row.length)).size!==1)return null;
    return {schemaVersion:"table-structure-version-v1",entityUid:String(expectedEntityUid),version:raw.version,status:raw.status,reasonCodes:raw.reason_codes.slice(0,100).map(value=>cleanText(value,120)).filter(Boolean),rows};
  }
  function tableStructureExportURL(structure,format){return structure?.status==="verified"&&/^[1-9][0-9]{0,18}$/.test(structure.entityUid)&&["csv","xlsx"].includes(format)?`${ROUTES.tableStructures}/export?entity_uid=${encodeURIComponent(structure.entityUid)}&format=${format}`:"";}
  function tableStructureGrid(structure,{editable=false}={}){const rows=structure?.rows||[];if(!rows.length)return detailState("empty","没有可显示的结构化行列","仍可依据上方原始截图进行核对。");return `<div class="fusion-table-structure-scroll" tabindex="0" aria-label="结构化表格行列"><table class="fusion-data-grid fusion-table-structure-grid"><tbody>${rows.map((row,rowIndex)=>`<tr><th scope="row">${rowIndex+1}</th>${row.map((value,columnIndex)=>`<td>${editable?`<input type="text" value="${esc(value)}" data-table-structure-cell data-row="${rowIndex}" data-column="${columnIndex}" aria-label="第 ${rowIndex+1} 行第 ${columnIndex+1} 列">`:esc(value)}</td>`).join("")}</tr>`).join("")}</tbody></table></div>`;}
  function tableStructureTerminal(status,errorCode=""){
    if(status==="loading")return detailState("loading","正在读取结构化行列","原始截图已经可以查看；结构读取不会阻挡截图核对。");
    if(errorCode==="table_structure_service_not_found")return detailState("empty","尚无结构化行列","仍可核对原始截图；系统不会把截图伪装成完整表格。");
    if(["table_structure_service_pending","table_structure_service_unverified"].includes(errorCode))return detailState("empty","结构仍待人工核验","原始截图保持可见；刷新后可继续人工核验。");
    return detailState("error","结构化行列暂时不可用","原始截图和其他公开证据仍可继续核对。");
  }
  function tableStructureHTML(tabId,row,payload={},interactive=false){
    if(row?.sourceScope!=="workspace"||row?.type!=="table"||!positiveId(row.assetId))return "";const structure=payload.tableStructure,stateName=payload.tableStructureState||"loading",errorCode=payload.tableStructureError||"";
    if(!structure)return `<section class="fusion-table-structure" data-table-structure-tab="${esc(tabId)}"><header><div><span>结构化行列</span><h2>表格结构</h2></div></header>${tableStructureTerminal(stateName,errorCode)}</section>`;
    const pending=["candidate","manual_review"].includes(structure.status),verified=structure.status==="verified",rejected=structure.status==="rejected",actions=pending?(interactive?`<div class="fusion-table-structure-actions" role="group" aria-label="表格结构审核"><button type="button" class="fusion-primary-action" data-table-structure-action="approve" data-table-structure-tab="${esc(tabId)}">确认结构</button><button type="button" data-table-structure-action="correct" data-table-structure-tab="${esc(tabId)}">编辑后确认</button><button type="button" data-table-structure-action="reject" data-table-structure-tab="${esc(tabId)}">不采用结构</button></div>`:`<button type="button" data-table-structure-primary="${esc(tabId)}">在主栏审核</button>`):"",exports=verified?`<div class="fusion-table-structure-exports"><a href="${esc(tableStructureExportURL(structure,"csv"))}" download rel="noopener">结构 CSV</a><a href="${esc(tableStructureExportURL(structure,"xlsx"))}" download rel="noopener">结构 XLSX</a></div>`:"";
    return `<section class="fusion-table-structure" data-table-structure-tab="${esc(tabId)}" data-table-structure-status="${esc(structure.status)}"><header><div><span>${verified?"已核验":pending?"待人工核验":rejected?"未采用":"结构化行列"}</span><h2>结构化行列</h2></div>${actions}${exports}</header>${rejected?detailState("empty","此结构未采用","原始截图仍保留，可继续作为权威视觉依据。"):`${structure.reasonCodes.length?`<p class="fusion-table-structure-reasons">${esc(structure.reasonCodes.join(" · "))}</p>`:""}${tableStructureGrid(structure,{editable:interactive&&pending})}`}<p class="fusion-table-structure-status" aria-live="polite"></p></section>`;
  }
  function tableStructureErrorCode(error){return ["table_structure_service_not_found","table_structure_service_pending","table_structure_service_unverified","table_structure_service_version_conflict","table_structure_service_corrupt","table_structure_service_unavailable"].includes(error?.code)?error.code:"table_structure_service_unavailable";}
  function tableStructureTab(tabId){return documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId)||null;}
  function updateTableStructureTab(tabId,patch){const tab=tableStructureTab(tabId);if(!tab)return null;return documentTabs.update(tabId,{payload:{...tab.payload,...patch}});}
  async function loadTableStructure(tabId,row){
    if(!tabId||row?.sourceScope!=="workspace"||row?.type!=="table"||!positiveId(row.assetId))return false;const generation=(tableStructureRequests.get(tabId)||0)+1;tableStructureRequests.set(tabId,generation);updateTableStructureTab(tabId,{tableStructure:null,tableStructureState:"loading",tableStructureError:""});renderDocumentTabs();
    try{const raw=await readOnlyJSON(`${ROUTES.tableStructures}?entity_uid=${encodeURIComponent(String(row.assetId))}&include_unverified=1`),structure=publicTableStructure(raw,String(row.assetId));if(!structure)throw safeError("table_structure_service_corrupt","表格结构响应无效。");if(tableStructureRequests.get(tabId)!==generation)return false;const tab=tableStructureTab(tabId);if(!tab||evidenceIdentity(tab.payload?.row)!==evidenceIdentity(row))return false;updateTableStructureTab(tabId,{tableStructure:structure,tableStructureState:"ready",tableStructureError:""});renderDocumentTabs();return true;}
    catch(error){if(tableStructureRequests.get(tabId)!==generation)return false;const tab=tableStructureTab(tabId);if(!tab||evidenceIdentity(tab.payload?.row)!==evidenceIdentity(row))return false;updateTableStructureTab(tabId,{tableStructure:null,tableStructureState:"terminal",tableStructureError:tableStructureErrorCode(error)});renderDocumentTabs();return false;}
  }
  function tableStructurePanel(tabId){return qa("[data-table-structure-tab]").find(node=>node.dataset.tableStructureTab===tabId)||null;}
  function tableStructureCorrectedRows(tabId){const panel=tableStructurePanel(tabId),structure=tableStructureTab(tabId)?.payload?.tableStructure;if(!panel||!structure)return null;const rows=structure.rows.map(row=>row.map(()=>""));for(const input of panel.querySelectorAll("[data-table-structure-cell]")){const row=Number(input.dataset.row),column=Number(input.dataset.column);if(!Number.isInteger(row)||!Number.isInteger(column)||!rows[row]||column>=rows[row].length)return null;rows[row][column]=String(input.value??"");}return rows;}
  async function reviewTableStructure(tabId,operation){
    const tab=tableStructureTab(tabId),structure=tab?.payload?.tableStructure,row=tab?.payload?.row;if(!tab||!structure||row?.sourceScope!=="workspace"||row?.type!=="table"||!positiveId(row.assetId)||!["candidate","manual_review"].includes(structure.status)||!["approve","correct","reject"].includes(operation))return false;const labels={approve:"确认当前结构",correct:"提交编辑后的结构",reject:"不采用当前结构"};if(!globalThis.confirm?.(`${labels[operation]}？原始截图不会被修改。`))return false;const rows=operation==="correct"?tableStructureCorrectedRows(tabId):null;if(operation==="correct"&&!rows)return false;const generation=(tableStructureRequests.get(tabId)||0)+1;tableStructureRequests.set(tabId,generation);const status=tableStructurePanel(tabId)?.querySelector?.(".fusion-table-structure-status");if(status)status.textContent="正在保存审核结果…";try{const body={entity_uid:String(row.assetId),expected_version:structure.version,operation,note:""};if(operation==="correct")body.rows=rows;const raw=await request(ROUTES.tableStructureReviews,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),next=publicTableStructure(raw,String(row.assetId));if(!next)throw safeError("table_structure_service_corrupt","表格结构响应无效。");if(tableStructureRequests.get(tabId)!==generation)return false;const current=tableStructureTab(tabId);if(!current||evidenceIdentity(current.payload?.row)!==evidenceIdentity(row))return false;updateTableStructureTab(tabId,{tableStructure:next,tableStructureState:"ready",tableStructureError:""});renderDocumentTabs();return true;}catch(error){if(tableStructureRequests.get(tabId)===generation){const current=tableStructurePanel(tabId)?.querySelector?.(".fusion-table-structure-status");if(current)current.textContent=error?.code==="table_structure_service_version_conflict"?"结构版本已变化，请刷新详情后重试。":"审核没有完成，原始截图和当前结构未改变。";}return false;}
  }
  function moveTableStructureReviewToPrimary(tabId){const tab=tableStructureTab(tabId);if(!tab)return false;pinDocumentTab(tabId);if(tab.groupId!=="primary"&&!documentTabs.move(tabId,"primary"))return false;paneController?.expandEditor("primary");activateDocumentTab(tabId);return true;}
  function bindTableStructureActions(tabId){if(!tabId)return;const panel=tableStructurePanel(tabId);if(!panel||panel.dataset.tableStructureBound==="true")return;panel.dataset.tableStructureBound="true";panel.querySelectorAll?.("[data-table-structure-action]").forEach(button=>button.addEventListener("click",()=>void reviewTableStructure(tabId,button.dataset.tableStructureAction)));panel.querySelector?.("[data-table-structure-primary]")?.addEventListener("click",()=>moveTableStructureReviewToPrimary(tabId));}
  const REVIEW_FIELDS=Object.freeze({item:new Set(["value_text","meaning","unit","context_explanation","source_page","source_locator","source_excerpt","evidence_type","source_precision"]),finding:new Set(["finding_text","value_text","meaning","context_explanation","source_page","source_locator","source_excerpt"]),table:new Set(["display_name","label","caption","context_explanation","conditions_text","methods_text","source_page","page_start","page_end","source_locator","source_excerpt","physical_quantities","materials","variables","tags"]),figure:new Set(["display_name","label","caption","context_explanation","conditions_text","methods_text","source_page","page_start","page_end","source_locator","source_excerpt","physical_quantities","materials","variables","tags"])});
  const REVIEW_FIELD_LABELS=Object.freeze({value_text:"数值",meaning:"物理意义",unit:"单位",context_explanation:"上下文解释",source_page:"原文页",source_locator:"原文定位",source_excerpt:"原文短摘录",evidence_type:"证据类型",source_precision:"来源精度",finding_text:"结论正文",display_name:"显示名称",label:"标签",caption:"图注 / 表注",conditions_text:"条件",methods_text:"方法",page_start:"起始页",page_end:"结束页",physical_quantities:"物理量",materials:"材料",variables:"变量",tags:"标签集"});
  const REVIEW_SCORE_LABELS=Object.freeze({agreement:"一致性",factuality:"事实性",completeness:"完整性",evidence:"证据性",overall:"综合"});
  function cleanReviewValue(value,depth=0){if(depth>3)return null;if(value===null||typeof value==="boolean"||typeof value==="number"&&Number.isFinite(value))return value;if(typeof value==="string")return cleanText(value,2000);if(Array.isArray(value))return value.slice(0,32).map(item=>cleanReviewValue(item,depth+1));if(value&&typeof value==="object")return Object.fromEntries(Object.entries(value).slice(0,32).map(([key,item])=>[cleanText(key,120),cleanReviewValue(item,depth+1)]).filter(([key])=>key));return null;}
  function publicReviewCandidate(raw,type){if(raw===null)return null;if(!raw||typeof raw!=="object"||Array.isArray(raw)||!REVIEW_FIELDS[type])return null;const candidate={};for(const key of REVIEW_FIELDS[type])if(Object.hasOwn(raw,key)){const value=cleanReviewValue(raw[key]);if(value!==null)candidate[key]=value;}return candidate;}
  function publicReviewQueue(raw){if(raw?.schema_version!=="review-queue-v1"||raw.source_scope!=="workspace"||raw.source_id!=="workspace"||!Number.isInteger(raw.total)||raw.total<0||raw.total>500||!Array.isArray(raw.items)||raw.items.length>500)return null;const items=raw.items.map((item,index)=>{const type=cleanText(item?.entity_type,20),paperUid=cleanText(item?.paper_uid,80),token=cleanText(item?.review_token,140),scores=item?.scores,operations=item?.allowed_operations;if(!REVIEW_FIELDS[type]||!/^paper_[0-9a-f]{32}$/.test(paperUid)||!/^rq_[A-Za-z0-9_-]{32,96}$/.test(token)||!Number.isFinite(item?.expires_at)||item.source_scope!=="workspace"||item.source_id!=="workspace"||!item.paper||typeof item.paper!=="object"||!scores||typeof scores!=="object"||!operations||typeof operations!=="object")return null;const safeScores={};for(const key of Object.keys(REVIEW_SCORE_LABELS)){const value=Number(scores[key]);if(!Number.isFinite(value)||value<0||value>1)return null;safeScores[key]=value;}const candidate=publicReviewCandidate(item.candidate,type),alternate=publicReviewCandidate(item.alternate,type);if(!candidate)return null;return {reviewToken:token,expiresAt:Number(item.expires_at),sourceScope:"workspace",sourceId:"workspace",paperUid,paper:{title:cleanText(item.paper.title,1200),doi:cleanText(item.paper.doi,500)},entityType:type,candidate,alternate,conflictReason:cleanText(item.conflict_reason,2000),scores:safeScores,allowedOperations:{approve:operations.approve?.available===true,reject:operations.reject?.available===true,correct:operations.correct?.available===true},reviewOrdinal:index};});if(items.some(item=>item===null)||raw.total!==items.length)return null;return {schemaVersion:"review-queue-v1",total:raw.total,items};}
  const reviewDisplayValue=value=>typeof value==="string"?value:JSON.stringify(value,null,2);
  function reviewCandidateColumn(title,candidate){if(!candidate)return `<section class="fusion-review-candidate-column empty"><h2>${esc(title)}</h2><p>后端没有提供可对照的备选内容。</p></section>`;const rows=Object.entries(candidate).map(([key,value])=>`<div><dt>${esc(REVIEW_FIELD_LABELS[key]||key)}</dt><dd>${esc(reviewDisplayValue(value))}</dd></div>`).join("");return `<section class="fusion-review-candidate-column"><h2>${esc(title)}</h2><dl>${rows}</dl></section>`;}
  function reviewInspectorHTML(item,status=""){if(!item)return `<section class="fusion-inspector-section"><span>待审核</span><h2>尚未选择候选</h2><p>打开待审核列表后，候选状态会只显示在文献检查器中。</p></section>`;return `<section class="fusion-inspector-section"><span>待审核候选 · ${esc(EVIDENCE_LABELS[item.entityType]||item.entityType)}</span><h2>${esc(item.paper.title||"未命名论文")}</h2><p>${esc(item.conflictReason||"需要人工核对主候选与备选。")}</p></section><section class="fusion-inspector-section"><span>当前状态</span><p>${esc(status||"等待你确认。凭证不会写入页面缓存。")}</p></section>`;}
  function reviewCorrectionFields(item){const fields=Object.keys(item.candidate).filter(key=>REVIEW_FIELDS[item.entityType].has(key));return fields.map(key=>{const value=item.candidate[key],complex=value&&typeof value==="object";return `<label><span>${esc(REVIEW_FIELD_LABELS[key]||key)}</span><textarea data-review-correction-field="${esc(key)}" data-review-complex="${complex?"true":"false"}" rows="${key==="source_excerpt"||key==="caption"?4:2}">${esc(complex?JSON.stringify(value,null,2):String(value??""))}</textarea></label>`;}).join("");}
  function reviewCandidateHTML(item,{interactive=true,status="",pending=false,retry=false,refresh=false}={}){if(!item)return detailState("empty","候选内容未在当前会话中恢复","审核凭证与候选正文不写入本地标签缓存。请刷新待审核列表。");const scores=Object.entries(REVIEW_SCORE_LABELS).map(([key,label])=>`<div><dt>${label}</dt><dd>${Math.round(item.scores[key]*100)}%</dd></div>`).join(""),actions=interactive?`<section class="fusion-review-actions"><h2>审核操作</h2><p id="fusion-review-action-status" data-kind="${status?"warning":"info"}">${esc(status||"操作前会再次明确确认；重复点击会被阻止。")}</p><div class="fusion-action-cluster"><button type="button" class="fusion-primary-action" data-review-action="approve"${!item.allowedOperations.approve||pending?" disabled":""}>批准</button><button type="button" data-review-action="reject"${!item.allowedOperations.reject||pending?" disabled":""}>不采用</button></div>${item.allowedOperations.correct?`<details class="fusion-review-correction"><summary>纠正候选公开字段</summary><div>${reviewCorrectionFields(item)}</div><button type="button" data-review-action="correct"${pending?" disabled":""}>确认纠正</button></details>`:""}${retry?`<button type="button" class="fusion-primary-action" data-review-retry>重试恢复搜索索引</button>`:""}${refresh?`<button type="button" data-review-refresh-current>刷新待审核列表</button>`:""}</section>`:"<p>在主编辑组打开此标签后可执行审核操作。</p>";return `<article class="fusion-review-document" data-review-entity-type="${esc(item.entityType)}"><header><span>人工审核 · ${esc(EVIDENCE_LABELS[item.entityType]||item.entityType)}</span><h1>${esc(item.paper.title||"未命名论文")}</h1><p>${esc(item.paper.doi||"未提供 DOI")}</p></header><section class="fusion-review-conflict"><strong>冲突原因</strong><p>${esc(item.conflictReason||"后端未提供额外说明。")}</p></section><div class="fusion-review-comparison">${reviewCandidateColumn("主候选",item.candidate)}${reviewCandidateColumn("备选",item.alternate)}</div><section class="fusion-review-scores"><h2>五项评分</h2><dl>${scores}</dl></section>${actions}</article>`;}
  function reviewTabIdentity(item){return {paperUid:item.paperUid,sourceScope:"workspace",sourceId:"workspace",entityType:item.entityType,reviewOrdinal:String(item.reviewOrdinal)};}
  function updateReviewCount(){const node=q("#fusion-review-queue-count");if(node)node.textContent=state.reviewQueue.loading?"…":String(state.reviewQueue.total);}
  function renderReviewQueue(){const host=q("#fusion-literature-content");if(!host)return false;state.reviewQueue.mode=true;host.setAttribute("aria-busy",state.reviewQueue.loading?"true":"false");if(state.reviewQueue.loading){host.innerHTML=literatureState("loading","正在读取待审核候选…");return true;}if(!state.reviewQueue.items.length){host.innerHTML=`<section class="fusion-review-queue"><header><div><span>人工质量门</span><h1>待审核</h1></div><button type="button" id="fusion-review-refresh">刷新列表</button></header>${detailState("empty","当前没有待审核候选","已发布证据与未通过质量门的候选仍保持分离。")}</section>`;q("#fusion-review-refresh")?.addEventListener("click",()=>void loadReviewQueue(true));return true;}host.innerHTML=`<section class="fusion-review-queue"><header><div><span>人工质量门</span><h1>待审核</h1><p>${state.reviewQueue.total} 项候选；批准、不采用或纠正都需要你明确确认。</p></div><button type="button" id="fusion-review-refresh">刷新列表</button></header><div class="fusion-review-queue-list">${state.reviewQueue.items.map((item,index)=>`<button type="button" data-review-index="${index}"><span>${esc(EVIDENCE_LABELS[item.entityType]||item.entityType)} · ${esc(item.paper.doi||"未提供 DOI")}</span><strong>${esc(item.paper.title||"未命名论文")}</strong><small>${esc(item.conflictReason||"需要人工核对")}</small><b>${Math.round(item.scores.overall*100)}%</b></button>`).join("")}</div></section>`;q("#fusion-review-refresh")?.addEventListener("click",()=>void loadReviewQueue(true));qa("[data-review-index]").forEach(button=>button.addEventListener("click",()=>openReviewCandidate(Number(button.dataset.reviewIndex),button)));return true;}
  async function loadReviewQueue(force=false){if(state.reviewQueue.loading)return false;if(state.reviewQueue.loaded&&!force){updateReviewCount();if(state.reviewQueue.mode)renderReviewQueue();return true;}const generation=++state.reviewQueue.request;state.reviewQueue.loading=true;updateReviewCount();if(state.reviewQueue.mode)renderReviewQueue();try{const result=publicReviewQueue(await readOnlyJSON(ROUTES.reviewQueue));if(!result)throw safeError("review_queue_invalid","待审核列表格式无效。");if(generation!==state.reviewQueue.request)return false;state.reviewQueue.items=result.items;state.reviewQueue.total=result.total;state.reviewQueue.loaded=true;return true;}catch(error){if(generation===state.reviewQueue.request){state.reviewQueue.items=[];state.reviewQueue.total=0;state.reviewQueue.loaded=true;if(state.reviewQueue.mode){q("#fusion-literature-content").innerHTML=literatureState("error",`${cleanText(error?.code||"review_queue_unavailable",80)} · 待审核列表暂时不可用`);}}return false;}finally{if(generation===state.reviewQueue.request){state.reviewQueue.loading=false;updateReviewCount();if(state.reviewQueue.mode&&state.reviewQueue.loaded&&q("#fusion-literature-content")?.querySelector?.("[data-literature-state='loading']"))renderReviewQueue();}}}
  async function openReviewQueue(){state.reviewQueue.mode=true;switchView("paper",{focus:false,activateTab:false});if(!state.reviewQueue.loaded)await loadReviewQueue();else renderReviewQueue();q("#fusion-literature-content")?.focus?.({preventScroll:true});return true;}
  function bindReviewActions(tabId,item){qa("[data-review-action]").forEach(button=>button.addEventListener("click",()=>void performReviewAction(tabId,button.dataset.reviewAction)));q("[data-review-retry]")?.addEventListener("click",()=>void retryReviewAction(tabId));q("[data-review-refresh-current]")?.addEventListener("click",async()=>{await loadReviewQueue(true);closeReviewDetail({focus:false});renderReviewQueue();q("#fusion-review-refresh")?.focus?.({preventScroll:true});});}
  function renderReviewDetail(tab,item,{status="",retry=false,refresh=false}={}){const body=q("#fusion-evidence-detail-body");if(!body)return false;q("#fusion-detail-heading").textContent=item?.paper.title||"待审核候选";q("#fusion-detail-identity").textContent=`人工审核 · ${item?EVIDENCE_LABELS[item.entityType]||item.entityType:"需要刷新"}`;syncDetailImageDownload();syncDetailEvidenceExports();body.setAttribute("aria-busy","false");body.innerHTML=item?reviewCandidateHTML(item,{status,pending:state.reviewQueue.pending.has(tab.tabId),retry,refresh}):`${reviewCandidateHTML(null)}<button type="button" id="fusion-review-refresh-current">刷新待审核列表</button>`;state.inspectorState.paper=item?{kind:"review-candidate",item,status}:null;updateInspector();bindReviewActions(tab.tabId,item);return true;}
  function showReviewDetail(tab,item){state.reviewQueue.mode=true;state.reviewDetail=item||null;state.reviewDetailTabId=tab.tabId;state.evidenceDetailOpen=false;state.evidenceDetail=null;qa("[data-view-panel]").forEach(panel=>{panel.hidden=true;panel.classList.remove("active");});q("#fusion-evidence-detail").hidden=false;q("#fusion-detail-pdf").hidden=true;q("#fusion-evidence-detail-body").hidden=false;return renderReviewDetail(tab,item,{status:tab.payload?.reviewStatus||"",retry:state.reviewQueue.retry.has(tab.tabId)});}
  function openReviewCandidate(index,focusReturn=null){const item=state.reviewQueue.items[index];if(!item)return false;const tab=openDocumentTab("review-candidate",`待审核 · ${item.paper.title||EVIDENCE_LABELS[item.entityType]}`,reviewTabIdentity(item),{ownerView:"paper",payload:{item,focusReturn,reviewStatus:""},pin:true});if(!tab)return false;return showReviewDetail(tab,item);}
  function closeReviewDetail({focus=true}={}){if(!state.reviewDetailTabId&&!state.reviewDetail)return false;const tab=documentTabs?.snapshot().tabs.find(value=>value.tabId===state.reviewDetailTabId),focusReturn=tab?.payload?.focusReturn;state.reviewDetail=null;state.reviewDetailTabId="";state.inspectorState.paper=null;q("#fusion-evidence-detail").hidden=true;qa("[data-view-panel]").forEach(panel=>{const active=panel.dataset.viewPanel==="paper";panel.hidden=!active;panel.classList.toggle("active",active);});if(state.reviewQueue.mode)renderReviewQueue();if(focus&&focusReturn?.isConnected)focusReturn.focus({preventScroll:true});return true;}
  function reviewCorrectionPayload(item){const fields={};for(const node of qa("[data-review-correction-field]")){const key=node.dataset.reviewCorrectionField;if(!REVIEW_FIELDS[item.entityType]?.has(key)||!Object.hasOwn(item.candidate,key))throw safeError("review_queue_invalid","纠正字段无效。");const raw=String(node.value??"");if(node.dataset.reviewComplex==="true"){try{fields[key]=JSON.parse(raw);}catch(_error){throw safeError("review_queue_invalid",`${REVIEW_FIELD_LABELS[key]||key} 的格式无效。`);}}else fields[key]=raw;}if(!Object.keys(fields).length)throw safeError("review_queue_invalid","没有可纠正的公开字段。");return fields;}
  async function refreshAfterReview(){const paperId=positiveId(state.paper?.id);state.searchRequest+=1;state.searchResults=[];state.searchTotal=0;state.searchHasMore=false;state.searchExportReady=false;syncSearchExports();if(!paperId)return true;try{const [papersRaw,evidenceRaw]=await Promise.all([readOnlyJSON(ROUTES.papers),readOnlyJSON(`${ROUTES.evidence}?q=&paper_ids=${paperId}&limit=1000&quality=published`)]),papers=(Array.isArray(papersRaw)?papersRaw:[]).map(publicPaper).filter(Boolean),paper=papers.find(item=>item.id===paperId),evidence=(Array.isArray(evidenceRaw?.rows)?evidenceRaw.rows:[]).map(publicEvidence).filter(Boolean);if(!paper)return false;state.papers=papers;state.paper=paper;state.evidence=evidence;state.literatureStatus="ready";renderPaperCatalog();const tabId=documentTabId("paper",{paperId:String(paperId)});if(documentTabs?.snapshot().tabs.some(tab=>tab.tabId===tabId))documentTabs.update(tabId,{payload:{paperId,paper,evidenceCount:evidence.length,evidence}});renderDocumentTabs();return true;}catch(_error){return false;}}
  async function performReviewAction(tabId,action,{bodyOverride=null}={}){const tab=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId),item=tab?.payload?.item;if(!item||state.reviewQueue.pending.has(tabId)||!["approve","reject","correct"].includes(action))return null;let body=bodyOverride;if(!body){if(!item.allowedOperations[action])return null;try{body={review_token:item.reviewToken,action,...(action==="correct"?{fields:reviewCorrectionPayload(item)}:{})};}catch(error){renderReviewDetail(tab,item,{status:error.message});return null;}}const labels={approve:"批准这条候选",reject:"不采用这条候选",correct:"按当前字段纠正并批准这条候选"};if(typeof globalThis.confirm!=="function"||!globalThis.confirm(`${labels[action]}？\n\n这会修改本机文献工作区，完成后会刷新待审核列表。`))return null;state.reviewQueue.pending.add(tabId);renderReviewDetail(tab,item,{status:"正在保存审核决定…"});try{const result=await request(ROUTES.reviewActions,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),current=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId);if(!current||current.payload?.item?.reviewToken!==item.reviewToken)return null;if(result?.schema_version!=="review-result-v1"||!["saved","saved_index_pending"].includes(result.status)||result.paper_uid!==item.paperUid||result.entity_type!==item.entityType)throw safeError("review_result_invalid","审核结果无效。");const pending=result.status==="saved_index_pending",message=pending?"数据已保存，索引待恢复。":"审核决定已保存，搜索索引已更新。";if(pending&&result.next_action==="retry_same_request")state.reviewQueue.retry.set(tabId,body);else state.reviewQueue.retry.delete(tabId);documentTabs.update(tabId,{payload:{...current.payload,reviewStatus:message,result,needsQueueRefresh:false}});await Promise.all([loadReviewQueue(true),refreshAfterReview()]);const latest=documentTabs.snapshot().tabs.find(value=>value.tabId===tabId);if(state.reviewDetailTabId===tabId)renderReviewDetail(latest,item,{status:message,retry:pending});setLiteratureActionStatus(message,pending?"warning":"success");return result;}catch(error){const current=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId);if(!current)return null;const refresh=error?.nextAction==="refresh_queue"||["review_token_expired","review_candidate_changed"].includes(error?.code),message=`${cleanText(error?.code||"review_queue_unavailable",80)} · ${cleanText(error?.message||"审核操作未完成。",300)}${error?.stage?` · 阶段 ${cleanText(error.stage,80)}`:""}`;documentTabs.update(tabId,{payload:{...current.payload,reviewStatus:message,needsQueueRefresh:refresh}});return null;}finally{state.reviewQueue.pending.delete(tabId);const current=documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId);if(current&&state.reviewDetailTabId===tabId)renderReviewDetail(current,item,{status:current.payload?.reviewStatus||"",retry:state.reviewQueue.retry.has(tabId),refresh:current.payload?.needsQueueRefresh===true});}}
  async function retryReviewAction(tabId){const body=state.reviewQueue.retry.get(tabId);if(!body)return null;return performReviewAction(tabId,body.action,{bodyOverride:body});}
  function renderPaperCatalog({revealSelected=false}={}) {
    const host=q("#fusion-paper-catalog");if(!host)return;
    host.setAttribute("aria-busy","false");
    if(!state.papers.length){host.innerHTML='<div class="fusion-state-card" data-literature-state="empty"><strong>当前没有可浏览的文献</strong><span>请使用中央顶部“导入 PDF”加入本机文献工作区。</span></div>';return;}
    const needle=state.paperCatalogQuery.toLocaleLowerCase(),papers=needle?state.papers.filter(paper=>[paper.title,paper.firstAuthor,paper.doi].some(value=>String(value||"").toLocaleLowerCase().includes(needle))):state.papers;
    if(!papers.length){host.innerHTML='<div class="fusion-state-card" data-literature-state="empty"><strong>没有匹配的文献</strong><span>可按题目、作者或 DOI 搜索；清空输入可恢复全部文献。</span></div>';return;}
    host.innerHTML=papers.map(paper=>`<button type="button" class="fusion-tree-row${paper.id===state.paper?.id?" active":""}" data-paper-id="${paper.id}" aria-pressed="${paper.id===state.paper?.id?"true":"false"}" title="${esc(paper.title)}"><span>${esc(paper.title)}</span><b>${paper.count}</b></button>`).join("");
    qa("#fusion-paper-catalog [data-paper-id]").forEach(button=>{button.addEventListener("click",event=>{if(event.detail>1)return;void selectPaper(Number(button.dataset.paperId),{reveal:true,preview:true});});button.addEventListener("dblclick",()=>{if(!pinDocumentIdentity("paper",{paperId:String(button.dataset.paperId)}))void selectPaper(Number(button.dataset.paperId),{reveal:true,preview:false,pin:true});});});
    if(revealSelected)q(`[data-paper-id="${state.paper?.id||0}"]`)?.scrollIntoView?.({block:"nearest",inline:"nearest"});
  }
  function resetEvidenceCounts(value="—") { ["item","finding","table","figure"].forEach(type=>{const node=q(`#fusion-count-${type}`);if(node)node.textContent=String(value);}); }
  function renderLiterature() {
    const host=q("#fusion-literature-content");if(!host)return;host.setAttribute("aria-busy","false");
    if(state.literatureStatus==="error"){resetEvidenceCounts();q("#fusion-current-paper-state").textContent="读取失败";q("#fusion-current-paper-count").textContent="—";host.innerHTML=literatureState("error","真实文献暂时无法载入");return;}
    if(!state.paper){resetEvidenceCounts(0);q("#fusion-current-paper-state").textContent="空";q("#fusion-current-paper-count").textContent="0";host.innerHTML=literatureState("empty","当前没有可浏览的文献");return;}
    const paper=state.paper,counts={item:0,finding:0,table:0,figure:0};state.evidence.forEach(row=>counts[row.type]+=1);
    ["item","finding","table","figure"].forEach(type=>{const node=q(`#fusion-count-${type}`);if(node)node.textContent=String(counts[type]);});
    q("#fusion-current-paper-state").textContent=paper.status||"只读";q("#fusion-current-paper-count").textContent=String(state.evidence.length);
    const paperSelection=state.selectionByView.paper,indexSelected=paperSelection?.index??0,lines=state.evidence.map((row,index)=>`<button type="button" class="fusion-evidence-line${index===indexSelected?" selected":""}" data-evidence-index="${index}" aria-selected="${index===indexSelected?"true":"false"}" aria-label="打开${esc(row.title)}的证据详情"><b>${esc(row.value||row.label||({table:"原始表格",figure:"论文图片"})[row.type]||"证据")}${row.unit?` <small>${esc(row.unit)}</small>`:""}</b><span>${esc(row.title)} · 第 ${esc(row.page)} 页</span><em>${({item:"测量条目",finding:"研究结论",table:"完整表格",figure:"论文图片"})[row.type]}</em></button>`).join("");
    host.innerHTML=`<article class="fusion-paper-heading"><span>文献资料 · 当前资料库</span><h1>${esc(paper.title)}</h1><p>${esc([paper.firstAuthor,paper.year,paper.doi,paper.material].filter(Boolean).join(" · ")||"公开元数据未提供")}</p><dl><div><dt>处理状态</dt><dd>${esc(paper.status)}</dd></div><div><dt>证据条目</dt><dd>${state.evidence.length}</dd></div><div><dt>访问方式</dt><dd>安全浏览</dd></div></dl></article><section class="fusion-readonly-block"><header><strong>四类证据概览</strong><span>仅查看</span></header>${lines||'<div class="fusion-state-card" data-literature-state="empty"><strong>这篇文献暂无公开证据</strong><span>目录信息仍可浏览。</span></div>'}</section>`;
    qa("[data-evidence-index]").forEach(button=>{button.addEventListener("click",event=>{if(event.detail>1)return;void openEvidenceDetail(Number(button.dataset.evidenceIndex),button,{preview:true});});button.addEventListener("dblclick",()=>{const index=Number(button.dataset.evidenceIndex),row=state.evidence[index];if(row&&!pinDocumentIdentity(privateTableIdentity(row)?"personal-table":"evidence",documentIdentity(row)))void openEvidenceDetail(index,button,{preview:false,pin:true});});});
    renderSearchEvidence();selectEvidence(Math.min(indexSelected,Math.max(0,state.evidence.length-1)),{focus:false,view:"paper"});
  }
  function renderSearchEvidence() {
    const host=q("#fusion-search-results");if(!host)return;const rows=state.searchResults,selected=state.selectionByView.search?.index??0,labels={item:"测量条目",finding:"研究结论",table:"完整表格",figure:"论文图片"},sourceLabels={workspace:"本机文献",official:"官方资料库",private:"我的实验"};
    const groups=["item","finding","table","figure"].map(type=>({type,rows:rows.map((row,index)=>({row,index})).filter(entry=>entry.row.type===type)})).filter(group=>group.rows.length);
    const results=groups.map(group=>`<section class="fusion-result-group" data-result-group="${group.type}"><header><h2>${labels[group.type]}</h2><span>${group.rows.length} 条</span></header>${group.rows.map(({row,index})=>`<button type="button" class="fusion-result${index===selected?" selected":""}" role="option" aria-selected="${index===selected?"true":"false"}" data-search-evidence-index="${index}" aria-label="打开${esc(row.title)}的证据详情"><span>${labels[row.type]} · ${sourceLabels[row.sourceScope]}</span><b>${esc(row.value||row.label||({table:"原始表格",figure:"论文图片"})[row.type]||"证据")}${row.unit?` <small>${esc(row.unit)}</small>`:""}</b><h3>${esc(row.title)}</h3><span class="fusion-result-meta">${esc(row.articleTitle||state.paper?.title||"")} · 第 ${esc(row.page)} 页</span>${row.excerpt?`<span class="fusion-result-excerpt">${esc(row.excerpt)}</span>`:""}</button>`).join("")}</section>`).join("");
    const terminal=state.literatureStatus==="error"?literatureState("error","真实证据暂时无法载入"):literatureState("empty",state.paper?"当前论文暂无公开证据":"当前没有可浏览的文献");
    host.innerHTML=results||terminal;
    if(state.searchHasMore)host.insertAdjacentHTML("beforeend",`<button type="button" class="fusion-load-more" id="fusion-search-load-more">继续加载（已显示 ${rows.length} / ${state.searchTotal}）</button>`);
    qa("[data-search-evidence-index]").forEach(button=>{button.addEventListener("click",event=>{if(event.detail>1)return;void openEvidenceDetail(Number(button.dataset.searchEvidenceIndex),button,{preview:true});});button.addEventListener("dblclick",()=>{const index=Number(button.dataset.searchEvidenceIndex),row=state.searchResults[index];if(row&&!pinDocumentIdentity(privateTableIdentity(row)?"personal-table":"evidence",documentIdentity(row)))void openEvidenceDetail(index,button,{preview:false,pin:true});});});
    q("#fusion-search-load-more")?.addEventListener("click",()=>void runPreciseSearch({append:true}));
  }
  function selectEvidence(index,{focus=true,view=state.view}={}) {
    const rows=view==="search"?state.searchResults:state.evidence,row=rows[index];if(!row){state.selectionByView[view]=null;state.inspectorState[view]=null;if(view===state.view)state.selectedEvidence=null;updateInspector();return false;}
    state.selectionByView[view]={index,row,identity:evidenceIdentity(row)};state.inspectorState[view]={kind:"evidence",row,identity:evidenceIdentity(row)};if(view===state.view){state.evidenceIndex=index;state.selectedEvidence=row;}
    const selector=view==="search"?"[data-search-evidence-index]":"[data-evidence-index]";qa(selector).forEach(button=>{const active=Number(button.dataset.evidenceIndex??button.dataset.searchEvidenceIndex)===index;button.classList.toggle("selected",active);button.setAttribute("aria-selected",active?"true":"false");});updateInspector();
    if(focus)buttonForEvidence(index,view)?.focus({preventScroll:true});return true;
  }
  function buttonForEvidence(index,view=state.view){return view==="search"?q(`[data-search-evidence-index="${index}"]`):q(`[data-evidence-index="${index}"]`);}
  const EVIDENCE_LABELS=Object.freeze({item:"测量条目",finding:"研究结论",table:"原始表格",figure:"论文图片"});
  function evidenceIdentity(row){return row?.sourceScope==="workspace"?`workspace:${row.type}:${row.assetId||row.itemId||0}`:`${row?.sourceScope||""}:${row?.sourceId||""}:${row?.entityUid||""}`;}
  function privateTableIdentity(row){return row?.sourceScope==="private"&&row?.type==="table"&&typeof row.sourceId==="string"&&row.sourceId&&typeof row.entityUid==="string"&&row.entityUid?{sourceId:row.sourceId,entityUid:row.entityUid,key:`private:${row.sourceId}:${row.entityUid}`}:null;}
  function publicPersonalTablePage(raw,identity,requestedPage){
    if(raw?.schema_version!=="personal-table-page-v1"||raw.source_id!==identity.sourceId||raw.entity_uid!==identity.entityUid||!Number.isInteger(raw.page)||raw.page!==requestedPage||!Number.isInteger(raw.page_size)||raw.page_size<1||raw.page_size>100||!Number.isInteger(raw.total)||raw.total<0||typeof raw.has_next!=="boolean"||!Array.isArray(raw.columns)||!Array.isArray(raw.rows)||!Array.isArray(raw.series)||!raw.conditions||typeof raw.conditions!=="object"||Array.isArray(raw.conditions))return null;
    const columns=raw.columns.slice(0,500).map(value=>({name:cleanText(value?.name,500),role:cleanText(value?.role,40),dataType:cleanText(value?.data_type,40),meaning:cleanText(value?.meaning,1000),unit:value?.unit===null?null:cleanText(value?.unit,200)})).filter(value=>value.name),columnNames=new Set(columns.map(value=>value.name));if(!columns.length||columnNames.size!==columns.length)return null;
    const rows=raw.rows.slice(0,raw.page_size).map(value=>value&&typeof value==="object"&&!Array.isArray(value)?Object.fromEntries(columns.map(column=>[column.name,cleanText(value[column.name],10000)])):null);if(rows.some(value=>value===null))return null;
    const conditions=Object.entries(raw.conditions).slice(0,200).map(([key,value])=>[cleanText(key,500),cleanText(value,2000)]).filter(([key])=>key),series=raw.series.slice(0,200).map(value=>({name:cleanText(value?.name,500),xColumn:cleanText(value?.x_column,500),yColumn:cleanText(value?.y_column,500),uncertaintyColumn:value?.uncertainty_column===null?null:cleanText(value?.uncertainty_column,500),description:value?.description===null?null:cleanText(value?.description,2000)})).filter(value=>value.name);
    return {schemaVersion:"personal-table-page-v1",sourceId:identity.sourceId,entityUid:identity.entityUid,title:cleanText(raw.title,500),sheetName:cleanText(raw.sheet_name,500),columns,conditions,series,page:raw.page,pageSize:raw.page_size,total:raw.total,hasNext:raw.has_next,rows};
  }
  function personalTableInspector(page){
    if(!page)return `<section class="fusion-inspector-section"><span>我的实验</span><h2>表格详情</h2><p>正在读取已确认的私人实验表格。</p></section>`;
    const conditions=page.conditions.length?`<section class="fusion-inspector-section"><span>实验条件</span><dl>${page.conditions.map(([key,value])=>`<dt>${esc(key)}</dt><dd>${esc(value)}</dd>`).join("")}</dl></section>`:"",series=page.series.length?`<section class="fusion-inspector-section"><span>测量序列</span>${page.series.map(value=>`<article class="fusion-private-series"><strong>${esc(value.name)}</strong><p>${esc(value.xColumn)} → ${esc(value.yColumn)}${value.uncertaintyColumn?` · 不确定度 ${esc(value.uncertaintyColumn)}`:""}</p>${value.description?`<small>${esc(value.description)}</small>`:""}</article>`).join("")}</section>`:"";
    return `<section class="fusion-inspector-section"><span>列定义 · ${page.columns.length} 列</span>${page.columns.map(column=>`<article class="fusion-private-column"><strong>${esc(column.name)}</strong><dl><dt>角色</dt><dd>${esc(column.role||"—")}</dd><dt>意义</dt><dd>${esc(column.meaning||"—")}</dd><dt>单位</dt><dd>${esc(column.unit||"—")}</dd></dl></article>`).join("")}</section>${conditions}${series}`;
  }
  function renderPersonalTablePage(row,page){
    const host=q("#fusion-evidence-detail-body");if(!host)return false;host.setAttribute("aria-busy","false");q("#fusion-detail-heading").textContent=page.title||row.title;q("#fusion-detail-identity").textContent="原始表格 · 我的实验";
    const first=(page.page-1)*page.pageSize+1,last=Math.min(page.total,page.page*page.pageSize),header=page.columns.map(column=>`<th scope="col"><strong>${esc(column.name)}</strong><small>${esc([column.meaning,column.unit].filter(Boolean).join(" · ")||column.role)}</small></th>`).join(""),rows=page.rows.map((value,index)=>`<tr><th scope="row">${first+index}</th>${page.columns.map(column=>`<td>${esc(value[column.name])}</td>`).join("")}</tr>`).join("");
    host.innerHTML=`<article class="fusion-detail-sheet fusion-private-table-detail"><header><span>我的实验 · 已确认数据</span><h1>${esc(page.title||row.title)}</h1><p>${esc(page.sheetName||"工作表")} · ${page.total} 行 · ${page.columns.length} 列</p></header><div class="fusion-private-table-toolbar"><span>${page.total?`第 ${first}–${last} 行，共 ${page.total} 行`:"当前表格没有数据行"}</span><div><button type="button" id="fusion-private-table-prev"${page.page<=1?" disabled":""}>上一页</button><b>第 ${page.page} 页</b><button type="button" id="fusion-private-table-next"${page.hasNext?"":" disabled"}>下一页</button></div></div>${page.total?`<div class="fusion-private-table-scroll" tabindex="0" aria-label="${esc(page.title||row.title)}分页原始表格"><table class="fusion-data-grid fusion-private-table-grid"><thead><tr><th scope="col">行</th>${header}</tr></thead><tbody>${rows}</tbody></table></div>`:detailState("empty","表格没有数据行","列定义、实验条件和测量序列仍可在右侧检查器核对。")}</article>`;
    q("#fusion-private-table-prev")?.addEventListener("click",event=>void loadPrivateTablePage(row,page.page-1,{focusReturn:event.currentTarget}));q("#fusion-private-table-next")?.addEventListener("click",event=>void loadPrivateTablePage(row,page.page+1,{focusReturn:event.currentTarget}));state.personalTablePage=page;state.inspectorState.personal={kind:"private-table",page};updateInspector();return true;
  }
  function renderPersonalTableTerminal(row,kind,title,message){const host=q("#fusion-evidence-detail-body");if(!host)return;syncDetailImageDownload();syncDetailEvidenceExports(row);host.setAttribute("aria-busy",kind==="loading"?"true":"false");q("#fusion-detail-heading").textContent=row.title;q("#fusion-detail-identity").textContent="原始表格 · 我的实验";host.innerHTML=`<article class="fusion-detail-sheet fusion-private-table-detail"><header><span>我的实验 · 已确认数据</span><h1>${esc(row.title)}</h1></header>${detailState(kind,title,message)}</article>`;}
  async function loadPrivateTablePage(row,page=1,{focusReturn=null}={}){
    const identity=privateTableIdentity(row);if(!identity||!Number.isInteger(page)||page<1)return false;const request=++state.evidenceDetailRequest,originView=state.view,tab=documentTabs?.activeTab(),tabRequest=tab?.kind==="personal-table"?documentTabs.beginRequest(tab.tabId):0;state.personalTablePage=null;renderPersonalTableTerminal(row,"loading","正在读取原始表格",`正在读取第 ${page} 页，每页最多 50 行。`);updateInspector();
    try{const url=`${ROUTES.personalTable}?source_id=${encodeURIComponent(identity.sourceId)}&entity_uid=${encodeURIComponent(identity.entityUid)}&page=${page}&page_size=50`,raw=await readOnlyJSON(url),result=publicPersonalTablePage(raw,identity,page);if(!result)throw safeError("personal_table_invalid","个人实验表格响应无效。");if(tabRequest)documentTabs.completeRequest(tab.tabId,tabRequest,{title:result.title||tab.title,payload:{...tab.payload,row,page:result}});if(request!==state.evidenceDetailRequest||!state.evidenceDetailOpen||state.view!==originView||privateTableIdentity(state.evidenceDetail)?.key!==identity.key){renderDocumentTabs();return false;}renderPersonalTablePage(row,result);if(tabRequest){const current=documentTabs.snapshot().tabs.find(value=>value.tabId===tab.tabId);documentTabs.update(tab.tabId,{title:result.title||current?.title||tab.title,payload:{...current?.payload,row,page:result,html:q("#fusion-evidence-detail-body")?.innerHTML||""}});}renderDocumentTabs();if(focusReturn){const target=page>1?q("#fusion-private-table-prev"):q("#fusion-private-table-next");target?.focus({preventScroll:true});}return true;}
    catch(_error){if(request!==state.evidenceDetailRequest||!state.evidenceDetailOpen||state.view!==originView||privateTableIdentity(state.evidenceDetail)?.key!==identity.key)return false;state.personalTablePage=null;renderPersonalTableTerminal(row,"error","原始表格暂时无法读取","当前详情没有被替换为其他记录；可返回搜索结果后重试。");updateInspector();return false;}
  }
  function detailDefinitionList(row){const values=[["论文",row.articleTitle],["DOI",row.doi],["来源页",row.pageEnd&&row.pageEnd!==row.page?`${row.page}–${row.pageEnd}`:row.page],["原文定位",row.locator],["作者",[row.firstAuthor,row.correspondingAuthor].filter(Boolean).join(" · ")],["物理量",row.quantities.join("、")],["变量 / 表头",Object.entries(row.variables).map(([key,value])=>`${key}：${value}`).join("；")],["材料 / 样品",row.materials.join("、")],["实验条件",row.conditions],["方法",row.methods],["关联条目",["table","figure"].includes(row.type)?`${row.linkedItemCount} 条`:""]].filter(([,value])=>String(value??"").trim());return values.length?`<dl class="fusion-detail-fields">${values.map(([label,value])=>`<div><dt>${esc(label)}</dt><dd>${esc(value)}</dd></div>`).join("")}</dl>`:"";}
  function detailState(kind,title,message){return `<div class="fusion-detail-terminal" data-detail-state="${kind}"><strong>${esc(title)}</strong><p>${esc(message)}</p></div>`;}
  function renderEvidenceDetail(row,{imageState="ready",tabId=documentTabs?.activeTab("primary")?.tabId||""}={}) {
    const host=q("#fusion-evidence-detail-body");if(!host||!row)return;host.setAttribute("aria-busy","false");q("#fusion-detail-heading").textContent=row.title;q("#fusion-detail-identity").textContent=`${EVIDENCE_LABELS[row.type]} · ${{workspace:"本机文献工作区",official:"官方资料库",private:"我的实验"}[row.sourceScope]}`;
    const tab=tableStructureTab(tabId);
    syncDetailImageDownload(row);syncDetailEvidenceExports(row);
    const visual=["table","figure"].includes(row.type),quantity=row.type==="finding"?(row.value||"定性结论"):(row.value?`${row.value}${row.unit?` ${row.unit}`:""}`:row.label||EVIDENCE_LABELS[row.type]);
    let media="";
    if(visual&&row.sourceScope!=="workspace")media=detailState("unavailable","该资料源不提供原图","这里只展示资料包或私人库公开的结构化字段；不会借用本机工作区编号，也不会生成替代图。");
    else if(visual&&imageState==="loading")media=detailState("loading","正在读取权威原图","图片来自本机 PDF 的 PyMuPDF 截图；不会调用模型或重新绘制。");
    else if(visual&&imageState==="error")media=detailState("error","原图暂时无法显示","结构化证据仍可核对；可返回列表后重试，现有数据未改变。");
    else if(visual&&row.imageUrl)media=`<figure class="fusion-detail-visual"><img id="fusion-detail-image" src="${esc(row.imageUrl)}" alt="${esc(`${row.label||row.title}的PDF权威截图`)}"><figcaption>${esc(row.caption||"原文未提供图注")}</figcaption></figure>`;
    else if(visual)media=detailState("unavailable","原图未提供","当前公开证据没有可安全加载的原图地址；系统不会伪造图片。");
    host.innerHTML=`<article class="fusion-detail-sheet" data-evidence-type="${row.type}" data-evidence-detail-tab="${esc(tabId)}"><header><span>${esc(EVIDENCE_LABELS[row.type])}</span><h1>${esc(row.title)}</h1><p>${esc([row.label,row.articleTitle,row.doi].filter(Boolean).join(" · "))}</p></header><div class="fusion-detail-layout"><section class="fusion-detail-primary"><div class="fusion-detail-measure"><small>${row.type==="finding"?"证据类型":"证据值 / 对象"}</small><strong>${esc(quantity)}</strong></div>${row.findingText?`<section><h2>论文报告的结论</h2><p>${esc(row.findingText)}</p></section>`:""}${media}${tableStructureHTML(tabId,row,tab?.payload||{},true)}${row.caption&&!media.includes("figcaption")?`<section><h2>原始图注</h2><p>${esc(row.caption)}</p></section>`:""}${row.context?`<section><h2>文章中的含义</h2><p>${esc(row.context)}</p></section>`:""}${row.sourceContext?`<section><h2>原文语境</h2><p>${esc(row.sourceContext)}</p></section>`:""}${row.excerpt?`<blockquote><span>原文证据</span><p>${esc(row.excerpt)}</p></blockquote>`:""}</section><aside>${detailDefinitionList(row)}${row.tags.length?`<div class="fusion-detail-tags">${row.tags.map(tag=>`<span>${esc(tag)}</span>`).join("")}</div>`:""}<p class="fusion-detail-provenance">截图由本机 PyMuPDF 视觉链建立；AI 不读取图片像素，也不从曲线推断数值。</p></aside></div></article>${evidenceChatHTML(tabId)}`;
    const image=q("#fusion-detail-image");if(image){image.addEventListener("load",()=>{if(state.evidenceDetailOpen&&evidenceIdentity(state.evidenceDetail)===evidenceIdentity(row))image.closest?.("figure")?.setAttribute("data-image-state","ready");},{once:true});image.addEventListener("error",()=>{if(state.evidenceDetailOpen&&evidenceIdentity(state.evidenceDetail)===evidenceIdentity(row))renderEvidenceDetail(row,{imageState:"error"});},{once:true});}
    const pdf=q("#fusion-detail-open-pdf");if(pdf)pdf.disabled=!detailPDFURL(row);bindTableStructureActions(tabId);bindEvidenceChatActions();syncSelectedEvidenceAI();
  }
  function showEvidenceDetailShell(row,focusReturn,{openTab=true,preview=true,pin=false}={}) {
    state.evidenceDetailOpen=true;state.evidenceDetail=row;state.evidenceDetailReturn={view:state.view,element:focusReturn||buttonForEvidence(state.evidenceIndex)};state.evidenceDetailPDF=false;
    const tab=openTab?openDocumentTab(privateTableIdentity(row)?"personal-table":"evidence",row.title,documentIdentity(row),{payload:{evidenceIdentity:evidenceIdentity(row),row,focusReturn:state.evidenceDetailReturn.element,status:"loading"},preview,pin}):documentTabs?.activeTab();
    qa("[data-view-panel]").forEach(panel=>{panel.hidden=true;panel.classList.remove("active");});const detail=q("#fusion-evidence-detail");if(detail)detail.hidden=false;q("#fusion-detail-pdf").hidden=true;q("#fusion-evidence-detail-body").hidden=false;
    syncDetailImageDownload();syncDetailEvidenceExports();q("#fusion-detail-heading").textContent=row.title;q("#fusion-detail-identity").textContent=`${EVIDENCE_LABELS[row.type]} · 正在读取`;q("#fusion-evidence-detail-body").setAttribute("aria-busy","true");q("#fusion-evidence-detail-body").innerHTML=detailState("loading","正在准备证据详情","只读取当前公开证据，不调用 AI，也不修改数据。");return tab;
  }
  async function openEvidenceDetail(index,focusReturn=null,{openTab=true,preview=true,pin=false}={}) {
    const rows=state.view==="search"?state.searchResults:state.evidence,row=rows[index];if(!row)return false;selectEvidence(index,{focus:false});if(openTab&&documentTabs&&paneViewport()>=900){const tab=await openSecondaryEvidence(row,state.view,{preview,pin});return Boolean(tab);}const tab=showEvidenceDetailShell(row,focusReturn,{openTab,preview,pin}),request=++state.evidenceDetailRequest,tabRequest=tab?documentTabs?.beginRequest(tab.tabId):0,identity=evidenceIdentity(row),originView=state.view;
    if(privateTableIdentity(row))return loadPrivateTablePage(row,1);
    if(["item","finding"].includes(row.type)){renderEvidenceDetail(row);if(tab)documentTabs?.completeRequest(tab.tabId,tabRequest,{payload:{...tab.payload,row,status:"ready"}});renderDocumentTabs();return true;}
    if(row.sourceScope==="workspace"&&!row.assetId){renderEvidenceDetail(row,{imageState:"error"});if(tab)documentTabs?.completeRequest(tab.tabId,tabRequest,{payload:{...tab.payload,row,status:"error"}});renderDocumentTabs();return false;}
    renderEvidenceDetail(row,{imageState:row.sourceScope==="workspace"?"loading":"ready"});
    try {const url=row.sourceScope==="workspace"?`${ROUTES.visualAsset}/${row.assetId}`:`${ROUTES.federatedEvidence}?source_scope=${encodeURIComponent(row.sourceScope)}&source_id=${encodeURIComponent(row.sourceId)}&entity_uid=${encodeURIComponent(row.entityUid)}`,raw=await readOnlyJSON(url);const detail=publicEvidence(raw);if(!detail||detail.type!==row.type||detail.sourceScope!==row.sourceScope||(row.sourceScope==="workspace"&&detail.assetId!==row.assetId)||(row.sourceScope!=="workspace"&&evidenceIdentity(detail)!==identity))throw safeError("fusion_evidence_identity_changed","证据身份已变化。");if(tab)documentTabs?.completeRequest(tab.tabId,tabRequest,{payload:{...tab.payload,row:detail,status:"ready"}});if(request!==state.evidenceDetailRequest||!state.evidenceDetailOpen||state.view!==originView||evidenceIdentity(state.evidenceDetail)!==identity){renderDocumentTabs();return false;}state.evidenceDetail=detail;renderEvidenceDetail(detail,{tabId:tab?.tabId||""});if(tab)documentTabs?.update(tab.tabId,{payload:{...tab.payload,row:detail,status:"ready",html:q("#fusion-evidence-detail-body")?.innerHTML||""}});renderDocumentTabs();if(tab&&detail.sourceScope==="workspace"&&detail.type==="table")void loadTableStructure(tab.tabId,detail);return true;}
    catch(_error){if(tab)documentTabs?.completeRequest(tab.tabId,tabRequest,{payload:{...tab.payload,row,status:"error"}});if(request!==state.evidenceDetailRequest||!state.evidenceDetailOpen||state.view!==originView||evidenceIdentity(state.evidenceDetail)!==identity){renderDocumentTabs();return false;}renderEvidenceDetail(row,{imageState:"error"});renderDocumentTabs();return false;}
  }
  function clearSourceHighlightTimer(){if(state.sourceHighlightTimer!==null){globalThis.clearTimeout?.(state.sourceHighlightTimer);state.sourceHighlightTimer=null;}}
  function sourceHighlightCapability(row){if(row?.sourceScope!=="workspace")return null;if(["item","finding"].includes(row.type)&&Number.isSafeInteger(row.itemId)&&row.itemId>0)return {kind:"text",url:`/api/six-data/${row.itemId}/source-highlight.png`};if(["table","figure"].includes(row.type)&&row.imageUrl&&Array.isArray(row.bbox)&&row.bbox.length===4)return {kind:"visual",url:row.imageUrl};return null;}
  function closeDetailPDF({focus=true}={}){if(!state.evidenceDetailPDF)return false;state.evidenceDetailPDF=false;state.sourceHighlightRequest+=1;state.sourceHighlight=null;clearSourceHighlightTimer();const highlight=q("#fusion-detail-source-highlight"),pdfPanel=q("#fusion-detail-pdf"),body=q("#fusion-evidence-detail-body"),closed=pdfController?.close("detail");if(highlight)highlight.hidden=true;q("#fusion-detail-highlight-image")?.removeAttribute?.("src");if(pdfPanel)pdfPanel.hidden=true;if(body){body.hidden=false;const scrollTop=Number.isFinite(closed?.scrollTop)?closed.scrollTop:state.evidenceDetailReturn?.scrollTop;if(Number.isFinite(scrollTop))body.scrollTop=scrollTop;}if(focus)(closed?.returnFocus?.isConnected?closed.returnFocus:q("#fusion-detail-open-pdf"))?.focus?.({preventScroll:true});return true;}
  function detailPDFURL(row){if(row?.sourceScope==="workspace"&&Number.isSafeInteger(row.paperId)&&row.paperId>0)return `/api/papers/${encodeURIComponent(String(row.paperId))}/pdf`;if(["official","private"].includes(row?.sourceScope)&&row.collectionKind==="literature_collection"&&row.pdfAvailable===true&&row.sourceId&&row.paperUid)return `/api/desktop/federated-pdf?source_id=${encodeURIComponent(row.sourceId)}&paper_uid=${encodeURIComponent(row.paperUid)}`;return "";}
  function openDetailPDF({openTab=true}={}){const row=state.evidenceDetail,url=detailPDFURL(row);if(!state.evidenceDetailOpen||!url||!pdfController)return false;const sourceTab=documentTabs?.activeTab()?.tabId||"",focusReturn=q("#fusion-detail-open-pdf"),body=q("#fusion-evidence-detail-body"),pdfPanel=q("#fusion-detail-pdf"),frame=q("#fusion-detail-pdf-frame"),title=q("#fusion-detail-pdf-title"),capability=sourceHighlightCapability(row),highlight=q("#fusion-detail-show-highlight"),scrollTop=body?.scrollTop||0;pinDocumentTab(sourceTab);state.evidenceDetailPDF=true;if(state.evidenceDetailReturn)state.evidenceDetailReturn.scrollTop=scrollTop;const tab=openTab?openDocumentTab("pdf",row.articleTitle||"证据来源原文",{...documentIdentity(row),paperId:String(row.paperId||""),paperUid:String(row.paperUid||"")},{ownerView:state.view,payload:{evidenceIdentity:evidenceIdentity(row),url,returnTabId:sourceTab,scrollTop},groupId:documentTabs?.activeTab()?.groupId||"primary",pin:true}):null;if(body)body.hidden=true;if(pdfPanel)pdfPanel.hidden=false;if(title)title.textContent=row.articleTitle||"证据来源原文";if(highlight){highlight.disabled=!capability;highlight.textContent="重新显示高亮";}pdfController.open({viewerId:"detail",frame,container:pdfPanel,url,page:Number(row.page)||1,returnTabId:sourceTab,returnFocus:focusReturn,scrollTop,highlight:capability});if(tab)documentTabs?.update(tab.tabId,{payload:{...tab.payload,url,returnTabId:sourceTab,scrollTop,page:Number(row.page)||1}});q("#fusion-detail-close-pdf")?.focus({preventScroll:true});if(capability)void showSourceHighlight({autoHide:true,sourceTabId:sourceTab});return true;}
  async function showSourceHighlight({autoHide=false,sourceTabId=""}={}){const row=state.evidenceDetail,capability=sourceHighlightCapability(row);if(!state.evidenceDetailPDF||!capability)return false;clearSourceHighlightTimer();const generation=++state.sourceHighlightRequest,identity=evidenceIdentity(row),panel=q("#fusion-detail-source-highlight"),status=q("#fusion-detail-highlight-status"),button=q("#fusion-detail-show-highlight");if(panel)panel.hidden=false;if(status)status.textContent=capability.kind==="visual"?"正在显示表格或图片在原文中的区域…":"正在读取原文高亮位置…";try{let imageUrl=capability.url;if(capability.kind==="text"){const data=await readOnlyJSON(`/api/six-data/${row.itemId}/source-view`);if(data?.image_url!==capability.url)throw safeError("source_highlight_invalid","原文高亮地址无效。");imageUrl=data.image_url;}if(generation!==state.sourceHighlightRequest||!state.evidenceDetailPDF||evidenceIdentity(state.evidenceDetail)!==identity)return false;state.sourceHighlight={identity,imageUrl,sourceTabId};const image=q("#fusion-detail-highlight-image");if(image)image.src=imageUrl;if(status)status.textContent=capability.kind==="visual"?"表格或图片在原文中的权威截图区域。":"原文高亮位置；如需继续阅读，可隐藏高亮返回 PDF。";if(button){button.disabled=false;button.textContent="隐藏高亮";}if(autoHide)state.sourceHighlightTimer=globalThis.setTimeout?.(()=>{if(state.evidenceDetailPDF&&state.sourceHighlight?.identity===identity&&state.sourceHighlight?.sourceTabId===sourceTabId){if(panel)panel.hidden=true;if(button)button.textContent="重新显示高亮";}state.sourceHighlightTimer=null;},1600)??null;return true;}catch(_error){if(generation===state.sourceHighlightRequest){state.sourceHighlight=null;if(status)status.textContent="当前证据没有可安全恢复的高亮位置；PDF 仍可继续阅读。";}return false;}}
  function toggleSourceHighlight(){const panel=q("#fusion-detail-source-highlight");if(!panel)return false;clearSourceHighlightTimer();if(!panel.hidden){panel.hidden=true;q("#fusion-detail-show-highlight").textContent="重新显示高亮";return true;}void showSourceHighlight({sourceTabId:state.sourceHighlight?.sourceTabId||""});return true;}
  function closeEvidenceDetail({focus=true}={}){if(!state.evidenceDetailOpen)return false;state.evidenceDetailRequest+=1;closeDetailPDF({focus:false});const previous=state.evidenceDetailReturn;state.evidenceDetailOpen=false;state.evidenceDetail=null;state.personalTablePage=null;state.evidenceDetailReturn=null;syncDetailImageDownload();syncDetailEvidenceExports();q("#fusion-evidence-detail").hidden=true;qa("[data-view-panel]").forEach(panel=>{const active=panel.dataset.viewPanel===state.view;panel.hidden=!active;panel.classList.toggle("active",active);});if(focus&&previous?.view===state.view&&previous.element?.isConnected)previous.element.focus({preventScroll:true});return true;}
  function tabById(tabId){return documentTabs?.snapshot().tabs.find(value=>value.tabId===tabId)||null;}
  function restoredEvidenceMatches(row,tab){if(!row||!tab)return false;if(tab.identity.sourceScope==="workspace"){const entityId=String(row.assetId||row.itemId||"");return row.sourceScope==="workspace"&&row.type===tab.identity.entityType&&entityId===tab.identity.entityUid;}return row.sourceScope===tab.identity.sourceScope&&row.sourceId===tab.identity.sourceId&&row.type===tab.identity.entityType&&row.entityUid===tab.identity.entityUid;}
  function markHydrationRetry(tabId,generation){const current=tabById(tabId),binding=documentTabs?.requestBinding(tabId);if(!current||binding?.generation!==generation||current.payload?.hydration!=="pending")return false;documentTabs.update(tabId,{payload:{status:"idle",hydration:"retry"}});return true;}
  async function hydratePaperTab(tab){
    const paperId=positiveId(tab.identity.paperId),paper=state.papers.find(value=>value.id===paperId);
    if(!paper)throw safeError("tab_source_unavailable","这篇论文已不在当前目录。");
    const page=await readOnlyJSON(`${ROUTES.evidence}?q=&paper_ids=${paperId}&limit=1000&quality=published`),evidence=(Array.isArray(page?.rows)?page.rows:[]).map(publicEvidence).filter(Boolean);
    return {paperId,paper,evidenceCount:evidence.length,evidence,status:"ready"};
  }
  async function hydrateEvidenceTab(tab){
    let row=null;
    if(tab.identity.sourceScope==="workspace"){
      const paperId=positiveId(tab.identity.paperId);
      if(!paperId)throw safeError("tab_identity_incomplete","工作区证据缺少论文身份。");
      const page=await readOnlyJSON(`${ROUTES.evidence}?q=&paper_ids=${paperId}&limit=1000&quality=published`);
      row=(Array.isArray(page?.rows)?page.rows:[]).map(publicEvidence).filter(Boolean).find(value=>restoredEvidenceMatches(value,tab));
    }else{
      row=publicEvidence(await readOnlyJSON(`${ROUTES.federatedEvidence}?source_scope=${encodeURIComponent(tab.identity.sourceScope)}&source_id=${encodeURIComponent(tab.identity.sourceId)}&entity_uid=${encodeURIComponent(tab.identity.entityUid)}`));
      if(!restoredEvidenceMatches(row,tab))row=null;
    }
    if(!row)throw safeError("tab_source_unavailable","当前资料源中找不到这条证据。");
    const payload={evidenceIdentity:evidenceIdentity(row),row,status:"ready"};
    if(tab.kind!=="personal-table")return payload;
    const identity=privateTableIdentity(row),raw=await readOnlyJSON(`${ROUTES.personalTable}?source_id=${encodeURIComponent(identity.sourceId)}&entity_uid=${encodeURIComponent(identity.entityUid)}&page=1&page_size=50`),page=publicPersonalTablePage(raw,identity,1);
    if(!page)throw safeError("personal_table_invalid","个人实验表格响应无效。");
    return {...payload,page};
  }
  function hydratePdfTab(tab){
    const paperId=positiveId(tab.identity.paperId),url=paperId?`/api/papers/${paperId}/pdf`:tab.identity.sourceId&&tab.identity.paperUid?`/api/desktop/federated-pdf?source_id=${encodeURIComponent(tab.identity.sourceId)}&paper_uid=${encodeURIComponent(tab.identity.paperUid)}`:"";
    if(!url)throw safeError("tab_identity_incomplete","PDF 标签缺少可恢复的公开身份。");
    return {url,page:1,status:"ready"};
  }
  function hydrateLibrarianTab(tab){
    const session=state.librarianSessions.find(value=>value.id===tab.identity.conversationId),section=tab.identity.entityType;
    if(!session)throw safeError("tab_source_unavailable","对应的图书管理员历史已不可用。");
    const assistant=[...session.messages].reverse().find(value=>value.role==="assistant")?.content||"",html=section==="answer"?`<article class="fusion-librarian-answer"><h2>历史回答</h2><div class="fusion-librarian-answer-text">${esc(assistant).replace(/\\n/g,"<br>")}</div></article>`:detailState("empty","请从历史回答重新打开引用","标签身份已恢复；敏感正文从未写入布局缓存。");
    return {section,status:"ready",html};
  }
  async function hydratePackageJobTab(tab){
    const jobId=tab.identity.jobId;
    if(!jobId)throw safeError("tab_identity_incomplete","资料包任务缺少公开任务身份。");
    const base=tab.identity.operation==="official"?"/api/desktop/evidence-package-jobs":"/api/desktop/package-center/jobs",job=await readOnlyJSON(`${base}/${encodeURIComponent(jobId)}`);
    return {job,status:"ready"};
  }
  function hydrateTabPayload(tab){
    if(tab.kind==="paper")return hydratePaperTab(tab);
    if(tab.kind==="evidence"||tab.kind==="personal-table")return hydrateEvidenceTab(tab);
    if(tab.kind==="pdf")return hydratePdfTab(tab);
    if(tab.kind==="librarian")return hydrateLibrarianTab(tab);
    if(tab.kind==="package-job")return hydratePackageJobTab(tab);
    throw safeError("tab_restore_unsupported","此类标签需要从原入口重新打开。");
  }
  async function hydrateDocumentTab(tab){
    if(!tab||tab.payload!==undefined&&tab.payload?.hydration!=="retry")return Boolean(tab);
    const expectedGroupId=tab.groupId,generation=documentTabs.beginRequest(tab.tabId);
    documentTabs.update(tab.tabId,{payload:{status:"loading",hydration:"pending"}});renderDocumentTabs();
    try{
      const payload=await hydrateTabPayload(tab),current=tabById(tab.tabId);
      if(!current||!documentTabs.completeRequest(tab.tabId,generation,{payload},expectedGroupId)){markHydrationRetry(tab.tabId,generation);renderDocumentTabs();return false;}
      renderDocumentTabs();if(payload?.row?.sourceScope==="workspace"&&payload.row.type==="table")void loadTableStructure(tab.tabId,payload.row);return true;
    }catch(error){
      const current=tabById(tab.tabId);
      if(current&&!documentTabs.completeRequest(tab.tabId,generation,{payload:{status:"error",errorCode:cleanText(error?.code||"tab_restore_failed",80)}},expectedGroupId))markHydrationRetry(tab.tabId,generation);
      renderDocumentTabs();return false;
    }
  }
  function visibleActiveTabsForHydration(){
    const snapshot=documentTabs?.snapshot();
    if(!snapshot)return [];
    const geometry=paneGeometry(snapshot),tabs=[];
    if(!geometry.primaryCollapsed){const primary=documentTabs.activeTab("primary");if(primary)tabs.push(primary);}
    if(geometry.hasSecondary&&!geometry.secondaryCollapsed){const secondary=documentTabs.activeTab("secondary");if(secondary)tabs.push(secondary);}
    return tabs.filter((tab,index,values)=>(tab.payload===undefined||tab.payload?.hydration==="retry")&&values.findIndex(value=>value.tabId===tab.tabId)===index);
  }
  async function hydrateActiveRestoredTabs(){
    const tabs=visibleActiveTabsForHydration();
    await Promise.all(tabs.map(tab=>hydrateDocumentTab(tab)));
    const active=documentTabs?.activeTab();
    if(active)activateDocumentTab(active.tabId);
    return tabs.length;
  }

  async function selectPaper(id,{reveal=false,preview=true,pin=false,openTab=true,activateTab=true,groupId=null}={}) {
    const paper=state.papers.find(candidate=>candidate.id===id);if(!paper)return false;const changed=state.paper?.id!==id;if(changed&&!state.literatureTask.jobId)aiExperience?.reset("literature_extraction");state.reviewQueue.mode=false;closeReviewDetail({focus:false});closeEvidenceDetail({focus:false});closeCurrentPDF({focus:false});state.paper=paper;renderPaperCatalog();
    const tab=openTab?openDocumentTab("paper",paper.title,{paperId:String(paper.id)},{ownerView:"paper",payload:{paperId:paper.id,paper,evidenceCount:0,evidence:[]},activate:activateTab,preview,pin,groupId}):null,tabRequest=tab?documentTabs?.beginRequest(tab.tabId):0;renderPaperCatalog({revealSelected:reveal});
    const request=++state.literatureRequest;state.evidenceIndex=0;state.selectionByView.paper=null;state.literatureStatus="loading";q("#fusion-literature-content").innerHTML=literatureState("loading","正在读取真实文献证据…");
    q("#fusion-search-results").innerHTML=literatureState("loading","正在读取真实文献证据…");resetEvidenceCounts();q("#fusion-current-paper-state").textContent="载入中";q("#fusion-current-paper-count").textContent="—";
    try {const page=await readOnlyJSON(`${ROUTES.evidence}?q=&paper_ids=${encodeURIComponent(String(id))}&limit=1000&quality=published`),evidence=(Array.isArray(page?.rows)?page.rows:[]).map(publicEvidence).filter(Boolean);if(tab)documentTabs?.completeRequest(tab.tabId,tabRequest,{payload:{paperId:paper.id,paper,evidenceCount:evidence.length,evidence}});if(request!==state.literatureRequest){renderDocumentTabs();return false;}state.evidence=evidence;state.searchResults=state.evidence;state.literatureStatus="ready";renderLiterature();renderPaperCatalog();syncPaperActions();projectLiteratureRecovery();renderDocumentTabs();return true;}
    catch(_error){if(request!==state.literatureRequest)return false;state.evidence=[];state.literatureStatus="error";renderLiterature();renderSearchEvidence();renderPaperCatalog();return false;}
  }
  async function loadLiterature() {
    const request=++state.literatureRequest;state.literatureStatus="loading";
    try {const papers=await readOnlyJSON(ROUTES.papers);if(request!==state.literatureRequest)return false;state.papers=(Array.isArray(papers)?papers:[]).map(publicPaper).filter(Boolean);renderPaperCatalog();if(!state.papers.length){state.paper=null;state.evidence=[];state.searchResults=[];state.literatureStatus="empty";renderLiterature();renderSearchEvidence();syncPaperActions();return true;}const restored=documentTabs?.snapshot().tabs.filter(tab=>tab.payload===undefined)||[],requestedId=positiveId(documentTabs?.activeTab()?.identity?.paperId)||positiveId(restored.find(tab=>tab.identity?.paperId)?.identity.paperId),paper=state.papers.find(value=>value.id===requestedId)||state.papers[0];return selectPaper(paper.id,{openTab:!restored.length,activateTab:!restored.length,preview:!restored.length});}
    catch(_error){if(request!==state.literatureRequest)return false;state.papers=[];state.paper=null;state.evidence=[];state.literatureStatus="error";renderPaperCatalog();renderLiterature();renderSearchEvidence();return false;}
  }
  function setOperation(message,kind="info") {const node=q("#fusion-status-operation");if(node){node.textContent=message;node.dataset.kind=kind;}}
  function setLiteratureActionStatus(message,kind="info") {const node=q("#fusion-literature-action-status");if(node){node.textContent=message;node.dataset.kind=kind;}setOperation(message,kind);}
  function clearLiteratureReceipt(){state.literatureReceipt=null;const panel=q("#fusion-literature-receipt"),body=q("#fusion-literature-receipt-body");if(panel){panel.hidden=true;panel.open=false;}if(body)body.replaceChildren();}
  function visualReceiptText(value){return value.visual_stage_status==="ready"?`表格 ${value.table_candidate_count??value.tableCandidateCount} 项 · 图片 ${value.figure_candidate_count??value.figureCandidateCount} 项`:"未检测到可发布表格或图片，请核对原文";}
  function renderLiteratureReceipt(result){if(!validLiteratureCommit(result))return false;state.literatureReceipt=result;const panel=q("#fusion-literature-receipt"),body=q("#fusion-literature-receipt-body");if(!panel||!body)return false;const indexReady=result.status==="completed"&&result.search_index?.status==="refreshed",partition=cleanText(result.dataset_receipt?.paper_partition||"未分配",20);body.innerHTML=`<dl><div><dt>提取候选</dt><dd>${result.candidate_count} 条</dd></div><div><dt>质量门</dt><dd>双分支核验</dd></div><div><dt>正式发布</dt><dd>${result.published_item_count} 条${result.existing_item_count?`（已有 ${result.existing_item_count} 条）`:""}</dd></div><div><dt>视觉证据</dt><dd>${visualReceiptText(result)}</dd></div><div><dt>人工审核</dt><dd>${result.manual_review_count} 项</dd></div><div data-receipt-index><dt>搜索索引</dt><dd>${indexReady?"已更新，可立即检索":"数据已保存，索引待恢复"}</dd></div><div><dt>数据集</dt><dd>已登记为 ${esc(partition)} 划分候选；尚未生成 JSONL / Parquet 文件</dd></div></dl>`;panel.hidden=false;return true;}
  function publicLiteratureReceiptSummary(raw){const counts=["candidate_count","published_item_count","existing_item_count","manual_review_count","table_candidate_count","figure_candidate_count"],visualStatus=raw?.visual_stage_status;if(raw?.schema_version!=="literature-extraction-receipt-summary-v1"||!["completed","saved_index_pending"].includes(raw.status)||typeof raw.visual_evidence_ready!=="boolean"||!["ready","not_found"].includes(visualStatus)||raw.visual_evidence_ready!==(visualStatus==="ready")||counts.some(key=>!Number.isInteger(raw[key])||raw[key]<0)||!raw.search_index||!["refreshed","pending"].includes(raw.search_index.status)||!['train','validation','test'].includes(raw.dataset_partition))return null;const documentCount=raw.search_index.document_count;if(documentCount!==null&&(!Number.isInteger(documentCount)||documentCount<0))return null;return {schemaVersion:"literature-extraction-receipt-summary-v1",status:raw.status,candidateCount:raw.candidate_count,publishedItemCount:raw.published_item_count,existingItemCount:raw.existing_item_count,manualReviewCount:raw.manual_review_count,tableCandidateCount:raw.table_candidate_count,figureCandidateCount:raw.figure_candidate_count,visualEvidenceReady:raw.visual_evidence_ready,visualStageStatus:visualStatus,searchIndex:{status:raw.search_index.status,documentCount},datasetPartition:raw.dataset_partition};}
  function renderLiteratureReceiptSummary(summary){if(summary?.schemaVersion!=="literature-extraction-receipt-summary-v1")return false;state.literatureReceipt=summary;const panel=q("#fusion-literature-receipt"),body=q("#fusion-literature-receipt-body");if(!panel||!body)return false;const indexReady=summary.searchIndex.status==="refreshed",documents=summary.searchIndex.documentCount===null?"":`，${summary.searchIndex.documentCount} 条文档`;body.innerHTML=`<dl><div><dt>提取候选</dt><dd>${summary.candidateCount} 条</dd></div><div><dt>正式发布</dt><dd>${summary.publishedItemCount} 条${summary.existingItemCount?`（已有 ${summary.existingItemCount} 条）`:""}</dd></div><div><dt>视觉证据</dt><dd>${summary.visualStageStatus==="ready"?`表格 ${summary.tableCandidateCount} 项 · 图片 ${summary.figureCandidateCount} 项`:"未检测到可发布表格或图片，请核对原文"}</dd></div><div><dt>人工审核</dt><dd>${summary.manualReviewCount} 项</dd></div><div data-receipt-index><dt>搜索索引</dt><dd>${indexReady?`已更新${documents}`:"数据已保存，索引待恢复"}</dd></div><div><dt>数据集</dt><dd>已登记为 ${esc(summary.datasetPartition)} 划分候选</dd></div></dl>`;panel.hidden=false;showSearchIndexRecovery(!indexReady);return true;}
  function updateLiteratureReceiptIndex(message){const value=q("#fusion-literature-receipt-body [data-receipt-index] dd");if(value)value.textContent=message;}
  function showSearchIndexRecovery(show=true){const button=q("#fusion-repair-search-index");if(button)button.hidden=!show;}
  async function recoverSearchIndex(){const button=q("#fusion-repair-search-index");if(button)button.disabled=true;setLiteratureActionStatus("正在恢复本机搜索索引；不会重新提取、发布或调用模型…","loading");try{const result=await request(ROUTES.searchIndexRecovery,{method:"POST",headers:{"Content-Type":"application/json"},body:"{}"});if(result?.schema_version!=="search-index-recovery-v1"||result.status!=="ready"||!Number.isInteger(result.document_count)||result.document_count<0)throw safeError("search_index_recovery_invalid","搜索索引恢复结果无效。");showSearchIndexRecovery(false);updateLiteratureReceiptIndex(`已恢复，可检索 ${result.document_count} 条证据`);setLiteratureActionStatus(`搜索索引已恢复，可检索 ${result.document_count} 条证据；没有重复提取或收费。`,"success");await Promise.all([loadLiterature(),state.view==="search"&&state.searchMode==="precise"?runPreciseSearch():Promise.resolve()]);return result;}catch(error){showSearchIndexRecovery(true);updateLiteratureReceiptIndex("数据已保存，索引恢复仍未完成");setLiteratureActionStatus(`${cleanText(error?.code||"search_index_recovery_failed",80)} · ${cleanText(error?.message||"搜索索引暂未恢复；已保存的科学数据不受影响。",400)}`,"error");return null;}finally{if(button)button.disabled=false;}}
  function currentLiteratureRecovery(){if(!state.paper)return null;const doi=cleanText(state.paper.doi,300).toLowerCase(),title=cleanText(state.paper.title,500).toLowerCase(),tasks=state.literatureTask.directory?.tasks||[];return tasks.find(entry=>{const paper=entry.paper||{},taskDoi=cleanText(paper.doi,300).toLowerCase(),taskTitle=cleanText(paper.title,500).toLowerCase();return doi&&taskDoi?taskDoi===doi:title&&taskTitle===title;})||null;}
  function syncPaperActions() {const ready=Boolean(positiveId(state.paper?.id)),extraction=q("#fusion-start-extraction"),pdf=q("#fusion-open-pdf"),recovery=currentLiteratureRecovery();if(extraction){extraction.textContent=recovery?.nextAction==="resume_extraction"?"▶ 恢复上次提取":"▶ 开始自动提取与核验";extraction.disabled=!ready||recovery?.nextAction==="review_call_outcome";}if(pdf)pdf.disabled=!ready;setFixedDownloadLink(q("#fusion-paper-export-csv"),ready?currentPaperExportURL("csv",state.paper.id):"");setFixedDownloadLink(q("#fusion-paper-export-xlsx"),ready?currentPaperExportURL("xlsx",state.paper.id):"");}
  function publicLiteratureTaskDirectory(raw){if(raw?.schema_version!=="literature-extraction-task-directory-v1"||!Array.isArray(raw.tasks)||!raw.startup_recovery||!Array.isArray(raw.issues))return null;const tasks=raw.tasks.map(task=>{const token=typeof task?.resume_token==="string"&&task.resume_token.length<=256?task.resume_token:"",nextAction=cleanText(task?.next_action,80),stateName=cleanText(task?.state,40),stage=cleanText(task?.stage,80),paper=task?.paper&&typeof task.paper==="object"?{title:cleanText(task.paper.title,500),doi:cleanText(task.paper.doi,300)}:null,hasReceipt=Object.prototype.hasOwnProperty.call(task||{},"receipt"),receipt=task?.receipt===null?null:publicLiteratureReceiptSummary(task?.receipt),counts=[task?.completed_calls,task?.spent_calls,task?.max_calls,task?.updated_at,task?.expires_at];if(!paper||!hasReceipt||task.receipt!==null&&!receipt||counts.some(value=>!Number.isFinite(Number(value))||Number(value)<0)||!["resume_extraction","retry_finalization","open_search","restart_extraction","review_call_outcome"].includes(nextAction)||!["authorized","running","paused","validated","completed","failed","outcome_unknown"].includes(stateName)||(["resume_extraction","retry_finalization"].includes(nextAction)&&!token))return null;return {resumeToken:token,state:stateName,stage,paper,completedCalls:Number(task.completed_calls),spentCalls:Number(task.spent_calls),maxCalls:Number(task.max_calls),updatedAt:Number(task.updated_at),expiresAt:Number(task.expires_at),nextAction,receipt};});if(tasks.some(task=>!task))return null;return {tasks,startupRecovery:{recovered:Number(raw.startup_recovery.recovered)||0,alreadyCompleted:Number(raw.startup_recovery.already_completed)||0,skippedOrBlocked:Number(raw.startup_recovery.skipped_or_blocked)||0},issues:raw.issues.map(issue=>({code:cleanText(issue?.code,120),count:Number(issue?.count)||0})).filter(issue=>issue.code)};}
  function projectLiteratureRecovery(){const task=currentLiteratureRecovery();syncPaperActions();if(!task)return false;const calls=`已完成 ${task.completedCalls}/${task.maxCalls} 次调用`;if(task.nextAction==="resume_extraction")setLiteratureActionStatus(`发现可恢复的提取任务（${calls}）。点击“恢复上次提取”后，将先显示后续范围与预算，不会自动发送模型请求。`,"warning");else if(task.nextAction==="retry_finalization")setLiteratureActionStatus(`本机正在或已尝试零模型收尾（${calls}）；不会重复调用模型。`,"info");else if(task.nextAction==="open_search"){if(task.receipt)renderLiteratureReceiptSummary(task.receipt);setLiteratureActionStatus(task.receipt?"上次提取已由本机恢复完成；收据已恢复，可前往搜索。":"上次提取已由本机恢复完成；旧任务没有可恢复的计数收据，可前往搜索。","success");}else if(task.nextAction==="review_call_outcome")setLiteratureActionStatus("上次调用结果无法确认。为避免重复收费，已阻止自动重试；请先核对提供商用量与任务状态。","error");return true;}
  async function loadLiteratureTaskDirectory(){try{const value=publicLiteratureTaskDirectory(await readOnlyJSON(ROUTES.literatureTaskDirectory));if(!value)throw safeError("literature_task_directory_invalid","文献任务恢复目录无效。");state.literatureTask.directory=value;state.literatureTask.resume=value.tasks[0]||null;projectLiteratureRecovery();return value;}catch(_error){state.literatureTask.directory=null;state.literatureTask.resume=null;syncPaperActions();return null;}}
  function aiRoute(scope,action){if(!AI_SCOPES.has(scope)||!["prepare","execute","execute-jobs"].includes(action))throw safeError("ai_action_scope_invalid","该 AI 功能未受支持。");return `/api/desktop/ai/actions/${scope}/${action}`;}
  async function loadAIContext() {
    if(state.aiContext)return state.aiContext;
    const gate=globalThis.AutoResearchAIConsent;if(typeof gate?.accepted!=="function"||typeof gate?.remember!=="function"||typeof gate?.disclosureSummary!=="function"||typeof gate?.updateTrustedProviders!=="function")throw safeError("ai_consent_gate_unavailable","AI 数据披露授权组件未载入；本次请求已安全停止。");
    const [catalog,settings]=await Promise.all([readOnlyJSON(ROUTES.aiProviders),readOnlyJSON(ROUTES.aiSettings)]);
    if(catalog?.schema_version!=="ai-desktop-catalog-v1"||!Array.isArray(catalog.providers)||settings?.schema_version!=="ai-runtime-public-state-v1")throw safeError("ai_public_state_invalid","受信 AI 提供商状态不可用。");
    if(gate.updateTrustedProviders(catalog.providers)!==true)throw safeError("ai_provider_catalog_invalid","受信 AI 提供商目录不可用。");
    const profile=catalog.providers.find(provider=>provider?.provider_id===settings.provider_id);if(!profile||typeof profile.display_name!=="string")throw safeError("ai_provider_untrusted","当前 AI 提供商不在受信目录中。");
    state.aiCatalog=catalog;state.aiSettings=settings;state.aiContext={provider_id:settings.provider_id,label:profile.display_name};return state.aiContext;
  }
  function aiStatus(message,kind="info"){const node=q("#fusion-ai-settings-status");if(node){node.textContent=message;node.dataset.kind=kind;}if(state.view==="settings")setOperation(message,kind);}
  function aiErrorCopy(error,fallback){const code=cleanText(error?.causeCode||error?.code,120),message=AI_ERROR_CODE_COPY[code]||fallback,nextAction=cleanText(error?.nextAction,120),action=nextAction?(AI_ERROR_ACTION_COPY[nextAction]||`下一步：${nextAction}`):"";return [message,code?`代码：${code}`:"",error?.stage?`阶段：${cleanText(error.stage,80)}`:"",action].filter(Boolean).join(" · ");}
  function selectedAIProfile(){const id=q("#fusion-ai-provider")?.value;return state.aiCatalog?.providers?.find(provider=>provider.provider_id===id)||null;}
  function readinessEntry(raw){return raw&&typeof raw==="object"&&["ready","blocked","verification_required","unavailable"].includes(raw.state)?{state:raw.state,reasonCode:cleanText(raw.reason_code,120),nextAction:cleanText(raw.next_action,120),verifiedUntil:Number.isFinite(raw.verified_until)?Number(raw.verified_until):null}:null;}
  function readinessCopy(entry){return entry?AI_REASON_COPY[entry.reasonCode]||"当前状态暂不可用；不会发送 AI 业务请求。":"状态尚未载入。";}
  function readinessLabel(entry){return entry?.state==="ready"?"可用":entry?.state==="verification_required"?"需要验证":entry?.state==="blocked"?"需要配置":"不可用";}
  function readinessExpiry(entry){if(!entry?.verifiedUntil)return "";const date=new Date(entry.verifiedUntil*1000);return Number.isFinite(date.getTime())?` · 有效至 ${date.toLocaleString("zh-CN",{hour12:false})}`:"";}
  function publicAIReadiness(raw){if(raw?.schema_version!=="ai-readiness-v1"||!raw.businesses||typeof raw.businesses!=="object")return null;const providerConnection=readinessEntry(raw.provider_connection),harness=readinessEntry(raw.harness),businesses={};for(const scope of AI_SCOPES){businesses[scope]=readinessEntry(raw.businesses[scope]);if(!businesses[scope])return null;}return providerConnection&&harness?{providerConnection,harness,businesses}:null;}
  function updateAIStatusSummary(readiness=publicAIReadiness(state.aiSettings?.readiness)){const node=q("#fusion-status-ai");if(!node)return;if(!readiness){node.textContent="AI 状态不可用";node.dataset.kind="error";return;}const rows=Object.values(readiness.businesses);if(readiness.providerConnection.state!=="ready"){node.textContent="AI 需配置或验证";node.dataset.kind="warning";}else if(readiness.harness.state!=="ready"){node.textContent="AI Harness 故障";node.dataset.kind="error";}else if(rows.some(entry=>entry.state!=="ready")){node.textContent="AI 业务需验证";node.dataset.kind="warning";}else{node.textContent="AI 可用";node.dataset.kind="success";}}
  function renderAIReadiness(){const readiness=publicAIReadiness(state.aiSettings?.readiness),connection=q("#fusion-ai-connection-state"),harness=q("#fusion-ai-harness-state"),host=q("#fusion-ai-business-readiness");if(connection)connection.textContent=readinessLabel(readiness?.providerConnection);if(q("#fusion-ai-connection-reason"))q("#fusion-ai-connection-reason").textContent=readinessCopy(readiness?.providerConnection)+readinessExpiry(readiness?.providerConnection);if(harness)harness.textContent=readinessLabel(readiness?.harness);if(q("#fusion-ai-harness-reason"))q("#fusion-ai-harness-reason").textContent=readinessCopy(readiness?.harness);if(host)host.innerHTML=readiness?Object.entries(AI_SCOPE_LABELS).map(([scope,label])=>{const entry=readiness.businesses[scope],action=entry.nextAction==="test_business_capability"?`<button type="button" data-ai-readiness-scope="${scope}">验证此项能力</button>`:entry.nextAction==="test_connection"?'<button type="button" data-ai-readiness-connection>验证连接</button>':entry.nextAction==="save_credential"?'<button type="button" data-ai-readiness-key>配置密钥</button>':entry.nextAction==="repair_harness_runtime"?'<button type="button" data-ai-readiness-repair>查看修复提示</button>':"";return `<section role="listitem" data-state="${esc(entry.state)}"><div><strong>${esc(label)}</strong><small>${esc(readinessCopy(entry)+readinessExpiry(entry))}</small></div><span>${esc(readinessLabel(entry))}</span>${action}</section>`;}).join(""):"<p>AI 业务状态暂不可用；不会发送业务请求。</p>";updateAIStatusSummary(readiness);return Boolean(readiness);}
  function renderCustomProvider(){const configured=state.aiCustom?.schema_version==="custom-ai-provider-v1"&&state.aiCustom.configured===true,active=state.aiSettings?.provider_id==="custom",status=q("#fusion-ai-custom-status");if(status)status.textContent=configured?`${cleanText(state.aiCustom.display_name,80)} 已配置；地址和密钥不会回显，修改时请重新输入。`:"尚未配置高级提供商。";const remove=q("#fusion-ai-custom-delete");if(remove)remove.disabled=!configured||active;for(const input of qa("[data-fusion-custom-task]")){const task=input.dataset.fusionCustomTask;input.value=cleanText(state.aiCustom?.task_models?.[task],160);}if(q("#fusion-ai-custom-name"))q("#fusion-ai-custom-name").value=configured?cleanText(state.aiCustom.display_name,80):"";if(q("#fusion-ai-custom-endpoint"))q("#fusion-ai-custom-endpoint").value="";}
  function renderAISettings(providerId=state.aiSettings?.provider_id){const provider=q("#fusion-ai-provider");if(!provider||!state.aiCatalog||!state.aiSettings)return false;provider.innerHTML=state.aiCatalog.providers.filter(profile=>AI_PROVIDERS.has(profile.provider_id)).map(profile=>`<option value="${esc(profile.provider_id)}">${esc(profile.display_name)}</option>`).join("");provider.value=providerId;provider.disabled=false;const profile=selectedAIProfile(),tasks={extraction:"文献提取",analysis:"证据分析",librarian_planning:"图书管理员规划",librarian_synthesis:"图书管理员回答"};q("#fusion-ai-models").innerHTML=Object.entries(tasks).map(([task,label])=>{const models=profile?.model_options?.[task]||[],selected=providerId===state.aiSettings.provider_id?state.aiSettings.task_models?.[task]:models[0];return `<div class="fusion-setting-row"><div><strong>${label}</strong><small>由当前提供商目录限定</small></div><select data-fusion-ai-task="${task}">${models.map(model=>`<option value="${esc(model)}"${model===selected?" selected":""}>${esc(model)}</option>`).join("")}</select></div>`;}).join("");const active=providerId===state.aiSettings.provider_id;q("#fusion-ai-model-save").disabled=!profile;q("#fusion-ai-key-save").disabled=!profile||!active;q("#fusion-ai-key-delete").disabled=!active||state.aiCredential?.configured!==true;q("#fusion-ai-test").disabled=!active||state.aiCredential?.configured!==true;q("#fusion-ai-test-plan").textContent="连接验证最多调用模型 1 次；每项业务能力按服务端返回的实际调用上限单独确认。";renderAIReadiness();renderCustomProvider();return true;}
  async function loadSelectedCredential(providerId){state.aiCredential=await request(`/api/desktop/ai/credentials/${providerId}`);if(q("#fusion-ai-credential-state"))q("#fusion-ai-credential-state").textContent=state.aiCredential?.configured?"本机加密存储：已保存":"本机加密存储：未配置";return state.aiCredential;}
  async function loadAISettingsUI(){try{state.aiContext=null;const [,custom]=await Promise.all([loadAIContext(),readOnlyJSON(ROUTES.aiCustom)]);if(custom?.schema_version!=="custom-ai-provider-v1"||custom.provider_id!=="custom")throw safeError("ai_custom_state_invalid","高级提供商状态无效。");state.aiCustom=custom;await loadSelectedCredential(state.aiSettings.provider_id);renderAISettings();aiStatus(`已载入 ${state.aiContext.label} 配置与业务状态。`,"success");return true;}catch(_error){renderAIReadiness();aiStatus("无法读取 AI 设置；所有 AI 动作保持关闭。","error");return false;}}
  async function selectAIProvider(providerId){if(!AI_PROVIDERS.has(providerId)||!state.aiCatalog?.providers?.some(profile=>profile.provider_id===providerId))return false;try{await loadSelectedCredential(providerId);renderAISettings(providerId);return true;}catch(_error){aiStatus("无法读取所选提供商的密钥状态。","error");return false;}}
  async function saveAIModels(){const profile=selectedAIProfile();if(!profile||!Number.isInteger(state.aiSettings?.revision))return;const taskModels=Object.fromEntries(qa("[data-fusion-ai-task]").map(select=>[select.dataset.fusionAiTask,select.value]));aiStatus("正在保存提供商与模型…","loading");try{await request(ROUTES.aiSettings,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({provider_id:profile.provider_id,task_models:taskModels,expected_revision:state.aiSettings.revision})});state.aiContext=null;await loadAISettingsUI();}catch(_error){aiStatus("AI 设置未保存；请重新读取后再试。","error");}}
  async function saveAIKey(){const profile=selectedAIProfile(),input=q("#fusion-ai-key"),apiKey=String(input?.value||"");if(!profile||profile.provider_id!==state.aiSettings?.provider_id||!apiKey)return aiStatus("请先选择已启用的提供商并粘贴 API 密钥。","error");input.value="";aiStatus("正在保存到当前电脑的私有加密存储…","loading");try{await request(`/api/desktop/ai/credentials/${profile.provider_id}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({api_key:apiKey})});await loadAISettingsUI();return await runAIVerification(null);}catch(error){aiStatus(aiErrorCopy(error,"密钥未能保存；不会回显或发送密钥。"),"error");return false;}}
  async function deleteAIKey(){const profile=selectedAIProfile();if(!profile||typeof globalThis.confirm!=="function"||!globalThis.confirm(`确认删除 ${profile.display_name} 的本机 API 密钥？`))return;try{await request(`/api/desktop/ai/credentials/${profile.provider_id}`,{method:"DELETE"});await loadAISettingsUI();aiStatus("本机 API 密钥已删除。","success");}catch(_error){aiStatus("密钥未删除，请稍后重试。","error");}}
  function verificationLimit(scope=null){const plan=state.aiCatalog?.capability_test;if(plan?.provider_id!==state.aiSettings?.provider_id)return 0;if(scope===null)return Number(plan.connection_maximum_model_calls)===1?1:0;const value=Number(plan.business_maximum_model_calls?.[scope]);return AI_SCOPES.has(scope)&&Number.isInteger(value)&&value>0&&value<=AI_VERIFICATION_CALL_LIMITS[scope]?value:0;}
  async function runAIVerification(scope=null){if(state.aiBusy){aiStatus("已有 AI 验证正在进行，请等待当前任务结束。","loading");return false;}state.aiBusy=true;try{if(!(await loadAISettingsUI()))return false;const profile=selectedAIProfile(),limit=verificationLimit(scope);if(!profile||profile.provider_id!==state.aiSettings?.provider_id||!Number.isInteger(state.aiSettings.revision)||!limit)throw safeError("ai_test_state_invalid","验证状态尚未准备好，请重新载入 AI 设置。");const readiness=publicAIReadiness(state.aiSettings?.readiness);if(scope&&readiness?.providerConnection.state!=="ready")throw Object.assign(safeError("ai_runtime_verification_required","AI 连接验证已过期或配置已变化。"),{causeCode:"ai_connection_verification_expired",stage:"business_capability_preflight",nextAction:"verify_connection"});const body={expected_revision:state.aiSettings.revision,...(scope?{scope}:{})},prepared=await request(`/api/desktop/ai/providers/${profile.provider_id}/test-actions`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),calls=Number(prepared?.maximum_calls);if(prepared?.schema_version!=="server-prepared-ai-action-v1"||prepared.scope!=="capability_test"||prepared.provider_id!==profile.provider_id||!Number.isInteger(calls)||calls<1||calls>limit||(!scope&&calls!==1))throw safeError("ai_test_invalid","验证计划无效。");const label=scope?`${AI_SCOPE_LABELS[scope]}能力验证`:`${profile.display_name} 连接验证`;if(typeof globalThis.confirm!=="function"||!globalThis.confirm(`${label}\n\n模型：${Array.isArray(prepared.model)?prepared.model.join("、"):prepared.model}\n最多调用 ${calls} 次，可能产生 API 费用。\n\n是否继续？`)){aiStatus("已取消验证；没有执行模型调用。","info");return false;}const issued=await request(ROUTES.consent,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action_id:prepared.action_id})});if(issued?.schema_version!=="ai-consent-v1"||issued.scope!=="capability_test"||typeof issued.nonce!=="string"||!issued.nonce)throw safeError("ai_test_consent_invalid","验证授权无效。");await request(`/api/desktop/ai/providers/${profile.provider_id}/test`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action_id:prepared.action_id,consent_nonce:issued.nonce})});await loadAISettingsUI();aiStatus(scope?`${AI_SCOPE_LABELS[scope]}已验证。`:"提供商连接已验证。","success");return true;}catch(error){const fallback=scope?`${AI_SCOPE_LABELS[scope]}验证未完成；不会发送业务请求。`:"连接验证未完成；不会回显密钥或自动重试。";aiStatus(aiErrorCopy(error,fallback),"error");return false;}finally{state.aiBusy=false;}}
  async function testAIConnection(){return runAIVerification(null);}
  async function ensureAIReadiness(scope){if(!AI_SCOPES.has(scope)||!(await loadAISettingsUI()))return false;let readiness=publicAIReadiness(state.aiSettings?.readiness);if(!readiness)return false;if(readiness.providerConnection.state!=="ready"){if(readiness.providerConnection.nextAction==="save_credential"){switchView("settings");selectSettingsSection("ai");q("#fusion-ai-key")?.focus();aiStatus("请先保存当前提供商的 API 密钥。","error");return false;}if(readiness.providerConnection.nextAction!=="test_connection"||!(await runAIVerification(null)))return false;readiness=publicAIReadiness(state.aiSettings?.readiness);}
    if(readiness?.harness.state!=="ready"&&["librarian","selected_evidence_chat"].includes(scope)){aiStatus(readinessCopy(readiness.harness),"error");return false;}let business=readiness?.businesses?.[scope];if(business?.state==="verification_required"&&business.nextAction==="test_business_capability"){if(!(await runAIVerification(scope)))return false;readiness=publicAIReadiness(state.aiSettings?.readiness);business=readiness?.businesses?.[scope];}if(business?.state!=="ready"){aiStatus(readinessCopy(business),"error");return false;}return true;}
  async function saveCustomProvider(){if(state.aiSettings?.provider_id==="custom")return aiStatus("请先切换并保存 DeepSeek 或 OpenAI，再修改高级提供商。","error");const displayName=String(q("#fusion-ai-custom-name")?.value||"").trim(),chatEndpoint=String(q("#fusion-ai-custom-endpoint")?.value||"").trim(),taskModels=Object.fromEntries(qa("[data-fusion-custom-task]").map(input=>[input.dataset.fusionCustomTask,String(input.value||"").trim()]));if(!displayName||!chatEndpoint||Object.values(taskModels).some(value=>!value))return aiStatus("请完整填写显示名称、HTTPS 地址与四项任务模型。","error");try{await request(ROUTES.aiCustom,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({display_name:displayName,chat_endpoint:chatEndpoint,task_models:taskModels,expected_revision:Number(state.aiCustom?.revision||0)})});await loadAISettingsUI();aiStatus("高级提供商已保存；地址不会回显。","success");return true;}catch(_error){aiStatus("高级提供商未保存；请核对公开 HTTPS Chat Completions 地址。","error");return false;}}
  async function deleteCustomProvider(){if(state.aiSettings?.provider_id==="custom")return aiStatus("请先切换并保存 DeepSeek 或 OpenAI，再删除高级提供商。","error");const revision=Number(state.aiCustom?.revision);if(!Number.isInteger(revision)||revision<1||typeof globalThis.confirm!=="function"||!globalThis.confirm("确认删除高级提供商配置？已保存的密钥也应单独删除。"))return false;try{await request(`${ROUTES.aiCustom}/${revision}`,{method:"DELETE"});await loadAISettingsUI();aiStatus("高级提供商配置已删除。","success");return true;}catch(_error){aiStatus("高级提供商未删除，请刷新状态后重试。","error");return false;}}
  async function preparedAuthorization(scope,domainRequest,detail="") {
    let prepared=null;
    if(scope==="librarian"){const readiness=publicAIReadiness(state.aiSettings?.readiness),alreadyReady=readiness?.providerConnection.state==="ready"&&readiness?.harness.state==="ready"&&readiness?.businesses?.librarian?.state==="ready";if(!alreadyReady){prepared=await request(aiRoute(scope,"prepare"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(domainRequest)});if(prepared?.librarian_core_version==="librarian-v3")return {localResult:prepared};if(!(await ensureAIReadiness(scope)))return null;prepared=null;}}
    else if(scope==="literature_extraction"){try{prepared=await request(aiRoute(scope,"prepare"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(domainRequest)});}catch(error){if(!["verify_connection","test_connection","test_business_capability","save_credential","repair_harness_runtime"].includes(cleanText(error?.nextAction,120)))throw error;if(!(await ensureAIReadiness(scope)))return null;prepared=await request(aiRoute(scope,"prepare"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(domainRequest)});}}
    else if(!(await ensureAIReadiness(scope)))return null;
    prepared=prepared||await request(aiRoute(scope,"prepare"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(domainRequest)});
    if(scope==="librarian"&&prepared?.librarian_core_version==="librarian-v3")return {localResult:prepared};
    const context=await loadAIContext(),disclosureVersion=globalThis.AutoResearchAIConsent?.disclosureVersions?.[scope];
    const calls=Number(prepared?.maximum_calls);if(prepared?.schema_version!=="server-prepared-ai-action-v1"||prepared.scope!==scope||prepared.provider_id!==context.provider_id||prepared.disclosure_version!==disclosureVersion||typeof prepared.action_id!=="string"||!prepared.action_id||!Number.isInteger(calls)||calls<1||calls>AI_CALL_LIMITS[scope])throw safeError("ai_prepared_action_invalid","AI 操作准备结果无效，请重试。");
    const gate=globalThis.AutoResearchAIConsent,disclosureContext={...context,disclosure_version:disclosureVersion},disclosureAccepted=gate.accepted(scope,disclosureContext),disclosure=disclosureAccepted?"":gate.disclosureSummary(scope,disclosureContext);if(!disclosureAccepted&&!disclosure)throw safeError("ai_consent_gate_unavailable","AI 数据披露授权组件未载入；本次请求已安全停止。");
    const model=Array.isArray(prepared.model)?prepared.model.join("、"):String(prepared.model||"当前模型"),label=String(prepared.display||"AI 操作"),budgetLabel=scope==="literature_extraction"?`本次完整提取任务最多调用 ${calls} 次，可能产生 API 费用。`:`本阶段最多调用 ${calls} 次，可能产生 API 费用。`,prompt=[disclosure,label,detail,`模型：${model}`,budgetLabel,"是否继续？"].filter(Boolean).join("\n\n");
    if(typeof globalThis.confirm!=="function"||!globalThis.confirm(prompt))return null;
    if(!disclosureAccepted&&gate.remember(scope,disclosureContext)!==true)throw safeError("ai_consent_gate_unavailable","AI 数据披露确认未能记录；本次请求已安全停止。");
    const issued=await request(ROUTES.consent,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action_id:prepared.action_id})});
    if(issued?.schema_version!=="ai-consent-v1"||issued.scope!==scope||typeof issued.nonce!=="string"||!issued.nonce)throw safeError("ai_consent_invalid","AI 授权凭证无效，请重试。");
    return {actionId:prepared.action_id,nonce:issued.nonce};
  }
  function validAIExecutionJob(job,scope){return job?.schema_version==="ai-execution-job-v1"&&job.scope===scope&&typeof job.job_id==="string"&&/^ai_job_[A-Za-z0-9_-]{24,160}$/.test(job.job_id)&&["queued","running","completed","failed"].includes(job.status)&&Array.isArray(job.events);}
  function literaturePaperKey(paper){const doi=cleanText(paper?.doi,300).toLowerCase(),title=cleanText(paper?.title,500).toLowerCase();return doi?`doi:${doi}`:title?`title:${title}`:"";}
  function rememberLiteratureJob(job,{paper=null}={}){if(!validAIExecutionJob(job,"literature_extraction"))return false;state.literatureTask.jobId=job.job_id;state.literatureTask.status=job.status;state.literatureTask.job=job;if(paper)state.literatureTask.paperKey=literaturePaperKey(paper);for(const event of job.events){const stage=cleanText(event?.stage,80);if(["initial_focus","coverage_gap","coverage_verification","adversarial_branches","third_review","publishing","completed"].includes(stage)){state.literatureTask.stage=stage;const eventState=job.status==="failed"?"error":stage==="completed"||job.status==="completed"?"success":"running";aiProgress("literature_extraction",stage,eventState,cleanText(event?.label,300),cleanText(event?.detail,500));}}return true;}
  async function executePrepared(scope,authorization,{existingJob=null,deadlineMs=31*60*1000,literaturePaper=null}={}){
    let job=existingJob||await request(aiRoute(scope,"execute-jobs"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action_id:authorization.actionId,consent_nonce:authorization.nonce})});
    if(!validAIExecutionJob(job,scope))throw safeError("ai_execution_job_invalid","AI 任务状态无效；没有自动重试或重复收费。");
    if(scope==="literature_extraction")rememberLiteratureJob(job,{paper:literaturePaper});
    const deadline=Date.now()+Math.max(30*60*1000,Number(deadlineMs)||0),seen=new Set();
    for(;;){
      for(const event of Array.isArray(job.events)?job.events:[]){const sequence=Number(event?.sequence),key=`${job.job_id}:${sequence}`;if(!Number.isSafeInteger(sequence)||sequence<1||seen.has(key))continue;seen.add(key);projectAIActivity(scope,{...event,job_id:job.job_id});}
      if(job.status==="completed"){if(!job.result||typeof job.result!=="object"||Array.isArray(job.result))throw safeError("business_action_result_invalid","AI 结果未通过安全校验。");return job.result;}
      if(job.status==="failed"){const error=job.error&&typeof job.error==="object"?job.error:{};throw safeError(String(error.cause_code||error.code||"business_action_execution_failed"),String(error.message||"AI 业务动作未能完成。"),error);}
      if(!["queued","running"].includes(job.status))throw safeError("ai_execution_job_invalid","AI 任务状态无效；没有自动重试或重复收费。");
      if(Date.now()>=deadline){if(scope==="literature_extraction"){state.literatureTask.status=job.status;const message="任务仍在后台继续，可离开此页并稍后返回查看；系统不会重复准备、授权或收费。";if(!state.literatureTask.paperKey||state.literatureTask.paperKey===literaturePaperKey(state.paper))setLiteratureActionStatus(message,"info");else setOperation(message,"info");return {schema_version:"ai-execution-background-v1",job_id:job.job_id,status:job.status};}throw safeError("ai_execution_job_timeout","AI 任务等待超时；不会自动重试或重复收费。",{retryable:true,next_action:"retry_same_request"});}
      await new Promise(resolve=>globalThis.setTimeout(resolve,350));
      job=await request(`/api/desktop/ai/jobs/${job.job_id}`);
      if(!validAIExecutionJob(job,scope))throw safeError("ai_execution_job_invalid","AI 任务状态无效；没有自动重试或重复收费。");
      if(scope==="literature_extraction")rememberLiteratureJob(job,{paper:literaturePaper});
    }
  }
  function applyLiteratureResult(result){if(!validLiteratureCommit(result))throw safeError("literature_commit_invalid","文献处理没有返回完整发布收据；未显示处理完成。");const resultKey=literaturePaperKey(result.paper),currentKey=literaturePaperKey(state.paper),belongsToCurrent=Boolean(resultKey&&resultKey===currentKey);state.literatureTask.status="completed";state.literatureTask.stage="completed";state.literatureTask.paperKey=resultKey||state.literatureTask.paperKey;const visual=result.visual_stage_status==="ready"?`表格 ${result.table_candidate_count} 项、图片 ${result.figure_candidate_count} 项`:"未检测到可发布表格或图片，请核对原文";if(!belongsToCurrent){setOperation(`后台文献处理完成：${cleanText(result.paper?.title||"另一篇论文",120)}；打开该论文可查看收据。`,"success");aiProgress("literature_extraction","completed","success","后台文献提取完成",visual);return false;}renderLiteratureReceipt(result);if(result.status==="saved_index_pending"){showSearchIndexRecovery(true);setLiteratureActionStatus(`科学记录已保存（${visual}），但搜索索引待恢复；点击“恢复搜索索引”即可继续。`,"warning");aiProgress("literature_extraction","publishing","error","数据已保存，搜索索引待恢复",visual);}else{showSearchIndexRecovery(false);setLiteratureActionStatus(`处理完成：发布 ${result.published_item_count} 条，${visual}，人工复核 ${result.manual_review_count} 项。`,"success");aiProgress("literature_extraction","completed","success","提取、发布与索引完成",`发布 ${result.published_item_count} 条 · ${visual} · 待人工复核 ${result.manual_review_count} 项`);}return true;}
  async function reconnectLiteratureJob(){if(state.literatureTask.watch)return state.literatureTask.watch;const watch=(async()=>{if(!state.literatureTask.directory)await loadLiteratureTaskDirectory();const listed=await readOnlyJSON("/api/desktop/ai/actions/literature_extraction/jobs");if(listed?.schema_version!=="ai-execution-job-list-v1"||listed.scope!=="literature_extraction"||!Array.isArray(listed.jobs))throw safeError("ai_execution_job_list_invalid","文献任务目录无效。");const job=listed.jobs[0];if(!job)return null;if(!validAIExecutionJob(job,"literature_extraction"))throw safeError("ai_execution_job_invalid","文献任务状态无效。");const recovery=currentLiteratureRecovery(),resultPaper=job.status==="completed"?job.result?.paper:null,jobPaper=resultPaper||(recovery&&["authorized","running","paused","validated"].includes(recovery.state)?recovery.paper:null);rememberLiteratureJob(job,{paper:jobPaper});const currentOwnsJob=Boolean(state.literatureTask.paperKey&&state.literatureTask.paperKey===literaturePaperKey(state.paper));if(job.status==="completed"){applyLiteratureResult(job.result);return job.result;}if(job.status==="failed"){const detail=job.error&&typeof job.error==="object"?job.error:{};if(!currentOwnsJob){setOperation("另一篇论文的后台提取任务未完成；打开对应论文可查看恢复提示。","warning");return null;}throw safeError(String(detail.cause_code||detail.code||"business_action_execution_failed"),String(detail.message||"文献处理未完成。"),detail);}const message="已重新连接后台文献任务；可继续浏览其他页面，完成后返回查看收据。";if(currentOwnsJob)setLiteratureActionStatus(message,"loading");else setOperation(message,"loading");const result=await executePrepared("literature_extraction",null,{existingJob:job,literaturePaper:jobPaper});if(result?.schema_version!=="ai-execution-background-v1")applyLiteratureResult(result);return result;})();state.literatureTask.watch=watch;try{return await watch;}catch(error){const message=aiErrorCopy(error,"文献任务状态暂不可用；不会自动重试或重复收费。");if(!state.literatureTask.paperKey||state.literatureTask.paperKey===literaturePaperKey(state.paper))setLiteratureActionStatus(message,"error");else setOperation(message,"error");return null;}finally{if(state.literatureTask.watch===watch)state.literatureTask.watch=null;}}
  function harnessUnavailableMessage(error){const code=String(error?.code||"");if(code==="harness_dependency_mismatch")return "AI 执行环境版本不兼容，当前无法安全使用；不会回退旧 AI 接口。";if(code==="harness_budget_exhausted")return "本次授权预算已用完，系统没有自动重试或继续收费。请缩小问题范围后重新发起一次。";if(code==="harness_provider_unavailable")return AI_ERROR_CODE_COPY[code];if(code==="harness_provider_response_invalid")return AI_ERROR_CODE_COPY[code];if(["harness_runtime_unavailable","harness_runtime_failed"].includes(code))return "AI 执行环境或文献证据源当前不可用；请检查 API 配置、Harness 状态和资料来源。精确检索仍可使用。";if(["harness_private_forbidden","harness_scope_unsupported"].includes(code))return "当前资料来源不支持受保护的 AI 解释；不会发送数据或回退旧 AI 接口。";if(code&&error?.message)return `${code} · ${cleanText(error.message,500)}`;return "本次 AI 请求未完成；精确检索和本机数据不受影响。";}
  function visibleEvidenceDetail(){
    const active=documentTabs?.activeTab?.(), candidates=[active,documentTabs?.activeTab?.("secondary"),documentTabs?.activeTab?.("primary")],seen=new Set();
    for(const tab of candidates){
      if(!tab||seen.has(tab.tabId))continue;seen.add(tab.tabId);
      const surface=tab.groupId==="secondary"?q("#fusion-secondary-editor-surface"):q("#fusion-primary-editor-surface"),row=tab.payload?.row;
      if(tab.kind==="evidence"&&tab.payload?.status==="ready"&&!surface?.hidden&&row&&evidenceIdentity(row)===tab.payload?.evidenceIdentity)return {tab,row,groupId:tab.groupId};
    }
    return null;
  }
  function activeEvidenceChatHost(){const detail=visibleEvidenceDetail();if(!detail)return null;const surface=detail.groupId==="secondary"?q("#fusion-secondary-editor-surface"):q("#fusion-primary-editor-surface");return [...(surface?.querySelectorAll?.("[data-evidence-chat]")||[])].find(node=>node.dataset.evidenceTabId===detail.tab.tabId)||null;}
  function selectedEvidenceAIIdentity(row=visibleEvidenceDetail()?.row) {
    if(!["paper","search"].includes(state.view)||!row||!["official","workspace"].includes(row.sourceScope)||!["item","finding","table","figure"].includes(row.type))return null;
    const sourceId=row.sourceScope==="workspace"?"workspace":cleanText(row.sourceId,500),entityUid=row.sourceScope==="workspace"?String(row.assetId||row.itemId||""):cleanText(row.entityUid,500);return sourceId&&entityUid?{key:`${row.sourceScope}:${sourceId}:${row.type}:${entityUid}`,sourceScope:row.sourceScope,sourceId,entityType:row.type,entityUid}:null;
  }
  function publicSelectedEvidenceAnnotations(result,identity){const entity=result?.entity;if(!identity||!entity||cleanText(entity.type,20)!==identity.entityType||cleanText(entity.id,500)!==identity.entityUid)return null;const pages=(Array.isArray(result.evidence_pages)?result.evidence_pages:[]).map(Number).filter((value,index,values)=>Number.isSafeInteger(value)&&value>0&&values.indexOf(value)===index).slice(0,20),notes=(Array.isArray(result.evidence_notes)?result.evidence_notes:[]).map(value=>cleanText(value,4000)).filter(Boolean).slice(0,8),limitations=(Array.isArray(result.limitations)?result.limitations:[]).map(value=>cleanText(value,1000)).filter(Boolean).slice(0,8);return {pages,notes,limitations};}
  function cleanEvidenceMessage(message){const role=message?.role,content=cleanText(message?.content,12000);if(!["user","assistant"].includes(role)||!content)return null;const annotations=message?.annotations&&typeof message.annotations==="object"?{pages:(Array.isArray(message.annotations.pages)?message.annotations.pages:[]).map(Number).filter(value=>Number.isSafeInteger(value)&&value>0).slice(0,20),notes:(Array.isArray(message.annotations.notes)?message.annotations.notes:[]).map(value=>cleanText(value,4000)).filter(Boolean).slice(0,8),limitations:(Array.isArray(message.annotations.limitations)?message.annotations.limitations:[]).map(value=>cleanText(value,1000)).filter(Boolean).slice(0,8)}:null;return {role,content,...(annotations?{annotations}:{})};}
  function storedEvidenceMessage(message){const clean=cleanEvidenceMessage(message);if(!clean)return null;if(!clean.annotations)return clean;const annotations={pages:clean.annotations.pages.slice(0,20),notes:clean.annotations.notes.map(value=>cleanText(value,1000)).filter(Boolean).slice(0,20),limitations:clean.annotations.limitations.map(value=>cleanText(value,1000)).filter(Boolean).slice(0,20)};return {role:clean.role,content:clean.content,annotations};}
  function evidenceHistoryIdentity(value){if(!value||!["official","workspace"].includes(value.sourceScope)||!["item","finding","table","figure"].includes(value.entityType))return null;const sourceId=value.sourceScope==="workspace"?"workspace":cleanText(value.sourceId,256),entityUid=cleanText(value.entityUid,256);if(!sourceId||!entityUid||/[\\/]/.test(sourceId)||/[\\/]/.test(entityUid))return null;return {key:`${value.sourceScope}:${sourceId}:${value.entityType}:${entityUid}`,sourceScope:value.sourceScope,sourceId,entityType:value.entityType,entityUid};}
  function publicEvidenceHistory(payload){if(payload?.schema_version!=="evidence-chat-history-v1"||!Number.isSafeInteger(payload.revision)||payload.revision<0||typeof payload.storage!=="string"||!payload.storage||!Array.isArray(payload.threads))return null;const threads=new Map();for(const raw of payload.threads){const identity=evidenceHistoryIdentity({sourceScope:raw?.source_scope,sourceId:raw?.source_id,entityType:raw?.entity_type,entityUid:raw?.entity_uid}),messages=Array.isArray(raw?.messages)?raw.messages.slice(-20).map(storedEvidenceMessage):[];if(!identity||messages.some(message=>!message))return null;threads.set(identity.key,{identity,title:cleanText(raw?.title,500)||"当前证据",messages});}return {revision:payload.revision,storage:cleanText(payload.storage,120),threads};}
  function applyEvidenceHistory(payload,{projectCurrent=true}={}){const clean=publicEvidenceHistory(payload);if(!clean)throw safeError("evidence_chat_history_invalid","证据对话历史响应无效。");state.evidenceChat.historyRevision=clean.revision;state.evidenceChat.historyBackend="desktop-secure";state.evidenceChat.historyThreads=clean.threads;state.evidenceChat.historyMessage="对话历史已在本机加密保存。";for(const [key,thread] of clean.threads)if(!state.evidenceChat.threads.has(key))state.evidenceChat.threads.set(key,thread.messages.map(message=>({...message})));if(projectCurrent&&state.evidenceChat.identity&&!state.evidenceChat.messages.length){const thread=clean.threads.get(state.evidenceChat.identity);if(thread)state.evidenceChat.messages=thread.messages.map(message=>({...message}));}return clean;}
  async function loadEvidenceChatHistory({projectCurrent=true}={}){try{applyEvidenceHistory(await readOnlyJSON(ROUTES.evidenceChatHistory),{projectCurrent});}catch(_error){state.evidenceChat.historyBackend="unavailable";state.evidenceChat.historyMessage="本机对话历史暂不可用；本轮问答仍可继续。";}syncSelectedEvidenceAI();renderSelectedEvidenceChat();return state.evidenceChat.historyThreads.size;}
  async function mutateEvidenceChatHistory(makeBody,{retry=true}={}){try{return applyEvidenceHistory(await request(ROUTES.evidenceChatHistory,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(makeBody(state.evidenceChat.historyRevision))}),{projectCurrent:false});}catch(error){if(retry&&error?.code==="evidence_chat_history_revision_conflict"){await loadEvidenceChatHistory({projectCurrent:false});if(state.evidenceChat.historyBackend==="desktop-secure")return mutateEvidenceChatHistory(makeBody,{retry:false});}throw error;}}
  function queueEvidenceChatSave(identity,row,messages){const safeIdentity=evidenceHistoryIdentity(identity),safeMessages=messages.slice(-20).map(storedEvidenceMessage).filter(Boolean);if(!safeIdentity||!safeMessages.length)return false;const title=cleanText(row?.title,500)||"当前证据";state.evidenceChat.historySave=state.evidenceChat.historySave.catch(()=>false).then(async()=>{if(state.evidenceChat.historyBackend==="loading")await loadEvidenceChatHistory({projectCurrent:false});if(state.evidenceChat.historyBackend!=="desktop-secure")throw safeError("evidence_chat_history_unavailable","本机对话历史暂不可用。");await mutateEvidenceChatHistory(expected_revision=>({operation:"upsert",expected_revision,thread:{source_scope:safeIdentity.sourceScope,source_id:safeIdentity.sourceId,entity_type:safeIdentity.entityType,entity_uid:safeIdentity.entityUid,title,messages:safeMessages}}));state.evidenceChat.historyMessage="当前证据对话已在本机加密保存。";}).catch(()=>{state.evidenceChat.historyMessage="回答已完成，但本机历史保存暂不可用；你可以继续提问。";}).finally(()=>syncSelectedEvidenceAI());return true;}
  function evidenceAnnotationsHTML(value){if(!value)return "";const pages=value.pages?.length?`<p><strong>依据页码</strong> ${esc(value.pages.map(page=>`第 ${page} 页`).join(" · "))}</p>`:"",notes=value.notes?.length?`<details><summary>查看回答所依据的证据说明</summary>${value.notes.map(note=>`<blockquote>${esc(note)}</blockquote>`).join("")}</details>`:"",limitations=value.limitations?.length?`<div class="fusion-evidence-answer-limitations"><strong>限制</strong><ul>${value.limitations.map(item=>`<li>${esc(item)}</li>`).join("")}</ul></div>`:"";return pages||notes||limitations?`<section class="fusion-evidence-answer-notes">${pages}${notes}${limitations}<div class="fusion-evidence-answer-actions"><button type="button" data-evidence-ai-open-detail>返回当前证据</button><button type="button" data-evidence-ai-open-pdf>打开原文并定位</button></div></section>`:"";}
  function renderSelectedEvidenceChat() {
    const host=activeEvidenceChatHost(),output=host?.querySelector?.("[data-evidence-ai-output]");if(!output)return;aiExperience?.renderConversation(output,state.evidenceChat.messages,{assistantLabel:"证据 AI",assistantMark:"AI",busy:state.evidenceChat.busy,busyLabel:"正在核对当前证据",emptyTitle:"围绕当前证据连续追问",emptyText:"可以询问含义、条件、来源和与同论文证据的关系。"});const assistantNodes=[...(output.querySelectorAll?.('.fusion-chat-message[data-role="assistant"]')||[])],assistantMessages=state.evidenceChat.messages.filter(message=>message.role==="assistant");assistantMessages.forEach((message,index)=>{const node=assistantNodes[index],html=evidenceAnnotationsHTML(message.annotations);if(!node||!html||node.querySelector?.(".fusion-evidence-answer-notes"))return;const wrapper=document.createElement?.("div");if(!wrapper)return;wrapper.innerHTML=html;const notes=wrapper.firstElementChild||wrapper;node.querySelector?.("div")?.append?.(notes);});bindEvidenceChatActions();
  }
  function rememberEvidenceThread(identity=state.evidenceChat.identity){if(!identity)return;state.evidenceChat.threads.delete(identity);state.evidenceChat.threads.set(identity,state.evidenceChat.messages.slice(-20).map(cleanEvidenceMessage).filter(Boolean));while(state.evidenceChat.threads.size>20)state.evidenceChat.threads.delete(state.evidenceChat.threads.keys().next().value);}
  function appendEvidenceThread(identity,message){const cleanMessage=cleanEvidenceMessage(message);if(!cleanMessage)return null;const current=identity===state.evidenceChat.identity,thread=current?state.evidenceChat.messages:Array.from(state.evidenceChat.threads.get(identity)||[],cleanEvidenceMessage).filter(Boolean);thread.push(cleanMessage);if(current)rememberEvidenceThread(identity);else{state.evidenceChat.threads.delete(identity);state.evidenceChat.threads.set(identity,thread.slice(-20));}return cleanMessage;}
  function syncSelectedEvidenceAI() {
    const detail=visibleEvidenceDetail(),host=activeEvidenceChatHost(),identity=selectedEvidenceAIIdentity(detail?.row);if(identity?.key!==state.evidenceChat.identity){rememberEvidenceThread();state.evidenceChat.request+=1;state.evidenceChat.identity=identity?.key||"";state.evidenceChat.messages=identity?Array.from(state.evidenceChat.threads.get(identity.key)||[],message=>({...message})):[];renderSelectedEvidenceChat();}state.evidenceChat.busy=Boolean(identity&&state.evidenceChat.pending.has(identity.key));
    const button=host?.querySelector?.("[data-evidence-ai-send]"),clear=host?.querySelector?.("[data-evidence-ai-clear]"),reason=host?.querySelector?.("[data-evidence-ai-reason]"),historyStatus=host?.querySelector?.("[data-evidence-history-status]"),question=host?.querySelector?.("[data-evidence-ai-question]"),context=host?.querySelector?.("[data-evidence-ai-context]"),progress=host?.querySelector?.("[data-evidence-ai-inline-progress]");if(!host||!button||!reason)return;
    const backgroundBusy=state.evidenceChat.pending.size>0&&!state.evidenceChat.busy;
    button.disabled=!identity||state.evidenceChat.pending.size>0;if(clear)clear.disabled=!identity||state.evidenceChat.busy||!state.evidenceChat.messages.length;if(question)question.disabled=!identity||state.evidenceChat.pending.size>0;if(progress){progress.hidden=!state.evidenceChat.busy;progress.textContent=state.evidenceChat.busy?"正在核对当前证据…":"";}
    if(context)context.textContent=identity?cleanText(detail?.row?.title||"当前证据",46):"当前证据";
    if(historyStatus)historyStatus.textContent=identity?state.evidenceChat.historyMessage:"此来源不会保存证据对话。";
    reason.textContent=backgroundBusy?"另一条证据正在回答；你可以继续浏览，完成后回答会保存回原证据。":identity?"仅发送这个详情标签中的证据、同论文兼容证据、问题和本轮对话；每次调用前都需明确授权。":"该证据来源暂不支持 Harness 解释；不会发送请求，也不会回退旧 AI 接口。";
  }
  async function clearSelectedEvidenceThread(){const detail=visibleEvidenceDetail(),identity=evidenceHistoryIdentity(selectedEvidenceAIIdentity(detail?.row));if(!identity||state.evidenceChat.busy)return false;if(typeof globalThis.confirm==="function"&&!globalThis.confirm("清空当前证据对话？\n\n这会删除这一条证据的本机加密对话，不会清空其他证据、图书管理员历史或科学数据。"))return false;try{await state.evidenceChat.historySave.catch(()=>false);if(state.evidenceChat.historyBackend==="loading")await loadEvidenceChatHistory({projectCurrent:false});if(state.evidenceChat.historyBackend!=="desktop-secure")throw safeError("evidence_chat_history_unavailable","本机证据对话历史暂不可用。");try{await mutateEvidenceChatHistory(expected_revision=>({operation:"delete",expected_revision,identity:{source_scope:identity.sourceScope,source_id:identity.sourceId,entity_type:identity.entityType,entity_uid:identity.entityUid}}));}catch(error){if(error?.code!=="evidence_chat_history_not_found")throw error;}state.evidenceChat.requests.set(identity.key,(state.evidenceChat.requests.get(identity.key)||0)+1);state.evidenceChat.threads.delete(identity.key);state.evidenceChat.historyThreads.delete(identity.key);if(state.evidenceChat.identity===identity.key)state.evidenceChat.messages=[];state.evidenceChat.historyMessage="当前证据对话已从本机加密历史中删除。";renderSelectedEvidenceChat();syncSelectedEvidenceAI();return true;}catch(_error){state.evidenceChat.historyMessage="当前对话未删除；本机历史暂不可用，请稍后重试。";syncSelectedEvidenceAI();return false;}}
  function focusCurrentEvidenceFromChat(){const detail=visibleEvidenceDetail();if(!detail)return false;activateDocumentTab(detail.tab.tabId);const surface=detail.groupId==="secondary"?q("#fusion-secondary-editor-surface"):q("#fusion-primary-editor-surface");surface?.querySelector?.("[data-evidence-detail-tab]")?.focus?.({preventScroll:true});return true;}
  function openCurrentEvidencePDFFromChat(){const detail=visibleEvidenceDetail();if(!detail||!detailPDFURL(detail.row))return false;if(state.evidenceDetailOpen&&evidenceIdentity(state.evidenceDetail)===evidenceIdentity(detail.row))return openDetailPDF();const control=qa("[data-secondary-open-pdf]").find(button=>button.dataset.secondaryOpenPdf===detail.tab.tabId);control?.click?.();return Boolean(control);}
  function bindEvidenceChatActions(){qa("[data-table-structure-tab]").forEach(panel=>bindTableStructureActions(panel.dataset.tableStructureTab));qa("[data-evidence-ai-form]").forEach(form=>{if(form.dataset.bound==="true")return;form.dataset.bound="true";form.addEventListener("submit",event=>{event.preventDefault();void submitSelectedEvidenceAI();});const input=form.querySelector?.("[data-evidence-ai-question]");input?.addEventListener("keydown",event=>{if(event.key==="Enter"&&!event.shiftKey&&!event.isComposing){event.preventDefault();form.requestSubmit?.();}});});qa("[data-evidence-ai-clear]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.addEventListener("click",()=>void clearSelectedEvidenceThread());});qa("[data-evidence-ai-open-detail]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.addEventListener("click",focusCurrentEvidenceFromChat);});qa("[data-evidence-ai-open-pdf]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.disabled=!detailPDFURL(visibleEvidenceDetail()?.row);button.addEventListener("click",openCurrentEvidencePDFFromChat);});}
  async function submitSelectedEvidenceAI() {
    const detail=visibleEvidenceDetail(),host=activeEvidenceChatHost(),identity=selectedEvidenceAIIdentity(detail?.row),row=detail?.row,question=cleanText(host?.querySelector?.("[data-evidence-ai-question]")?.value,2000);if(!identity||!row||!question||state.evidenceChat.pending.size>0)return null;pinDocumentTab(detail.tab.tabId);
    const key=identity.key,generation=(state.evidenceChat.requests.get(key)||0)+1;state.evidenceChat.requests.set(key,generation);state.evidenceChat.pending.add(key);state.evidenceChat.request+=1;state.evidenceChat.busy=true;aiProgress("selected_evidence_chat","prepare","running","正在冻结当前证据与对话范围");syncSelectedEvidenceAI();const history=state.evidenceChat.messages.slice(-6).map(({role,content})=>({role,content}));
    try {aiProgress("selected_evidence_chat","authorization","waiting","等待你确认本轮发送范围");const authorization=await preparedAuthorization("selected_evidence_chat",{source_scope:identity.sourceScope,source_id:identity.sourceId,entity_type:identity.entityType,entity_uid:identity.entityUid,question,history});if(!authorization){aiProgress("selected_evidence_chat","authorization","cancelled","已取消；没有发送本轮问题");return null;}appendEvidenceThread(key,{role:"user",content:question});const input=activeEvidenceChatHost()?.querySelector?.("[data-evidence-ai-question]");if(String(input?.value||"").trim()===question)input.value="";aiProgress("selected_evidence_chat","reasoning","running","正在围绕当前证据核验与解释");if(key===state.evidenceChat.identity)renderSelectedEvidenceChat();const result=await executePrepared("selected_evidence_chat",authorization);if(state.evidenceChat.requests.get(key)!==generation)return null;aiProgress("selected_evidence_chat","organizing","running","正在整理回答与来源说明");if(typeof result?.answer!=="string"||!result.answer.trim())throw safeError("selected_evidence_chat_invalid","选中证据回答无效。");const annotations=publicSelectedEvidenceAnnotations(result,identity);if(!annotations)throw safeError("selected_evidence_chat_invalid","选中证据回答身份无效。");appendEvidenceThread(key,{role:"assistant",content:result.answer,annotations});queueEvidenceChatSave(identity,row,state.evidenceChat.threads.get(key)||state.evidenceChat.messages);aiProgress("selected_evidence_chat","completed","success","本轮回答完成");if(key===state.evidenceChat.identity)renderSelectedEvidenceChat();return result;}
    catch(error){if(state.evidenceChat.requests.get(key)===generation){const message=harnessUnavailableMessage(error);appendEvidenceThread(key,{role:"assistant",content:message});aiProgress("selected_evidence_chat","organizing","error","本轮回答未完成",aiErrorCopy(error,message));if(key===state.evidenceChat.identity)renderSelectedEvidenceChat();}return null;}
    finally {if(state.evidenceChat.requests.get(key)===generation)state.evidenceChat.pending.delete(key);syncSelectedEvidenceAI();renderSelectedEvidenceChat();}
  }
  async function uploadPDF(file) {
    if(!(file instanceof Blob)||file.type!=="application/pdf"&&!String(file.name||"").toLowerCase().endsWith(".pdf"))throw safeError("pdf_required","请选择 PDF 文件。");
    const generation=++state.literatureUploadAction;setLiteratureActionStatus("正在验证 PDF、识别重复项并加入工作区…","loading");
    const result=await request(`${ROUTES.upload}?filename=${encodeURIComponent(String(file.name||"paper.pdf"))}`,{method:"POST",headers:{"Content-Type":"application/pdf"},body:file});if(generation!==state.literatureUploadAction)return null;
    await loadLiterature();if(result?.paper_id)await selectPaper(Number(result.paper_id),{reveal:true});setLiteratureActionStatus(String(result?.message||"PDF 已导入；不会自动调用模型。"),"success");return result;
  }
  function validLiteratureCommit(value){return value?.schema_version==="literature-extraction-commit-result-v2"&&["completed","saved_index_pending"].includes(value.status)&&value.paper&&typeof value.paper.title==="string"&&value.paper.title.trim()&&(value.paper.doi===null||typeof value.paper.doi==="string")&&typeof value.visual_evidence_ready==="boolean"&&["ready","not_found"].includes(value.visual_stage_status)&&value.visual_evidence_ready===(value.visual_stage_status==="ready")&&["candidate_count","published_item_count","existing_item_count","manual_review_count","table_candidate_count","figure_candidate_count"].every(key=>Number.isInteger(value[key])&&value[key]>=0)&&value.extraction_receipt?.schema_version==="literature-extraction-receipt-v1"&&value.publication_receipt?.schema_version==="literature-publication-receipt-v1"&&value.dataset_receipt?.schema_version==="dataset-membership-receipt-v1";}
  async function runLiteratureExtraction() {
    if(!state.paper?.id)return;const taskPaper={id:state.paper.id,title:state.paper.title,doi:state.paper.doi};showSearchIndexRecovery(false);clearLiteratureReceipt();const recovery=currentLiteratureRecovery();let forceRescan=false,domainRequest=null;if(recovery?.nextAction==="review_call_outcome"){projectLiteratureRecovery();return;}if(recovery?.nextAction==="resume_extraction")domainRequest={job_token:recovery.resumeToken};else{if(state.paper.requiresRescanConfirmation===true){if(typeof globalThis.confirm!=="function"||!globalThis.confirm("这篇论文已有扫描记录。再次自动提取与核验会重新调用模型并可能产生 API 费用。\n\n是否确认重新扫描？")){setLiteratureActionStatus("已取消重新扫描；没有准备或执行模型调用。","info");return;}forceRescan=true;}domainRequest={paper_id:taskPaper.id,force_rescan:forceRescan};}state.literatureTask.paperKey=literaturePaperKey(taskPaper);state.literatureAction+=1;setLiteratureActionStatus(recovery?"正在准备恢复上次提取…":"正在检查 PDF 与提取前置条件…","loading");aiProgress("literature_extraction","prepare","running","正在免费检查论文、PDF 与可恢复任务");
    try {const authorization=await preparedAuthorization("literature_extraction",domainRequest,recovery?"后端将从已保存检查点继续；已经成功的模型调用不会重复收费。":"一次授权覆盖四类提取、双分支质量核验、发布与索引；不会逐阶段重复确认。");if(!authorization){setLiteratureActionStatus("已取消；没有执行文献提取模型调用。","info");aiProgress("literature_extraction","authorization","cancelled","已取消；没有执行模型调用");return;}aiProgress("literature_extraction","initial_focus","running",recovery?"正在从安全检查点继续文献提取":"正在聚焦论文范围并提取四类候选");const result=await executePrepared("literature_extraction",authorization,{literaturePaper:taskPaper});if(result?.schema_version==="ai-execution-background-v1")return;const visible=applyLiteratureResult(result);state.literatureTask.resume=null;if(visible&&state.paper?.id===taskPaper.id)await selectPaper(taskPaper.id);await loadLiteratureTaskDirectory();}
    catch(error){const message=aiErrorCopy(error,"处理未完成；本机已有数据不受影响，可稍后重试。");setLiteratureActionStatus(message,"error");aiProgress("literature_extraction",state.literatureTask.stage||"initial_focus","error","文献处理未完成",message);}
  }
  function runPDFAction(viewerId,action){if(!pdfController||!["paper","detail"].includes(viewerId)&&!viewerId.startsWith("secondary:"))return false;const methods={"fit-width":"fitWidth","fit-page":"fitPage","zoom-in":"zoomIn","zoom-out":"zoomOut",reset:"reset"},method=methods[action];return method?Boolean(pdfController[method]?.(viewerId)):false;}
  function bindPDFControls(){qa("[data-pdf-action][data-pdf-viewer]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.addEventListener("click",()=>{const viewer=button.dataset.pdfViewer==="secondary"?activeSecondaryPDFViewerId:button.dataset.pdfViewer;runPDFAction(viewer,button.dataset.pdfAction);});});}
  function projectSecondaryPDF(tab){if(activeSecondaryPDFViewerId){pdfController?.close(activeSecondaryPDFViewerId);activeSecondaryPDFViewerId="";}if(tab?.kind!=="pdf"||!tab.payload?.url||!pdfController)return false;const frame=q("[data-secondary-pdf-frame]"),container=frame?.closest?.("[data-secondary-document-kind='pdf']");if(!frame||!container)return false;activeSecondaryPDFViewerId=`secondary:${tab.tabId}`;return Boolean(pdfController.open({viewerId:activeSecondaryPDFViewerId,frame,container,url:tab.payload.url,page:Number(tab.payload.page)||1,returnTabId:tab.payload.returnTabId,scrollTop:Number(tab.payload.scrollTop)||0}));}
  function openCurrentPDF({openTab=true}={}){if(!state.paper?.id||!pdfController)return false;const viewer=q("#fusion-pdf-viewer"),frame=q("#fusion-pdf-frame"),content=q("#fusion-literature-content"),url=`/api/papers/${encodeURIComponent(String(state.paper.id))}/pdf`,sourceTab=documentTabs?.activeTab()?.tabId||"",focusReturn=q("#fusion-open-pdf"),scrollTop=content?.scrollTop||0;if(!viewer||!frame||!content)return false;pinDocumentTab(sourceTab);if(openTab)openDocumentTab("pdf",`${state.paper.title} · PDF`,{paperId:String(state.paper.id)},{ownerView:"paper",payload:{paperId:state.paper.id,url,returnTabId:sourceTab,scrollTop,page:1},groupId:documentTabs?.activeTab()?.groupId||"primary",pin:true});q("#fusion-pdf-title").textContent=state.paper.title;content.hidden=true;viewer.hidden=false;pdfController.open({viewerId:"paper",frame,container:viewer,url,page:1,returnTabId:sourceTab,returnFocus:focusReturn,scrollTop});q("#fusion-close-pdf")?.focus({preventScroll:true});setLiteratureActionStatus("正在中央查看当前论文原文；可随时返回。","info");return true;}
  function closeCurrentPDF({focus=true}={}){const viewer=q("#fusion-pdf-viewer"),content=q("#fusion-literature-content");if(!viewer||viewer.hidden)return false;const closed=pdfController?.close("paper");viewer.hidden=true;content.hidden=false;if(Number.isFinite(closed?.scrollTop))content.scrollTop=closed.scrollTop;if(focus)(closed?.returnFocus?.isConnected?closed.returnFocus:q("#fusion-open-pdf"))?.focus?.({preventScroll:true});return true;}
  function setSearchMode(mode) {if(!["precise","librarian"].includes(mode))return false;if(state.searchMode==="librarian"&&mode!=="librarian")cancelLibrarianRequest("已切换到精确检索；当前等待结果不会写入新页面。");state.searchMode=mode;qa("[data-search-mode-panel]").forEach(panel=>panel.hidden=panel.dataset.searchModePanel!==mode);qa("[data-search-context-mode]").forEach(panel=>panel.hidden=panel.dataset.searchContextMode!==mode);for(const [id,value]of [["#fusion-run-precise-search","precise"],["#fusion-open-librarian","librarian"]]){const button=q(id);button?.classList.toggle("active",value===mode);button?.setAttribute("aria-selected",value===mode?"true":"false");}const primaryLabel=q("#fusion-primary-tab-label"),contextTitle=q("#fusion-context-title");if(primaryLabel)primaryLabel.textContent=mode==="precise"?"精确检索":"图书管理员";if(state.view==="search"&&contextTitle)contextTitle.textContent=mode==="precise"?"搜索与筛选":"研究对话";coordinateTaskLayout("search-mode");syncSearchExports();return true;}
  function setSearchSource(source,{run=false}={}){if(!["workspace","official","private","all"].includes(source))return false;state.searchSource=source;state.searchExportReady=false;state.searchRequest+=1;qa("[data-search-source]").forEach(button=>{const active=button.dataset.searchSource===source;button.classList.toggle("active",active);button.setAttribute("aria-pressed",active?"true":"false");});syncSearchExports();if(run)void runPreciseSearch();return true;}
  function selectedEvidenceTypes(){return ["item","finding","table","figure"].filter(type=>state.evidenceTypes.has(type));}
  function toggleEvidenceType(type,{run=true}={}){if(!["item","finding","table","figure"].includes(type))return false;if(state.evidenceTypes.has(type)){if(state.evidenceTypes.size===1)return false;state.evidenceTypes.delete(type);}else state.evidenceTypes.add(type);qa("[data-evidence-type]").forEach(button=>{const active=state.evidenceTypes.has(button.dataset.evidenceType);button.classList.toggle("active",active);button.setAttribute("aria-pressed",active?"true":"false");});state.searchRequest+=1;state.selectionByView.search=null;state.selectedEvidence=null;if(run&&state.view==="search"&&state.searchMode==="precise")void runPreciseSearch();return true;}
  async function runPreciseSearch({append=false}={}) {if(append&&!state.searchHasMore)return false;closeEvidenceDetail({focus:false});if(!append){state.searchPage=0;state.searchOffset=0;}const query=String(q("#fusion-search-query")?.value||"").trim(),source=state.searchSource,types=selectedEvidenceTypes(),generation=++state.searchRequest,pageNumber=append?state.searchPage+1:1,offset=append?state.searchOffset:0;state.searchExportReady=false;setSearchMode("precise");syncSearchExports();if(!append)q("#fusion-search-results").innerHTML=literatureState("loading","正在检索所选四类真实证据…");else q("#fusion-search-load-more")?.setAttribute("disabled","");try{const federated=source!=="workspace",scope=source==="official"?"&source_scope=official":source==="private"?"&source_scope=private":"",typeQuery=federated?types.map(type=>`&entity_type=${encodeURIComponent(type)}`).join(""):`&types=${encodeURIComponent(types.join(","))}`,url=federated?`${ROUTES.federatedSearch}?q=${encodeURIComponent(query)}&page=${pageNumber}&page_size=100${scope}${typeQuery}`:`${ROUTES.evidence}?q=${encodeURIComponent(query)}&limit=100&offset=${offset}&quality=published${typeQuery}`,page=await readOnlyJSON(url);if(generation!==state.searchRequest||state.view!=="search"||state.searchMode!=="precise"||source!==state.searchSource||query!==String(q("#fusion-search-query")?.value||"").trim()||types.join(",")!==selectedEvidenceTypes().join(","))return;const raw=federated?(Array.isArray(page?.results)?page.results.map(hit=>hit?.document):[]):(Array.isArray(page?.rows)?page.rows:[]),received=raw.map(publicEvidence).filter(Boolean),combined=append?[...state.searchResults,...received]:received,seen=new Set();state.searchResults=combined.filter(row=>{const identity=evidenceIdentity(row);if(!identity||seen.has(identity))return false;seen.add(identity);return true;});state.searchTotal=Math.max(state.searchResults.length,Number(page?.total)||0);state.searchPage=pageNumber;state.searchOffset=offset+raw.length;state.searchHasMore=raw.length>0&&(federated?Boolean(page?.has_next):state.searchOffset<state.searchTotal);state.evidenceIndex=append?state.evidenceIndex:0;state.selectionByView.search=append?state.selectionByView.search:(state.searchResults[0]?{index:0,row:state.searchResults[0],identity:evidenceIdentity(state.searchResults[0])}:null);state.selectedEvidence=state.selectionByView.search?.row||null;state.searchQuery=query;state.searchExportReady=source==="workspace";syncSearchExports();renderSearchEvidence();updateInspector();const counts={item:0,finding:0,table:0,figure:0};state.searchResults.forEach(row=>counts[row.type]+=1);for(const type of Object.keys(counts))q(`#fusion-count-${type}`).textContent=String(counts[type]);const sourceLabel={workspace:"本机文献",official:"官方资料库",private:"我的实验",all:"全部资料源"}[source];setOperation(`${sourceLabel}检索完成 · 已显示 ${state.searchResults.length} / ${state.searchTotal} 条`,"success");}catch(error){if(generation===state.searchRequest){state.searchExportReady=false;syncSearchExports();if(append){renderSearchEvidence();setOperation(`${cleanText(error?.code||"search_failed",80)} · 继续加载失败，已有结果不受影响。`,"error");}else{state.searchPage=0;state.searchOffset=0;q("#fusion-search-results").innerHTML=literatureState("error",`${cleanText(error?.code||"search_failed",80)} · ${cleanText(error?.message||"当前资料源检索暂时不可用",300)}`);}}}}
  function newConversationId(){return globalThis.crypto?.randomUUID?.()||`fusion-${Date.now()}-${Math.random().toString(36).slice(2)}`;}
  const LIBRARIAN_HISTORY_LIMIT=20,LIBRARIAN_HISTORY_RETENTION=30*24*60*60*1000,LIBRARIAN_HISTORY_STORAGE=new Set(["macos-keychain-aes-256-gcm","macos-preview-local-key-aes-256-gcm"]);
  function librarianSessionTitle(messages){const first=messages.find(message=>message.role==="user")?.content||"新对话";return first.length>36?`${first.slice(0,36)}…`:first;}
  function storedLibrarianArticle(raw){if(!raw||typeof raw!=="object"||Array.isArray(raw))return null;const title=cleanText(raw.title||raw.article_title,1000);if(!title)return null;return {title,reason:cleanText(raw.reason||raw.why_recommended,3000),firstAuthor:cleanText(raw.firstAuthor||raw.first_author,300),year:Number.isSafeInteger(Number(raw.year))?Number(raw.year):null,doi:cleanText(raw.doi,300),level:cleanText(raw.level||raw.recommendation_level,40),refs:librarianRefs(raw.refs||raw.supporting_refs),warning:cleanText(raw.warning||raw.coverage_warning,1000),jumpEvidence:publicEvidence(raw.jumpEvidence||raw.jump_evidence)};}
  function normalizeLibrarianSession(raw,now=Date.now()){if(!raw||typeof raw!=="object"||Array.isArray(raw))return null;const id=cleanText(raw.id,256),created=cleanText(raw.created_at,80),updated=cleanText(raw.updated_at||raw.created_at,80),stamp=Date.parse(updated);if(!id||!Array.isArray(raw.messages)||!Number.isFinite(stamp)||stamp>now+5*60*1000||stamp<now-LIBRARIAN_HISTORY_RETENTION)return null;const messages=raw.messages.slice(-16).map(message=>{const role=message?.role,content=cleanText(message?.content,50000);return ["user","assistant"].includes(role)&&content?{role,content}:null;}).filter(Boolean);if(!messages.length)return null;return {id,title:cleanText(raw.title,160)||librarianSessionTitle(messages),created_at:Number.isFinite(Date.parse(created))?created:updated,updated_at:updated,messages,results:(Array.isArray(raw.results)?raw.results:[]).map(publicEvidence).filter(Boolean).slice(0,100),articles:(Array.isArray(raw.articles)?raw.articles:[]).map(storedLibrarianArticle).filter(Boolean).slice(0,20)};}
  function normalizeLibrarianSessions(value){const seen=new Set();return (Array.isArray(value)?value:[]).map(item=>normalizeLibrarianSession(item)).filter(item=>item&&!seen.has(item.id)&&seen.add(item.id)).sort((left,right)=>Date.parse(right.updated_at)-Date.parse(left.updated_at)).slice(0,LIBRARIAN_HISTORY_LIMIT);}
  function renderLibrarianHistory(){const host=q("#fusion-librarian-history"),clear=q("#fusion-librarian-history-clear");if(clear)clear.disabled=state.librarianHistoryBackend!=="desktop-secure"||state.librarianBusy||!state.librarianSessions.length;if(!host)return;const query=state.librarianHistoryQuery.trim().toLocaleLowerCase("zh-CN"),sessions=state.librarianSessions.filter(session=>!query||`${session.title} ${session.messages.map(message=>message.content).join(" ")}`.toLocaleLowerCase("zh-CN").includes(query));if(!sessions.length){host.innerHTML=`<div class="fusion-librarian-history-empty">${query?"没有匹配的历史对话":state.librarianHistoryBackend==="loading"?"正在读取历史对话…":"完成第一次回答后会保存在这里"}</div>`;return;}host.innerHTML=sessions.map(session=>{const active=session.id===state.librarianConversationId,stamp=new Date(session.updated_at).toLocaleString("zh-CN",{month:"numeric",day:"numeric",hour:"2-digit",minute:"2-digit"});return `<article class="fusion-librarian-history-item${active?" active":""}"><button type="button" class="fusion-librarian-history-open" data-librarian-history-open="${esc(session.id)}"><strong>${esc(session.title)}</strong><small>${esc(stamp)} · ${session.results.length} 条证据</small></button><button type="button" class="fusion-librarian-history-delete" data-librarian-history-rename="${esc(session.id)}" aria-label="重命名 ${esc(session.title)}">✎</button><button type="button" class="fusion-librarian-history-delete" data-librarian-history-delete="${esc(session.id)}" aria-label="删除 ${esc(session.title)}">×</button></article>`;}).join("");host.querySelectorAll("[data-librarian-history-open]").forEach(button=>button.addEventListener("click",()=>restoreLibrarianSession(button.dataset.librarianHistoryOpen)));host.querySelectorAll("[data-librarian-history-rename]").forEach(button=>button.addEventListener("click",()=>renameLibrarianSession(button.dataset.librarianHistoryRename)));host.querySelectorAll("[data-librarian-history-delete]").forEach(button=>button.addEventListener("click",()=>deleteLibrarianSession(button.dataset.librarianHistoryDelete)));}
  function queueLibrarianHistorySave(){if(state.librarianHistoryBackend!=="desktop-secure")return false;const sessions=JSON.parse(JSON.stringify(state.librarianSessions));state.librarianHistorySave=state.librarianHistorySave.then(()=>request(ROUTES.librarianHistory,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sessions})})).then(()=>{const privacy=q("#fusion-librarian-history-privacy");if(privacy)privacy.textContent="历史已在本机加密保存；不会进入科学数据库或资料包。";}).catch(error=>{const privacy=q("#fusion-librarian-history-privacy");if(privacy)privacy.textContent=`历史保存暂停：${cleanText(error?.message||"本机加密存储不可用",180)}`;});return true;}
  function saveCurrentLibrarianSession(){if(!state.librarianConversationId||!state.librarianMessages.some(message=>message.role==="user"))return false;const previous=state.librarianSessions.find(session=>session.id===state.librarianConversationId),now=new Date().toISOString(),session=normalizeLibrarianSession({id:state.librarianConversationId,title:previous?.title||librarianSessionTitle(state.librarianMessages),created_at:previous?.created_at||now,updated_at:now,messages:state.librarianMessages,results:state.librarianResults,articles:state.librarianArticles});if(!session)return false;state.librarianSessions=normalizeLibrarianSessions([session,...state.librarianSessions.filter(item=>item.id!==session.id)]);renderLibrarianHistory();queueLibrarianHistorySave();return true;}
  async function loadLibrarianHistory(){const privacy=q("#fusion-librarian-history-privacy");try{const payload=await readOnlyJSON(ROUTES.librarianHistory);if(!LIBRARIAN_HISTORY_STORAGE.has(payload?.storage))throw safeError("desktop_secure_history_unavailable","本机对话历史没有使用受支持的加密存储。");state.librarianHistoryBackend="desktop-secure";state.librarianSessions=normalizeLibrarianSessions(payload.sessions);if(privacy)privacy.textContent="历史保存在本机加密状态区；不会进入科学数据库或资料包。";}catch(error){state.librarianHistoryBackend="unavailable";state.librarianSessions=[];if(privacy)privacy.textContent=`历史不可用：${cleanText(error?.message||"本机加密存储不可用",180)}`;}renderLibrarianHistory();return state.librarianSessions.length;}
  function restoreLibrarianSession(sessionId){if(state.librarianBusy)return false;const session=state.librarianSessions.find(item=>item.id===sessionId);if(!session)return false;state.librarianAction+=1;state.librarianConversationId=session.id;state.librarianMessages=session.messages.map(message=>({...message}));state.librarianResearch=null;state.librarianResults=session.results.map(row=>({...row}));state.librarianArticles=session.articles.map(article=>({...article}));state.librarianResultsOpen=false;state.researchMemoryCandidate=null;q("#fusion-librarian-question").value="";q("#fusion-librarian-status").textContent="已恢复历史对话；未重新调用模型。历史不伪造结构化报告，引用与推荐文章可从右侧结果重新打开。";renderLibrarianConversation();renderLibrarianResultNavigator();renderLibrarianHistory();renderResearchMemories();q("#fusion-librarian-question")?.focus({preventScroll:true});return true;}
  function renameLibrarianSession(sessionId){if(state.librarianBusy)return false;const session=state.librarianSessions.find(item=>item.id===sessionId);if(!session||typeof globalThis.prompt!=="function")return false;const title=cleanText(globalThis.prompt("重命名对话",session.title),160);if(!title)return false;session.title=title;session.updated_at=new Date().toISOString();state.librarianSessions=normalizeLibrarianSessions(state.librarianSessions);renderLibrarianHistory();queueLibrarianHistorySave();return true;}
  function deleteLibrarianSession(sessionId){if(state.librarianBusy)return false;const wasActive=state.librarianConversationId===sessionId;state.librarianSessions=state.librarianSessions.filter(item=>item.id!==sessionId);queueLibrarianHistorySave();if(wasActive)startNewLibrarianConversation({save:false});else renderLibrarianHistory();return true;}
  async function clearLibrarianHistory(){if(state.librarianBusy||state.librarianHistoryBackend!=="desktop-secure"||!state.librarianSessions.length)return false;if(typeof globalThis.confirm==="function"&&!globalThis.confirm("清空全部图书管理员历史？\n\n这会删除本机加密历史文件及其专用密钥，不会删除研究记忆、科学数据库或证据。"))return false;const button=q("#fusion-librarian-history-clear");if(button)button.disabled=true;try{await state.librarianHistorySave.catch(()=>false);const result=await request(ROUTES.librarianHistory,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"clear"})});if(result?.cleared!==true)throw safeError("desktop_secure_history_rejected","本机加密历史未确认清除。");state.librarianSessions=[];startNewLibrarianConversation({save:false});const privacy=q("#fusion-librarian-history-privacy");if(privacy)privacy.textContent="全部对话历史及其专用加密密钥已删除；研究记忆与科学数据库未改变。";renderLibrarianHistory();return true;}catch(error){const privacy=q("#fusion-librarian-history-privacy");if(privacy)privacy.textContent=`历史清除失败：${cleanText(error?.message||"本机加密存储不可用",180)}`;renderLibrarianHistory();return false;}}
  function researchMemorySourceRef(row){if(!row||!["official","workspace"].includes(row.sourceScope)||!["item","finding","table","figure"].includes(row.type))return null;const page=Number(row.page);if(!Number.isSafeInteger(page)||page<1)return null;const entityUid=row.sourceScope==="workspace"?String(row.itemId||row.assetId||""):cleanText(row.entityUid,256),sourceId=row.sourceScope==="workspace"?"workspace":cleanText(row.sourceId,256);if(!entityUid||!sourceId)return null;const value={source_scope:row.sourceScope,source_id:sourceId,entity_type:row.type,entity_uid:entityUid,page,title:cleanText(row.title,500)};if(row.paperUid)value.paper_uid=cleanText(row.paperUid,256);if(row.doi)value.doi=cleanText(row.doi,256);return value;}
  function publicResearchMemory(raw){if(raw?.schema_version!=="research-memory-item-v1"||raw.approval!=="user_approved"||typeof raw.memory_uid!=="string"||!raw.memory_uid||typeof raw.title!=="string"||typeof raw.content!=="string"||!Array.isArray(raw.source_refs)||!raw.source_refs.length)return null;const sourceRefs=raw.source_refs.map(ref=>{if(!ref||!["official","workspace"].includes(ref.source_scope)||!["item","finding","table","figure"].includes(ref.entity_type)||typeof ref.source_id!=="string"||typeof ref.entity_uid!=="string"||!Number.isSafeInteger(Number(ref.page))||Number(ref.page)<1)return null;return {source_scope:ref.source_scope,source_id:cleanText(ref.source_id,256),entity_type:ref.entity_type,entity_uid:cleanText(ref.entity_uid,256),paper_uid:cleanText(ref.paper_uid,256),doi:cleanText(ref.doi,256),page:Number(ref.page),title:cleanText(ref.title,500)};}).filter(Boolean);if(sourceRefs.length!==raw.source_refs.length)return null;return {schema_version:"research-memory-item-v1",memory_uid:cleanText(raw.memory_uid,96),title:cleanText(raw.title,120),content:cleanText(raw.content,2000),source_refs:sourceRefs,approval:"user_approved",origin:raw.origin==="assistant_suggested"?"assistant_suggested":"user_created",created_at:cleanText(raw.created_at,80),updated_at:cleanText(raw.updated_at,80)};}
  function clearResearchMemoryCandidate(){state.researchMemoryCandidate=null;renderResearchMemories();}
  function researchMemoryFailure(error){const conflict=error?.code==="research_memory_revision_conflict";state.researchMemoryMessage=conflict?"研究记忆已在其他操作中更新，已刷新列表；请确认后重试。":cleanText(error?.message||"研究记忆未更新。",200);return conflict;}
  function renderResearchMemories(){const host=q("#fusion-research-memory-list"),status=q("#fusion-research-memory-status"),clear=q("#fusion-research-memory-clear"),remember=q("#fusion-librarian-remember"),candidate=state.researchMemoryCandidate,disabled=state.researchMemoryMutationBusy;if(remember){remember.disabled=!(state.researchMemoryBackend==="desktop-secure"&&candidate?.source_refs?.length&&!state.librarianBusy&&!disabled);remember.title=remember.disabled?"完成一次带已核验引用的回答后才可保存":"逐条确认后保存到独立研究记忆";}if(clear)clear.disabled=disabled||!state.researchMemories.length;if(status)status.textContent=state.researchMemoryMutationBusy?"正在安全更新研究记忆…":state.researchMemoryMessage||(state.researchMemoryBackend==="loading"?"正在读取本机研究记忆…":state.researchMemoryBackend==="desktop-secure"?`已保存 ${state.researchMemories.length} 条；只在你逐条确认后写入。`:"研究记忆当前不可用。");if(!host)return;if(!state.researchMemories.length){host.innerHTML='<div class="fusion-state-card"><strong>还没有研究记忆</strong><span>完成一轮带引用的图书管理员回答后，可以逐条选择“记住本轮结论”。</span></div>';return;}host.innerHTML=state.researchMemories.map(item=>`<article class="fusion-research-memory-item"><div><strong>${esc(item.title)}</strong><p>${esc(item.content)}</p><small>${item.source_refs.length} 个已核验来源 · ${esc(item.updated_at?new Date(item.updated_at).toLocaleString("zh-CN"):"本机")}</small></div><div class="fusion-research-memory-actions"><button type="button" data-research-memory-edit="${esc(item.memory_uid)}"${disabled?" disabled":""}>编辑</button><button type="button" data-research-memory-delete="${esc(item.memory_uid)}"${disabled?" disabled":""}>删除</button></div></article>`).join("");host.querySelectorAll("[data-research-memory-edit]").forEach(button=>button.addEventListener("click",()=>void editResearchMemory(button.dataset.researchMemoryEdit)));host.querySelectorAll("[data-research-memory-delete]").forEach(button=>button.addEventListener("click",()=>void deleteResearchMemory(button.dataset.researchMemoryDelete)));}
  async function loadResearchMemories({preserveMessage=false}={}){try{const payload=await readOnlyJSON(ROUTES.researchMemories);if(payload?.schema_version!=="research-memory-list-v1"||!LIBRARIAN_HISTORY_STORAGE.has(payload.storage)||!Number.isSafeInteger(payload.revision)||payload.revision<0||!Array.isArray(payload.items))throw safeError("research_memory_invalid","研究记忆响应无效。");const items=payload.items.map(publicResearchMemory);if(items.some(item=>!item))throw safeError("research_memory_invalid","研究记忆包含无效条目。");state.researchMemoryBackend="desktop-secure";state.researchMemoryRevision=payload.revision;state.researchMemories=items;if(!preserveMessage)state.researchMemoryMessage="";}catch(error){state.researchMemoryBackend="unavailable";state.researchMemoryRevision=0;state.researchMemories=[];state.researchMemoryMessage=cleanText(error?.message||"研究记忆当前不可用。",200);}renderResearchMemories();return state.researchMemories.length;}
  async function mutateResearchMemories(body){if(state.researchMemoryBackend!=="desktop-secure"||state.researchMemoryMutationBusy)return false;state.researchMemoryMutationBusy=true;state.researchMemoryMessage="";renderResearchMemories();try{const payload=await request(ROUTES.researchMemories,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...body,expected_revision:state.researchMemoryRevision})});if(payload?.schema_version!=="research-memory-list-v1"||!Number.isSafeInteger(payload.revision)||!Array.isArray(payload.items))throw safeError("research_memory_invalid","研究记忆更新响应无效。");const items=payload.items.map(publicResearchMemory);if(items.some(item=>!item))throw safeError("research_memory_invalid","研究记忆更新包含无效条目。");state.researchMemoryRevision=payload.revision;state.researchMemories=items;return true;}finally{state.researchMemoryMutationBusy=false;renderResearchMemories();}}
  async function rememberCurrentResearch(){const candidate=state.researchMemoryCandidate;if(!candidate?.source_refs?.length||state.researchMemoryBackend!=="desktop-secure"||state.researchMemoryMutationBusy)return false;if(typeof globalThis.confirm==="function"&&!globalThis.confirm(`将本轮结论与 ${candidate.source_refs.length} 个已核验来源保存到本机研究记忆？\n\n不会保存 PDF 正文、私人实验或完整提示词。`))return false;const item={user_approved:true,origin:"assistant_suggested",title:candidate.title,content:candidate.content,source_refs:candidate.source_refs};try{if(!await mutateResearchMemories({action:"create",item}))return false;state.researchMemoryCandidate=null;state.researchMemoryMessage="本轮结论已保存；可在这里编辑或删除，聊天历史不受影响。";q("#fusion-librarian-status").textContent="本轮结论已保存到研究记忆；可在设置 → 数据与资料库中编辑或删除。";renderResearchMemories();return true;}catch(error){const conflict=researchMemoryFailure(error);if(conflict)await loadResearchMemories({preserveMessage:true});renderResearchMemories();return false;}}
  async function editResearchMemory(memoryUid){if(state.researchMemoryMutationBusy)return false;const item=state.researchMemories.find(value=>value.memory_uid===memoryUid);if(!item||typeof globalThis.prompt!=="function")return false;const title=cleanText(globalThis.prompt("编辑研究记忆标题",item.title),120);if(!title)return false;const content=cleanText(globalThis.prompt("编辑研究记忆内容；保存即表示你重新确认这条内容",item.content),2000);if(!content)return false;try{if(!await mutateResearchMemories({action:"update",item:{...item,title,content,user_approved:true}}))return false;state.researchMemoryMessage="研究记忆已更新；聊天历史没有改变。";renderResearchMemories();return true;}catch(error){const conflict=researchMemoryFailure(error);if(conflict)await loadResearchMemories({preserveMessage:true});renderResearchMemories();return false;}}
  async function deleteResearchMemory(memoryUid){if(state.researchMemoryMutationBusy||typeof globalThis.confirm==="function"&&!globalThis.confirm("删除这条研究记忆？聊天历史不会被删除。"))return false;try{if(!await mutateResearchMemories({action:"delete",memory_uid:memoryUid}))return false;state.researchMemoryMessage="研究记忆已删除；聊天历史没有改变。";renderResearchMemories();return true;}catch(error){const conflict=researchMemoryFailure(error);if(conflict)await loadResearchMemories({preserveMessage:true});renderResearchMemories();return false;}}
  async function clearResearchMemories(){if(state.researchMemoryMutationBusy||!state.researchMemories.length||typeof globalThis.confirm==="function"&&!globalThis.confirm("清空全部研究记忆？聊天历史不会被删除，此操作不可撤销。"))return false;try{if(!await mutateResearchMemories({action:"clear",confirm_clear:true}))return false;state.researchMemoryMessage="研究记忆已清空；聊天历史仍然保留。";renderResearchMemories();return true;}catch(error){const conflict=researchMemoryFailure(error);if(conflict)await loadResearchMemories({preserveMessage:true});renderResearchMemories();return false;}}
  function librarianConversationEvidence(){const rows=[],seenRefs=new Set(),seenIdentities=new Set();for(const row of state.librarianResults){const ref=cleanText(row?.ref,20),bundleRef=cleanText(row?.bundleRef,20),sourceScope=row?.sourceScope,sourceId=sourceScope==="workspace"?"workspace":cleanText(row?.sourceId,256),entityType=row?.type,entityUid=cleanText(row?.entityUid,256),identity=`${sourceScope}:${sourceId}:${entityType}:${entityUid}`;if(!/^R[1-9][0-9]{0,3}$/.test(ref)||!/^B[1-9][0-9]{0,3}$/.test(bundleRef)||!["official","workspace"].includes(sourceScope)||!["item","finding","table","figure"].includes(entityType)||!sourceId||!entityUid||/[\\/]/.test(sourceId)||/[\\/]/.test(entityUid)||seenRefs.has(ref)||seenIdentities.has(identity))continue;seenRefs.add(ref);seenIdentities.add(identity);rows.push({ref,source_scope:sourceScope,source_id:sourceId,entity_type:entityType,entity_uid:entityUid,bundle_ref:bundleRef});if(rows.length>=40)break;}return rows;}
  function librarianRequest(question){const payload={question,history:state.librarianMessages.slice(-8).map(({role,content})=>({role,content})),conversation_id:state.librarianConversationId||(state.librarianConversationId=newConversationId()),use_research_memory:q("#fusion-librarian-use-memory")?.checked===true},conversationEvidence=librarianConversationEvidence();if(conversationEvidence.length)payload.conversation_evidence=conversationEvidence;if(state.librarianResearch){payload.research_state=state.librarianResearch.state;payload.state_token=state.librarianResearch.token;}return payload;}
  function librarianRefs(value){return Array.isArray(value)?value.map(item=>cleanText(item,20)).filter(item=>/^R[1-9][0-9]{0,3}$/.test(item)).slice(0,20):[];}
  function librarianPresentation(result,rows=[]){const scopes=cleanList(result?.query_analysis?.source_scopes,4).filter(value=>["official","workspace"].includes(value)),fallbackScopes=[...new Set(rows.map(row=>row.sourceScope).filter(value=>["official","workspace"].includes(value)))],sourceScopes=scopes.length?scopes:fallbackScopes,sourceLabel=sourceScopes.length?sourceScopes.map(value=>value==="official"?"官方资料库":"本机文献工作区").join(" + "):"当前文献资料",counts=result?.match_counts&&typeof result.match_counts==="object"?result.match_counts:{},safeCount=key=>Math.max(0,Math.min(100000,Number.isSafeInteger(Number(counts[key]))?Number(counts[key]):0)),policy=cleanText(result?.retrieval_policy,80),policyLabel={none:"无需检索",focused:"定向检索",review_map:"主题综述",resolve_anchors:"引用追问"}[policy]||"有界检索",intent=cleanText(result?.intent?.kind||result?.intent?.intent,80),intentLabel={research_lookup:"定向研究问题",research_review:"主题综述",conversation:"普通对话",system_capability:"能力说明",clarification:"需要澄清"}[intent]||"研究问题";return {sourceScopes,sourceLabel,policy,policyLabel,intent,intentLabel,direct:safeCount("direct"),adjacent:safeCount("adjacent"),expansion:safeCount("expansion"),candidateCount:Math.max(0,Number(result?.candidate_count)||0),citedCount:Math.max(0,Number(result?.cited_count)||rows.length),bundleCount:Math.max(0,Number(result?.bundle_count)||0),reviewThemeCount:Array.isArray(result?.review_map)?result.review_map.length:0};}
  function renderLibrarianPresentation(result,rows=[]){const meta=librarianPresentation(result,rows),memoryCount=Number.isSafeInteger(Number(result?.research_memory_count))?Math.max(0,Math.min(1000,Number(result.research_memory_count))):0,memory=memoryCount?`<div data-librarian-memory-count><span>研究记忆</span><strong>本次使用 ${memoryCount} 条已核验记忆</strong></div>`:"";return `<section class="fusion-librarian-meta" aria-label="本轮检索说明"><div><span>资料范围</span><strong>${esc(meta.sourceLabel)}</strong></div><div><span>检索策略</span><strong>${esc(meta.policyLabel)} · ${esc(meta.intentLabel)}</strong></div><div><span>证据匹配</span><strong>直接 ${meta.direct} · 相关 ${meta.adjacent} · 扩展 ${meta.expansion}</strong></div><div><span>核验规模</span><strong>候选 ${meta.candidateCount} · 引用 ${meta.citedCount} · 证据组 ${meta.bundleCount}${meta.reviewThemeCount?` · 主题 ${meta.reviewThemeCount}`:""}</strong></div>${memory}</section>`;}
  function renderLibrarianReport(report){
    if(report?.schema_version!=="research-report-v1")return "";const sections=[],direct=report.direct_conclusion&&typeof report.direct_conclusion==="object"?report.direct_conclusion:null,directText=cleanText(direct?.text,12000);if(directText)sections.push(`<section><h3>直接结论</h3><p>${esc(directText)}</p>${librarianRefs(direct?.refs).length?`<small>${esc(librarianRefs(direct.refs).join(" · "))}</small>`:""}</section>`);
    const matrix=Array.isArray(report.evidence_matrix)?report.evidence_matrix.slice(0,50):[];if(matrix.length)sections.push(`<section><h3>证据矩阵</h3><div class="fusion-librarian-matrix">${matrix.map(row=>`<article><strong>${esc(cleanText(row?.property||row?.article_title||"证据",500))}</strong>${cleanText(row?.result,4000)?`<p>${esc(cleanText(row.result,4000))}</p>`:""}<small>${esc([cleanText(row?.material,500),cleanText(row?.conditions,1000),cleanText(row?.article_title,500),row?.source_page?`第 ${row.source_page} 页`:"",librarianRefs(row?.refs).join(" · ")].filter(Boolean).join(" · "))}</small></article>`).join("")}</div></section>`);
    const related=Array.isArray(report.related_evidence)?report.related_evidence.slice(0,50):[];if(related.length)sections.push(`<section><h3>相关证据</h3>${related.map(row=>`<article><p>${esc(cleanText(row?.summary,4000))}</p><small>${esc([...(Array.isArray(row?.relaxed_constraints)?row.relaxed_constraints.map(value=>cleanText(value,300)):[]),...librarianRefs(row?.refs)].filter(Boolean).join(" · "))}</small></article>`).join("")}</section>`);
    const gaps=cleanList(report.database_gaps,50);if(gaps.length)sections.push(`<section><h3>数据库缺口</h3><ul>${gaps.map(value=>`<li>${esc(value)}</li>`).join("")}</ul></section>`);const followups=cleanList(report.suggested_followups,20);if(followups.length)sections.push(`<section><h3>建议追问</h3><ul>${followups.map(value=>`<li>${esc(value)}</li>`).join("")}</ul></section>`);return sections.length?`<article class="fusion-librarian-report"><h2>结构化研究报告</h2>${sections.join("")}</article>`:"";
  }
  function publicLibrarianArticle(raw){if(!raw||typeof raw!=="object"||Array.isArray(raw))return null;const title=cleanText(raw.article_title,1000);if(!title)return null;return {title,reason:cleanText(raw.why_recommended,3000),firstAuthor:cleanText(raw.first_author,300),year:Number.isSafeInteger(Number(raw.year))?Number(raw.year):null,doi:cleanText(raw.doi,300),level:cleanText(raw.recommendation_level,40),refs:librarianRefs(raw.supporting_refs),warning:cleanText(raw.coverage_warning,1000),jumpEvidence:publicEvidence(raw.jump_evidence)};}
  function prepareLibrarianSpace(mode="results"){
    if(mode==="results")documentTabs?.focusGroup?.("primary");
    Object.assign(workspaceReveal,{surface:null,taskId:""});renderDocumentTabs();return coordinateTaskLayout(`librarian-${mode}`);
  }
  function setLibrarianResultsOpen(open,{manageSpace=true}={}){const panel=q("#fusion-librarian-panel"),results=q("#fusion-librarian-results"),toggle=q("#fusion-librarian-results-toggle");state.librarianResultsOpen=Boolean(open&&(state.librarianResults.length||state.librarianArticles.length));if(panel)panel.classList.toggle("results-open",state.librarianResultsOpen);if(results)results.hidden=!state.librarianResultsOpen;if(toggle){toggle.hidden=!(state.librarianResults.length||state.librarianArticles.length);toggle.setAttribute("aria-expanded",state.librarianResultsOpen?"true":"false");}if(manageSpace){if(state.librarianResultsOpen)prepareLibrarianSpace("results");else coordinateTaskLayout("librarian-results-closed");}return state.librarianResultsOpen;}
  function renderLibrarianResultNavigator(){const body=q("#fusion-librarian-results-body"),count=q("#fusion-librarian-results-count"),total=state.librarianResults.length+state.librarianArticles.length;if(count)count.textContent=String(total);if(!body){setLibrarianResultsOpen(false,{manageSpace:false});return;}const evidence=state.librarianResults.map((row,index)=>`<button type="button" class="fusion-librarian-result" data-librarian-result="${index}"><span>${esc(row.ref||EVIDENCE_LABELS[row.type]||row.type)} · 第 ${esc(row.page)} 页</span><strong>${esc(row.title)}</strong><small>${esc(row.articleTitle||row.doi||"公开证据")}</small></button>`).join(""),articles=state.librarianArticles.map((article,index)=>`<button type="button" class="fusion-librarian-result recommended" data-librarian-article="${index}" ${article.jumpEvidence||article.refs.length?"":"disabled"}><span>推荐论文${article.refs.length?` · ${esc(article.refs.join(" / "))}`:""}</span><strong>${esc(article.title)}</strong><small>${esc([article.firstAuthor,article.year,article.doi].filter(Boolean).join(" · ")||article.reason)}</small></button>`).join("");body.innerHTML=`${evidence?`<div class="fusion-librarian-result-heading"><span>引用证据</span><b>${state.librarianResults.length}</b></div>${evidence}`:""}${articles?`<div class="fusion-librarian-result-heading"><span>推荐论文</span><b>${state.librarianArticles.length}</b></div>${articles}`:""}`||`<div class="fusion-state-card" data-literature-state="empty"><strong>没有可跳转结果</strong><span>本轮回答没有返回已验证的公开证据身份。</span></div>`;qa("[data-librarian-result]").forEach(button=>button.addEventListener("click",()=>void openLibrarianEvidence(Number(button.dataset.librarianResult),button)));qa("[data-librarian-article]").forEach(button=>button.addEventListener("click",()=>void openLibrarianArticle(Number(button.dataset.librarianArticle),button)));setLibrarianResultsOpen(state.librarianResultsOpen,{manageSpace:false});}
  async function openLibrarianEvidence(index,trigger=null){const row=state.librarianResults[index];if(!row)return false;setLibrarianResultsOpen(false,{manageSpace:false});state.selectedEvidence=row;state.selectionByView.search={index:-1,row,identity:evidenceIdentity(row)};prepareLibrarianSpace("detail");updateInspector();const tab=await openSecondaryEvidence(row,"search",{preview:false,pin:true});if(!tab)return false;qa("[data-librarian-result]").forEach(button=>button.removeAttribute("aria-current"));trigger?.setAttribute?.("aria-current","true");activeEvidenceChatHost()?.querySelector?.("[data-evidence-ai-question]")?.focus?.({preventScroll:true});return true;}
  async function openLibrarianArticle(index,trigger=null){const article=state.librarianArticles[index];if(!article)return false;let row=article.jumpEvidence;for(const ref of article.refs){row=row||state.librarianResults.find(candidate=>candidate.ref===ref);}if(!row)return false;const existing=state.librarianResults.findIndex(candidate=>evidenceIdentity(candidate)===evidenceIdentity(row));return existing>=0?openLibrarianEvidence(existing,trigger):openLibrarianEvidenceForRow(row,trigger);}
  async function openLibrarianEvidenceForRow(row,trigger=null){if(!row)return false;const existing=state.librarianResults.findIndex(candidate=>evidenceIdentity(candidate)===evidenceIdentity(row));if(existing>=0)return openLibrarianEvidence(existing,trigger);state.librarianResults.push(row);renderLibrarianResultNavigator();return openLibrarianEvidence(state.librarianResults.length-1,trigger);}
  function renderRecommendedArticles(value){const articles=(Array.isArray(value)?value:[]).map(article=>article?.title?article:publicLibrarianArticle(article)).filter(Boolean).slice(0,20);if(!articles.length)return "";return `<section class="fusion-librarian-recommendations"><h2>推荐阅读论文</h2>${articles.map((article,index)=>`<button type="button" class="fusion-librarian-article-card" data-librarian-article-link="${index}" ${article.jumpEvidence||article.refs?.length?"":"disabled"}><strong>${esc(article.title)}</strong><p>${esc(article.reason)}</p><small>${esc([article.firstAuthor,article.year,article.doi,article.level,(article.refs||[]).join(" · ")].filter(Boolean).join(" · "))}</small>${article.warning?`<em>${esc(article.warning)}</em>`:""}</button>`).join("")}</section>`;}
  function renderSuggestedActions(value){const actions=(Array.isArray(value)?value:[]).filter(action=>action?.schema_version==="suggested-action-v1"&&action.answerable===true&&cleanText(action.text,180)).slice(0,10);return actions.length?`<section class="fusion-librarian-actions"><h2>可以继续追问</h2>${actions.map((action,index)=>`<button type="button" data-librarian-suggested-action="${index}">${esc(cleanText(action.text,180))}</button>`).join("")}</section>`:"";}
  function bindLibrarianContentActions(root=document){const nodes=selector=>[...(root?.querySelectorAll?.(selector)||[])];nodes("[data-librarian-evidence-link]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.addEventListener("click",()=>void openLibrarianEvidence(Number(button.dataset.librarianEvidenceLink),button));});nodes("[data-librarian-article-link]").forEach(button=>{if(button.dataset.bound==="true")return;button.dataset.bound="true";button.addEventListener("click",()=>void openLibrarianArticle(Number(button.dataset.librarianArticleLink),button));});}
  function renderLibrarianConversation(){const output=q("#fusion-librarian-output");if(!output)return;if(aiExperience)aiExperience.renderConversation(output,state.librarianMessages,{assistantLabel:"图书管理员",assistantMark:"AR",busy:state.librarianBusy,busyLabel:"正在检索并核对引用",emptyTitle:"直接描述你的研究问题",emptyText:"我会先理解条件，再检索四类证据，最后给出可追溯回答、引用论文和相关建议。"});else output.innerHTML=state.librarianMessages.map(message=>`<article data-role="${message.role}"><strong>${message.role==="assistant"?"图书管理员":"你"}</strong><p>${esc(message.content)}</p>${message.richHTML||""}</article>`).join("");const assistantNodes=output.querySelectorAll?.('.fusion-chat-message[data-role="assistant"]')||[],assistantMessages=state.librarianMessages.filter(message=>message.role==="assistant");assistantMessages.forEach((message,index)=>{if(!message.richHTML||!assistantNodes[index])return;const rich=document.createElement("div");rich.className="fusion-librarian-rich";rich.innerHTML=message.richHTML;assistantNodes[index].querySelector("div")?.append(rich);const actions=Array.isArray(message.suggestedActions)?message.suggestedActions:[];rich.querySelectorAll?.("[data-librarian-suggested-action]").forEach(button=>button.addEventListener("click",()=>{const action=actions[Number(button.dataset.librarianSuggestedAction)],text=cleanText(action?.text,180),input=q("#fusion-librarian-question");if(text&&input){input.value=text;input.focus({preventScroll:true});}}));});bindLibrarianContentActions(output);}
  function startNewLibrarianConversation({save=true}={}){if(state.librarianBusy)return false;if(save)saveCurrentLibrarianSession();state.librarianAction+=1;state.librarianConversationId=null;state.librarianMessages=[];state.librarianResearch=null;state.librarianResults=[];state.librarianArticles=[];state.librarianResultsOpen=false;state.researchMemoryCandidate=null;renderLibrarianResultNavigator();setLibrarianResultsOpen(false,{manageSpace:false});q("#fusion-librarian-question").value="";q("#fusion-librarian-status").textContent="可以开始新的研究问题";aiExperience?.reset("librarian");renderLibrarianConversation();renderLibrarianHistory();renderResearchMemories();q("#fusion-librarian-question")?.focus({preventScroll:true});return true;}
  function renderLibrarianFinal(result,question){
    if(result?.librarian_core_version!=="librarian-v3"||typeof result.answer!=="string")throw safeError("librarian_result_invalid","图书管理员最终结果无效。");
    const localFallback=result.summary_mode==="local_deterministic_after_harness_rejection",fallbackNotice=localFallback?'<aside class="fusion-librarian-fallback" role="status"><strong>模型回答未采用</strong><span>模型输出没有通过引用与格式核验；以下内容由本机已冻结证据确定性整理，没有采用模型正文，也没有再次调用模型。</span></aside>':"";
    if(state.librarianMessages.at(-1)?.role!=="user"||state.librarianMessages.at(-1)?.content!==question)state.librarianMessages.push({role:"user",content:question});state.librarianResearch=result.research_state&&result.state_token?{state:result.research_state,token:result.state_token}:null;
    const bundleRefByEvidenceRef=new Map((Array.isArray(result.evidence_bundles)?result.evidence_bundles:[]).flatMap(bundle=>/^B[1-9][0-9]{0,3}$/.test(cleanText(bundle?.id,20))?(Array.isArray(bundle?.refs)?bundle.refs:[]).map(ref=>[cleanText(ref,20),cleanText(bundle.id,20)]):[])),rows=(Array.isArray(result.results)?result.results:[]).map(publicEvidence).filter(Boolean).map(row=>({...row,bundleRef:bundleRefByEvidenceRef.get(row.ref)||row.bundleRef})),articles=(Array.isArray(result.recommended_articles)?result.recommended_articles:[]).map(publicLibrarianArticle).filter(Boolean),answer=cleanText(result.answer,50000),meta=renderLibrarianPresentation(result,rows),evidence=rows.length?`<section class="fusion-librarian-evidence"><h2>引用证据</h2>${rows.map((row,index)=>`<button type="button" class="fusion-librarian-citation" data-librarian-evidence-link="${index}"><strong>${esc(row.title)}</strong><span>${esc(row.articleTitle)} · 第 ${esc(row.page)} 页</span><p>${esc(row.excerpt)}</p><small>打开独立证据详情</small></button>`).join("")}</section>`:"",report=renderLibrarianReport(result.report),recommendations=renderRecommendedArticles(articles),actions=renderSuggestedActions(result.suggested_actions),richHTML=`${fallbackNotice}${meta}${report}${evidence}${recommendations}${actions}`,conversationId=state.librarianConversationId||newConversationId();state.librarianConversationId=conversationId;
    state.librarianResults=rows;state.librarianArticles=articles;state.librarianResultsOpen=Boolean(rows.length||articles.length);renderLibrarianResultNavigator();
    state.librarianMessages.push({role:"assistant",content:answer,richHTML,suggestedActions:Array.isArray(result.suggested_actions)?result.suggested_actions:[]});state.researchMemoryCandidate=rows.length?{conversation_id:conversationId,generation:state.librarianAction,title:cleanText(question||"研究结论",120),content:cleanText(answer,2000),source_refs:rows.map(researchMemorySourceRef).filter(Boolean).slice(0,20)}:null;renderLibrarianConversation();
    const answerHTML=`${fallbackNotice}<article class="fusion-librarian-answer"><span>LIBRARIAN V3 · ${esc(librarianPresentation(result,rows).sourceLabel)}</span><h2>${localFallback?"本机核验证据整理":"回答"}</h2><div class="fusion-librarian-answer-text">${esc(answer).replace(/\n/g,"<br>")}</div></article>${meta}${report}`,citationsHTML=evidence,recommendationsHTML=recommendations;openDocumentTab("librarian",cleanText(question,80)||"图书管理员回答",{conversationId,entityType:"answer"},{ownerView:"search",payload:{section:"answer",html:answerHTML},groupId:"primary"});if(rows.length)openDocumentTab("librarian",`引用证据 · ${rows.length}`,{conversationId,entityType:"citations"},{ownerView:"search",payload:{section:"citations",html:citationsHTML},activate:false,groupId:"primary"});if(articles.length)openDocumentTab("librarian",`推荐文章 · ${articles.length}`,{conversationId,entityType:"recommendations"},{ownerView:"search",payload:{section:"recommendations",html:recommendationsHTML},activate:false,groupId:"primary"});bindLibrarianContentActions();
    setLibrarianResultsOpen(Boolean(rows.length||articles.length));const completionLabel=localFallback?`模型回答未采用 · 已保留 ${rows.length} 条本机核验证据`:`回答完成 · ${rows.length} 条引用可直接打开`;q("#fusion-librarian-status").textContent=completionLabel;const sessionLabel=q("#fusion-librarian-current-session span");if(sessionLabel)sessionLabel.textContent=cleanText(state.librarianMessages.find(message=>message.role==="user")?.content,60)||"当前研究对话";aiProgress("librarian","completed",localFallback?"warning":"success",completionLabel);saveCurrentLibrarianSession();renderResearchMemories();
  }
  async function submitLibrarian(event){event?.preventDefault?.();if(state.librarianBusy)return;const question=String(q("#fusion-librarian-question")?.value||"").trim(),send=q("#fusion-librarian-send");if(!question)return;state.librarianBusy=true;state.researchMemoryCandidate=null;if(send)send.disabled=true;const generation=++state.librarianAction;q("#fusion-librarian-status").textContent="正在准备图书管理员请求…";aiProgress("librarian","prepare","running","正在理解研究问题与约束");renderLibrarianConversation();renderResearchMemories();try{aiProgress("librarian","authorization","waiting","等待你确认检索范围与预算");const authorization=await preparedAuthorization("librarian",librarianRequest(question));if(generation!==state.librarianAction||state.view!=="search"||state.searchMode!=="librarian")return;if(!authorization){q("#fusion-librarian-status").textContent="已取消，没有发起模型调用。";aiProgress("librarian","authorization","cancelled","已取消；没有发起模型调用");return;}state.librarianMessages.push({role:"user",content:question});q("#fusion-librarian-question").value="";saveCurrentLibrarianSession();renderLibrarianConversation();aiProgress("librarian","planner","running","正在检索并召回四类证据");const result=authorization.localResult||await executePrepared("librarian",authorization);if(generation!==state.librarianAction)return;aiProgress("librarian","organizing","running","正在核验引用并组织回答");renderLibrarianFinal(result,question);}catch(error){if(generation===state.librarianAction){state.researchMemoryCandidate=null;const message=harnessUnavailableMessage(error);q("#fusion-librarian-status").textContent=message;state.librarianMessages.push({role:"assistant",content:message});saveCurrentLibrarianSession();aiProgress("librarian","organizing","error","本次检索未完成",aiErrorCopy(error,message));}}finally{if(generation===state.librarianAction){state.librarianBusy=false;if(send)send.disabled=false;renderLibrarianConversation();renderResearchMemories();}}}
  function cancelLibrarianRequest(message="已停止。") {if(!state.librarianBusy)return false;state.librarianAction+=1;state.librarianBusy=false;state.researchMemoryCandidate=null;q("#fusion-librarian-status").textContent=message;aiProgress("librarian","organizing","cancelled",message);renderLibrarianConversation();renderResearchMemories();return true;}
  let personalImportController=null;
  function personalImport(){
    if(personalImportController)return personalImportController;
    const create=globalThis.AutoResearchFusionPersonalImport?.createPersonalImportController;if(typeof create!=="function")return null;
    personalImportController=create({
      state,q,qa,esc,cleanText,safeError,request,
      native:{selectPersonalFile:async()=>{const picker=globalThis.pywebview?.api?.select_personal_data_file;if(typeof picker!=="function")throw safeError("personal_picker_unavailable","请在桌面 App 中使用系统文件选择器。");return picker.call(globalThis.pywebview.api);}},
      authorizeAI:domainRequest=>preparedAuthorization("personal_suggestion",domainRequest),executeAI:authorization=>executePrepared("personal_suggestion",authorization),aiProgress,aiErrorCopy,resetAI:scope=>aiExperience?.reset(scope)??false,
      isPersonalActive:()=>state.view==="personal",setOperation,selectCell,
      projectInspector:value=>{state.row=0;state.column=0;state.inspectorState.personal=value;updateInspector();},
      openImportedTable:(rawAction,generation)=>openImportedPersonalTable(rawAction,generation),
      confirmAction:message=>typeof globalThis.confirm==="function"&&globalThis.confirm(message),
    });return personalImportController;
  }
  function personalMethod(name,fallback=null){return(...args)=>{const controller=personalImport();return controller?controller[name](...args):fallback;};}
  const previewSheet=personalMethod("previewSheet",null),publicPersonalImportPage=personalMethod("publicPersonalImportPage",null),loadPersonalPreviewPage=personalMethod("loadPersonalPreviewPage",false),loadPersonalSearchStatus=personalMethod("loadPersonalSearchStatus",false),refreshPersonalSearch=personalMethod("refreshPersonalSearch",false),choosePersonalSheet=personalMethod("choosePersonalSheet",false),choosePersonalFile=personalMethod("choosePersonalFile",null),requestPersonalSuggestion=personalMethod("requestPersonalSuggestion"),applyPersonalSuggestion=personalMethod("applyPersonalSuggestion",false),addPersonalSeries=personalMethod("addPersonalSeries",false),removePersonalSeries=personalMethod("removePersonalSeries",false),refreshPersonalSeriesColumns=personalMethod("refreshPersonalSeriesColumns",0),parsePersonalConditions=personalMethod("parsePersonalConditions",null),collectPersonalDraft=personalMethod("collectPersonalDraft",null),publicPersonalImportNextAction=personalMethod("publicPersonalImportNextAction",null),confirmPersonalImport=personalMethod("confirmPersonalImport");
  async function openImportedPersonalTable(rawAction,generation=state.personalAction){
    const action=publicPersonalImportNextAction(rawAction);if(!action||generation!==state.personalAction||state.view!=="personal")return false;
    const row={type:"table",title:action.label,sourceScope:action.sourceScope,sourceId:action.sourceId,entityUid:action.entityUid};
    if(paneViewport()>=900){const tab=await openSecondaryEvidence(row,"personal",{preview:false,pin:true});if(generation!==state.personalAction||state.view!=="personal")return false;if(tab){coordinateTaskLayout("personal-import-result");return true;}return false;}
    paneController?.expandEditor("primary");const tab=openDocumentTab("personal-table",row.title,documentIdentity(row),{ownerView:"personal",payload:{evidenceIdentity:evidenceIdentity(row),row,status:"loading"},groupId:"primary",preview:false,pin:true});if(!tab)return false;showEvidenceDetailShell(row,q("#fusion-personal-confirm"),{openTab:false,preview:false,pin:true});const opened=await loadPrivateTablePage(row,1);return generation===state.personalAction&&state.view==="personal"&&opened;
  }

  let packageController=null;
  function packageCenter(){
    if(packageController)return packageController;
    const create=globalThis.AutoResearchFusionPackage?.createPackageCenterController;if(typeof create!=="function")return null;
    packageController=create({
      state:state.package,q,qa,request,safeError,cleanText,esc,setOperation,
      native:{
        selectEvidencePackage:async()=>{const picker=globalThis.pywebview?.api?.select_evidence_package;if(typeof picker!=="function")throw safeError("package_picker_unavailable","请在桌面 App 中使用系统文件选择器。");return picker.call(globalThis.pywebview.api);},
        selectPackageDestination:async filename=>{const picker=globalThis.pywebview?.api?.select_package_export_destination;if(typeof picker!=="function")throw safeError("package_destination_unavailable","请在桌面 App 中选择保存位置。");return picker.call(globalThis.pywebview.api,filename);},
        selectDatasetDestination:async filename=>{const picker=globalThis.pywebview?.api?.select_dataset_export_destination;if(typeof picker!=="function")throw safeError("dataset_destination_unavailable","系统保存窗口尚未就绪。");return picker.call(globalThis.pywebview.api,filename);},
      },
      projectJob:(job,family)=>{state.inspectorState.package={kind:"package-job",job};const operation=family==="official"?"official":"transfer";openDocumentTab("package-job",`资料包任务 · ${cleanText(job.stage||"处理中",40)}`,{operation,jobId:String(job.job_id)},{ownerView:"package",payload:{job},activate:false});renderDocumentTabs();updateInspector();},
      openOfficialSearch:()=>{switchView("search");setSearchMode("precise");setSearchSource("official");void runPreciseSearch();},
      getCurrentPaperId:()=>state.paper?.id||null,isActiveView:()=>state.view==="package",
    });return packageController;
  }
  function packageMethod(name,fallback=null){return(...args)=>{const controller=packageCenter();return controller?controller[name](...args):fallback;};}
  const selectPackageWorkflow=packageMethod("selectPackageWorkflow",false),publicOfficialPackageResult=packageMethod("publicOfficialPackageResult",null),publicActiveOfficialPackageSummary=packageMethod("publicActiveOfficialPackageSummary",null),renderOfficialPackageResult=packageMethod("renderOfficialPackageResult",false),rememberOfficialPackageResult=packageMethod("rememberOfficialPackageResult",false),publicDatasetPlan=packageMethod("publicDatasetPlan",null),publicDatasetReceipt=packageMethod("publicDatasetReceipt",null),updateDatasetExportButton=packageMethod("updateDatasetExportButton",false),planDataset=packageMethod("planDataset"),exportDataset=packageMethod("exportDataset"),loadPackageCenter=packageMethod("loadPackageCenter",null),importOfficialPackage=packageMethod("importOfficialPackage"),planLiteraturePackage=packageMethod("planLiteraturePackage"),planPersonalPackage=packageMethod("planPersonalPackage"),exportPlannedPackage=packageMethod("exportPlannedPackage"),inspectUserPackage=packageMethod("inspectUserPackage"),importUserPackage=packageMethod("importUserPackage");
  function safeStoreAppearance() {
    try { localStorage.setItem(STORAGE_KEY, JSON.stringify({version:1,theme:state.theme,density:state.density})); } catch (_error) {}
  }
  function showSettingsStatus(message,kind="info") {const node=q("#fusion-settings-status");if(!node)return;node.hidden=false;node.dataset.kind=kind;node.textContent=message;}
  function applyAppearance(theme = state.theme, density = state.density,{cache=true}={}) {
    if (!["system","light","dark"].includes(theme) || !["comfortable","compact"].includes(density)) return false;
    state.theme=theme; state.density=density;
    document.documentElement.dataset.theme=theme; document.documentElement.dataset.density=density;
    qa("[data-theme-choice]").forEach(button=>button.classList.toggle("active",button.dataset.themeChoice===theme));
    qa("[data-density-choice]").forEach(button=>button.classList.toggle("active",button.dataset.densityChoice===density));
    if(cache)safeStoreAppearance(); return true;
  }
  function readAppearance() {
    try { const saved=JSON.parse(localStorage.getItem(STORAGE_KEY)||"null"); return applyAppearance(saved?.theme||document.documentElement.dataset.theme||"system",saved?.density||document.documentElement.dataset.density||"comfortable"); }
    catch (_error) { return applyAppearance("system","comfortable"); }
  }
  function hydrateSettings(dto) {
    if(dto?.schema_version!=="desktop-settings-v1"||!Number.isInteger(dto.revision)||!dto.appearance)return false;
    if(!applyAppearance(dto.appearance.theme,dto.appearance.density))return false;state.settingsRevision=dto.revision;showSettingsStatus("桌面外观设置已同步。","success");return true;
  }
  async function loadSettings() {
    try {const dto=await readOnlyJSON(ROUTES.settings);if(!hydrateSettings(dto))throw new Error("fusion_settings_invalid");return true;}
    catch(_error){showSettingsStatus("桌面设置暂时不可用；本次会话继续使用当前外观。","error");return false;}
  }
  async function chooseAppearance(theme,density) {
    applyAppearance(theme,density);showSettingsStatus("正在保存桌面外观设置…","loading");
    try {await patchPreferences();return true;}catch(_error){
      try {const latest=await readOnlyJSON(ROUTES.settings);if(latest?.schema_version==="desktop-settings-v1"&&Number.isInteger(latest.revision))state.settingsRevision=latest.revision;}catch(_ignored){}
      showSettingsStatus("外观保存失败；本次会话仍保留当前选择，可稍后重试。","error");state.theme=theme;state.density=density;safeStoreAppearance();return false;
    }
  }
  function evidenceInspector(view) {
    const inspector=state.inspectorState[view];if(inspector?.kind==="review-candidate")return reviewInspectorHTML(inspector.item,inspector.status);const row=state.inspectorState[view]?.row;if(!row)return `<section class="fusion-inspector-section"><span>真实文献 · 只读</span><h2>${state.literatureStatus==="error"?"证据暂时无法载入":"尚未选择证据"}</h2><p>精确检索不会调用 AI；收费动作会在执行前逐次确认。</p></section>`;
    return `<section class="fusion-inspector-section"><span>${esc(row.type.toUpperCase())} · 真实只读证据</span><div class="metric">${esc(row.value)}${row.unit?` <small>${esc(row.unit)}</small>`:""}</div><h2>${esc(row.title)}</h2></section><section class="fusion-inspector-section"><dl><dt>来源页</dt><dd>${esc(row.page)}</dd><dt>论文</dt><dd>${esc(row.articleTitle||state.paper?.title||"—")}</dd><dt>状态</dt><dd>只读</dd></dl>${row.excerpt?`<p>${esc(row.excerpt)}</p>`:""}</section>`;
  }
  const INSPECTOR_RENDERERS=Object.freeze({
    paper:()=>evidenceInspector("paper"),search:()=>evidenceInspector("search"),personal:()=>personalInspector(),
    package:()=>{const job=state.inspectorState.package?.job;return `<section class="fusion-inspector-section"><span>资料包安全</span><h2>${job?esc(job.stage||"资料包任务"):"三类数据彼此隔离"}</h2><p>${job?`当前进度 ${Math.max(0,Math.min(100,Math.round(Number(job.progress||0))))}%`:"官方资料包只读且经签名审计；用户包只做 SHA-256 完整性校验，不加密、不认证发送者身份。"}</p></section>`;},
    settings:()=>`<section class="fusion-inspector-section"><span>设置 · ${esc(SETTINGS_LABELS[state.settingsSection]||"设置")}</span><h2>外观与受信 AI</h2><p>外观由桌面设置同步；API 密钥只写入当前电脑的私有加密存储，页面不会回显。</p></section>`
  });
  function inspectorForView(view){return INSPECTOR_RENDERERS[view]?.()||`<section class="fusion-inspector-section"><span>Fusion 工作台</span><h2>详情检查器</h2></section>`;}
  function updateInspector() {
    const title={paper:"论文检查器",search:"证据检查器",personal:"列检查器",package:"安全边界",settings:"设置说明"}[state.view];
    q("#fusion-inspector-title").textContent=state.evidenceDetailOpen&&privateTableIdentity(state.evidenceDetail)?"实验表格检查器":title;q("#fusion-inspector-body").innerHTML=state.evidenceDetailOpen&&privateTableIdentity(state.evidenceDetail)?personalTableInspector(state.personalTablePage):inspectorForView(state.view);bindColumnEditor();syncSelectedEvidenceAI();
  }
  const SETTINGS_LABELS={appearance:"设置：外观",language:"设置：语言",ai:"设置：AI 与密钥",data:"设置：数据",about:"设置：关于"};
  const DETAIL_TAB_LABELS={paper:"证据详情",search:"证据与来源",personal:"列核验",package:"任务详情",settings:"设置说明"};
  function switchView(name,{focus=true,activateTab=true,preserveGroup=false}={}) {
    if(!VIEWS.includes(name)) return false;
    const previous=state.view;if(previous!==name){closeReviewDetail({focus:false});closeEvidenceDetail({focus:false});if(previous==="paper")closeCurrentPDF({focus:false});if(previous==="search"){state.searchRequest+=1;cancelLibrarianRequest("已离开图书管理员；当前等待结果不会写入新页面。");}if(previous==="personal"){state.personalAction+=1;state.personalPageRequest+=1;}} state.view=name;state.selectedEvidence=state.selectionByView[name]?.row||null;state.evidenceIndex=state.selectionByView[name]?.index||0; document.body.dataset.view=name;
    if(!preserveGroup)documentTabs?.focusGroup("primary");coordinateTaskLayout("view");renderDocumentTabs();
    qa("[data-view-panel]").forEach(panel=>{const active=panel.dataset.viewPanel===name;panel.hidden=!active;panel.classList.toggle("active",active);});
    qa(".fusion-nav[data-view]").forEach(button=>{const active=button.dataset.view===name;button.classList.toggle("active",active);active?button.setAttribute("aria-current","page"):button.removeAttribute("aria-current");});
    qa("[data-context-view]").forEach(panel=>{const active=panel.dataset.contextView===name;panel.hidden=!active;panel.classList.toggle("active",active);});
    q("#fusion-context-title").textContent=name==="search"&&state.searchMode==="librarian"?"研究对话":VIEW_LABELS[name]; q("#fusion-breadcrumb").textContent=`${VIEW_LABELS[name]} / Fusion 科研工作台`;
    q("#fusion-primary-tab-label").textContent=name==="settings"?SETTINGS_LABELS[state.settingsSection]:{paper:"当前论文",search:state.searchMode==="librarian"?"图书管理员":"精确检索",personal:state.personalPreview?"实验数据核验":"选择实验文件",package:"资料包中心"}[name];
    q("#fusion-detail-tab-label").textContent=DETAIL_TAB_LABELS[name];
    q("#fusion-status-context").textContent={paper:"真实文献与核验",search:"多来源证据检索",personal:state.personalPreview?"本机实验集中核验":"选择真实实验文件",package:"资料包与导出",settings:"外观与受信 AI"}[name];
    if(name==="settings")selectSettingsSection(state.settingsSection);
    closeDrawers(false);applyPaneLayout();updateInspector();syncDrawerAccessibility();syncSearchExports();if(name==="package")void loadPackageCenter();if(name==="personal"){void loadPersonalSearchStatus();if(state.personalPreview&&(!state.personalPreviewPage||state.personalPreviewPage.importId!==state.personalStatus?.import_id||state.personalPreviewPage.sheetIndex!==state.personalSheetIndex))void loadPersonalPreviewPage(state.personalSheetIndex,1);}if(name==="paper")void loadLiteratureTaskDirectory().then(()=>reconnectLiteratureJob());if(activateTab&&name==="paper"&&state.paper)openDocumentTab("paper",state.paper.title,{paperId:String(state.paper.id)},{ownerView:"paper",payload:{paperId:state.paper.id,paper:state.paper,evidenceCount:state.evidence.length,evidence:state.evidence},preview:true});
    if(focus&&previous!==name) q("#fusion-editor")?.focus({preventScroll:true}); return true;
  }
  function closeDrawers(restore=true) {
    q("#fusion-context")?.classList.remove("drawer-open");q("#fusion-inspector")?.classList.remove("drawer-open");qa("[data-open-drawer]").forEach(button=>button.setAttribute("aria-expanded","false"));const closeAll=q("[data-close-all-drawers]");if(closeAll)closeAll.hidden=true;state.drawer=null;syncDrawerAccessibility();
    if(restore&&state.focusReturn?.isConnected) state.focusReturn.focus(); state.focusReturn=null;
  }
  function openDrawer(kind,trigger) {
    if(!["context","inspector"].includes(kind)) return;closeDrawers(false);state.drawer=kind;state.focusReturn=trigger;
    const panel=q(kind==="context"?"#fusion-context":"#fusion-inspector");panel?.classList.add("drawer-open");panel?.setAttribute("aria-hidden","false");trigger?.setAttribute("aria-expanded","true");q("[data-close-all-drawers]").hidden=false;
    panel?.querySelector("button")?.focus();
  }
  function syncDrawerAccessibility() {
    const projection=workspaceProjection(),contextHidden=projection.context==="hidden"||projection.context==="drawer"&&state.drawer!=="context",inspectorHidden=projection.inspector==="hidden"||projection.inspector==="drawer"&&state.drawer!=="inspector";q("#fusion-context")?.setAttribute("aria-hidden",contextHidden?"true":"false");q("#fusion-inspector")?.setAttribute("aria-hidden",inspectorHidden?"true":"false");
  }
  function openCommandPalette(trigger=q("#fusion-command")) {
    const palette=q("#fusion-command-palette");if(!palette)return false;state.paletteOpen=true;state.paletteFocusReturn=trigger;palette.hidden=false;trigger?.setAttribute("aria-expanded","true");const options=qa("[data-command-view],[data-command-action]");options.forEach((button,index)=>button.tabIndex=index===0?0:-1);options[0]?.focus();return true;
  }
  function closeCommandPalette(restore=true) {
    const palette=q("#fusion-command-palette");if(palette)palette.hidden=true;q("#fusion-command")?.setAttribute("aria-expanded","false");state.paletteOpen=false;if(restore&&state.paletteFocusReturn?.isConnected)state.paletteFocusReturn.focus();state.paletteFocusReturn=null;
  }
  function executeCommand(button){const view=button?.dataset?.commandView,action=button?.dataset?.commandAction;if(view){closeCommandPalette(false);switchView(view);q(`.fusion-nav[data-view="${view}"]`)?.focus();return true;}closeCommandPalette(false);if(action==="toggle-context")return togglePane("context",q("#fusion-command"));if(action==="toggle-inspector")return togglePane("inspector",q("#fusion-command"));if(action==="toggle-primary")return togglePane("primary");if(action==="toggle-secondary")return togglePane("secondary");if(action==="split-active")return splitActiveTab();if(action==="reopen-closed")return reopenClosedTab();return false;}
  function handlePaletteKey(event) {if(event.key==="Escape"){event.preventDefault();closeCommandPalette();return;}handleRovingTabs(event,"[data-command-view],[data-command-action]",button=>button.focus());if(event.key==="Enter"||event.key===" "){event.preventDefault();executeCommand(event.currentTarget);}}
  function selectCell(row,column,{focus=true}={}) {
    const page=state.personalPreviewPage,rows=page?.rows||[],columns=page?.columns||[];if(!rows.length||!columns.length)return false;state.row=Math.max(0,Math.min(row,rows.length-1));state.column=Math.max(0,Math.min(column,columns.length-1));
    qa("#fusion-data-grid [data-cell]").forEach(cell=>{const active=Number(cell.dataset.row)===state.row&&Number(cell.dataset.column)===state.column;cell.classList.toggle("selected",active);cell.tabIndex=active?0:-1;cell.setAttribute("aria-selected",active?"true":"false");});
    qa("#fusion-data-grid th[data-column]").forEach(cell=>cell.classList.toggle("column-selected",Number(cell.dataset.column)===state.column));state.inspectorState.personal={kind:"cell",row:state.row,column:state.column};updateInspector();
    if(focus) q(`#fusion-data-grid [data-row="${state.row}"][data-column="${state.column}"]`)?.focus({preventScroll:true});return true;
  }
  function personalInspector() {if(!state.personalPreview)return `<section class="fusion-inspector-section"><span>个人实验</span><h2>尚未选择文件</h2><p>选择 CSV、TSV 或 XLSX 后，这里会显示当前工作表和核验状态。</p></section>`;const sheet=previewSheet(),page=state.personalPreviewPage,columns=sheet?.columns||[],row=page?.rows?.[state.row],column=columns[state.column],value=Array.isArray(row)?row[state.column]:"",globalRow=page?.rows?.length?(page.page-1)*page.pageSize+state.row+1:0;return `<section class="fusion-inspector-section"><span>本机文件检查</span><h2>${esc(sheet?.sheet_name||"实验工作表")}</h2><p>${Number(sheet?.row_count||0)} 行 · ${columns.length} 列。当前确认只导入这个工作表；AI 预填不是确认。</p></section>${column&&row?`<section class="fusion-inspector-section"><span>第 ${globalRow} 行 · 当前真实单元格</span><div class="metric">${esc(value)}</div><h2>${esc(column.source_name)}</h2><p>${esc(column.meaning||"物理意义待核验")}${column.unit?` · ${esc(column.unit)}`:""}</p></section>`:""}${!page?`<section class="fusion-inspector-section"><span>分页状态</span><p>${esc(state.inspectorState.personal?.message||"正在读取真实表格。")}</p></section>`:""}<section class="fusion-inspector-section"><span>当前状态</span><p>${state.personalStatus?.indexable===true?"已核验并进入私人搜索。":"尚未确认；可在中央逐列修改角色、意义和单位。"}</p></section>`;}
  function bindColumnEditor() {}
  function updateColumnDefinition(){return false;}
  function selectSettingsSection(name,{focus=false}={}) {
    if(!["appearance","language","ai","data","about"].includes(name))return false;state.settingsSection=name;state.inspectorState.settings={kind:"settings",section:name};qa("[data-settings-panel]").forEach(panel=>panel.hidden=panel.dataset.settingsPanel!==name);qa("[data-settings-section]").forEach(button=>{const active=button.dataset.settingsSection===name;button.classList.toggle("active",active);button.setAttribute("aria-selected",active?"true":"false");button.tabIndex=active?0:-1;});if(state.view==="settings")q("#fusion-primary-tab-label").textContent=SETTINGS_LABELS[name];if(focus)q(`[data-settings-section="${name}"]`)?.focus();return true;
  }
  function renderSheet(index=state.personalSheetIndex,{focus=false}={}) {const ok=choosePersonalSheet(Number(index));if(ok&&focus)selectCell(0,0,{focus:true});return ok;}
  function handleGridKey(event) {
    if(!event.target.matches?.("#fusion-data-grid [data-cell]")) return;const moves={ArrowLeft:[0,-1],ArrowRight:[0,1],ArrowUp:[-1,0],ArrowDown:[1,0]};
    if(moves[event.key]){event.preventDefault();selectCell(state.row+moves[event.key][0],state.column+moves[event.key][1]);}
    else if(event.key==="Home"){event.preventDefault();selectCell(event.metaKey||event.ctrlKey?0:state.row,0);}
    else if(event.key==="End"){event.preventDefault();const rows=state.personalPreviewPage?.rows||[],columns=state.personalPreviewPage?.columns||[];selectCell(event.metaKey||event.ctrlKey?rows.length-1:state.row,columns.length-1);}
    else if(event.key==="PageUp"){event.preventDefault();selectCell(state.row-10,state.column);}
    else if(event.key==="PageDown"){event.preventDefault();selectCell(state.row+10,state.column);}
  }
  function handleShortcut(event) {
    if(event.key==="Escape"&&state.reviewDetailTabId){event.preventDefault();closeReviewDetail();return;}
    if(event.key==="Escape"&&state.evidenceDetailPDF){event.preventDefault();closeDetailPDF();return;}
    if(event.key==="Escape"&&state.evidenceDetailOpen){event.preventDefault();closeEvidenceDetail();return;}
    if(event.key==="Escape"&&state.paletteOpen){event.preventDefault();closeCommandPalette();return;}
    if(event.key==="Escape"&&state.drawer){event.preventDefault();closeDrawers();return;}
    if(event.target.matches?.("input,textarea,select,[contenteditable='true']")) return;const modifier=event.metaKey||event.ctrlKey;
    const byKey={"1":"paper","2":"search","3":"personal","4":"package"};if(modifier&&byKey[event.key]){event.preventDefault();switchView(byKey[event.key]);}
    else if(modifier&&event.key.toLowerCase()==="k"){event.preventDefault();openCommandPalette();}
    else if(modifier&&event.key===","){event.preventDefault();switchView("settings");}
  }
  function handleRovingTabs(event,selector,activate) {
    if(!["ArrowLeft","ArrowRight","ArrowUp","ArrowDown","Home","End"].includes(event.key)) return;const buttons=qa(selector).filter(button=>!button.disabled),current=buttons.indexOf(event.currentTarget);if(current<0||!buttons.length)return;event.preventDefault();
    const forward=event.key==="ArrowRight"||event.key==="ArrowDown",next=event.key==="Home"?0:event.key==="End"?buttons.length-1:(current+(forward?1:-1)+buttons.length)%buttons.length;buttons[next].focus();activate(buttons[next]);
  }
  function initialize() {
    readAppearance();readPaneLayout();applyPaneLayout();renderDocumentTabs();const restoredActive=documentTabs?.activeTab(),initialView=restoredActive?.ownerView||"paper";switchView(initialView,{focus:false,activateTab:false,preserveGroup:true});const restorationPrerequisites=Promise.all([loadLiterature().then(async()=>{await loadLiteratureTaskDirectory();return reconnectLiteratureJob();}),loadLibrarianHistory(),loadEvidenceChatHistory()]);void Promise.all([loadReleaseInfo(),loadSettings(),loadAISettingsUI(),loadReviewQueue(),loadResearchMemories(),restorationPrerequisites]).then(()=>hydrateActiveRestoredTabs()).catch(()=>false);
    qa(".fusion-nav[data-view]").forEach(button=>{button.addEventListener("click",()=>switchView(button.dataset.view));button.addEventListener("keydown",event=>handleRovingTabs(event,".fusion-nav[data-view]",next=>{switchView(next.dataset.view,{focus:false});next.focus();}));});
    qa("[data-theme-choice]").forEach(button=>button.addEventListener("click",()=>void chooseAppearance(button.dataset.themeChoice,state.density)));
    qa("[data-density-choice]").forEach(button=>button.addEventListener("click",()=>void chooseAppearance(state.theme,button.dataset.densityChoice)));
    qa("[data-open-drawer]").forEach(button=>{const kind=button.dataset.openDrawer;button.setAttribute("aria-controls",kind==="context"?"fusion-context":"fusion-inspector");button.setAttribute("aria-expanded","false");button.addEventListener("click",()=>openDrawer(kind,button));});
    qa("[data-close-drawer]").forEach(button=>button.addEventListener("click",()=>closeDrawers()));q("[data-close-all-drawers]")?.addEventListener("click",()=>closeDrawers());
    bindPaneSeparators();
    q("#fusion-command")?.addEventListener("click",event=>openCommandPalette(event.currentTarget));qa("[data-command-view],[data-command-action]").forEach(button=>{button.addEventListener("keydown",handlePaletteKey);button.addEventListener("click",()=>executeCommand(button));});
    qa("[data-settings-section]").forEach(button=>{button.addEventListener("click",()=>selectSettingsSection(button.dataset.settingsSection));button.addEventListener("keydown",event=>handleRovingTabs(event,"[data-settings-section]",next=>selectSettingsSection(next.dataset.settingsSection,{focus:true})));});selectSettingsSection("appearance");
    q("#fusion-ai-provider")?.addEventListener("change",event=>void selectAIProvider(event.currentTarget.value));q("#fusion-ai-model-save")?.addEventListener("click",()=>void saveAIModels());q("#fusion-ai-key-save")?.addEventListener("click",()=>void saveAIKey());q("#fusion-ai-key-delete")?.addEventListener("click",()=>void deleteAIKey());q("#fusion-ai-test")?.addEventListener("click",()=>void testAIConnection());q("#fusion-ai-custom-save")?.addEventListener("click",()=>void saveCustomProvider());q("#fusion-ai-custom-delete")?.addEventListener("click",()=>void deleteCustomProvider());q("#fusion-ai-business-readiness")?.addEventListener("click",event=>{const scope=event.target.closest?.("[data-ai-readiness-scope]")?.dataset.aiReadinessScope;if(scope)void runAIVerification(scope);else if(event.target.closest?.("[data-ai-readiness-connection]"))void runAIVerification(null);else if(event.target.closest?.("[data-ai-readiness-key]"))q("#fusion-ai-key")?.focus();else if(event.target.closest?.("[data-ai-readiness-repair]"))aiStatus("请修复或更新桌面 Harness 运行环境后重新打开设置页；当前不会回退旧 AI 接口。","error");});
    q("#fusion-split-tab")?.addEventListener("click",()=>{splitActiveTab();q("#fusion-tab-more")?.removeAttribute?.("open");});
    q("#fusion-reopen-tab")?.addEventListener("click",()=>{reopenClosedTab();q("#fusion-tab-more")?.removeAttribute?.("open");});
    qa("[data-pane-toggle]").forEach(button=>button.addEventListener("click",()=>{togglePane(button.dataset.paneToggle,button);button.closest?.("details")?.removeAttribute?.("open");}));
    q("#fusion-secondary-move-primary")?.addEventListener("click",()=>{const active=documentTabs?.activeTab("secondary");if(active&&documentTabs.move(active.tabId,"primary")){renderDocumentTabs();activateDocumentTab(active.tabId);}});
    q("#fusion-data-grid")?.addEventListener("keydown",handleGridKey);
    q("#fusion-detail-back")?.addEventListener("click",()=>state.reviewDetailTabId?closeReviewDetail():closeEvidenceDetail());q("#fusion-detail-open-pdf")?.addEventListener("click",openDetailPDF);q("#fusion-detail-close-pdf")?.addEventListener("click",()=>closeDetailPDF());q("#fusion-detail-close-pdf-view")?.addEventListener("click",()=>closeDetailPDF());q("#fusion-detail-show-highlight")?.addEventListener("click",toggleSourceHighlight);bindPDFControls();
    q("#fusion-paper-filter")?.addEventListener("input",event=>{state.paperCatalogQuery=String(event.currentTarget.value||"").trim();renderPaperCatalog();});q("#fusion-import-pdf")?.addEventListener("click",()=>q("#fusion-pdf-file")?.click());q("#fusion-pdf-file")?.addEventListener("change",event=>{const file=event.currentTarget.files?.[0];event.currentTarget.value="";if(file)void uploadPDF(file).catch(error=>setLiteratureActionStatus(`${cleanText(error?.code||"pdf_import_failed",80)} · ${cleanText(error?.message||"PDF 导入未完成；现有文献不受影响。",500)}`,"error"));});q("#fusion-start-extraction")?.addEventListener("click",()=>void runLiteratureExtraction());q("#fusion-repair-search-index")?.addEventListener("click",()=>void recoverSearchIndex());q("#fusion-open-pdf")?.addEventListener("click",openCurrentPDF);q("#fusion-close-pdf")?.addEventListener("click",()=>closeCurrentPDF());q("#fusion-open-review-queue")?.addEventListener("click",()=>void openReviewQueue());
    qa("[data-search-source]").forEach(button=>button.addEventListener("click",()=>setSearchSource(button.dataset.searchSource,{run:state.view==="search"&&state.searchMode==="precise"})));qa("[data-evidence-type]").forEach(button=>{button.addEventListener("click",()=>toggleEvidenceType(button.dataset.evidenceType));button.addEventListener("keydown",event=>handleRovingTabs(event,"[data-evidence-type]",next=>next.focus()));});setSearchSource("workspace");q("#fusion-run-precise-search")?.addEventListener("click",()=>void runPreciseSearch());q("#fusion-open-librarian")?.addEventListener("click",()=>{setSearchMode("librarian");q("#fusion-librarian-question")?.focus();});q("#fusion-librarian-form")?.addEventListener("submit",event=>void submitLibrarian(event));q("#fusion-librarian-question")?.addEventListener("keydown",event=>{if(event.key==="Enter"&&!event.shiftKey&&!event.isComposing){event.preventDefault();event.currentTarget.form?.requestSubmit?.();}});q("#fusion-librarian-history-query")?.addEventListener("input",event=>{state.librarianHistoryQuery=String(event.currentTarget.value||"");renderLibrarianHistory();});q("#fusion-librarian-new")?.addEventListener("click",startNewLibrarianConversation);q("#fusion-librarian-history-clear")?.addEventListener("click",()=>void clearLibrarianHistory());q("#fusion-librarian-remember")?.addEventListener("click",()=>void rememberCurrentResearch());q("#fusion-research-memory-clear")?.addEventListener("click",()=>void clearResearchMemories());q("#fusion-librarian-results-toggle")?.addEventListener("click",()=>setLibrarianResultsOpen(!state.librarianResultsOpen));q("#fusion-librarian-results-close")?.addEventListener("click",()=>setLibrarianResultsOpen(false,{manageSpace:false}));q("#fusion-search-query")?.addEventListener("keydown",event=>{if(event.key==="Enter"){event.preventDefault();void runPreciseSearch();}});
    bindEvidenceChatActions();
    personalImport()?.bind();if(initialView!=="personal")void loadPersonalSearchStatus();
    packageCenter()?.bind();
    renderLibrarianConversation();renderSelectedEvidenceChat();document.addEventListener("keydown",handleShortcut);globalThis.addEventListener?.("resize",syncResponsivePaneLayout);observePaneContainer();syncResponsivePaneLayout();
  }
  globalThis.AutoResearchFusion=Object.freeze({initialize,switchView,renderSheet,selectCell,selectPaper,selectEvidence,openEvidenceDetail,closeEvidenceDetail,openDetailPDF,closeDetailPDF,showSourceHighlight,toggleSourceHighlight,detailPDFURL,publicEvidence,publicReviewQueue,reviewCandidateHTML,loadReviewQueue,openReviewQueue,openReviewCandidate,performReviewAction,closeReviewDetail,privateTableIdentity,publicPersonalTablePage,loadPrivateTablePage,publicPersonalImportPage,loadPersonalPreviewPage,loadPersonalSearchStatus,refreshPersonalSearch,publicPersonalImportNextAction,openImportedPersonalTable,returnFromSecondaryPersonalTable,renderLibrarianFinal,renderLibrarianConversation,startNewLibrarianConversation,renderLibrarianResultNavigator,setLibrarianResultsOpen,openLibrarianEvidence,openLibrarianArticle,prepareLibrarianSpace,loadLibrarianHistory,saveCurrentLibrarianSession,restoreLibrarianSession,clearLibrarianHistory,loadResearchMemories,renderResearchMemories,clearResearchMemoryCandidate,rememberCurrentResearch,editResearchMemory,deleteResearchMemory,clearResearchMemories,loadLiterature,renderPaperCatalog,loadSettings,hydrateSettings,chooseAppearance,openDrawer,closeDrawers,openCommandPalette,closeCommandPalette,executeCommand,applyAppearance,updateColumnDefinition,selectSettingsSection,runPreciseSearch,setSearchMode,setSearchSource,toggleEvidenceType,selectedEvidenceTypes,selectPackageWorkflow,renderDocumentTabs,renderEditorSurfaces,activateDocumentTab,hydrateDocumentTab,hydrateActiveRestoredTabs,visibleActiveTabsForHydration,captureTabPresentation,renderStableTabMount,secondaryDocumentHTML,readPaneLayout,applyPaneLayout,setPaneLayoutValue,resetPaneLayout,paneValueFromPointer,startPaneDrag,movePaneDrag,endPaneDrag,handlePaneKey,bindPaneSeparators,syncResponsivePaneLayout,coordinateTaskLayout,workspaceProjection,togglePane,splitActiveTab,reopenClosedTab,paneController,paneLayout,workspaceReveal,taskLayoutIdentity,projectAIActivity,currentPaperExportURL,workspaceSearchExportURL,evidenceExportIdentity,evidenceExportURL,syncPaperActions,syncSearchExports,syncDetailImageDownload,syncDetailEvidenceExports,uploadPDF,runLiteratureExtraction,openCurrentPDF,closeCurrentPDF,choosePersonalFile,requestPersonalSuggestion,applyPersonalSuggestion,addPersonalSeries,removePersonalSeries,refreshPersonalSeriesColumns,parsePersonalConditions,collectPersonalDraft,confirmPersonalImport,preparedAuthorization,executePrepared,reconnectLiteratureJob,loadLiteratureTaskDirectory,publicLiteratureTaskDirectory,publicLiteratureReceiptSummary,renderLiteratureReceiptSummary,currentLiteratureRecovery,ensureAIReadiness,runAIVerification,publicAIReadiness,readinessCopy,renderAIReadiness,submitSelectedEvidenceAI,selectedEvidenceAIIdentity,clearSelectedEvidenceThread,publicSelectedEvidenceAnnotations,publicOfficialPackageResult,publicActiveOfficialPackageSummary,renderOfficialPackageResult,rememberOfficialPackageResult,publicDatasetPlan,publicDatasetReceipt,updateDatasetExportButton,planDataset,exportDataset,loadPackageCenter,importOfficialPackage,planLiteraturePackage,planPersonalPackage,exportPlannedPackage,inspectUserPackage,importUserPackage,loadAISettingsUI,selectAIProvider,saveAIModels,saveAIKey,deleteAIKey,testAIConnection,saveCustomProvider,deleteCustomProvider,state,documentTabs,aiExperience});
  if(document.readyState==="loading") document.addEventListener("DOMContentLoaded",initialize,{once:true});else initialize();
})();
