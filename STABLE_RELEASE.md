# Auto Research Evidence 阶段稳定版

## 版本身份

- 版本：`2026.07.18-chinese-visual-semantics.1`
- Git 标签：`evidence-demo-2026-07-18-chinese-visual-semantics-1`
- 证据库结构：`v11`
- 本地编辑端：`http://127.0.0.1:8765/`
- 只读分享端：由 `scripts/start_readonly_ngrok.command` 生成临时公网地址

本次科研排版与多文章检索改进作为上述稳定版的维护快照发布，不提升用户侧版本号：35 篇固定语料审计仍如实包含 5 篇无效 PDF 占位与 13 篇待抽取论文，不能伪装成完整语料发布通过。维护快照标签为 `evidence-demo-2026-07-18-scientific-search-checkpoint-1`，对应 bundle 为 `/Users/USER/Zotero/auto-research-backups/auto-research-scientific-search-2026-07-18.bundle`。

2026-07-19 新增的条目级 DeepSeek 证据对话同样作为维护检查点，不提升上述语料稳定版版本号。它只在本地编辑端开放，读取当前条目与原始 PDF 的相关文字页，不改写数据库；只读分享端不提供模型调用入口。检查点标签为 `evidence-demo-2026-07-19-context-chat-checkpoint-1`，bundle 为 `/Users/USER/Zotero/auto-research-backups/auto-research-context-chat-2026-07-19.bundle`。

## 当前数据快照

| 对象 | 数量 | 说明 |
|---|---:|---|
| 已登记论文 | 35 | 以 DOI 或题目作为跨设备身份 |
| 原始六列记录 | 4,754 | SQLite 与版本历史完整保留 |
| 可报告数值来源 | 3,710 | 同一事实可保留多处原文证据 |
| 独立物理事实 | 2,484 | 校对、搜索、进度和导出的默认口径 |
| 已折叠重复提及 | 1,226 | 仅折叠展示，不删除原始证据 |
| 定性实验结论 | 770 | 与数值事实分开检索 |
| 隔离的旧文字值 | 1,044 | 不出现在数值事实、校对和数值搜索中 |
| 原文图表资产 | 243 | 40 个表格、203 幅图片，均来自本地 PDF 裁剪 |
| 十篇质量回归集 | 10/10 | 完整 PDF、六列、搜索、原文定位和图表均通过 |

## 本版重点修复

- 数据条目、原始表格和论文图片搜索结果新增本地“AI 解读”：点选后从左侧展开 DeepSeek 对话，默认问题可修改，并支持基于同一证据对象的连续追问。回答展示所用 PDF 页码、证据说明和局限。
- 对话只提交当前结构化对象与原始 PDF 的少量相关文字页，不提交全库；刷新页面即清空会话。该辅助解释不会写回科研记录，也不会改变自动质量门或搜索结果。
- 公网只读端隐藏 AI 入口并拒绝调用接口，避免分享用户消耗模型额度或访问本地 PDF 上下文。
- 搜索卡片中的数值与单位改用 Times/STIX/Cambria Math 科研排版栈；科学计数法、正负号和幂指数采用真正的上标显示，单位与数值同字号。该调整只改变显示，不改写数据库中的原始数值和单位。
- 搜索范围与单篇文章校对选择完全分离：默认检索全库，也可按题目、DOI、一作或通讯作者勾选多篇文章；同一范围同步作用于数值、实验结论、表格、图片和 CSV/Excel 导出。
- 既有图表标题在搜索页按“材料/对象 + 研究重点”两级显示；新上传论文的 DeepSeek 图表编目要求标题优先包含材料、关键条件或对比双方以及物理量，避免“名义与实测成分”等空泛标题。既有图表元数据未重新抽取或批量改写。
- 人工核验不再是自动发布的前置条件：双路一致或第三次复核通过的结果自动进入搜索；仍不达标的候选自动隔离。校对页保留为可选纠错入口，不再形成必须逐条放行的待办队列。
- 图表中文编目重新成为质量门的强制合约：每个自动发布图表必须有上下文支持的中文短标题、中文解释和至少两个专属标签；原文图号、原始图注、截图、哈希与 PDF 定位保持不变。
- 对抗式质量管线复用项目原有图表提示与清洗器，不再存在英文 `Figure/Table` 元数据旁路。第三位 DeepSeek 可给出完整修正版，修正版按既有必填边界重新计分，自动发布阈值仍为 85。
- 固定十篇测试集的 91 个图表已完成真实 DeepSeek 专项重建：中文标题 91/91、中文上下文解释 91/91、专属标签 91/91，仅编号标题 0/91；中文标题、解释和标签均进入原有表格/图片搜索。
- 新增 `evidence-quality-visuals <文章>`，仅重建图表检索语义，不重新裁图、不运行 OCR、不改变人工校对历史。
- 新增 DeepSeek 对抗式质量门：完整性分支与精确性分支并行独立抽取数值、结论和图表语义，再按原文证据身份逐项匹配并评分。
- 默认 85 分为自动发布阈值。双路一致的高分项直接进入搜索；低分或单路项由第三次独立 DeepSeek 复核；仍未通过的项进入“质量待审”，不会出现在搜索或导出中。
- 校对页显示自动质量门进度、分阶段状态和各类通过数；被拦截对象提供失败原因，供需要时进行可选纠错。
- 条目、实验结论、表格和图片搜索均可按“双路一致、第三次复核、历史稳定数据”等来源过滤，并显示质量来源与得分。
- 旧单路提交入口已封口：网页兼容接口、文章自动工作流以及命令行带 `--commit` 的旧命令都会进入同一个对抗式质量门；不带提交参数的单路命令只生成诊断预览。
- 既有 4,754 条历史记录和 243 个本地图表以 `legacy_stable` 继续可搜，不要求追溯重跑；新候选只有通过质量门才可发布。
- 完整撤销未达质量要求的 MinerU 云端图表增强实验：移除活动数据库中的云端运行、候选和来源版本，移除界面入口、接口、搜索覆盖、缓存、配置入口与项目钥匙串凭据。
- 恢复并固定此前稳定的本地图表链路：PyMuPDF 从真实 PDF 建立高分辨率截图，DeepSeek 只依据图注与相邻文字生成语义元数据，图表搜索继续直接使用本地稳定资产。
- 云端实验只保留在旧 Git 标签和旧 bundle 中作为开发审计历史，不参与当前启动、数据库、检索、校对或只读分享。
- 表格截图不再使用固定高度。优先使用 PDF 的实际表格边界，完整覆盖表题、表头和最后一行数据；无结构表格才使用连续表线兜底。
- “List of Tables/Figures”目录页和 `Table 1 lists...`、`Fig. 9a...` 等正文引用不再被误认成真实图表。
- 搜索卡片按完整宽高比缩放原图，不再为了铺满卡片裁掉核心数据；放大窗口继续显示原始高分辨率截图。
- 图表的原文编号、中文短名称、原始图注、中文解释和专属标签分开保存。证据位置变化时旧 DeepSeek 元数据自动失效并重新编目。
- 首批 10 篇内容有效 PDF 均已具备可用的六列数据、搜索、原文定位和图表；其中双路 DeepSeek 全文质量门完成 8 篇，另 2 篇因当时网络请求失败而保留既有稳定数据。图表中文语义随后已对 10 篇共 91 个图表独立补齐。
- 短英文词不再进行宽松模糊匹配，减少 `less/than/half` 等普通词造成的大量无关搜索结果；“钨”和 `W` 仍返回一致的元素检索结果。

## 验收结果

- 条目级证据对话维护检查点：172 项自动测试通过；真实 DeepSeek 对条目 #1174 返回中文回答及 PDF 页引用，调用前后数据库哈希一致；编辑端和只读端浏览器隔离通过。
- Python 自动测试：170/170 通过；Python 编译通过。
- 真实 DeepSeek 副本烟雾测试：一页生成 11 个候选，8 个双路通过、2 个第三次复核通过、1 个 84 分候选进入人工审核；正式数据库未写入测试候选。
- SQLite 完整性、外键、必需索引、AI 运行产物和 243 个图表文件全部通过。
- 十篇固定回归审计：10 篇可定位、10 篇 PDF 有效、10 篇数据可搜、10 篇图表可用；共 1,668 个独立事实、2,731 处数值证据、91 个图表，2,729 处可高亮定位。
- 十篇图表中文语义审计：91/91 中文标题、91/91 中文解释、91/91 专属标签；中文关键词搜索可直接命中标题、解释和标签。
- 目标论文自检：195 个独立事实，226/226 处自动数据证据可高亮定位；“辐照温度”和“硬度”搜索及 CSV/Excel 导出通过。
- 用户报告的回归样例已检查：Figure 9 不再混入 Figure 8；Table 2、ODS-NiCoFeCr Table 1、SiC Table 1/Table 7 和涂层成分 Table 1 均包含核心数据且无整页正文污染。
- 两处历史人工确认/修正版本仍保留；本次维护没有重新抽取文章、调用 DeepSeek 或批量改写科学数据。

## 启动和恢复

- 活动项目目录：`/Users/USER/Zotero/auto-research`；旧 iCloud 目录仅作备份。
- 本地编辑端：双击 `/Users/USER/Zotero/打开本地编辑工作台.command`。
- 只读公网链接：双击 `/Users/USER/Zotero/创建导师公网链接.command`。
- 完整 Git 备份：`/Users/USER/Zotero/auto-research-backups/auto-research-chinese-visual-semantics-2026-07-18.bundle`
- 图表语义修复前数据库备份：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-before-chinese-visual-repair-2026-07-18.sqlite`
- 恢复时可从 bundle 克隆项目，再使用标签 `evidence-demo-2026-07-18-chinese-visual-semantics-1` 定位本版本。

## 重要边界

- “稳定版”表示程序、数据关系、证据截图和检索链路通过验收，不表示 DeepSeek 输出已经获得物理学人工认可；正常使用不要求用户逐条核验，必要时仍可在数据检查页纠错。
- DeepSeek 当前读取 PDF 文字层生成候选与图表编目信息；图表像素由本地程序裁剪供人工核对，系统不自动读取或猜测曲线点。
- 自动质量分衡量候选的双路一致性、原文支持、字段完整性和证据定位。即使达到 100 分，也不等于已经证明整篇论文绝无遗漏；科研结论仍可在校对页继续修正。
- 默认数值界面只展示可报告数值事实；旧文字值保留在历史/隔离层，科学定性观察进入“实验结论”搜索。
