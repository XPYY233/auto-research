# 教师展示说明

本版本用于展示“论文上传/识别、实验数据抽取、图表检索、证据回溯和条目级 AI 解读”的完整闭环。对外只读页与本地编辑端共用同一数据库和搜索页面，不维护第二套数据。

## 推荐展示顺序

1. 默认进入“问图书管理员”，直接输入“高熵合金在中子辐照后，硬度和缺陷结构有哪些变化？”。
2. 展示图书管理员如何明确检索边界、用 `[R编号]` 引用并返回数据、表格、图片和实验结论四类现有证据。
3. 切换“精确检索”，搜索 `温度`，展示数值、具体意义、实验语境、论文、DOI 和作者。
4. 搜索 `钨`，说明中文元素名可匹配 `W` 和 `tungsten`；再切换原始表格和论文图片。
5. 点击数据条目的“原文证据”，展示 PDF 页内高亮与原文片段。
6. 点击任一数据或图表的详情，展示条目级 DeepSeek 证据对话；说明它与全库图书管理员职责不同且都不会修改数据库。
7. 用题目、DOI 或作者限定多篇论文，说明范围同时约束图书管理员和精确检索。
8. 下载 CSV 或 Excel；只在本地编辑端展示文章切换、自动处理、校对、人工补录和质量拦截。

## 当前展示数据

- 已登记 60 篇论文和 60 个文档。
- 6,501 条原始六列记录、5,048 处可报告数值来源、3,142 个独立事实、937 条定性结论。
- 291 个本地图表资产：59 个表格、232 幅图片，记录的图片文件全部存在。
- 固定 50 篇真实性语料全部具有真实、可打开且身份匹配的 PDF；其中 17 篇数据就绪、30 篇图表就绪、33 篇仍待全文抽取。
- 通过质量门的结果自动进入搜索，剩余低分候选进入“自动拦截”；人工检查用于纠错和建立训练/校准样本。

## 重点文章

- 题目：`Irradiation effects in high entropy alloys and 316H stainless steel at 300 °C`
- DOI：`10.1016/j.jnucmat.2018.08.031`
- 231 个独立物理事实；403/403 处自动数值证据可回到 PDF 高亮。
- 4 个原文表格、10 幅原文图片。
- 搜索 `温度`、`硬度`、`钨` 或 `Wei-Ying Chen` 均可命中。

## 启动入口

- 本地编辑端：双击 `/Users/USER/Zotero/打开本地编辑工作台.command`。
- 只读公网端：双击 `/Users/USER/Zotero/创建导师公网链接.command`，复制程序显示的 `https://...ngrok-free.app` 地址。

公网地址由 ngrok 免费服务临时生成，每次启动可能变化。分享时只发送只读地址；本地编辑地址 `127.0.0.1` 只能在本机打开。公网端与本地端共用同一套搜索页面、Search V2 和图书管理员，区别仅是公网权限层禁止上传和修改。

## 展示时应主动说明的边界

- 软件功能和当前证据链已达到稳定演示状态，但最初“至少 30 篇完整处理”的语料目标尚未达到，目前为 17 篇。
- 自动质量评分代表原文支持、字段完整度和多路模型一致性，不等于物理学人工确认。
- 图表截图来自原始 PDF；系统不读取曲线像素并猜测精确数据点。
- DeepSeek 证据对话只解释当前条目及其相关 PDF 页面，不会写回数据库。
