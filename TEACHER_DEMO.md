# 教师展示说明

本版本用于展示“实验文献数据提取、证据回溯和全库检索”的完整闭环。对外分享页面只提供检索、原文证据和导出，与本地编辑端共用同一数据库；本地数据更新后无需维护第二套页面。

## 推荐展示顺序

1. 在“数据条目”中输入 `温度`，展示具体意义、文章语境、题目、DOI 和作者的联合检索。
2. 搜索 `钨`，说明中文元素名可匹配 `W` 和 `tungsten`。
3. 点击“指定多篇文章”，用题目、DOI 或作者勾选两篇论文，说明这一范围会同步作用于数值、结论、表格、图片及导出；切回“全部文章”即可恢复全库搜索。
4. 输入 `Table 3`，展示同一原表中的数据默认折叠；点击“查看原始表格”打开完整原文截图。
5. 切换到“原始表格”或“论文图片”，搜索物理量、材料或实验条件，展示中文标题、上下文解释和智能标签。
6. 返回“数据条目”，点击任一记录的“原文证据”，展示 PDF 页内高亮和证据片段。
7. 在本地编辑端点击同一记录的“AI 解读”，展示默认问题、相关 PDF 页引用和连续追问；说明它只解释当前证据且不修改数据库。公网只读演示不显示此入口。
8. 分别点击“CSV”和“Excel”，说明导出包含当前条件下的全部匹配记录，可直接用于 Origin、Python 或 Excel。
9. 在本地编辑端进入“校对数据”，用题目、作者、DOI、研究方向或处理状态缩小文章范围；筛选不会切换文章，也不会重新调用 DeepSeek。
10. 切换“数据条目 / 原始表格 / 论文图片 / 自动拦截”，展示六列纠错、图表纠错、追加式版本历史和对抗式质量门结果。

## 当前展示数据

- 已登记论文：35 篇；其中 30 篇为内容有效 PDF，5 篇仍是下载跳转或验证占位文件。
- 当前数据：4,754 条原始六列记录、3,710 处可报告数值来源、2,484 个独立物理事实。
- 图表证据：243 个本地高分辨率资产，包括 40 个表格和 203 幅图片。
- 十篇稳定回归集：10/10 篇均可搜索、查看原文证据和图表，共 1,668 个独立事实、2,731 处数值证据和 91 个图表。
- 双路 DeepSeek 全文质量门：10 篇中 8 篇完成，2 篇因当时网络失败而继续使用既有稳定数据；91 个图表的中文标题、解释和标签已单独完成。
- 数据默认通过双路 DeepSeek 与第三次低分复核自动决定是否进入搜索，不要求用户逐条放行；未通过者进入“自动拦截”，需要时可在本地端纠错。

## 重点演示文章

- 题目：`Irradiation effects in high entropy alloys and 316H stainless steel at 300 °C`
- DOI：`10.1016/j.jnucmat.2018.08.031`
- 当前事实：225 个独立物理事实，339/339 处自动数值证据可回到 PDF 高亮定位。
- 图表：4 个原文表格、10 幅原文图片。
- 两条历史人工学习样本仍保留；重新提取产生了新的事实版本，因此当前事实层不冒充已人工确认。

## 启动入口

- 本地编辑端：双击 `/Users/USER/Zotero/打开本地编辑工作台.command`。
- 只读公网端：双击 `/Users/USER/Zotero/创建导师公网链接.command`，复制程序显示的 `https://...ngrok-free.app` 地址。

公网地址由 ngrok 免费服务临时生成，每次重新启动可能变化。分享时只发送只读地址，不发送本地编辑端地址。

## 展示边界

- 稳定表示程序、证据关系和检索链路经过验证，不表示 DeepSeek 输出已经获得物理学人工认可；正常使用不以人工逐条核验为前提。
- DeepSeek 读取 PDF 文字层生成数据和图表语义；图表截图由本地程序从 PDF 裁取。系统不读取或猜测曲线精确点。
- 35 篇全库中已有 17 篇具备结构化数值事实，另 13 篇有效 PDF 待全文抽取，5 篇需先重新关联真实 PDF。
