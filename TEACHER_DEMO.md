# 教师展示说明

本版本用于展示“实验文献数据提取、证据回溯和全库检索”的完整闭环。对外分享页面为只读检索页，与本地编辑端共用同一数据库；本地数据更新后，只读端无需单独维护。

## 推荐展示顺序

1. 在“数据条目”中输入 `温度`，展示具体意义、文章语境、题目、DOI 和作者的联合检索；说明输入停顿后会自动搜索，也可以按 `/` 快速聚焦搜索框。
2. 搜索 `钨`，展示中文元素名可匹配 `W` 和 `tungsten`。
3. 切换“仅人工核验 / 仅待审核”和“正文 / 表格 / 图片关联 / 人工补录”筛选，展示筛选条件会同步作用于检索与导出。
4. 输入 `Table 3`，展示同一原表的 12 条数据默认只展开 3 条；点击“查看原始表格”打开高分辨率原文截图。
5. 切换到“原始表格”，搜索 `纳米硬度`，打开 Table 3，展示左侧完整原表和右侧物理量、变量、材料、条件、方法与原文解释。
6. 切换到“论文图片”，搜索 `位错环密度 随剂量`，打开 Figure 8，说明系统保存原图和证据关系，但不猜测曲线精确点。
7. 返回数据条目，点击任一记录的“原文证据”，展示 PDF 页内高亮和证据片段。
8. 分别点击“CSV”和“Excel”，说明导出包含当前条件下的全部匹配记录，可直接用于 Origin、Python 或 Excel。
9. 在本地编辑端打开“校对数据”，先在文章导航器中输入 `El-Atwani` 展示作者检索，再切换“辐照模拟/计算”或“五篇测试集 v1”范围；说明筛选只缩小候选论文，不会自动切换文章或重新调用 DeepSeek。
10. 点击“开始分层校准（20 条）”，展示系统从 112 条待审核数据中选出覆盖不同来源与数据形态的代表样本；暂离或刷新后可点击“继续本轮校准”恢复同一批次，再展示六列校对、原始版本和“确认并下一条”。

## 当前展示数据

- 全库：34 篇已登记文章，1,314 条六列数据。
- 重点文章：`Irradiation effects in high entropy alloys and 316H stainless steel at 300 °C`。
- DOI：`10.1016/j.jnucmat.2018.08.031`。
- 重点文章：114 条已保存数据，114/114 可回到原文高亮定位。
- 当前人工审核进度：2 条已审核、112 条待审核。
- 可视证据：4 个原文表格、10 个原文图片对象，均来自最终出版 PDF 第 2–9 页。
- 最新自动抽取候选组合对人工基准的覆盖率为 110/114（96.5%）；该指标衡量候选覆盖，不等同于物理结论准确率。

## 启动入口

- 本地编辑端：双击 `/Users/USER/Zotero/打开本地编辑工作台.command`。
- 只读公网端：双击 `/Users/USER/Zotero/创建导师公网链接.command`，复制程序显示的 `https://...ngrok-free.app` 地址。

公网地址由 ngrok 免费服务临时生成，每次重新启动可能变化。分享时只发送只读地址，不发送本地编辑端地址。

## 展示边界

- AI 生成的记录默认保留审核状态；已发表数据不自动等同于物理结论可靠。
- 图中曲线不被自动猜测为精确数表；无法可靠定位的内容进入人工核验。
- 重点文章仍有 112 条待人工审核，展示时应称为“已提取、可回溯的候选数据”，不要称为全部完成定标的数据集。
