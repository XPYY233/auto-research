# 阶段性全项目体检 2026-07-27

## 结论

- 程序与当前已发布数据：达到稳定展示条件。
- 初始“至少 30 篇完成处理”目标：尚未完成；固定 50 篇中 17 篇数据就绪。
- 科学准确率最终验收：尚未完成；缺少独立于 DeepSeek 的人工金标准。
- 本次维护没有重新生成稳定图表、删除数据或改写校对历史。

## 已验证范围

- SQLite v11 完整性、外键、索引、稳定键、质量发布关系和全部 291 个图表文件。
- 6,501 条原始记录、5,048 处可报告来源、3,142 个独立事实、937 条定性结论与 1,453 条隔离文字值。
- 固定 50 篇 PDF 的可打开性、内容有效性和 DOI/题目身份：50/50 通过。
- 目标论文 231 个事实、403/403 处自动证据高亮；`温度`、`硬度`、`钨`、作者检索和 CSV/Excel 通过。
- 本地编辑端和只读端真实 HTTP 运行；只读搜索与原文证据通过，写入接口返回 403，数据库哈希不变。
- 187 项 Python 测试、Python 编译、前端语法、macOS 启动脚本、依赖、Git 对象、空白和凭据检查。

## 本次发现并修复

1. 批量质量任务被中断时，批次 JSON 可能保留“运行中”。现在捕获中断并写入明确状态。
2. 进程被强制关闭时，数据库运行记录可能无法走异常收口。新增安全维护命令，只修复运行元数据。
3. 成功记录可能残留旧错误文字。成功路径和维护命令现在都会清除该错误。
4. 目标审计只验证单篇样例，却把结果写成全项目完成。现在同时检查固定语料，真实结论为未完成。
5. 长期规范中的文章选择器仍写成 35 篇。现已与固定 50 篇配置一致。
6. 稳定版、教师演示和网页版本身份过期。现已统一到 2026-07-27 健康稳定版。

## 未达到初始规划的部分

1. 固定语料仅 17/50 篇数据就绪，低于初始 30 篇完成目标；33 篇仍待全文抽取。
2. 图表覆盖为 30/50 篇，尚未覆盖全部真实 PDF。
3. 双路 DeepSeek 一致性能够降低错误，但不能衡量共同遗漏；仍需至少 5–10 篇逐条人工金标准来估算准确率和召回率。
4. 组内长期联网仍依赖本机在线和临时 ngrok 地址；尚未形成常驻托管、权限、备份和并发提交服务。
5. GitHub 尚未配置网络远端；当前 `origin` 指向本地历史 bundle，不能直接推送。

## 下一阶段推荐顺序

1. 先补齐 13 篇，使数据就绪达到 30 篇初始验收线；每 3–5 篇运行一次固定语料审计。
2. 从不同实验/计算类型选 5–10 篇建立人工金标准，分别统计数值精确率、条件绑定准确率、证据定位率和漏提率。
3. 再决定 GitHub 形态：私有完整开发仓库，或公开的代码加脱敏演示数据仓库。
4. GitHub 稳定后再选择低成本常驻只读托管，避免把开发数据库和论文内容直接暴露到公网。
