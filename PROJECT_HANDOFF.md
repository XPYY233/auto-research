# Auto Research 当前状态

更新：2026-09-20。此文件是当前进度唯一入口；旧检查点移至 docs/history。

## 授权与流程

实施已批准的工程拯救计划：私有 GitHub、Apple Silicon Mac、保留全部功能。
所有后续工作采用 GitHub Issue → 分支 → PR → 自动检查 → 合并 → 唯一候选 → 实机验收 → Release。
本地用于开发、数据保护和验证；问题、审查、完成证据与发布身份在 GitHub 可追溯。
Windows 冻结；不新增功能、不购买订阅、不默认调用收费模型。保留至少 30% 账户额度（用户最新授权）。

## 已取得的证据

- 私有 GitHub `XPYY233/auto-research` 已保存 593 次净化历史、原提交映射及问题/PR/自动检查记录。历史未压成初始化提交；各项完成证据与状态以关联 Issue/PR 为准。
- 原 Zotero 科研目录保留；独立开发仓库位于 Developer/auto-research，生产数据库、论文及私人状态不进 Git。
- 9月10日保护归档的隔离恢复通过：2735 文件条目、63 PDF、数据库计数及 quick_check、Git 对象、四类加密历史认证。
- 9月19日当前数据另有一致性快照及恢复报告：992 文件条目、64 PDF、309 图像哈希，数据库表计数及四类加密历史认证通过。详细私人材料仅留本机保护目录。
- 统一工程入口、合成离线样本、共享/Mac 串行 pytest、零静态生产循环依赖、职责检查、敏感文件检查与依赖审计已建立。
- 私有仓库服务端分支保护返回 403（需 Pro）；现在是受控 PR 约定，不是服务端强制保护。不购买订阅。
- 空 Application Support 工作区能够由冻结 App 初始化并通过真实 HTTP 冒烟；配置取 App 资源，不需要开发数据。

- **旧库迁移已完成本机验收（#2、#10、#14）**：复制、逐项迁移、校验、原子发布与中断恢复已实现。查明 27 条哈希冲突是旧采样签名误写到完整哈希字段；显式、双重证据校正保留原/新身份。真实复制演练通过 21 表、535 原文件、550 引用，6 个既有历史输出缺失如实登记。已用审查通过的切换代码激活 Application Support 工作区；重启后核对有效目录并打开新文章 PDF，21 表、来源文件与 388 份私人/官方文件再次核验通过，4 份已结束加密断点可认证。原目录保留；真实回退场景仍属于 #4 交付验收。见 [迁移说明](docs/WORKSPACE_MIGRATION.md)。

## 当前安装候选

安装版为 1.2.0/build56，唯一候选 `4aa606e35c384154ac580dd2835974a3`，源码 `e7234535aef22bed73d56723ee7f58a03845107f`。
全新哈希锁构建、空工作区冻结 HTTP 冒烟、签名、逐文件事务安装通过，build55 回退副本已保留。安装本身未改变数据库任何表记录。
目标文章经安装版“AI 补全图表”完成：5 张原图全部自动发布（4 双路通过、1 第三路通过），检索显示 63 条证据，Figure 1 实际渲染；未重新抽取正文。全库 data_items/data_versions/table_structure_versions 与补全前一致，5 张图的 ID、路径、哈希及内容一致。
实机发现完成后论文详情短暂保留旧状态，正在修复异步目录刷新顺序并准备 build57；build56 不标稳定。详见 #33 和 PR37。

## 当前阻断与下一步

用户再次明确：产品彻底移除人工审核功能，包括入口、按钮、队列与发布前置条件。提取与发布全部由 AI 自动核验完成；历史数据库的 manual_review 仅为兼容隔离状态，不代表待用户处理。此要求覆盖旧的人工审核交互约定。

优先级：先按拯救计划验收核心业务、数据恢复与独立交付；再推进职责拆分和界面优化。#27 定位/AI 布局、#26 路由拆分排在核心阻断之后。

用户报告的新文章缺图问题已在 build56 自动补全后显示，仍须完成刷新修复和重启验收才关闭 #33。原始阻断：后台存在 5 张待审图像和哈希核验不等于用户流程修复；需核验提取发现→AI 自动核验→自动发布→目录/详情显示，AI 核验通过后自动发布，未通过者保留失败原因；禁止补造表格。

#28 核心状态修复（源码，未安装）：目录按已保存的提取结果与四类待审候选显示状态；旧六列人工核验统计保留，不再冒充提取状态。真实 PDF 暂存/发布 → 审核 → 索引失败与重试 → 目录 → 重新打开数据库的联合回归通过；定性/表格单独存在的论文不再显示未扫描。模型输出采用离线合成输入，此结果不代表科学提取准确率或安装版验收。

#30 更新入口修复（源码）：备份跟随 App 的实际工作区，支持默认 Application Support；选择无效时停止，保留既有备份，验证 SQLite 后登记独立副本。已用隔离库验证 WAL 已提交数据、重复备份和发布失败清理。候选生成不会替换已安装 App。

#32 核心恢复修复（源码，待候选）：复现并修复数据库已提交、完成收据失败后，迟到取消使重启恢复入口丢失的问题。发布前持久记录 finalizing，重试只完成幂等本机发布和收据，不重新调用模型；边界之前仍可取消。74 项目标回归通过。历史已取消的任务不自动重写，新产生的未完成 finalizing 任务须使用包含此修复的版本恢复。

#33 第一部分（源码）：已接回两路 AI 图表语义及第三路核验，通过后原子发布。1423 共享 / 374 Mac 检查通过；没有调用收费模型。PR35/36 已合并：人工审核入口、队列、批准/拒绝和表格手工确认已退出生产界面；旧写入接口返回 410。

#33 第三部分（源码，待候选）：新增只补全图表的受控 AI 任务，复用原文快照与断点预算，正文/数值/既有表格版本保持不变；重复完成不重新收费。历史隔离记录保留，目录只统计同一图像的最新核验状态。准备 build56，实际文章和安装版验证尚未完成，问题仍开放。

1. **#3 功能与维护**：PDF 导入已接入共享 facade，真实 HTTP 回归通过；继续关键流程联合验收并按职责拆分 Fusion 与 Mac 业务入口、退出旧 Web 继承。零循环依赖只是一项门槛，不能代表重构已完成。
2. **#4 交付**：哈希锁和唯一候选已实现；第二台干净 Mac、全部使用流程、回退/卸载数据保留及分发许可仍待完成。当前仅实际验证 macOS 26.3.1 Apple Silicon，ad-hoc 签名，未公证。
3. **#5 科学质量**：20 篇人工金标准加至少 1 篇盲测尚未完成；固定适用维度 F1 ≥ 0.90，禁止无来源数值或错配原图。工程测试不代替人工科学核验。

三个总目标全部验收后才结束拯救计划。没有新的稳定 Release，不覆盖已有候选二进制。
