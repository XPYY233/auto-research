# 历史迁移

原历史源 HEAD：`d85878ab86d3c507732482a17999b92d7461151f`。
净化历史 HEAD：`99199f30360362585e505531c8c77e350c430def`。
保留 593 次提交，不合并成初始化提交；见 [一对一提交映射](commit-map.txt)。
数据专属提交允许成为空提交，其原始数据差异只在受保护的本机归档中保留。

净化移除全部 `data/`、`db/` 历史，并匿名化用户 home 前缀。作者、时间、说明和拓扑尽可能保留。
Git 身份改变是必要结果。**旧 App、旧标签和旧哈希仍指向原历史，不得宣称由迁移后哈希构建。**
旧标签暂不作为 GitHub Release 发布；原始引用、游离对象和完整 .git 在本机保护归档中。

本机保护归档另含原未提交改动、Application Support 私人/加密状态、63 个引用 PDF 及清单。
归档核验过 2735 个文件条目、SQLite quick_check；完整恢复演练仍须单独记录。
本机密文恢复需要对应密钥，跨机器须重新配置 API 凭据。归档及密钥不能上传。

上传前检查了 3896 个历史 blob（132719414 字节，无大文件跳过）：
未发现数据库/PDF、原用户 home 前缀或非测试凭据模式命中。
保留的 5 个二进制对象为 App 图标、空搜索页历史截图和工作动画；截图没有论文结果或私人路径。
凭据模式仅命中人工构造的测试样例；这不是全面安全或许可审计的替代。
