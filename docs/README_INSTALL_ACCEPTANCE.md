# README 源码安装验收

日期：2026-09-20；关联公开 Issue #8。

本轮只修改安装说明、有限本机使用授权和状态文档。实际构建使用从 GitHub 完整取回的干净源码 `d3cc3a737cea86725a0ed77c3b182da8111a03b8`；验证候选身份 `13138ed08c12482d883e1c2e44cf6123`。未替换维护者日常安装的 build57，未上传二进制或私人数据。

## 已验证

- Apple Silicon / macOS 26.3.1 / python.org Python 3.14.3；按 README 构建命令建立全新环境，固定工具和运行依赖哈希安装成功。
- 构建、签名、独立空工作区冒烟和制品清单生成成功；没有安装开发测试依赖或依赖 Node 来构建 App。
- 将实际 App 复制至独立临时 Applications 目录；复制前后文件清单、权限和符号链接一致，签名验证通过。
- 从非源码工作目录运行冻结可执行文件，子进程 PATH 只保留系统工具目录，使用隔离用户目录，未传递源码 Python 路径或 AI 凭据。
- 不提供 `--project-root`，首次 `--smoke-test` 自动创建默认 Application Support 空工作区；27 项 HTTP/资源检查、6 项冻结产品契约检查通过。
- 再次启动、仅移除隔离 App、重新复制 App 后启动均通过；空工作区 SQLite 文件保留且 SHA-256 一致。
- README shell 代码块语法和本地文档链接检查通过。第一次测试脚本因 macOS `/var` 与 `/private/var` 路径别名比较失败，改为解析真实路径后通过；未修改产品来绕过检查。

## 证据范围

这是同一台 Mac 上的干净源码构建、App 迁移及空工作区服务验收；未模拟 Finder 点击，也不等于第二台干净 Mac 或不同 macOS 版本验收。数据保留演练使用新建空库，不代表旧科研库跨版本迁移已经验收。未调用收费模型，未验证真实新论文提取质量。原有稳定交付与科学质量缺口继续跟踪公开 Issue #2、#3、#4。

安装者可依 README 生成和安装候选 App；当前没有公开稳定 DMG。保留版权与第三方许可说明，不将源码安装能力描述为正式发行完成。
