# build57 本机验收记录

日期：2026-09-20。关联 #3、#4、#33，PR34–38。

- 版本：1.2.0 / build57；候选 `a300d4af2fa94a0393b5241a95b99cdb`。
- 源码：`c53e3a53cb4cde97a09f05813a9205e910cb3ddc`。
- 环境：Apple Silicon / macOS 26.3.1 / Python 3.14.3。ad-hoc 签名，未公证。
- DMG SHA-256：`3ff62a471fbe034be28f8075756977bd2945b56d2de4c68a94d5bf0a4dde4d49`。由同一验收 App 封装，未重建。

## 已通过

统一离线检查 1422 共享测试、374 Mac 测试（1182 / 245 子测试），GitHub PR 检查通过。全新依赖哈希锁构建、空工作区冻结 HTTP 冒烟、签名、暂存和安装逐文件身份检查通过。build55 / build56 可回退。

人工审核入口、队列、批准/拒绝、表格手工确认已退出生产产品；旧写入接口返回 410。AI 自动核验通过才发布，不通过的结果自动隔离，历史审计记录保留。

用户报告的目标文章通过 build56 的“AI 补全图表”完成受控核验：4 张图双路通过、1 张第三路通过。build57 重启后目录和详情均为 AI 自动核验完成，63 条证据（原有 58 条与 5 张原图），Figure 1–5 均列出。Figure 1 在 build56、Figure 5 在 build57 实际渲染；全 5 图内容哈希验证通过。主 PDF 9 页，编号图标题分别在 2/4/5/6/7 页，未发现编号表格标题；不把未发现表格宣称为完整科学覆盖。

补全前后全库 data_items（7048）及 data_versions（7053）逐记录一致，既有 table_structure_versions 不变；原 5 张图的 ID、路径、哈希及文件内容一致。build57 升级和重启前后 28 张表全部逐记录一致，SQLite quick_check 通过。私人备份与截图不上传 GitHub。

完成后的目录刷新现等待返回，再以最新状态打开详情；行为测试覆盖迟到响应时用户已切换论文。取消与本机发布恢复回归证明已成功模型调用不重复收费。

## 未完成边界

这是本机可运行候选，未称稳定 Release。独立第二台 Mac、完整升级/回退/卸载演练、所有保留功能联合验收、分发许可以及 20+1 科学金标准仍待完成。查看原文定位和 AI 位置优化继续跟踪 #27，职责拆分跟踪 #26；整体拯救计划保持进行中。
