## 2026-09-19 审核队列纯契约拆分（#20）

- 从 Fusion 主控制器移出公开字段、嵌套边界、评分单位、来源图像 URL 与纠正字段校验；纯模块不持有 DOM、请求或存储。
- 导航、标签、确认与保存编排继续只有 Fusion 一处负责；缺失契约模块明确报错。
- 生产 HTML、Mac 静态路由、冻结资源与发布哈希一并登记；使用真实服务 DTO 和公开模块接口验证，不修改源码字符串执行新测试。
- 本次只改变源码，不重建或替换已安装候选。

## 2026-09-19 空工作区启动（#2，尚未安装）

- 默认 Application Support 空库；配置来自 App 只读资源，原 saved root 保持兼容。
- 暂存初始化、SQLite 校验与原子发布；不可用的显式/保存目录不自动回退。
- 自包含验证由候选创建自己的数据库，不复制开发数据或建立符号链接。
- 空库/无凭据真实 HTTP 源码冒烟通过，合成授权只验证类图，不执行模型或放开业务门。
- 旧库引用迁移、冻结 App、安装 UI 与第二台 Mac 仍须后续验收。

## 2026-09-19 工程重建基线（#1）

- 私有 GitHub 已保存 593 次净化历史；旧提交映射和原历史归档说明纳入文档。
- 回收未提交审核/发布刷新、迟到响应、布局焦点、幂等恢复和真实包往返修复。
- 四组显式循环已拆分，补包初始化边界；共享源码静态循环为零，运行文件职责检测完整。
- 统一串行离线检查：共享 1382、Mac 339 通过，无跳过；81 项私人历史语料显式不选中。
- 全新虚拟环境从哈希锁安装；依赖审计发现 cryptography/pytest 漏洞并更新，复审无已知漏洞。
- GitHub 分支保护因私有仓库套餐限制返回 403；不购买 Pro，不改公开，采用受控 PR 合并。
- 尚未构建/安装新 App；空工作区、迁移恢复、第二台 Mac 与人工科学验收仍未完成。

# Auto Research Evidence 项目日志

## 2026-09-05 10:57：真实提取到审核的断点复现

- 隔离合成一页PDF实际2调用/约35秒返回5候选、4发布、1人工复核；真实搜索可见4条，读隔离库核对300°C/1dpa/3.00GPa/4.00GPa。不是科学盲测完成；PDF含文字排列表1但没有检测到视觉候选，仍需核验发现能力。
- 审核入口缓存0，刷新后review_queue_invalid。精确根因为后端0–100评分而Fusion只接受0–1，旧前端测试使用自造小数DTO掩盖缺陷。新增真实service→JS回归先失败；原前端任务按明确文件范围修单位和完成后缓存，不恢复旧UI、不放宽科学质量门。
- 正常退出隔离进程后恢复canonical根，实机Table4原图/Figure10曲线仍可见；生产DB SHA不变。无新build/DMG，最新64%额度，保留50%。

## 2026-09-05：恢复50%额度线后的集成与实机补验

- 原审计重新对照：不是只看当前bug；Phase 7循环、大控制器与兼容消费者清理仍未完成。旧审计顶部build50改为历史，实时状态统一指向build54台账。
- 新增真实SQLite发布后检查点失败/服务重建/幂等收尾测试，记录保持单条且索引刷新，不新增模型调用；32项提取恢复目标通过。
- 两类用户包真实文件、SHA sidecar、独立接收、AES-GCM任务历史、完整Mac服务图重启、重新选文件幂等导入通过，38项包/API/安全/合并目标通过。未把只构造包模块导致私人源缺失的测试装配问题修成生产重复算法。
- 前端既有任务2项布局契约通过但其Computer Use权限阻断；root实际拖拽左栏和双组边缘并恢复成功，Table 4原图与Table 3核验网格可见。点击separator后方向键仍未改变焦点/比例，已下发最小焦点行为回归。没有新增build或修改生产数据库，50%最新保留线已写入长期规范/Skill。

## 2026-09-05 00:27：隔离提取运行中取消与重启

- 新建独立空库与明确合成的一页PDF；原生选择器公开open动作完成免费导入，未自动提取。授权弹窗期间Computer Use报告外部变化，随后任务已有2次调用，故不得记为“取消授权零调用”。
- 实机点击运行中停止，进入已取消并保留回执；同隔离根重启仍显示已取消/重新开始，未再启动收费任务。仅此取消路径通过，中断续跑及零调用最终收尾未通过。
- 已退出隔离根并明确恢复正式根。自动化过早读取可隐式启动第二实例，正常关闭提示实例后锁保护有效，不删锁。生产库哈希未变，额度71%，保留70%下限，不构建、不升版。

## 2026-09-05：聚合候选安装、真实回答与数据集落盘

- 同一build54/candidate集中安装7ed286b，共享1372通过/80跳过、Mac338通过及冻结HTTP/签名/事务安装通过。未生成新编号、DMG、UserKit或stable tag。
- 完整中文300°C问题实机1调用约5秒采用真实模型回答；16引用重启保留，R16原图与5×4核验表格可开。数据集计划从约两分钟降至约半分钟，4689条实际ZIP保存，JSONL/Parquet一致，SHA/CRC/论文split通过，重启完成回执可见。未审核2417条不会因本地验收导出变为已审核。
- 原生Save后AX超时不等于App死锁；正常退出重启及落盘收据已验证，即时反馈仍需复核。拖动工具继续noWindowsAvailable，不虚报拖拽验收通过。剩余提取恢复与完整布局/用户包门保持未通过。
- 核实e870094回退后清除被替代回退及本轮868MB构建输出，回退仅三用途；生产DB哈希未变。Skill的operations已真正去掉危险默认库/旧浏览器范例，当前状态统一指向安装验收台账。额度最后72%，前端停手，继续70%硬保留。

## 2026-09-04 23:58：真实数值误杀与数据集预检性能收口

- e870094完整中文图书管理员问题仍在1调用/7秒被拒，warning结束和9引用历史重开已通过。受控单次源码诊断明确是`2.97 ± 0.04 GPa`只匹配末尾误差与单位；修为完整测量值同源核验，修改中心值/误差/单位/引用与跨源比较仍拒绝。70项Harness目标通过，修复后1调用6.31秒源码回答被采用，仍须安装复验。
- 安装App数据集计划终于返回4689条/2417未审核，但约两分钟。现有前端任务只改包控制器与行为测试，补单飞、180秒等待上界和本机用时反馈，6目标通过。root对隔离4356条快照剖析，定性结论两两长文本比较占主要时间；严格上界剪枝后21.876→7.311秒，完整指纹逐字节一致。5项聚类/导出回归通过，不改变科学合并阈值。
- Skill Creator复核时发现operations引用仍保留默认生产库CLI与旧浏览器启动示例，现真正删除并替换为显式隔离服务规则；旧日志中“已删除”只覆盖入口、不构成引用文件核验。额度73%，继续保留70%；只聚合同一build54，不生成新编号或发布套件。

## 2026-09-04 20:45：集中候选已安装，锁屏暂停实机验收

- `e870094`集中修复已覆盖同一1.2.0/build54候选，完整共享1367通过/80跳过、Mac338通过，发布资源/Skill/冻结HTTP/深层签名/事务安装通过。当前App可执行SHA为`d0832795...92e59c`，精确身份与下一步在BUILD54_INSTALLED_ACCEPTANCE。
- 已核验当前回退为前一20cfea8，保留0.5/build24/前候选三用途，清除被覆盖的19:10候选回退及重复dist App。未生成新build、DMG、UserKit、bundle或稳定标签；未动用户生产库及paper_056。
- 安装后首轮Computer Use返回Mac锁屏，不能继续真实点击，也不能把自动测试写成稳定。当前正式根启动参数明确；等待用户解锁，主额度76%，硬保留70%。

## 2026-09-04：隔离CSV真实主链与规范收口

- 真实App原生CSV选择（键盘Return）→123行三页预览→手工映射X/Y/不确定度→一次确认→私人搜索→曲线→重启后重新搜索和打开通过。122有效点、1缺失、0非法，末行1.22/3.322/0.050原值保留；没有AI调用，没有写入用户私人库或生产科研库。之前AX Open超时不是已证明的App死锁。
- 前端协作另补分页ARIA行索引、加载/空/错误终态行数及已加载状态，2项目标通过；不称其为原生超时根因。共享完整1367通过/80跳过、Mac338通过（5项既有SWIG警告），均串行执行。Skill Creator结构校验通过。仍只保留一个build54候选，不宣称稳定。
- 校正验收血缘：完整中文图书管理员问题在安装App中仍曾拒绝；一过一拒的是源码重放，不能合并成安装后通过。保留安全拒绝，格式约束修复待安装验证。

## 2026-09-04 19:42：单候选实机复验、历史与终态收口

- 同一build54聚合安装到20cfea8；1364共享通过/80跳过、338 Mac通过，签名/冻结冒烟/事务安装通过。没有新build、DMG或UserKit。实际身份和回退统一在BUILD54_INSTALLED_ACCEPTANCE。
- Table 3原图、同源官方5x4核验行列以及DeepSeek逐材料数值/不确定度问答已真实显示。完整Librarian问题有通过也有跨bundle裸数字拒绝；两个受控源码诊断各1调用，只有公开数值类别诊断，没有记录模型正文。仍不可把一次成功当作科学质量或整体稳定。
- 前端已有任务完成warning结束计时、16条历史引用严格幂等恢复、v2披露；root补后端v1失效回归及数值表达格式约束，不放松任何科学门。48项聚合目标通过；协作13项与其重叠，不相加。未安装这批源码。
- 隔离CSV已进入暂存，随后WebView自动读取超时。主线程采样未证实死锁，未确认入库；已恢复普通App和正式根。不能以该自动化阻断编造产品根因或验收通过。
- 清除四个已识别诊断副本约244MB及临时脚本/采样，保护数据库SHA未变。规范按Skill Creator检查并删除默认生产库验收命令、旧官方唯一AI范围和每次中断都打bundle的冲突；未知数据不清理。主额度最新80%，硬保留70%。

## 2026-09-04：聚合安装后的真实失败与定点修复

- 同一build54聚合安装22807aa，严格签名/冻结HTTP/事务替换通过；实际Table 4原图显示。当前安装的功能、测试与SHA只在BUILD54_INSTALLED_ACCEPTANCE维护，不把源码提交当安装身份。
- 原生CSV自动验收仍未完成：隔离HOME/工作区中选择文件后Open无法自动激活且Computer Use超时；没有预览或确认入库。已退出隔离进程，恢复正常App与正式根。不能据此把原生导入标为通过，也不能未经区分就归因软件故障。
- 连接验证真实成功，四scope有效；复合300°C问题却先harness_invalid后business_action_invalid，未出现provider活动。不可变官方库+隔离工作区回放定位16个seed中末2项执行顺序错配：验证按source分组重排，引用表虽绑定seed但job.documents未同步；随后零调用fallback又被最小调用门拒绝。已保留seed顺序、原始稀疏R引用，并让模型前失败保持原始原因而不是伪装model fallback。另复现并修复token_urlsafe合法首字符-/_被Harness身份正则误拒；该独立bug尚不能无trace认定为首次实机错误的唯一原因。
- AI组合目标111通过；补齐模型前失败后Harness四文件76通过，均既有SWIG警告。混合源、稀疏引用和零模型拒绝均有执行断言；不重试收费，不修改model budget。源码尚未装入，未再次build。
- 安装回退路径改由Git共享目录定位主工作区，避免release-worktree父目录生成第二套备份根；临时main/worktree执行真实shell路径逻辑，14项目标通过。已核验移动这次回退到权威目录，移除被覆盖候选回退与重复dist App；0.5/build24/上个9f77aff候选三用途保留。
- 规范与Skill收敛当前权威引用，取消重复瞬时版本计数，明确来源根/nonce与引用执行验证、主账号70%保留线；现有前端任务只读诊断后停手，无新agent、无Windows。生产库SHA未变，资料包未改。

## 2026-09-04：聚合安装前恢复门

- 最终修复后共享全门1355通过、80按设计跳过（20.07s）；Mac337通过（27.20s）；生产JS语法、发布契约哈希及diff通过。以下初次计数只用于定位审查顺序，不累加为验证数量。
- 初次串行冻结门共享1352通过、80明确跳过；Mac337通过，均5项现有SWIG警告。前端只读审查发现私人表格primary重新激活依赖搜索结果，补充授权后只改Fusion与同一series运行时测试。
- 私人表格主栏现在直接恢复当前标签的已校验page；重启后异步page到达必须仍为同一活动主栏且owner页面相符，不抢其他标签。新增3项行为测试覆盖空搜索列表、已有曲线/滚动状态、重启加载/控件与迟到响应。79项目标通过，随后重跑最终共享全门；不跳过失败、不增加收费调用或版本号。
- 本次使用现有唯一build54-current工作树和本机已满足的锁定打包环境；生产SQLite字节SHA在构建前为0389a5aa0faf4967696e3c3a5a574eaaf0bec08a34f48c69b98f50c705aa80b0。隔离数值验收123行合成CSV与独立home已准备，不写入用户私人库，也不把临时根存为日常偏好。安装与真实UI结果须另记，以上不是稳定发布结论。

## 2026-09-04：完整私人系列曲线接线与额度边界（源码）

- 现有前端任务负责被动SVG投影及Fusion入口，root完成Mac静态保护、冻结资源、发布哈希和真实数值集成；双方不并行测试、不另起子agent。序列按确认后的原行顺序，不拟合、不插值，保留缺失断点与合法误差棒；原始单元格文本仍为数据权威。
- 实际123行合成CSV经真实确认/私人搜索/分页/完整系列接口进入JS验证，122个有效点、1个缺失，图线分成49与73点两段。关闭再打开同一确定性标签ID由会话递增请求号保护。首次73项验收暴露readOnlyJSON漏加series路由，root补齐后73项全部通过；40项Mac/发布/所有权、14项核心安全分别串行通过，存在重叠，不相加。5项已有SWIG弃用警告仍记录。
- 18:06在当前Mac App再次真实点击Table 4和Figure 10，原表截图及曲线图均显示；回到Table 4。原图显示不等于已审核结构化行列。新私人曲线尚未安装，浏览器隔离预览被安全策略阻断后未绕过；实际绘图/返回/重启门保留。
- 用户新增账户额度保留线70%；工具读取已用12%、剩余88%，已通知前端停手待命。后续先复查账户共享额度，留出收尾空间，禁止到阈值才开始长构建/全测。不得以额度将未验收候选称为稳定。无模型/App构建、Windows或生产数据操作。

## 2026-09-04：私人实验数值链与分页上下文（源码）

- root使用隔离合成123行CSV串起真实导入预览、确认、幂等、私人联合搜索、重建服务、分页和实际Fusion渲染；预览后改动所选原文件也不改变已捕获快照，缺失值不补零。未读取生产库或调用模型。
- 现有前端任务在明确文件范围修复主/次表格条件/序列显示和当前标签检查器绑定；root实际DTO→JS回查发现空尾页显示151–123行，协作修复后完整目标重跑通过。47项共享/Mac（5项既有SWIG警告）、66项前端串行通过；JS和共享Web哈希已同步。
- 共享完整测量序列投影及Mac只读接口复用同一已确认文件快照校验；保留原行顺序、原始文字及缺失间断，不做拟合或图片数字化；超过5000行明确拒绝绘图而不截断原表。曲线前端及真实App导入/搜索/绘图/重启仍未完成，契约见PERSONAL_SERIES_PLOT_CONTRACT.md。
- 本次表图实机复验被Mac锁屏阻断；只确认保存根和运行cwd正确，未把旧截图或源码测试冒充当前显示结果。不升build、不新增发布制品，保留此前Table 4原图恢复与未核验行列的区别。

## 2026-09-04：导出原任务恢复与回执刷新（源码）

- root与现有前端Codex任务按函数边界协作：root负责job等待/继续读取/数据集未知终态阻断；前端负责receipt并发刷新与revision单调性，未stage/commit或并行测试。
- 600次轮询未终态、网络中断或身份不符时，保留最后服务端状态，显式提供“继续查看原任务”，不新增导出POST；重复恢复单飞。终态成功复用同一严格回执投影，终态失败展示原安全错误，不伪造成功。
- force读取合并但保证之后的新GET；失败允许重入重试；迟到GET/POST不得覆盖更高revision或恢复已删回执。28项dataset/package/receipt/history目标串行通过（1.32s），JS、发布哈希、diff通过。首次目标测试暴露无意义的独立GET顺序约束，改为导入POST在先且后续三GET各一次，未降低实际行为断言。
- 17:03实机重新核对正式数据根与当前App，Table 4原表数值与Figure 10曲线图均可见；Table 4尚无已核验行列。本次没有生成App/DMG/新build，没有模型调用或生产DB操作；导出恢复/AI源码仍未安装，真实原生保存与重启验收未关闭。

## 2026-09-04：数据集真实文件链与范围竞态修复（源码）

- 使用实际PyArrow而非伪Parquet，串起123篇合成论文/492条四类记录、Mac opaque保存resolver、共享job/publisher和加密回执。逐条核对JSONL/Parquet、manifest SHA/size、三种论文划分、重建服务后的同一回执；未声称已点击真实原生保存窗口或完成App重启验收。
- 先运行新增测试，真实暴露中文文件名被core ASCII白名单拒绝，以及原生目标存在错误被压为通用失败。修复为有界安全Unicode文件名和safe_message边界，保留危险字符拒绝；ZIP最终发布改为原子不覆盖，另测生成期间用户文件出现时原内容不变、无成功回执、无残留暂存。
- 现有前端Codex任务只读发现私人范围迟到P0和重复导出P1，随后按精确文件边界修复：范围变化使旧请求/错误失效，picker回来重核plan/token/scope/风险；全过程单飞，取消保留计划。root串行48项领域/Mac与23项前端测试通过（既有5项SWIG警告），不把重复运行数累加。
- 轮询耗尽继续跟踪和force回执刷新丢失仍是导出队列下一步；本批无App/DMG/新build、模型、生产DB或Windows工作。

## 2026-09-04：AI活动反馈与跨标签草稿（源码协作）

- 现有前端Codex任务按两个小范围交付计时控制器及证据提交处修复，未stage/commit/测试/构建；root统一审查、验证、同步资源哈希。没有启动替代子agent或Windows工作。
- 计时累计真实活动区间，授权等待暂停，成功/失败/取消冻结，无DOM也停止；下一任务重新计时。里程碑不再显示伪精确完成百分比。root补齐实验列建议/证据问答/提取的对应活动文案，保留真实收据作为发布权威。
- 原标签/host/input在授权前捕获；授权期间切换到同文草稿的B，不再清除B，也不改变A的身份、历史和执行授权。80项进度、UI、证据历史、详情、个人导入及收据目标测试通过（13项0.77秒、67项1.90秒），包含真实控制器驱动的延迟授权出站断言；JS语法、哈希和diff检查通过。
- 均为源码修复，尚未安装，不改build54号、不生成App/DMG或调用模型。真实计时、草稿、科学回答/数据集及完整流程仍待聚合验收。

## 2026-09-04：有界联合召回与四类证据覆盖（源码）

- 发现宽泛材料查询会提前占满既有64候选池；优先查询材料、辐照条件和物理量的组合，再做属性/条件/材料补召回，仍最多12条本地查询。模型仍只接收16条和1次调用；候选保留各类合格代表，不让稀少表图被大量条目完全淹没。
- 不改硬条件与直接/相关分类，不为补齐四类而引入缺失两项以上条件的记录。既有不可变发布快照只读重放，58候选选出15直接、1相关，四类均有；相关表格明确缺温度。旧查询同快照为7直接、5相关，未重现早先临时数据根现场的0直接，因此实际问题仍需在聚合安装版完整复验。
- 84项目标检查串行通过；无生产SQLite读写、模型调用、Windows、构建或新制品。前端现有任务获明确文件边界，接着处理终态耗时与授权切标签清草稿，root继续唯一Git写入并串行验证。

## 2026-09-04：已核验表格进入证据AI上下文（源码）

- 根因：搜索摘要不包含独立审核表格服务的行列；原AI虽收到图注和同论文证据，却未收到界面已显示的核验数值。
- 新共享窄上下文适配复用既有官方/工作区详情权威，冻结当前表格行列、版本和稳定实体身份。新增独立内容快照，消费授权时重新读版本/行列，变化或撤回则旧动作失效；无可用结构只发缺失状态，超500单元格/24,000字符明确失败，不截断。Mac实际与冻结冒烟组合均接入，UI与AI使用同一工作区resolver。
- 精确目标107项通过（10.33秒；5项现有PyMuPDF/SWIG弃用警告），Python编译和diff检查通过。旧unittest入口仅收集84项，改用pytest补齐函数式测试；未把两套数量相加。未运行App构建、模型、Windows或生产数据库测试。真实模型解释是否完整正确留待单候选统一实机复验。
- 前端现有Codex任务按独立只读边界检查身份链，正常出站请求未发现串实体P0；其指出的授权切标签清草稿P1及精确请求测试缺口已登记，不在本批扩散修改前端。

## 2026-09-04：数据集计划契约修复（源码，未构建）

- 确认前端漏收`paper.title/doi/year`且误拒绝超过100项权利提示；修复为完整有界列表，保留长公开身份、严格计数/风险格式验证，每页50项、可前后翻页。新计划使旧翻页回调失效，翻页不改用户确认，长身份不撑开面板。
- 既有100,000记录限制配套论文数量限制；超限在计划阶段报错，不静默遗漏论文。未改变训练载荷/权利确认/私人选择边界。
- 84项builder/service/Fusion目标测试串行通过（1.87秒）；新增真实builder和service生成123篇/246项风险的跨边界测试，避免只用微型手写成功DTO验收。JS语法、资源哈希和diff检查通过。源码修复未装入App，实际保存文件、Parquet/JSONL一致性及回执验收仍待完成；无新build、模型调用或数据库改动。

## 2026-09-04：纠正临时数据根遗留，恢复工作区表图（同一build54）

- 复现工作区Table 4仅有文字、Figure 10原图失败。实际App cwd和持久偏好仍指向9月1日提取临时根，并非正式资料根；此前未检查这一项，是验收遗漏。
- 用现有不可变发布快照定位本论文4表10图并核对正式目录文件SHA，14项均在；未直接读取或修改生产SQLite。正常退出App后，只修正`project-root.txt`并重启原build54；真实界面确认Table 4原始表格和Figure 10曲线显示。
- 临时目录未删除、未合并，模型未调用、资料包未改、没有构建/DMG/新build。源码补充临时偏好启动阻断、显式验收根兼容和详情失败终态；项目规范/Skill新增有效数据根与退出验收恢复检查。其余验收问题仍保持打开。
- 73项桌面启动/Fusion详情与契约目标串行通过（1.58秒）；JavaScript语法、发布资源哈希与diff检查通过。新增防护仍待聚合候选构建，不冒充已装入App。

> 2026-08-01 之前关于浏览器工作台、导师只读页、固定端口和 ngrok 的条目只保留为历史决策记录，不是当前启动或交付说明。当前唯一产品入口是桌面 App，localhost 仅为 App 内部实现和维护测试边界。
>
> 过去逐缺陷顺延build的条目只记录事故经过，不是现行流程。当前规则是源码小改不升build、不生成制品；完整安装后用户验收表通过才分配一个新build。

## 2026-09-04：同一build54真实验收与失败清单（未发布）

- 现场安装清单为`1.2.0/build54`、`9f77aff`、`candidate`；继续原候选，不生成新build、DMG或UserKit。完整分项证据与未完成门写入`docs/BUILD54_INSTALLED_ACCEPTANCE.md`。
- 9月2日真实实验AI返回可编辑列解释和100%终态，未确认导入；图书管理员与证据AI虽已返回，但分别出现直接证据覆盖不足、未消费可见核验表格数值，不能继续沿用“四项AI已通过”的概括。
- 9月4日官方资料库读取正常结束并显示完整分类/PDF计数；设置页显示保存密钥、待验证连接、Harness与四业务状态。显式侧栏收起/恢复保持中央页面；鼠标拖拽工具返回`noWindowsAvailable`，不计通过。
- 数据集真实计划返回`dataset_plan_invalid`。隔离无网络投影复核确认前端接受1条合法权利风险但拒绝101条，而后端契约提供完整风险清单；此为待修的规模契约缺口，尚未确认是否为本次现场唯一原因。
- 本次只收集验收证据和更新记录，不修改业务代码，不重试模型、不构建、不写生产数据。功能协作任务因额度限制停止，root未伪造协作结论。

## 2026-09-02：文献提取安全取消与PDF选择零请求反馈（源码检查点，不升build）

- 新增会话绑定的文献任务取消路由。queued任务不调用模型即进入`cancelled`；running任务只登记取消请求，在收费调用安全边界保存成功回执后停止，不强杀网络请求，也不重复收费。
- 取消请求使用独立加密持久记录，不与正在写入的检查点CAS revision竞争；提供商结果未知时继续保持`literature_call_outcome_unknown`并阻断自动重试，不会被错误投影成已取消。
- Fusion仅在文献提取queued/running时显示“停止任务”，重复点击零重复请求；取消中、取消终态、刷新重连与任务目录均投影一致。关闭PDF选择器或空选择明确显示“未导入文件、未调用模型”，不会发上传、prepare或授权请求。
- 源码提交`d8fd944`和`ad72177`。后端目标117项、合并目标139项、Fusion与发布契约81项通过（套件重叠，不相加冒充全套）；未调用模型、未构建App、未生成新build/DMG/UserKit，生产SQLite和`paper_056`未暂存。

## 2026-09-02：文献提取检查点零模型收尾与重复收费阻断（源码检查点，不升build）

- 修复任务目录返回`retry_finalization`时Fusion仍可能落入新`prepare`的问题：新增严格的`recover-finalization`入口，只接受一次性恢复令牌，不接收provider、model、consent、task id或路径；现有validated检查点直接完成本机质量包校验、原子发布、搜索索引和真实发布收据，不调用模型。
- 服务端在新建文献提取prepared action前，按论文ID、当前稳定PDF SHA和已认证检查点核对未完成任务；发现`authorized/running/paused/validated/outcome_unknown`任务即在AI就绪、授权与模型调用前失败关闭。任务目录不可用、损坏或含异常项同样阻断，不以“普通新任务”兜底。
- Fusion把`resume_extraction / retry_finalization / open_search / restart_extraction / review_call_outcome`拆成五种用户可理解的动作；目录状态无法确认时按钮禁用，重新开始必须明确确认新的费用，已完成任务可直接前往本机搜索，失败不会删除检查点。
- 源码提交`e72fffe`。目标验证分别为111项提取/检查点/AI控制器/macOS路由、59项Fusion和27项路由/架构/发布契约通过（套件有重叠）；未调用模型、未构建、未生成build55，生产SQLite和`paper_056`未暂存。

## 2026-09-02：工作区表格严格链接同源官方核验结构（源码检查点，不升build）

- 修复“本机文献工作区的Table 3详情只有图注、意义和语境，没有真实行列表格”：新增只读的工作区→官方表格结构链接服务，只有DOI、工作区PDF实际SHA、页码和工作区截图实际SHA与活动官方包唯一匹配时才返回官方`verified`二维字符串结构；任何缺失、重复、源文件变化或哈希不一致均失败关闭。
- Fusion先读取工作区自己的表格结构；仅在工作区结构确实不存在时尝试严格链接，且保持工作区原始截图可见。链接表格标记为`官方资料库已核验 · 同源只读`，不可在工作区编辑或批准，不把官方状态复制进工作区；CSV/XLSX导出使用官方稳定实体身份。
- 对当前真实只读快照的验收命中工作区视觉资产`3`与官方实体`entity_table_03eea1197c731591f1040321514aefb2`，返回5行×4列，首行为`["", "Al0.3CoCrFeNi", "CoCrMnFeNi", "316H"]`。共享目标93项、macOS目标51项和发布契约15项通过，Python/JavaScript/JSON/差异检查通过。
- 源码提交为`7180df7`。当前安装App仍是`1.2.0/build54`、core commit `b2578a6`，尚未包含此修复；遵守单候选规则，没有创建build55、App、DMG、UserKit或新的发布工作树。生产SQLite与`paper_056`用户现场未暂存、未修改、未进入测试。

## 2026-09-02：官方 Table 3 安装版审核与真实导出闭环（build54 同批次）

- 从干净提交`b2578a6`覆盖构建并事务式安装同一`1.2.0/build54`，未生成build55。manifest保持`release_status=candidate`，可执行文件SHA-256为`1f0e7e7d334248049239cd57aa0628d559489eaa00d089d284c0c26576c8cbd7`，深层签名验证通过。
- 真实安装App在官方资料源检索并打开DOI `10.1016/j.jnucmat.2018.08.031`的Table 3；候选在主编辑组逐格核对后确认为`verified`，重启后仍显示完整5×4行列、已核验状态及CSV/XLSX入口。
- 两个文件均从真实App原生保存面板落盘并逐格复核：CSV SHA-256 `6da9f00af46bb286f7253eaebb4e740d5bb7f7d8fd2b735f8c08affff81b66e8`，XLSX SHA-256 `19bcb181b6c3e080d00a20e15f38b20061392284e6fe60d8c5d2a60a9a90e22a`；两者20个单元格与核验结构一致。
- App私有审校状态保存在独立AES-GCM文件中；生产SQLite验收前后SHA-256均为`0389a5aa0faf4967696e3c3a5a574eaaf0bec08a34f48c69b98f50c705aa80b0`，未进入提交、测试或构建。
- 制品治理按单候选规则执行：保留0.5功能比较、build24稳定回退和当前`b2578a6`候选回退；删除三份旧build54回退、两个旧发布App及干净发布工作树，约释放2.7GB。未知数据库build49工作树未动。
- 不可变官方`1.1.0`包没有原地改写。已核验sidecar若进入分发，必须生成独立新包版本并重新签名、完整审计和导入验收。工作区来源的表格展示问题属于随后单独批次，不与本官方来源验收混为完成。

## 2026-09-02：官方 Table 3 真实行列结构源码闭环（build54 同批次）

- 新增平台中立的官方表格候选/审核权威：只从身份已核验的官方PDF租约读取，绑定package、paper/entity、PDF SHA、页码与bbox；自动解析或人工转录都只能生成待核验候选，只有独立`approve/correct`版本可导出。
- macOS使用独立AES-GCM、原子CAS审校存储；Fusion按标签隔离加载、编辑和迟到请求，候选/拒绝/缺失不会伪装成表格或产生CSV/XLSX。旧包缺少可信bbox时在写入前失败关闭；维护者审计bbox不进入HTTP或renderer DTO。
- 现行真实官方包构建入口不再丢弃可选`evidence/table-structures.json`，并在制包前重新绑定package id/version、entity与本次PDF SHA；sidecar进入checksum、签名、内容指纹与安装审计，旧无sidecar包保持兼容。
- DOI `10.1016/j.jnucmat.2018.08.031`的Table 3已从同一官方PDF SHA、第5页和bbox建立5×4本地加密候选。源码阶段保持`manual_review`并通过真实隔离CSV/XLSX逐格一致性验收；随后安装版验收按上节完成批准和导出。整批154项定向回归通过，未生成build55、DMG或新资料包。
- 完整共享门首次发现签名包校验器反向依赖科学证据层并据架构预算失败；没有放宽基线，而是抽出仅依赖标准库的官方表格中立契约。独立只读审计确认v1规范JSON与SHA-256字节兼容、隐私门等价或更严；新增固定指纹金向量后，共享1,139项（80项按设计跳过）及macOS 323项串行通过。

## 2026-09-01：官方 Table 3 真实视觉与 PDF 返回链完成（build54 同批次）

- 提交`505d0c0`为官方资料包增加只读视觉资产租约：从已校验普通文件复制到不可变临时快照，流式提供PNG/JPEG，不向渲染器暴露路径或资产内部ID，并防止校验后的源文件变化影响已签发租约。
- 真实安装App已从官方资料搜索打开DOI `10.1016/j.jnucmat.2018.08.031`的Table 3，显示经哈希核对的来源截图，随后打开官方PDF并通过“返回来源标签”恢复同一详情、截图和证据AI对话。现有包没有人工核验的行列结构，因此界面明确显示“尚无已核验结构”，没有猜测单元格。
- 目标/发布回归154项、附加官方包回归46项、共享1,133项（80项按设计跳过）和macOS 309项通过；Python/JavaScript/发布哈希、冻结冒烟、签名、事务式安装均通过。生产SQLite SHA-256仍为`0389a5aa0faf4967696e3c3a5a574eaaf0bec08a34f48c69b98f50c705aa80b0`。
- 保持`1.2.0/build54`与`release_status=candidate`，不创建build55。下一批才处理结构化表格行列；不可变官方`1.1.0`包不得原地改写。

## 2026-09-01：官方资料包 PDF 原文链修复（build54 同批次）

- build54 实机已完成官方 `1.1.0` 包导入与重复导入，界面核对为 59 篇论文、4,369 条四类证据、59 份 PDF 和 291 项视觉资产，并可从资料包中心直接进入官方搜索。
- 真实搜索发现官方证据详情缺少“查看原文”。根因是官方仓库虽按已验证 PDF 清单返回 `pdf_available`，却未声明论文集合类型；Fusion 又只为私人包保留该能力字段，并且 PDF 请求依赖默认 `private` 来源。
- 官方仓库现在固定投影 `collection_kind=literature_collection`；Fusion 只有在官方或私人来源同时声明论文集合且 `pdf_available=true` 时才显示原文入口；PDF 请求必须显式携带来源范围、资料源身份和论文 UID，无 PDF、错误集合类型或错误来源均失败关闭。
- 共享 1,127 项（80 项按设计跳过）、macOS 308 项、86 项针对性契约以及 Python/JavaScript/发布哈希检查通过。仍保持 `1.2.0/build54`，不因本次修复顺延 build；待覆盖安装后完成官方 PDF 实机打开，再进入已排队的表格详情显示问题。

## 2026-09-01：build54隔离PDF提取预算修复（源码检查点）

- 真实安装App在隔离单页PDF提取前显示“最多512次调用”。确认该数字原先不仅是界面文案，而是真正写入prepared action、检查点和运行时的全局保险帽；用户取消授权，因此没有发生该次收费提取。
- 文献提取现按冻结PDF的实际页块数`C`生成任务硬预算：最多`8C+4`次调用、`72000C+20000`completion tokens；单页为12次/92,000 tokens，7页典型两块为20次/164,000 tokens。512与8.2M只保留为防御性绝对上限，不再作为每篇论文的授权值。
- 自动验证限制为每个分支/页块最多2批，整篇第三审核最多4批；超出候选保留为`manual_review`，不消失、不自动发布。模型响应字段经白名单重建，不能伪造`ai_verification`或质量门状态；旧512预算检查点在任何模型调用前以`literature_checkpoint_policy_changed`要求重新开始。
- coverage-gap后重新计算未解决缺口；存在待确认、拒绝项、未覆盖数值锚点或空库存时，最终覆盖收据明确为不完整，不再固定声明两类覆盖均完成。
- 文献提取、质量门、检查点、AI业务与Fusion相关目标回归已通过；本条仍只是源码检查点，不递增build、不生成App/DMG/UserKit。下一步在同一build54候选上重建并继续隔离单页PDF真实提取，然后完成官方资料包验收；新发现的表格详情缺失排在既有验收链之后。

## 2026-08-30：1.2单候选收敛与制品留存治理

- 图书管理员真实失败定位为本地科学校验器误判，而非DeepSeek或Harness卡住：旧规则把合金牌号`316H`识别为“316小时”，又把整份多来源回答中的每个单篇观察都当成跨资料束数值比较。当前源码改为逐句、逐引用、逐资料束核验；单一来源内只允许复述该引用已冻结的精确数值，跨资料束新增数值、差值、比例、范围和数值高低仍失败关闭。
- 隔离资料快照上的真实DeepSeek验收已通过四项业务：图书管理员同题1次调用约8秒并采用Harness回答、8条引用；选中证据问答返回答案/局限/来源页；合成实验表返回需人工审核的建议；一页合成PDF在约19秒内完成四类发现、视觉资产、原子发布和搜索索引刷新。四条链及AI/Harness目标回归154项通过；生产SQLite哈希仍为`0389a5aa0faf4967696e3c3a5a574eaaf0bec08a34f48c69b98f50c705aa80b0`。
- 上述仅是源码与隔离数据验收，不分配新build、不生成App/DMG/UserKit，也不替代最终安装WebView用户流程。下一步先完成已批准示意图对应的主标签/PDF返回、Librarian栏位和首屏主操作收敛，再串行跑完整测试并只生成一个候选。
- 用户实机截图证明build53仍存在两项P0：工作区证据详情虽然可见，证据AI却因公开搜索身份与Harness身份不一致而在`selected_evidence_preflight`失败；应用重启直接恢复第二编辑组时，生产HTML的静态空状态可能与真实标签内容同时显示。两项均只在源码候选修复，尚未生成新App或递增build。
- `2714615`建立唯一工作区公开证据身份权威，使Search V2、视觉详情与Harness使用同一`source_scope/source_id/entity_uid/paper_uid`；本地数据库编号不参与AI身份。相关公开DTO、Harness工作区、业务动作、Mac组合根和前端证据对话140项目标测试通过。
- `4d8cf50`让第二编辑器挂载器在首次恢复真实标签时清除生产HTML静态空状态，并用真实初始DOM补回归；Fusion标签/界面55项目标测试通过。安装App仍须在最终单一候选中复验，源码测试不写成用户问题已解决。
- 仓库外缓存完成两轮治理：先删除可重建的旧release工作树、失败候选和重复App，再把51份重叠完整Git bundle收敛为唯一经`git bundle verify`的`auto-research-v1.1.0-build24.bundle`（SHA-256 `5d7f134663a9fe6c9ffd67ba9915689180f263ae0c9ccc05f94649a444a942bd`）。累计释放约21GB；0.5功能基线、1.1稳定回退、当前1.2候选回退、官方包源和含未知数据库改动的build49工作树保留。
- 项目长期规则改为“源码检查点不升build、不构建、不打bundle；完整安装后用户验收表通过才分配一个新build”。同一发布批次只维护一个可覆盖临时候选，失败候选和重复制品在身份/哈希核验后删除。
- 当前语义版本继续为`1.2.0`，稳定声明暂停。下一步仍是同一源码候选上的图书管理员真实输出、证据问答、实验建议、隔离PDF提取和完整WebView用户流程；全部通过前不生成下一代App/DMG/UserKit。

## 2026-08-28：1.2.0 build51源码冻结候选

- 运行时代码检查点`5c2055f`完成任务感知栏位、图书管理员三层工作区、无选择时隐藏无意义栏位控制，以及AI错误阶段/恢复入口投影；没有恢复旧DOM、旧收费路由或第二导航所有者。
- root审查时把`WorkspaceLayoutController`投影保持为纯计算，按钮显隐只在应用布局阶段更新，避免拖拽、抽屉和命令面板互相触发可见性副作用。关联布局、标签、详情、历史/记忆、证据对话、AI进度/API状态65项通过。
- 完整源码门串行通过共享1116项（另80项明确外部样本跳过）和macOS307项；Python编译、10份生产JavaScript语法、发布哈希与差异检查通过。验证前后用户生产SQLite SHA-256均为`0389a5aa0faf4967696e3c3a5a574eaaf0bec08a34f48c69b98f50c705aa80b0`，`paper_056`现场未进入测试或提交。
- build51随后从干净提交`4040a73`完成冻结冒烟、ad-hoc签名、DMG和事务式安装；当前唯一可启动App为`1.2.0`/build51，build50已转为仓库外不可启动回退。DMG SHA-256为`8e5d2696037a6679cf533bb52018b380c55ee73a25ef50a08165459492977799`。
- 官方`1.1.0`资料包再次在隔离新根真实安装并审计为59篇、4,369条、59份PDF和291项视觉资产；候选UserKit ZIP SHA-256为`b67fc7e578e270d13e275598c7308d9f693d3f36ed93a02884f3a7abcc266289`，内含DMG、资料包、逐页检查过的8页中文教程和SHA-256清单。
- Mac锁屏仍阻断安装后真实WebView和四项AI验收，稳定声明继续暂停；build24仍是验证过的稳定回退点。Windows继续冻结。

## 2026-08-28：1.2.0 build50 Mac发布候选

- 从干净提交`45720ce`串行通过共享1109项（另80项明确外部样本跳过）、macOS305项、Python编译、10份生产JavaScript语法和14份发布资源哈希；未运行Windows测试或构建。
- 冻结App冒烟覆盖隔离schema-v12/60篇快照、Fusion资源、设置、历史、资料包与个人导入边界；严格深层签名、DMG校验和事务式安装通过。当前唯一可启动App为`/Applications/Auto Research.app`，版本`1.2.0`/build50；旧build49保存在仓库外不可启动回退副本。
- 文献提取改为每个“质量分支×完整页分段”一次合并提取，1/7/15页初始调用由6/12/24降至2/4/8；保留双分支、按需覆盖核验、第三审核、表图和曲线安全门。供应商硬超时、拒绝响应释放与“调用结果未知”错误血缘已经修复。
- DMG SHA-256为`284915825c4834f4a2ea224af3cb60e178ee36010f2828d00d2f7b7523b5bde2`。独立`1.1.0`官方资料包仍为59篇、4,369条、59 PDF、291资产，SHA-256为`909cc7323b8a91e3a238e8f49d03fe62022f5a9d078f2cc6785bb8a0b73a69c4`。
- Mac UserKit已原子生成并逐项复核，ZIP SHA-256为`69c2373a2e85cc9acce76d2bd780b2e64cbfab3d0f9b27652a5350213dbb5820`，内含DMG、官方包、8页中文教程和校验清单。
- Mac锁屏阻断安装后真实WebView点击验收；稳定声明继续暂停。生产SQLite与`paper_056`现场保持未暂存、未测试、未构建。Windows继续冻结。

## 2026-08-27：1.2工作台恢复Phase 3/4表格核验闭环

- 上传论文主链已经用隔离PDF证明不可变快照、四类暂存、原子发布、增量索引与来源定位连续成立；新视觉资产保持`manual_review`，不会因截图存在就进入正式搜索或训练数据。
- 新增保守PyMuPDF二维表格候选，只接受唯一、规则矩形的原始字符串与单元格几何；无法可靠解析时只保留权威截图，不推断表头、不转换数值、不从曲线像素制造数据。
- 表格结构采用追加式版本和CAS人工审核，可确认、逐格纠正或不采用；只有`verified`版本可导出literal CSV/XLSX，公式前缀按文本中和。
- `32cb790`把同一共享服务接入macOS与Fusion：原图和结构并列，主/次编辑组按标签与请求代次隔离，审核POST复用现有session、Origin、CSRF和64KiB限制；发布资源哈希同步。
- 表格恢复链139项目标测试、发布契约23项检查以及JavaScript/Python语法通过。未运行全套、构建、真实模型或安装App；当前阶段仍不是1.2稳定发布声明。

## 2026-08-27：1.2工作台恢复Phase 2

- `2371ace`恢复公开身份驱动的文档标签：论文、证据、PDF、私人实验表格、资料包任务和图书管理员标签可跨重启恢复身份，但正文、聊天、路径、滚动位置与焦点不进入`localStorage`。
- 第二编辑器使用稳定的逐标签挂载点；切换标签不再销毁DOM，滚动和焦点在当前会话内分别保存，关闭标签才回收。标签容量淘汰会同步修复编辑组的当前标签，不留下空白活动组。
- 恢复采用真正的懒加载：启动只读取当前可见的一至两个标签，未激活标签保持零请求；切换时才加载。异步响应绑定`tabId + groupId + generation`，请求期间移栏会拒绝旧结果并留下可重试状态，不串栏、不抢焦点。
- 资料包任务恢复明确区分官方包与用户转移包路由；文献初始加载不会覆盖恢复的活动标签或编辑组。发布契约哈希已同步。
- 串行目标验收通过49项Fusion、19项工作台/发布、14项macOS静态服务与3项架构守卫；JavaScript语法和差异检查通过。未运行全套、构建、模型调用或安装验收；生产SQLite与`paper_056`现场保持隔离。

## 2026-08-25：1.2工作台恢复Phase 1

- `21f030b`将工作台可见性收敛到单一`AutoResearchWorkspaceLayout`投影：活动栏外默认最多三层、按需第二编辑器，检查器与Librarian结果栏互斥，任何路径禁止五列墙。
- 图书管理员历史归入共享上下文栏；资料包中心改为单工作流选择，设置、资料包和实验页不再暴露无意义的第二编辑器或全局检查器。
- 新共享脚本已进入macOS PyInstaller清单、受保护静态资源白名单、冻结冒烟和`release-contract.json`哈希；Windows继续冻结。
- root独立补验60项共享前端/发布契约与5项macOS静态服务测试均通过；真实安装WebView多宽度检查仍是后续发布门，因此当前不恢复“稳定版”声明。

## 2026-08-25：1.2工作台恢复Phase 0

- 用户批准“0.5的从容与完整工作流 + VS Code的层级与按需分栏”为最终生产预期，并批准静态`expected-workbench.html`作为界面验收基准；停止按截图逐点修补。
- 全面功能/代码审计、布局专项审计和实施计划由`c8b2c66`冻结；`173c239`新增机器可读`workbench-recovery-contract-v1`，纠正安装build47、HEAD和历史稳定声明混用的问题。
- 上一轮被中断的Fusion标签hydration实验隔离为Git stash `quarantine-interrupted-fusion-experiment-20260825`，没有进入主线。root继续作为唯一Git写入者；同项目前端任务只在明确共享Web文件内执行Phase 1。
- 静态导入审计确认5组Python循环依赖。`2b672e4`记录循环基线并新增只允许缩小、不允许新增或扩张的架构门；后续Phase 7逐组物理解除。
- 生产SQLite SHA-256仍为`18b9a4a3a4cbdf9ffcbe4fe14fa0e4855727a6211c1f61902e3d30324903bc84`，未暂存、测试、构建或修改；`paper_056`现场同样保持隔离。Windows、完整测试、构建和模型调用继续冻结。

## 2026-08-24：v1.1 build41 分栏与可追踪 AI 候选

- 继续使用同项目用户可见 Codex 任务协作：前端任务只补分栏/结果栏运行时验收并做最终只读复核，功能任务只补 AI 后台任务/Harness 安全测试；root 是唯一 Git 写入、集成、全测、构建与发布者。任务之间没有并行全测、构建或模型调用。
- 拖动边界时不再重建编辑器 DOM，统一处理松手、取消、指针捕获丢失和窗口失焦；损坏的双编辑组折叠状态会自动恢复，关闭栏位不会删除标签、请求或滚动状态。
- 图书管理员增加可跳转的引用与推荐论文结果栏，点击后在第二编辑组打开真实证据并绑定右侧证据 AI；不同证据保存独立多轮对话，后台回答写回原证据。
- 四项收费动作改为会话绑定后台任务并投影真实安全活动。服务端从固定事件目录重建标签、阶段和进度，拒绝调用方提供的文本、未知工具与伪造完成事件，避免提示词、路径或内部信息进入界面。
- 后台任务增加30分钟无活动租约；即使前端没有继续轮询，晚到结果也不能覆盖超时失败，活动槽可以恢复。图书管理员推荐论文必须匹配冻结召回文档，题名、DOI与跳转身份均由真实文档投影；AI错误的恢复动作仅公开安全token并由前端映射中文。
- 串行验证为共享926项（80跳过）、macOS 237项及最终可靠性目标94项全部通过；JavaScript/Python、发布契约和差异检查通过。生产SQLite SHA-256保持`18b9a4a3a4cbdf9ffcbe14fa0e4855727a6211c1f61902e3d30324903bc84`。本条仍是源码候选，真实App和四项付费AI验收完成前不宣称稳定。

## 2026-08-23：v1.1 build27 实验预填能力门修复候选

- build26 真实App已通过DeepSeek连接、V4 Pro/Flash结构化输出与工具调用、文献提取/图书管理员/证据问答三个能力门。
- “实验表格AI预填”在发送模型请求前被前端拒绝：服务端正确声明能力验证最多2次固定探针，前端却把它与业务执行最多1次的预算混为同一常量；假目录测试也错误写成1，掩盖了问题。
- build27 新增独立`AI_VERIFICATION_CALL_LIMITS`并将个人预填能力门设为2，业务动作仍保持1；测试目录改为服务端真实计划值。build26不发布。

## 2026-08-23：v1.1 build26 DeepSeek验证修复候选

- build25 实机连接验收复现：凭据已保存且 HTTP 200，但 `deepseek-v4-pro` 在32-token上限内只生成推理内容，最终 `content` 为空；应用因此错误显示为通用“连接验证未完成”。
- 将固定结构化输出和工具调用探针统一提高到仍然有界的256-token上限；使用App现有安全凭据的一次真实连接探针已成功，密钥未进入输出或日志。
- `AIRuntimeStateError`、桌面HTTP投影和Fusion设置页现在保留并显示安全的 `cause_code / stage / next_action`，不再吞掉可操作原因。
- 语义版本保持1.1.0，候选构建号递增为26；四项业务、完整测试、App重建与发布套件仍是发布阻断项。

## 2026-08-22：v1.1 build25 分栏易用性与AI可用性候选

- 按用户要求继续使用同项目可见Codex任务协作：前端任务只负责Fusion分栏、自动右侧预览和AI设置投影，功能任务冻结provider/readiness/错误血缘，root唯一审查、暂存、提交与发布；其他任务未stage/commit，也未并行构建或调用模型。
- 新增独立`PaneLayoutController`和`fusion-pane-layout-v2`。侧栏48px内、编辑组96px内吸附为收起，至少保留一个编辑组；鼠标、键盘、按钮和命令面板共用同一状态源。证据单击自动进入secondary未固定预览，固定详情不会被下一次单击覆盖。
- 新增`ai-readiness-v1`、DeepSeek/OpenAI/custom三类provider管理、连接与四业务分层验证、稳定`reason_code/next_action`以及状态栏摘要。custom只允许通过本地SSRF、DNS重绑定、重定向和地址安全门的公开HTTPS Chat Completions服务；密钥和保存后的endpoint不回显。
- 文献提取增加免费本地preflight，缺失PDF等错误不再被AI通用错误遮蔽；prepared action仍是唯一收费授权，取消保持零业务调用，Harness失败不回退旧AI。
- 冻结接口后串行通过共享896项（80项显式外部样本跳过）与macOS 236项；前端/AI/发布目标测试、JavaScript/Python语法、release-contract同步与diff检查通过。生产SQLite SHA-256保持`18b9a4a3a4cbdf9ffcbe4fe14fa0e4855727a6211c1f61902e3d30324903bc84`。
- 当前只完成源码候选。干净构建、事务式安装、WebView分栏验收、用户在App内保存密钥后的最多25次真实付费AI验收、DMG和UserKit仍是发布阻断；build24保持稳定回退，Windows继续冻结。

## 2026-08-22：v1.1 build24 可拉伸Fusion工作台与最终Mac验收

- 在不恢复旧DOM、第二导航或平台算法副本的前提下，为上下文栏、两个完整编辑器组和右侧检查器加入三条VS Code式可拉伸边界；支持指针拖动、键盘方向键/Shift/Home/End、双击复位、响应式收敛和本机非敏感宽度记忆。
- 修复双编辑器状态隔离和表格/图片来源定位：公开DTO仅投影经过长度、类型、有限数和四坐标校验的`bbox`，不暴露`bbox_json`、路径或内部字段；真实表格原图与应用内PDF可按来源页显示、重新显示短暂高亮、返回原证据并关闭。
- 串行通过共享880项（80项历史外部样本明确跳过）和macOS 234项，以及Python/JavaScript、发布契约和差异检查。候选App从独立干净worktree构建；签名前清除候选目录扩展属性，解决嵌套Harness运行时的codesign内部错误，同时保留严格签名失败关闭。
- 真实Mac只读验收完成文献题名筛选、两个完整编辑器组、三条无障碍分隔线、Table 3真实PyMuPDF截图、应用内PDF第8页、高亮重显、返回详情和关闭PDF；未调用收费模型，Windows继续冻结。
- 已事务式安装`/Applications/Auto Research.app`，版本`1.1.0`/build`24`。DMG SHA-256为`f3363092dc8dfea9af4aaa9fe1920e164781c585536e7146d718c0cd7e4dfc5c`；含DMG、59篇官方包、中文教程和校验清单的Mac UserKit SHA-256为`2c943904090bbcfde8ce1aa45c2b107f1482905f7090bdd3b648abe2596ca99c`。
- 生产SQLite SHA-256仍为`dab4ed3aba74278a4a75db9424b3559edc83716ba29bc754175268852726e946`，保持用户未提交现场，未进入测试、App、DMG或资料包；`paper_056`工作区视觉目录同样未暂存。

## 2026-08-22：v1.1 Mac核心恢复、Harness与数据集发布门

- 本轮使用同项目Codex任务协作而非子Agent：前端任务独占Fusion文献滚动/筛选、真实标签双栏和审核队列界面；功能任务独占审核事务、共享服务与Mac安全路由；root保持唯一Git写入者，审查后修正稳定论文身份和递归路径泄漏，再统一集成。两个协作任务均未自行暂存、提交、全测、构建或调用模型。
- Fusion新增可恢复`DocumentTabStore`、最多两个完整编辑器组、服务端四类证据过滤、独立滚动文献目录、题名/作者/DOI筛选、五模块检查器隔离与PDF临时高亮返回链。新增人工审核标签支持批准、不采用、公开字段纠正及索引待恢复重试；没有恢复0.8 DOM、第二导航或跨页搬运节点。
- 图书管理员和选中证据AI改用`deepseek-harness-sdk==0.1.1rc1`与`deepseek-harness-runtime-bin==0.1.1rc1`。SDK wheel SHA-256为`2113aec229039da435bc44b275b487216d2b1c308d850521b88cea6ce3c1b762`，runtime wheel为`2707cd666ba49ee0963228873abf7850ca7ec5e782cca61e3603793bace0d1cf`；composition仅开放八个只读Auto Research工具，移除Shell、文件、PTY、编辑器、子Agent和任意网络。
- Harness继续受prepared action、一次性授权、provider/model白名单、调用/token预算、Credential generation与引用完整性门约束。加密历史最多20个会话或30天，设置可立即清除；旧收费路由固定410，版本/安全策略失败不回退旧循环。
- 新增`dataset-bundle-v1`三层实现：安全来源投影、JSONL/Parquet/数据卡与论文级确定性划分、异步原子导出。真实不可变快照计划为60篇、4,356条，缺失DOI 3、未审核2,417；含未审核记录必须再次确认，默认不含私人实验或PDF/图片。
- 新增14维人工金标准科学审计；测试覆盖TP/FP/FN、页码/IoU、重复、数据泄漏和路径/内部字段。禁止以模型一致性或软件回归通过推导科学准确率。
- 官方包v2输入规划发现60个历史PDF路径中5个实际为HTML；4份已从期刊静态资源或Europe PMC取得并核验。按用户冻结范围在manifest明确排除`10.2172/6065200`，不删除本机/Zotero条目。签名包为59篇、4,369条、59 PDF、291图表资产，SHA-256 `909cc7323b8a91e3a238e8f49d03fe62022f5a9d078f2cc6785bb8a0b73a69c4`；隔离导入、重复导入、全部PDF与全部资产运行时复核通过。旧视觉`review_status=draft`被诚实保留，不能称为科学人工审核完成。
- 更新机器可读模块所有权与债务：全仓Python约109,950行，Fusion前端约7,338行；`app.js`与若干大Python模块仍是P1，历史浏览器兼容与仓库内约1.06GB Mac制品仍是P2。
- 本轮冻结接口后串行通过共享874项（80项因未携带历史外部样本而明确跳过）与macOS 234项；Python/JavaScript、发布契约和差异检查通过。测试仍报告少量既有SQLite `ResourceWarning`，继续列为连接生命周期债务。
- 最终源码`3b8957282254a43236dc7af7943a55cf1a7e074f`已在独立干净worktree重建，冻结App冒烟、严格ad-hoc签名、事务式安装、版本/构建号、唯一可启动App与DMG校验通过。DMG SHA-256为`631a99293ab951e66ea412773b495966767a2fabe1cdf4291ae0d3c284f8e161`；包含DMG、1.1官方包、中文教程和校验清单的Mac UserKit SHA-256为`53379be6f54907cb9c4cd846a9a7562c223b00013eb029a9cba44954b18172b7`。系统锁屏暂时阻断最后一轮只读点击验收，发布门保持未关闭；旧App已保存为仓库外不可启动rollback。
- 生产SQLite SHA-256为`dab4ed3aba74278a4a75db9424b3559edc83716ba29bc754175268852726e946`，保持用户未提交现场，未进入资料包源、测试、App或DMG。

## 2026-08-22：Windows离线Setup成功经验冻结

- 成功路径固定为：Mac端物化离线Python、完整wheelhouse、锁定Inno Setup 6.7.3、WebView2 Evergreen x64、源码与官方包，并生成逐文件SHA；Windows端将整个Build Kit复制到C盘普通短路径后只运行本机构建脚本。
- 失败经验包括：网络无法取得`proxy_tools`/Inno、错误Inno版本、云端占位文件、Windows CRT文本模式破坏归档、Python 3.12缺少`os.fchmod`、Defender/索引器WinError 5/32/33以及临时文件句柄未关闭。构建脚本已分别用离线依赖、精确版本、输入物化、`O_BINARY`、条件权限、关闭句柄与有界重试处理。
- Setup能生成不等于正式Windows发布。仍需真实Win11安装、导包、搜索、PDF、上传、BYOK、升级和卸载验收后才允许改变`installer_ready=false / SETUP_PRESENT=NO`。本轮1.1不做Windows迁移。

## 2026-08-21：Windows v1 RC 全离线与文件系统可移植性收口

- 在真实 Windows 11 连续复现并修复三层阻断：归档文件描述符缺少 `O_BINARY`、Python 3.12 Windows 无 `os.fchmod` 且异常清理仍占用临时文件、Defender/索引器短时锁定原子替换与删除。共享普通文件操作现只对 WinError 5/32/33 有界重试，所有源/目标 fd 先关闭，其他错误继续失败关闭。
- 完成冻结运行时审计：版本、发布契约和官方包资源统一从 PyInstaller `_MEIPASS` 解析；Windows 进程存活不再使用 `os.kill(pid, 0)`；平台启动失败在无控制台 EXE 中显示稳定中文错误；不可用的 publisher/v12 维护模块不再进入 package-center 启动闭包。
- 构建套件新增并锁定 Microsoft 官方 WebView2 Evergreen x64 离线安装器。Setup 仅在缺失时静默安装；生成 Setup 前强制检查 WebView2/.NET/Python/PyMuPDF 原生组件、禁止模块、Fusion 资源，并实际运行无 UI 冻结 bootstrap smoke。
- 文件名、ADS、Windows 保留设备名、末尾点/空格、junction/reparse point、含空格工具链路径和严格 SemVer 均有失败关闭处理；用户包、官方包、私人表格和设置清理不再依赖 macOS 可删除已打开文件的语义。
- 目标包/个人/发布回归 182 项、Windows 平台契约 170/170、macOS 邻接 226/226 通过；真实 v1 官方包通过冻结输入验签与审计（SHA-256 `d1337a43aa4c0b83030a70e6a500bc60b85a994cb03d287396e895957ae4604d`，4,356 条四类实体）。共享全套779项中，699项通过，80项仍统一因用户已删除且未授权恢复的历史`data/pdfs/XJZQ42XP.pdf`在setup阶段报错；没有伪造或恢复论文。源码修复提交为 `ff7b5d2`；构建契约绑定该提交，旧 Build Kit 必须作废并从新提交重新生成。
- 本轮仍不能称 Windows 正式版：Mac 无法替代真实 Win11 Setup、Credential Manager、WebView2、Defender、安装/升级/卸载验收。新套件只负责让用户在 Windows 端离线生成 RC Setup，最终验收前保持 `installer_ready=false / SETUP_PRESENT=NO`。

## 2026-08-21：Windows 离线 Build Kit 二进制文件边界修复

- 真实 Windows 11 构建已通过离线 Python、锁定依赖与内置 Inno Setup，随后在 `[4/8]` 官方资料包审计处稳定返回“资料包复制过程中发生变化”。将套件从移动盘迁到 C 盘后错误不变，排除了盘符和下载占位因素。
- 根因是共享核心通过 `os.open` 复制 `.aresearch` 时未显式请求 Windows `O_BINARY`；Windows CRT 文本模式可能翻译或提前结束归档字节流，使复制字节数与 `st_size` 不一致。修复统一覆盖官方/用户资料包、个人 CSV/TSV/XLSX、PDF、SQLite、签名密钥与设置文件的低层文件描述符；目录 `fsync` 保持原样。
- 新增 Windows 二进制 flags 回归，使用含 CRLF 与控制字节的合成归档验证源、目标描述符均请求二进制模式且字节保持不变。相关包/个人/安全目标 139 项及完整 Windows 契约 158 项通过；Python 编译和差异检查通过。
- 共享全测后来随可移植性回归增长为 779 项，其中 80 项历史六列表测试因已不存在且未授权恢复的 `data/pdfs/XJZQ42XP.pdf` 统一在 setup 阶段报错；其余 699 项无失败。该缺失不是本次 Windows 构建修复造成，也没有为通过测试伪造或恢复论文 PDF。
- 本条只冻结源码修复。新的离线 Windows Build Kit 必须从修复提交重新生成并更新源码身份与全部 SHA；旧 ZIP 必须标记为不可使用。Windows 仍为 RC，只有真实机器生成 Setup 并完成安装、导包、搜索、上传、BYOK 与卸载验收后才能把 `installer_ready` 改为 true。

## 2026-08-21：v1.0.0 build22 功能冻结与发布前验证

- 将 Fusion 工作台从 0.9 功能恢复线收敛为首个 v1 稳定 Mac 候选；活动页不再出现“测试/体验/预览/合成/后续版本”等发布占位词，旧 0.8 DOM、第二导航和退役浏览器入口没有复活。
- 在唯一 Fusion 中完成文献导入与 staged extraction、四类证据与真实视觉详情、中央 PDF/返回、精确与联合搜索、图书管理员、选中证据 AI、私人表格一次核验与分页详情、官方/用户资料包和真实文件导出。导出文案明确：工作区批量导出只覆盖测量条目与研究结论，表格、图片、官方/私人来源使用单条导出或资料包。
- 修复用户论文集合 PDF 的中央阅读能力：只有已核验 `literature_collection` 私人来源且 DTO 同时具备 `source_id/paper_uid/pdf_available` 时才使用受保护 lease；私人实验来源不能声明 PDF 能力。
- 发布前串行通过共享核心 773 项、macOS 226 项，共 999 项；JavaScript/Python、release-contract 和 diff-check 通过。生产 SQLite SHA-256 保持 `d3e225d35f4c9e21fcf825405d0e38f28210c4479caac67b87be9f8382fb6f72`，Windows 未参与。
- 官方 v1 包从已审计不可变快照生成并在临时根完成首次 `installed`、重复 `already_active` 验收；包 SHA-256 为 `d1337a43aa4c0b83030a70e6a500bc60b85a994cb03d287396e895957ae4604d`，60 篇论文/4,356 条四类实体，零 PDF/零二进制资产。
- 当前阶段只冻结源码、教程与包内容。必须在干净发布工作区构建、备份旧 App、安装验收并生成绑定最终提交的 UserKit 后，才可宣布 v1 已交付。

## 2026-08-20：0.9.2-preview.1 build21 真实表格与论文图片恢复

- 复用既有 `/api/visual-assets/{id}` 与 `/image`，在Fusion中央唯一详情区恢复item、finding、table、figure四类详情；未恢复旧app.js、dialog或第二导航。
- 工作区table/figure显示真实PyMuPDF权威截图、图注、上下文、物理量、变量、材料、条件、方法和关联数；官方/私人来源只用稳定身份读取安全字段，无图片路由时明确不可用且不伪造。
- 详情、原图与PDF均在中央工作区，支持返回原列表/原证据、焦点恢复、Escape与晚到响应失效。公开投影保留finding正文和原文摘录的区分，并拒绝路径、Zotero key和内部备注。
- 串行回归通过共享753项、macOS 215项与Fusion详情11项；从干净提交 `83cf5e4` 构建并安装0.9.2/build21，冻结冒烟、codesign与DMG校验通过。DMG SHA-256为 `87e29d20d926111aac784d8883ea19330ac818b754b8f3f86171f22b94943a11`。
- 真实Mac验收打开Table 4与Figure 9原图、科研元数据、中央PDF及返回链，并从搜索结果打开同一真实表格。生产数据库SHA前后不变；未调用收费模型；Windows继续暂停。
- UserKit ZIP为 `Auto-Research-0.9.2-preview.1-build21-UserKit.zip`，SHA-256 `1848453eeff79e1ad8fe206e3f3475282116afd3bbeac7de9a8657a80dfd0331`。随附官方0.2资源包已验签，但Fusion资料包导入/回退/导出UI仍明确禁用。

## 2026-08-20：0.9.1-preview.2 build20 Fusion核心功能恢复

- 在Fusion唯一生产DOM内复用0.8成熟服务，恢复文献PDF导入、prepared-action自动提取入口、应用内PDF与返回链、Search V2/联合精确检索、图书管理员入口、CSV/TSV/XLSX安全预览与一次核验导入、DeepSeek/OpenAI受信设置；没有恢复旧0.8页面或第二导航。
- 三项用户要求固定在顶部主操作区：文献“导入PDF/开始自动提取与核验”、搜索“精确检索/图书管理员”、实验“选择CSV/TSV/XLSX”。AI仍只在实际调用前逐阶段披露并授权；本次验收未调用模型。
- 串行通过共享750项、macOS 214项、冻结HTTP冒烟、前端/Python检查；从干净提交 `056e2fb` 构建0.9.1/build20。App通过严格codesign，DMG通过hdiutil校验，SHA-256为 `470dabcf5b9f491f08807677e2a3068fe185748eed2f3ee21299492e6bd26396`。
- 真实Mac验收完成：只读查询“辐照温度”82条、图书管理员可见、实验文件选择可见、中央PDF打开并返回、DeepSeek/OpenAI设置载入。旧build19保存为不可启动rollback，系统只有一个可启动App；生产数据库SHA前后不变。
- UserKit ZIP及中文教程位于仓库外 `auto-research-releases/Auto-Research-0.9.1-preview.2-build20-UserKit.zip`。资料包/通用导出、选中证据AI、私人实验大表分页仍明确为后续边界；Windows继续暂停。

## 2026-08-20：0.9 Fusion功能恢复的首个界面契约

- 用户确认以0.9.1 Fusion工作台作为功能重构底座，Windows继续暂停；后续功能只能接入这一套生产DOM，不允许复活0.8页面或新增第二导航所有者。
- 将文献页“导入 PDF”与“开始自动提取与核验”并列放到顶部主工具栏；将搜索页“精确检索”与“图书管理员（官方全库）”固定在顶部；将实验页“选择 CSV / TSV / XLSX”放到顶部主操作区。页面底部不再重复生成这些关键入口。
- 中央次级标签按文献、搜索、实验、资料包分别预留证据详情、图书管理员、列与曲线详情、任务详情。当前入口仍明确标注后续接入并由Fusion只读策略禁用；本检查点不调用网络或模型、不写生产库、不生成误导性的功能版App。
- 低负载定向验证覆盖Fusion前端、macOS Fusion模式与版本契约；生产SQLite保持未暂存现场。真实PDF选择、自动提取、Librarian、个人文件选择仍需后续按版本串行接线和验收。

## 2026-08-13：0.9.1 Fusion GUI先行审核候选

- 用户冻结0.9串行路线：0.9.1只交付Fusion GUI，搜索详情/PDF、自动提取、私人实验详情和真实导出全部延后；Windows暂停到Mac GUI与主流程获明确认可。
- 生产DOM物理重排为标题栏、活动栏、上下文栏、中央标签区、右侧检查器与状态栏。文献与搜索从独立schema-v12快照只读展示，实验为可键盘操作的会话级合成CSV，资料包和未来业务按钮明确禁用并标注0.9.2+。
- macOS新增 `fusion-review` 后端能力模式：只允许受保护的论文目录、Search V2、UI mode、桌面外观设置GET，以及带CAS/CSRF的外观PATCH；所有模型、上传、提取、确认、PDF、导出、资料包与删除操作固定403。
- App包前端白名单收敛为四个Fusion资产；0.8旧页面脚本不被打包或提供。旧DOM专用测试退役，核心自检改为Fusion唯一导航、响应式抽屉、合成网格、零模型和禁用业务契约。
- 低负载验证：macOS 212/212、Fusion/发布/核心目标28/28、完整核心证据测试、候选HTTP冒烟全部通过；生产SQLite SHA-256仍为 `d3e225d35f4c9e21fcf825405d0e38f28210c4479caac67b87be9f8382fb6f72`。
- 源码实现提交为 `50a6b78`，安装事务的zsh保留变量问题由 `0b3242d` 修复，设置分类污染中央标签的问题由 `4fd4f24` 修复并增加运行时回归；最终标签 `auto-research-0.9.1-preview.1-build19` 指向 `4fd4f24`。0.8 build18已保存为不可启动回退，最终App安装于 `/Applications/Auto Research.app`。
- 真实WebView串行验收通过：文献/搜索/实验/资料包/设置单一导航，真实文献与右侧检查器同步，合成CSV工作表与键盘网格，日夜主题及紧凑密度跨重启，窄窗口检查器抽屉与Escape焦点恢复。未调用模型、未上传或执行业务写入。
- 交付目录为 `auto-research-releases/Auto-Research-0.9.1-preview.1-build19-FusionReview/`；DMG完成 `hdiutil verify`，SHA-256 `c1786d4b4ee6734390f36c878fdcf008f5d0c3ab405f5eac87031ffe40b92f69`。该版本仍只等待用户GUI认可，不代表第1—5项功能已恢复。

## 2026-08-13：0.8.0-preview.1 源码发布门冻结

- 首个 build 16 冻结候选在真实 PyInstaller 冒烟中被安全门拦截：launcher 在 `configure_core_paths()` 前提前导入共享 AI/settings 核心。没有安装 App、没有生成 DMG、没有对外发布。修复将这些导入延迟到运行路径绑定之后，并增加回归契约；为保持标签不可变，正式候选顺延为 build 17，build 16 仅作为失败保护点保留。
- build 17 随后被第二道 HTTP 前端契约冒烟拦截：检查器仍寻找 0.7 的授权 schema 与旧 DeepSeek 专用前端标记，而实际 0.8 已迁移到多提供商 prepared-action。更新 smoke 为 `ai-consent-v2`、四授权域与 prepare/execute 标记后，正式候选继续顺延为 build 18；build 17 同样没有安装或生成 DMG。
- build 18 从干净提交 `700e642` 构建成功，冻结 App 冒烟、ad-hoc codesign、DMG 校验、六类共享静态资源与真实 macOS 用户视角检查通过；系统只保留 `/Applications/Auto Research.app` 一个可启动副本，0.7 build15 改为非 `.app` rollback。
- 最终用户套件为 `Auto-Research-0.8.0-preview.1-build18-UserKit.zip`，SHA-256 `fc139f4b3a3f268b246218d01eb8805d71049596ad63cf0f241db1ae94ca7e27`；包含 DMG、官方 0.2、合成私人实验包、示例 CSV、中文指南与验收报告，不含 Git bundle、生产数据库、paper_056 或 PDF。Windows 另封装 `0.8.0-internal.1` 源码预览，仍无 Setup。

- 完成 Workbench A 日/夜/跟随系统主题、舒适/紧凑密度、统一活动栏/命令面板/设置中心，并以 B 文献阅读层级和 C 实验数值样式补充单一设计系统。个人实验与资料包 DOM 静态归位；人工补录、修正历史和宠物进度场景从生产前端物理退役。
- DeepSeek/OpenAI 进入同一受信 provider 注册表和按 provider 隔离的安全凭据；Librarian、选中证据、文献提取、个人建议全部改为服务端 prepared action、逐阶段知情同意、一次性 nonce、调用/token 预算和 stale 绑定。旧收费旁路固定退役。
- macOS 完成四域业务、分阶段文献最终提交、私人实验、资料包、设置与原生薄接线；Windows 完成相同共享 UI/DTO/AI 契约与 Credential Manager 薄适配，但继续 `installer_ready=false / SETUP_PRESENT=NO`。
- 串行发布门通过 1178 项（core 810、macOS 229、Windows 139），Python 编译、六个 JS 语法、release-contract 同步、diff-check 和 Git 对象检查通过。生产 SQLite 未测试、未暂存。
- 用户明确确认 `paper_056` 是遗留 bug 后，精确删除 4 个 DeepSeek run、2 个 quality run 和 13 个 visual 文件；未触碰生产 SQLite。0.7 build15 继续作为独立回退。

## 2026-08-13：0.8 续作——受信 AI 运行时与前两个业务域

- `118fd59` 完成 macOS 受信 AI 运行时：provider 凭据、runtime state 与执行 lease 共用同一锁和单一 `ProviderCredentialManager`；正式桌面入口删除 ambient `DEEPSEEK_API_KEY`，旧 DeepSeek credential route 只委托同一 manager，不再写环境变量或形成第二 secret authority。42 项定向/邻接测试通过。
- `0dde3c7` 将个人实验 AI 建议绑定到 server-prepared action。renderer 仅能提交 import/sheet locator；服务端拥有 prompt、task、模型、token 和调用参数。完整本地预览指纹与有界外发指纹分离，准备后变化及模型调用期间变化均拒绝；AI 永远只返回待核验建议。28 项定向测试通过。
- `0450345` 将选中证据详情 AI 绑定到 server-prepared action。选中证据与完整 PDF 作为 stale authority；PDF 由单一 `O_NOFOLLOW` 文件描述符读成不可变快照，同一字节生成上下文与 SHA，拒绝 symlink、同大小同 mtime 替换和读取中变化。公开结果严格白名单、长度有界，同一 action 重放零新增调用。10 项定向/兼容测试通过。
- 两次安全审查分别发现并关闭“截断列未进入 stale fingerprint”和“PDF 消息/哈希双重读取 TOCTOU”两个 P0；所有修复均在独立三文件提交内完成。没有网络、模型、全测、构建、App 或生产数据库操作。
- 当前开发 HEAD 为 `0450345`；0.7 build15/`888aae5` 仍是唯一稳定回退。下一步为 Librarian、文献提取两个共享业务适配，以及四域统一 registry/snapshot authority/HTTP；随后才恢复前端与 Windows。

## 2026-08-13：0.8 科研工作台开发暂停与可恢复检查点

- 视觉采用 Workbench A 骨架与中性工作台配色，吸收 B 的文献题名/阅读层级和 C 的实验数值/单位表现。共享前端已落地 light/dark/system、comfortable/compact、设置中心、单一活动栏/导航、键盘与 ARIA 基础，并迁移搜索和文献工作区；personal/package 已静态归位，不再跨页面 appendChild。
- 设置中心以 `desktop-settings-v1` 为后端权威，localStorage 只作首帧无闪烁缓存；语言首版仅 `zh-CN`。Windows 已消费相同 workbench 资产与设置 DTO，版本为 `0.8.0-internal.1`，但仍 `installer_ready=false`。
- 运行时 AI 从 DeepSeek 单一实现迁移为代码内受信 provider：首批 DeepSeek 与 OpenAI，endpoint/model 目录固定且禁止自定义 URL/redirect。OpenAI 普通业务只有在短时能力证明有效时才激活；DeepSeek 保留旧兼容激活。
- 所有 AI 知情同意升级为服务端 prepared action：不可变 job envelope 绑定 provider/revision/credential generation、模型、内容快照、executor 与调用/token 预算；renderer 只拿 action_id 与安全摘要。能力测试也走 prepare→consent→execute，一次性 nonce、冷却和缓存限制重复费用。
- macOS 已新增双 provider 原子凭据 envelope：每 provider 独立 Keychain/preview AES 记录，密钥与 generation 同一次原子写，删除为 generation++ tombstone；旧 DeepSeek route 只能成为同一 manager 的兼容视图。
- 本日小型验证证据：共享 prepared/provider 定向 70 项通过；前端第五批 36 项通过；macOS 凭据与旧 route/readiness 邻接 30 项通过。没有联网、模型调用、全测、构建、App 或生产数据库操作。
- 当前开发 HEAD 为 `228942a`；0.7 build15/`888aae5` 保持唯一稳定回退。0.8 暂停点、未提交前端第六批和恢复顺序见 `PROJECT_HANDOFF.md` 顶部，不能描述为已完成 App。

## 2026-08-12：0.7.0-preview.2 最终用户路径收口

- 增加版本化首次 AI 知情同意门，覆盖 Librarian、实际 DeepSeek 论文提取和私人表格建议；取消保持零网络请求，纯本地文献步骤不弹收费提示。
- 修复个人实验确认后的迟到搜索响应影响资料包视图，并将资料包导航收敛到共享 `switchView` 单一所有者；延迟 promise 运行时回归覆盖用户切页后不再滚动隐藏结果。
- 实机确认旧版空白截图不是数据删除；build 14 往返等待门通过。随后发现并修复 macOS 本机导出目录被 PyObjC Foundation mapping 误判为非本机的真实阻断，最终候选提升为 build 15。
- 共享核心 619、macOS 186、Windows 源码契约 125 项串行通过；生产 SQLite 和 `paper_056` 现场未进入测试、Git 或制品。最终制品、包和哈希记录在仓库外发布套件。

## 2026-08-11：macOS 0.7.0-preview.1 资料包中心内部发布

- 从干净源码 `4d1f4c8` 构建 Auto Research `0.7.0-preview.1`（bundle `0.7.0`，build `9`），并创建标签 `evidence-demo-2026-08-11-macos-package-center-0.7.0-preview-1`。冻结 App 冒烟、ad-hoc codesign、DMG 校验和构建清单均通过。
- 干净 worktree 串行通过 912 项联合测试（共享核心 605、macOS 182、Windows 125）；最终资料包与攻击回归另通过 92 项。生产 SQLite 与 `paper_056` 现场未进入测试、构建、资料包或 Git。
- 官方 `0.2.0-preview.1` 包 SHA-256 为 `89ec7f8dcdeea2d862d91aaf798674fd600c21fd75c4553d270a0c0b806f999e`，包含 60 篇论文和 4356 条四类证据，不含 PDF、图片、私人实验、路径或密钥。真实临时数据根完成 `0.1→0.2→重复0.2→回退0.1` 验收。
- DMG SHA-256 为 `968aa5936c90dfac5f0b50e8c96c6080ff87527e6d24e0cb7e08752b1958e133`。发布 ZIP 位于 `/Users/USER/Zotero/auto-research-releases/Auto-Research-0.7.0-preview.1-Kit.zip`，SHA-256 为 `baa03692a14665e6e1e4bc81b84564810183ec65a1024541c3bbb37c73282a18`。
- `/Applications` 中只保留一个当前 App；旧 `0.4.0` 与 `0.6.1` 移到仓库外备份。Windows 共享源码契约 125 项通过，但 `installer_ready=false / SETUP_PRESENT=NO`。

## 2026-08-09：0.7.0-preview.1 资料包中心源码候选与首轮架构拆分

- 共享 product 已落地官方包与两类用户包的统一资料包中心契约。`literature_collection` 和 `personal_experiments` 严格分离；用户包只做 SHA-256 完整性校验，明确未加密、来源未认证、仅限课题组内部，不能写入官方 active selector 或进入 Librarian。
- `package-job-v1` 将导出计划、权限确认、归档、检查、导入、完成和失败投影收敛为共享后台任务；平台只持原生 opaque token、任务调度和安全 HTTP 投影。发布身份由 `config/release-contract.json` 统一为 macOS `0.7.0-preview.1`、官方包 `0.2.0-preview.1` 与用户包 schema v1。
- 共享 UI 新增一级“导出 / 导入资料包”，覆盖官方版本/导入/回退、论文集合 selected/filtered/all、PDF逐篇权限、私人实验导出、用户包校验导入和 `code/stage/outcome/progress`。提交 `c2f2c06` 将资料包专属状态和行为从 `desktop_product.js` 拆入独立 `package_center.js`，不引入 Node 构建器，也不复制全局状态。
- macOS 提交 `6c00ca9` 已接入资料包中心 API、原生文件/目标 token、后台 runtime、官方/用户包服务与搜索刷新。导入论文集合后，搜索结果只按 `collection_kind/pdf_available/source_id/paper_uid` 显示 PDF 能力；PDF 通过受保护、无路径、同一文件描述符 lease 分块读取，私人实验不获得论文 PDF 入口。
- Windows 提交 `f649532` 复用同一共享 UI、资料包中心 DTO、后台任务和 PDF lease，并补齐原生保存位置 token；因为尚缺 Win11 安全 v12/private resolver 与真实 Setup，生产组合明确返回 `package_center_unavailable`，继续 `installer_ready=false`，没有复制共享打包或激活算法。
- `DesktopApplicationFacade + RouteSpec` 已有平台中立实现和契约测试，但仍是实验性迁移层，尚未替换 macOS/Windows 全部历史路由；不能因为类型已存在就宣称桌面路由治理完成。
- 本节是当时的源码候选记录；对应能力已在 2026-08-11 的 `0.7.0-preview.1` 内部发布中完成构建与验收。Windows 仍继续 `installer_ready=false`，没有 Setup。
- 本阶段没有重新抽取论文或改变科学数据完成度：固定50篇仍为17/50数据就绪、30/50图表就绪。

## 2026-08-09：macOS 0.6.0-preview.1 AI 辅助导入与文献处理发布

- 将“数据检查”和“上传文献”合并为“文献处理”，复用原有上传/当前文章 DOM、状态和后端；上传不自动产生模型费用，唯一收费动作仍需用户点击“开始自动提取与核验”。主导航固定为“文献处理、搜索数据、上传实验数据”。
- 个人 CSV/TSV/XLSX 新增平台中立 DeepSeek 待核验建议：只发送有界列信息和最多5行对齐样例，不发送路径、哈希、完整表格或密钥。用户只需修正错误并一次确认；后台保留草稿、revision/CAS、确认和搜索刷新门。
- 修复 AI 延迟响应覆盖用户编辑、识别中仍可确认、无条件逐列确认、空条件/空测量序列无法导入及暂存源文件变化等问题；安全审查确认 P0 关闭。
- 源码提交链：`bb698e0`、`81c25d3`、`6b298e0`、`a4720ba`、`7909100`。干净 release worktree 通过745项联合测试（479/157/109），最终 UI 另22项契约通过，并完成冻结 smoke、签名、DMG与实机界面检查。
- 唯一安装入口 `/Applications/Auto Research.app`，版本 `0.6.0` / build `7`。DMG：`/Users/USER/Zotero/auto-research-releases/Auto-Research-0.6.0-preview.1-macOS-arm64.dmg`，SHA-256 `57f782897fdd84262c1e1bf5553c7b0e81efaed707d064ebbfa8fa5aff039022`；可执行文件 SHA-256 `128627d8708c573c563313fb53503d2bf2ae984e39680d34ef5f13778f61d834`。
- Windows 共享源码已同步同一 UI 和核心服务，109项平台契约通过；仍为 `installer_ready=false / SETUP_PRESENT=NO`，等待 Win11 真机构建与全流程验收。

## 2026-08-09：macOS 0.5.1-preview.1 四入口工作台发布

- 新增独立一级入口“上传实验数据”，与“上传文献”同级；左栏现在固定为数据检查、搜索数据、上传文献、上传实验数据。“人工补录”和“修正历史”仅移除导航，底层旧数据与兼容处理器未删除。
- 新页面直接复用既有 CSV/TSV/XLSX 私人导入、逐列确认、revision/CAS、私人快照和联合搜索流程。确认或刷新成功后会切到可见的“我的实验”结果，不会后台搜索后留在空白页；官方资料包状态失败也不再阻止个人导入页面挂载。
- 功能提交 `12b253c`，版本提交 `7fa19f9`，测试契约修正 `63b34f2`。干净 release worktree 串行通过726项测试：共享核心467、macOS 154、Windows 105。
- 冻结 App smoke 验证 `primary_personal_import_navigation`、六路个人导入、联合检索、双原生选择器、Librarian V3、会话安全和旧工作区均进入二进制。随后在真实安装 App 中看到四入口，并点击验证独立上传页与“选择数据文件”按钮。
- 唯一安装入口为 `/Applications/Auto Research.app`（bundle `0.5.1`，build `6`）。上一 0.5.0 安装副本移至 `.app.rollback`，构建副本改为非 `.app` 后缀，避免系统重复识别。
- DMG：`/Users/USER/Zotero/auto-research-releases/Auto-Research-0.5.1-preview.1-macOS-arm64.dmg`；SHA-256 `7646527a9d7e59a64a11fdb9beb91f36b49b0eea12ae4d11b459db7e946b4529`。可执行文件 SHA-256 `7af68cc5933e2819c223cc41e81d986720f235ed70ecea50d1db4646bbe8c8bf`。
- Windows 未来制品会直接打包相同共享 UI，静态契约已验证四入口、同名原生选择器和去路径 DTO；但 Windows 仍无 Setup，且数据检查/上传文献生产路由未完成，继续保持 `installer_ready=false`。

## 2026-08-09：macOS 0.5.0-preview.1 内部预览发布

- 从干净源码 `9985386` 构建 Auto Research `0.5.0-preview.1`（bundle `0.5.0`，build `5`），并创建制品标签 `evidence-demo-2026-08-09-macos-workbench-preview-4`。旧 `0.4.0-preview.1` App/DMG 与 preview-3 标签继续保留为回退点。
- 干净 release worktree 串行通过722项测试：共享核心467、macOS 151、Windows契约104；另通过 Python 编译、`app.js`/`desktop_product.js` 语法、macOS脚本语法和差异检查。
- 冻结 App smoke 验证桌面会话/CSRF、加密历史、Search V2、导出、Librarian V3 状态字段、官方/私人联合检索、个人导入六路契约、BYOK readiness/credential 路由及双原生选择器均进入制品；临时 SQLite 前后哈希一致。
- App 通过 ad-hoc codesign、Info.plist 版本检查、DMG完整性校验与隔离 GUI 进程启动六秒无错误后关闭。DMG SHA-256：`cad6c8b8f2769b9006a0820fabf9ee22f03ca36ae5593f6f2e96adc88f2374a1`；可执行文件 SHA-256：`45db994969851ded745611899da4961a8964cbb5eae6c0cd530b91bc3bde947e`。
- 本次程序发布没有把科学数据库、PDF、截图、模型历史或开发者API密钥烘焙进 App。主工作区既有生产数据库与 `paper_056` 现场数据全程未暂存、未提交，也未作为构建测试数据。
- 该制品仍是依赖本机 checkout v12 的内部 macOS 预览；Windows 保持 `installer_ready=false`，没有 Setup 或 Win11 clean-machine 验收。程序稳定、语料完成度与科学准确率继续分别报告。

## 2026-08-09：Librarian V3、私人导入与共享工作台源码收口

- 当前源码检查点为 `64e0592`。Librarian V3 已接入共享前端：系统能力/普通对话走本地零召回路径，研究问题使用官方文献全库，签名 `research_state/state_token` 支持 R#/B# 多轮追问；私人实验和 `all` 不进入 Librarian 综合。
- `928c186` 完成共享搜索工作区：本地文献工作区继续使用 Search V2/Librarian，离线资料库以单次后端请求搜索 `official/private/all`，结果只使用 `item/table/figure/finding` 和稳定来源身份。私人导入 UI 固定执行选择、受限预览、草稿、明确复核、确认和搜索刷新，不显示路径或哈希，也不使用 `localStorage` 保存私人导入。
- `64e0592` 进一步绑定 `expected_revision/reviewed_revision`：用户复核后只要修改项目、样品、实验、条件、列定义或测量序列，旧复核即失效；renderer 只接收去路径、去哈希、去内部数据库/导入/草稿 ID 的公共投影。
- Windows 后端 parity 已在 `a134acd` 接入共享 HTTP bridge、原生资料包/私人表格选择、官方与私人联合搜索、私人导入、Librarian V3 bridge 和 readiness。源码能力不等于可交付安装包：`installer_ready=false`，没有 Setup，也没有 Windows 11 clean-machine 实机验收。
- 最后已构建的 macOS 制品仍来自 `f34bf68`（`0.4.0-preview.1` / preview-3）。上述 V3、私人导入和共享离线资料库源码晚于该制品；本轮不重写其版本、测试数或 DMG 身份，等待下一次受控构建与实机验收。

### `1b5ae67` 并行写入竞态复盘

- `1b5ae67` 在一次提交中同时落入 Windows 组合根、私人导入、联合搜索、Librarian bridge 和共享 webapp/UI 等多个所有权区域，暴露了并行任务共享工作树时的暂存与提交竞态。功能保留，混合提交方式不作为后续模板。
- 后续规则固定为：同一共享 checkout 同一时间只有一个 Git 写入者；其他任务保持只读；写入权显式交接；提交前只 stage 声明文件并打印 cached name list。生产数据库、`paper_056` 和现场运行产物默认隔离，除非用户单独授权数据 checkpoint。

## 2026-08-08：官方与私人联合搜索共享生命周期

- 新增平台中立 `FederatedSearchSession`，统一持有可选官方 source、可选私人 confirmed/indexable source 和既有 `FederatedEvidenceSearch` 不可变快照；macOS/Windows 后续只消费窄 Protocol，不再复制召回、排序、readiness 或 engine swap。
- official-only、private-only、both 三种组合均支持空查询浏览、精确关键词搜索、四类过滤和稳定身份 get；无 source 时 fail closed。结果继续只有 `item/finding/table/figure`，并原样保留 `source_scope/source_id/entity_uid`。
- 官方与私人可分别 install/replace/clear，私人 refresh 使用同一原子路径。所有变更先在旧 engine 之外构建新 engine，成功后锁内一次 swap；构建、身份或读取失败不替换旧可用组合。清官方不清私人，清最后一个 source 才变为未就绪。
- 新增 path-free `federated-search-readiness-v2`，只公开双源 readiness、总文档数和稳定 source ID/fingerprint；错误 DTO 不含路径或底层异常。本阶段未修改 desktop、web、personal、product、科学数据库或现有联合召回算法。

## 2026-08-08：Librarian V3 平台中立核心

- 在保护点 `auto-research-pre-librarian-v3-integration-2026-08-08` 之后实现本地七类意图路由和四种召回策略。系统能力与普通对话在索引指纹读取前返回，保持 0 召回、0 模型；宽泛研究问题进入本地 `review_map`，R#/B# 追问进入稳定 anchor 解析。
- 新增当前进程签名的 `research-state-v1`：绑定 conversation、turn、evidence fingerprint、state hash、父状态链和过期时间；公开状态仅含稳定 `source_scope/source_id/entity_uid/bundle_uid`，不含路径、内部异常或凭据。篡改、过期、跨会话、语料变化、未知/重复 anchor 和不同请求重放均 fail closed。
- 同一状态令牌只允许同一请求幂等重放并复用完整响应缓存；缓存缺失时拒绝再次产生模型费用。跨不兼容 bundle 的定量追问返回 `unsupported_comparison`，不会交给模型自由处理。
- 宽泛综述先本地聚类并限制每主题代表证据，再由 Pro 读取有界公开 DTO；候选文本中的提示注入不能改变意图、硬条件、候选、引用或预算。模型超时、非法/超长 JSON 时仍可生成确定性报告并解析稳定 anchor。
- 新增 `suggested-action-v1`，每条建议必须通过当前有界候选 dry-run 并给出可回答数量；未知 R# 与无证据领域漂移被剔除。旧 `answer/report/results`、五段报告和 `item/finding/table/figure` 保持兼容，只新增 V3 字段；本阶段未接 UI、desktop、personal、product，也未读取或写入生产数据库和 `paper_056`。

## 2026-08-01：macOS 0.4.0-preview.1 阶段收口

- Windows 最后收口提交 `3632377` 已生成 Win11 内部构建测试资料夹并同步到 OneDrive `Auto-Research-Windows-Internal-Test`。源码压缩包、官方资料包、单击入口、中文手册、身份文件和 READY 的 SHA-256 均复核通过；状态明确为 `BUILD KIT READY / SETUP_PRESENT=NO`，不冒充 Windows 安装包。Windows 82项定向测试通过，真实 production shared bridge、Setup 与 Win11 全流程冒烟留待下一阶段。

### 面向项目负责人 / 人类工程师

- Apple Silicon macOS 内部开发预览已完成官方资料包导入、只读联合搜索、原生单文件选择、BYOK 状态管理和统一桌面安全 bridge 接线。它仍依赖当前 checkout 的 schema-v12 可编辑工作区，不能描述为脱离源码即可分发的正式产品。
- 首个签名内部资料包约2.36 MB，SHA-256 为 `73672f94335604609d729671ab4a950e361b8cb569523a18980f0112c7c9f91d`；包含60篇论文元数据和4,356个实体（3,142条目、936结论、46表格、232图片），不含PDF和二进制图片。
- 最终干净 release worktree 串行完成591项联合测试：共享核心399、macOS 110、Windows 82；schema-v12 健康检查和稳定快照 SHA-256 `d62dc5c43ac9e0fb97e0ad2ecb85deaf50447fb7a036acae5147e8f6111236f6` 保持不变。
- 滚动状态修复后的 App 与 DMG 已重建并通过隔离 smoke、ad-hoc 签名、镜像校验和实机启动/204健康检查/干净退出。最终候选 DMG SHA-256 为 `89766b8efd8e2821abef0d7940dec513cd60ebaf2a36eea042adf784fa1483ee`。
- 旧浏览器工作台、导师只读页、固定端口和 ngrok 已退役，启动脚本只提供迁移提示。Windows 已完成受测模块和组合根，但尚无真实自包含 Setup 和 Win11 clean-machine 证据，必须最后收口。
- 产品构建稳定不等于语料完成或科学准确率已得到人工金标准：固定语料仍为17/50 data-ready、30/50 visual-ready，33篇仍需全文抽取。

### 面向 Agent / 后续工程师

- 当前制品标签：`evidence-demo-2026-08-01-macos-workbench-preview-3`；不要移动或重命名为稳定正式版。标签锁定实际构建源码提交 `f34bf68`，后续文档记录提交不改变该制品身份。
- 可编辑 schema-v12、只读 `distribution-sqlite-v1` 官方包和私人实验库是三个隔离数据域。官方包只允许通过 `OfficialEvidenceRepository` 审计后激活；不得 `ATTACH`、调用 `EvidenceDB.init()` 或写回科学库。
- 当前 ad-hoc macOS 预览的历史和 DeepSeek 凭据使用 Application Support 私有目录、独立随机密钥和 AES-256-GCM，避免代码身份变化触发系统密码框；正式签名发行才切换到 Keychain。Windows 使用 Credential Manager。
- 后续优先偿还运行时公共 API、跨平台资料包状态/错误契约、SemVer/包身份单源、官方库只读连接文件身份以及大型 product/desktop 模块拆分；不得在 macOS 与 Windows 中复制签名、审计或搜索算法。
- 生产数据库和 `paper_056` 现场产物仍是隔离的用户数据；代码提交、打包与测试均不得暂存、重置或借此“清理”它们。

## 2026-08-01：联合召回多词覆盖门修正

- 修正多词查询被单个通用词放行的问题：1–2 词要求全部命中，3 词及以上要求至少命中 `max(2, ceil(2/3 × 词数))`；候选先按覆盖词数、再按字段权重排序。
- `W` / `Ta` / `He` 等短元素符号改为独立 ASCII 词匹配，不再误命中 `preview` 或 `metadata_text` 等字段/值；空查询浏览和中文单词保持不变。4,356 文档合成回归中四词查询约 7.5 ms。

## 2026-08-01：内部产品安全基线与第一轮攻击修复

- 建立 `docs/SECURITY_MODEL.md`，将 API 密钥、用户/私人文献与实验数据、内部证据和可共享数据分级，并固定本地攻击范围、外发同意、发布阻断和事件响应规则。
- 模拟被污染的 `DEEPSEEK_BASE_URL` 把 Bearer 密钥重定向到攻击者主机；修复后仅允许受支持的 HTTPS `api.deepseek.com` 接收密钥，HTTP、其他主机、用户信息、非常规端口、路径、查询和片段均在发起网络请求前失败。
- 模拟恶意论文/模型字段进入 CSV 后触发 Excel 公式；所有 CSV 导出现在中和以 `= + - @`（含前导空白）开头的单元格，XLSX 继续使用非公式的 inline string，不执行外部内容。
- 未处理的服务端异常不再把底层异常文本、本机路径或实现细节返回给界面，统一返回稳定错误码。
- 共享核心与桌面服务入口都强制只接受 `127.0.0.1`、`localhost` 或 `::1`，即使绕过 CLI 直接调用服务构造器也不能绑定局域网/公网地址；PDF、CSV 和 Excel 响应统一标记为不可缓存。
- 本轮仅修改共享核心代码、测试和安全文档；没有修改生产 SQLite、PDF、图表、抽取结果、个人实验数据或当前未提交的可移植数据层。

## 2026-08-01：桌面产品方向与低负载联合收口

### 个人实验表格安全预览

- 建立个人实验数据与论文证据分离的契约：项目、样品、实验批次、测量序列、附属趋势图和用户备注属于私有域，不进入论文六列表；只有列含义和单位均由用户确认、关联完整且状态为 `confirmed` 的记录才能生成个人搜索投影。
- 新增受限只读 CSV/TSV/XLSX 预览器。文件大小、ZIP 条目/解压量、XML、工作表、行列、样例和单元格均有上限；解析不解压到磁盘，不写任何数据库或索引。
- 拒绝旧 `.xls`、宏工作簿、加密文件、外部链接、重复 ZIP 部件和 XML 实体；不执行 Excel 公式，只展示已有缓存值并返回警告。CSV 公式样文本保持原样，不执行。
- 自动建议表头含义、单位、数据类型和列角色，但 `meaning_confirmed`、`unit_confirmed` 与 `role_confirmed` 默认均为 false；路径不进入预览输出。所有测试文件仅在临时目录生成，没有读取用户真实表格或生产 SQLite。

### 私人实验仓库 v1

- 发布阻断 P1 收口：私人 schema 升级为 v2，首次初始化和 v1 迁移均使用原子事务；所有写操作使用 `BEGIN IMMEDIATE`，草稿增加 `revision` 比较交换，阻止陈旧草稿覆盖新版本或已确认记录。
- 文件身份强制为纯 basename，拒绝路径和 URI；私人根目录、`files/`、SQLite 和目的文件对符号链接、非普通文件及权限收紧失败均 fail closed。
- 同文件并发登记使用每操作唯一 staging；失败方只清理自己的临时文件或经 inode 确认且未登记的自有目的文件，不会删除成功方内容。测量序列同时禁止引用 `role=ignore` 列。
- 新增 basename/URI、符号链接、权限失败、schema DDL 回滚、陈旧草稿、并发 CAS、同文件登记竞态和登记故障注入回归；仅使用临时目录，未接入 web、desktop、product、官方库或联合召回。
- 新增显式 `data_root` 的独立私人仓库，只创建 `personal_experiments.sqlite` 和私有 `files/`，schema v1 覆盖项目、样品、实验批次、条件、列映射、测量序列、附件引用及项目/样品/批次备注；没有连接或 `ATTACH` 官方证据库。
- 文件保存前复核预览 SHA-256 与大小，只在私人目录记录相对路径；数据库符号链接、未知既有数据库、不完整/未来 schema、跨项目样品引用和变化后的文件均拒绝。
- 列角色新增 `role_confirmed`，与 `meaning_confirmed`、`unit_confirmed` 共同构成检索门。草稿可更新为已确认，已确认批次不可静默覆盖；搜索投影还会从 SQLite 再次核验全部确认标志。
- `personal-search-document-v1` 只返回私人仓库随机 `source_id`、项目/样品/批次语义、条件、列、测量序列、附件说明、备注和无路径文件身份；不返回 `data_root`、SQLite 路径或内部相对路径。目标测试全部使用 `TemporaryDirectory`。
- 私人导入状态固定为 `previewed → draft_saved → confirmed/indexable`，禁止跳过草稿直接确认；成功操作统一返回 `private-operation-result-v1`，明确状态、可检索性和是否实际发生变化。
- 新增 `PrivateRepositoryError` 的稳定 `private-repository-error-v1` 契约。错误详情仅允许受控白名单字段，不返回本机路径、SQLite 语句、约束文本或底层异常；桌面端后续只需按稳定错误码提供恢复动作。
- 确认操作继续使用单一 SQLite 事务，既有 `confirmed` 批次不可覆盖；状态迁移、确认门或写入失败均回滚并保留原可检索记录。本阶段未接入 web/API、公共索引、产品资料包或桌面端。
- 新增私人仓库只读搜索源 Protocol/DTO 和适配器。只有 `confirmed + indexable` 批次可被确定性拆分为测量序列 `item`、导入表格摘要 `table`、关联趋势图 `figure`、确认备注/结论 `finding`，不新增第五类对象。
- 四类私人文档携带 `source_scope=private`、稳定仓库 `source_id` 和不可反推内部主键的稳定 `entity_uid`，并保留项目、样品、材料、方法与条件；输出不含路径、文件哈希或 run/series/attachment/note/file 主键。
- 适配过程只读且测试验证前后私人 SQLite 字节一致；未 `ATTACH` 官方库，未接入 web、Search V2、桌面端、产品资料包或联合召回。

### 图书管理员模型分工、相关文章与抽取覆盖预警

- 官方能力核验确认 DeepSeek V4 Flash/Pro API 均为文本模型，不直接读取论文图像。抽取、独立验证和最终证据综合继续默认使用 V4 Pro；只有有本地规则兜底的 Librarian 检索式规划默认使用 V4 Flash。四个模型角色可分别配置，Flash 不会波及科学抽取或视觉语义链。
- 图书管理员响应升级为 `reasoning-presentation-v2`，新增顶层 `recommended_articles`：将本轮已召回的 item/finding/table/figure 按论文聚合，最多推荐六篇，显示 direct/adjacent/expansion 等级、条件覆盖、相关性质和支持 R#。它不新增第五类证据、不改变五段报告和 `agent_cited`。
- 相关文章区独立放在五段报告之后；对话历史仅自然保存该公开响应字段，没有修改桌面历史 storage labels、`readDesktopLibrarianHistory` 或 `loadLibrarianHistory`，也没有把研究简报授权写入历史。
- 初次只读审计发现 13 篇论文存在“有表格/图片资产但零可报告数值、零实验结论”的状态：12 篇为 `prompt_ready`、1 篇为 `benchmark_imported`，均没有正式 AI 抽取运行。随后 App 对 paper 56 的重复 PDF 启动但未完成预览抽取，新建 13 个图表资产，使现场动态计数变为 14 篇（另含 1 篇 `queued`）。这说明图表建档可先于全文抽取，不应解释为论文没有文本数据；paper 56 的运行/图表归属用户现场数据，本功能提交不纳入或处理。
- 推荐论文现在携带四类全库覆盖计数；只有 table/figure 而没有 item/finding 时，页面显示“全文证据抽取尚未完成”预警。本轮不重新抽取论文、不修改科学 SQLite、PDF、图表资产或审核历史。

- 产品入口正式收敛为个人桌面工作台：当前先交付 macOS 本机开发预览，正式用户端预计为 Windows。导师只读网页、独立浏览器工作台、8765/8766、ngrok 和浏览器启动器退役；既有 HTML/JavaScript、loopback webapp 与权限模式继续作为 App 内部实现和回归保护。
- 稳定 App 不应要求用户输入 Auto Research 编辑密码；意外系统凭据授权窗口被列为发布阻断。当时的正式发行规划为 macOS Keychain 与 Windows Credential Manager；当前 ad-hoc macOS 内部预览的实际存储边界已在本日志顶部更新为 Application Support 私有 AES-GCM 存储。
- 正式发行采用“桌面 App + 独立 `.aresearch` 证据包”：用户导入经过版本、哈希与签名校验的数据包后即可离线检索；官方包、用户私人库和 App 安装彼此分离。默认不含受限 PDF、本机路径、Zotero key、私人对话或开发者凭据，导入失败必须可回退。
- 新文献 DeepSeek 抽取和图书管理员 Agent 均采用 BYOK。用户可粘贴专门提供给他的 key 或使用自己的 key；首次 AI 调用需说明服务方、可能费用和数据外发范围，密钥只进操作系统凭据库，不进数据库、数据包、日志或 Git。
- 共享核心沿用最后完成的 253/253 自动测试与生产 SQLite SHA-256 `d62dc5c43ac9e0fb97e0ad2ecb85deaf50447fb7a036acae5147e8f6111236f6`。为降低电脑发热，本次只做文档、差异与单文件语法轻量收口；最终由桌面任务顺序执行一次联合全测、一次 macOS 构建和一次实机验收。
- 共享核心提交不创建稳定标签、不修改 `desktop/macos/**`、不纳入未跟踪数据库；最终桌面提交、标签、安装包和回退包由桌面任务统一生成。

## 2026-07-30

### 图书管理员研究简报稳定版

#### 人类工程师摘要

- 新增“导出研究简报”：只有当前服务进程 HMAC 签名、不是澄清回答、至少有一条实际引用且通过一致性门的五段回答可以转换为 `librarian-research-brief.md`。
- 聊天响应顶层加入 `research_brief` 信封：`snapshot_token`、`answered_at`、`evidence_fingerprint`、`eligible`、`ineligible_reason`；签名快照使用 `answered_at` 与 `evidence_version`。导出 POST 只接受当前进程签发的 `{snapshot, snapshot_token}`。
- 简报只收录规范 R# 唯一映射的 `agent_cited=true` 四类证据；澄清、零引用、孤儿/重复/无效引用、计数不一致或失效/篡改 token 都不会生成文件。它不是第五类结果，也不创建或修改科学记录。
- HMAC 只绑定规范化公开响应快照，不建立服务器端历史，不重新调用 DeepSeek，不重查 Search V2、SQLite 或 PDF。
- Markdown 固定包含研究问题、硬条件、五段报告、引用证据附录、模型/召回统计、引用完整性检查和明确限制。
- 导出采用公开字段白名单；本地路径/URL、Zotero 或设备 key、审核者、内部备注、桌面历史标识、PDF/图片载荷和模型协议不会进入文件。缺失题目、DOI、页码或原文片段保持为空，将状态设为 warning，并在文件中按 R# 展示缺失字段。
- 共享前端同时加入安全历史模式适配：Apple Silicon macOS 桌面产品层用 Keychain 管理密钥并以 AES-GCM 持久化；普通本地浏览器保留有限本地历史；公网只读端固定 `readonly-none`，不访问桌面历史或图书管理员 `localStorage`。任何模式都不写科学库。
- history schema 和保存策略完全不变；`research_brief` 授权只在瞬态 `state.librarianBriefAuth` 中存在，不进入 session meta/messages、`localStorage` 或桌面加密历史。重启或只恢复历史都必须重新检索后才能导出。
- 桌面产品层仍是独立打包边界；本轮核心提交只包含共享前端适配契约，不能表述为 `desktop/macos/**` 源码已随核心发布。
- 本轮核心回归最终为 253/253 项自动测试通过；只读 HTTP 仅对签名、合格、非澄清且有引用的回答下载 Markdown，其他写入接口仍返回 403。发布前对抗检查新增覆盖旧会话授权复活、科学计数法前缀误判、Unicode/HTML 单位绕过、模型协议混淆、跨 evidence bundle 比较、危险链接/本机路径清洗和 HMAC 类型碰撞。
- 独立只读浏览器验收中，“钨”问题召回 65 项（0 direct / 5 adjacent / 60 expansion），报告引用 5 项，研究简报 POST 返回 200，控制台无错误；刷新后历史为 0、导出禁用且无运行告警，证明 `readonly-none` 和瞬态授权没有持久化。
- 生产 SQLite 和稳定备份 SHA-256 均为 `d62dc5c43ac9e0fb97e0ad2ecb85deaf50447fb7a036acae5147e8f6111236f6`。健康口径保持 6,501 条原始记录、5,048 处可报告数值来源、3,142 个独立物理事实、937 条定性结论和 291 个图表资产。
- 固定语料仍为 17/50 data-ready、30/50 visual-ready；研究简报稳定不代表语料达到30篇，也不代表模型解释完成独立人工科学验收。
- 发布身份为 `2026.07.30-librarian-brief-stable.1`、schema v12；计划标签 `evidence-demo-2026-07-30-librarian-brief-stable-1` 在发布收口时创建。
- 稳定 SQLite 为 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-librarian-brief-stable-2026-07-30-v1.sqlite`；计划 Git bundle 为 `/Users/USER/Zotero/auto-research-backups/auto-research-librarian-brief-stable-2026-07-30-v1.bundle`，在本轮提交与标签完成后生成并补录 SHA-256。
- 面向项目负责人的独立摘要见 `docs/logs/HUMAN_ENGINEERING_LOG_2026-07-30.md`。

#### Agent 实现细节

- 当前进程 HMAC、资格门、R#/`agent_cited` 一致性、确定性排序、字段白名单、来源 warning、只读 HTTP 和历史后端适配的逐项实现/回归记录见 `docs/logs/AGENT_IMPLEMENTATION_LOG_2026-07-30.md`。
- 完整产品契约、输入结构、隐私/科学边界和验收清单见 `docs/LIBRARIAN_RESEARCH_BRIEF.md`。

### 图书管理员第三阶段推理升级与第四阶段回答呈现

- 在原“DeepSeek规划—Search V2四类召回—DeepSeek总结”稳定链路内增量加入确定性科研条件层，没有新建第五类结果、向量数据库或另一套科学数据。
- 将材料、辐照类型、粒子、温度、剂量/注量、物理量和样品状态解析为硬条件；同义词、中英文元素名称/符号只用于软召回。当前轮显式条件覆盖历史，并修复上一轮“中子”被错误继承到本轮“离子辐照”的跨轮混合。
- 硬条件只由本地确定性解析器与有界历史继承产生；DeepSeek 仅负责规划检索、选择有界证据和解释，不能创建或跨字段注入硬条件。补齐科学计数法注量及等价单位、`300 keV`/`300 K`、`Ni/He` 粒子与材料角色、`W` 功率单位与钨材料等边界回归。
- 候选按本地规则分为 `direct`、`adjacent` 和 `expansion`；相关证据只能缺一个硬条件并显示被放宽的条件。证据按论文、记录中的实际材料和完整实验条件成组，跨材料/温度/剂量/状态的数值不会自动组成前后比较。
- 后端输出固定五段式科研报告：直接结论、证据矩阵、相关证据、数据库空白和二至三个建议追问；保留旧 `answer/results` 字段兼容既有前端与历史。
- 页面新增硬条件标签、证据矩阵、放宽条件提示、建议追问按钮和最新轮 `[R#]` 到四类证据卡的跳转；历史轮引用只作记录。新问题提交时立即清空旧证据卡，避免把上一轮结果挂到新问题下。
- 条目、结论、表格、图片继续横向切换；卡片明确显示直接/相关/扩展、报告引用和证据组。预计进度与真实耗时分开，降低高频无障碍播报。
- 修复 Search V2 指纹长期引用不存在的 `visual_asset_versions` 问题；图表校对、质量候选和数据—图表关联变化现在能使索引及 Agent 缓存失效。索引格式升级为 v3。
- 新增统一公开 DTO，Search V2、精确条目/结论搜索、图表搜索/详情和图书管理员均不再返回本地路径、Zotero key、本机文章 key、审核者或内部备注。
- 增加模型完整性门：拒绝孤儿引用、把相关证据写成直接结论、证据中不存在的数值以及跨不兼容证据组的定量比较；模型上下文按四类轮转保底，不再因前排条目过长挤掉表格或图片。
- 真实 `deepseek-v4-pro` 查询在当前库召回四类共71项，分为4条直接、8条相关和59条扩展；生成3行证据矩阵、6项相关证据及3个建议追问。浏览器验证五段报告、引用跳转、四类切换和表格截图均正常。
- 本轮未重新提取论文、未修改六列科学事实、图表截图或校对历史。固定50篇仍为17/50数据就绪、30/50图表就绪；程序升级不改变语料完成度。
- 全量自动测试增至226项并通过；当前数据库仍为 schema v12、291个图表资产，其中243个是历史冻结基线而非当前总数。
- 改造前保护提交/标签为 `17e6f60` / `evidence-demo-2026-07-30-pre-librarian-reasoning-presentation-1`；保护 SQLite 为 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-pre-librarian-reasoning-presentation-2026-07-30-v1.sqlite`，SHA-256 为 `c9d63be31ad660294e52a7d093d37d7c4bbbfb22b3c2e5138005070f6f3b5038`。
- 发布身份为 `2026.07.30-librarian-reasoning-stable.1`，功能实现提交 `8e9c4c1`，标签 `evidence-demo-2026-07-30-librarian-reasoning-stable-1`。最终 SQLite、Git bundle 和独立 Skill 分别为 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-librarian-reasoning-stable-2026-07-30-v1.sqlite`、`/Users/USER/Zotero/auto-research-backups/auto-research-librarian-reasoning-stable-2026-07-30-v1.bundle`、`/Users/USER/Zotero/auto-research-backups/auto-research-evidence-maintainer-skill-2026-07-30-v2.zip`；各自 SHA-256 见相邻 `.sha256`。

### 阶段性交班与项目 Skill

- 新增 `PROJECT_HANDOFF.md`，将历史 Zotero/PDF 语料工作流与当前实验数据证据库工作流明确分开，并记录项目目标、阶段历史、当前数据边界、六列模型、证据分类、DeepSeek 对抗式质量门、本地图表链路、Search V2、图书管理员、只读分享、凭据、发布和回滚规则。
- 新增项目内可移植 Skill `skills/auto-research-evidence-maintainer/`。Skill 采用渐进加载：入口只保留任务路由和核心不变量，当前状态、工作流、禁止事项、运维发布、历史决策分别放入按需读取的 references，降低新账号恢复项目时的上下文消耗。
- Skill 已通过官方 `quick_validate.py` 校验，并通过软链接安装到当前账户的 `~/.codex/skills/auto-research-evidence-maintainer`；项目内 Git 跟踪副本是权威来源。
- 生成独立 Skill 压缩包 `/Users/USER/Zotero/auto-research-backups/auto-research-evidence-maintainer-skill-2026-07-30-v1.zip`，SHA-256 为 `ea3198cf0b9d85e880e85c1bccc632c346e8bc8ea6e7d3d4a12e77ad22f5e747`；压缩包以技能目录为根，可直接解压到新账户的 `~/.codex/skills`。
- 生成交班时 SQLite 快照 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-project-handoff-skill-2026-07-30-v1.sqlite`，SHA-256 为 `b1c9703f46577e6caac1246ca79a89f02ac4817a5ff93cbfae52b35680d264ee`。
- 在 `AGENT.md` 和 `README.md` 增加交班入口，明确300篇 Zotero PDF 检查点不等于证据库已经处理300篇；当前固定50篇仍为17篇数据就绪、30篇图表就绪。
- 本轮只增加文档和 Skill，不重新提取论文、不修改科学证据、不重建图表，也不改变数据库结构。

## 2026-07-29

### 图书管理员覆盖召回与回答稳定性修复

- 复现用户实际问题：旧版后端已召回 18 项候选，但页面沿用“表格”标签只显示 2 项；同时 DeepSeek 最终 JSON 总结失败，页面只能显示机械候选计数，造成“只有两条证据且 Agent 不智能”的双重误导。
- 将自由工具循环改为三段式流程：DeepSeek 规划短检索式、本地 Search V2 对条目/表格/图片/结论做覆盖性召回、DeepSeek 对候选进行条件核验与中文总结。模型规划失败时仍执行本地保底检索式。
- 增加复杂问题分解与有限概念扩展；例如“缺陷结构”会在 Agent 召回层扩展为位错环、空洞、气泡、位错密度和微观结构，但不改变精确检索规则。
- 候选结果不再收缩成少量引用卡片：页面同时显示召回总数和回答引用数，四类结果分别计数，引用卡片优先并标记“回答引用”，其余标记“扩展候选”。新问题默认打开回答引用最多的类型。
- DeepSeek 总结必须区分同时满足全部条件的直接证据与部分相关证据；JSON 总结失败时依次使用无工具文本总结和确定性引用摘要，DSML/内部协议仍不可进入页面。
- 真实问题“高熵合金在中子辐照后，硬度和缺陷结构有哪些变化？”多次运行均召回 58 项候选；发布服务的参考回答引用 6 项，并明确报告没有直接证据。第二次相同请求命中缓存，除 `cache_hit` 外的完整响应哈希一致。
- 增加数据库源指纹约束的一小时稳定结果缓存：相同问题和相同有限历史会整体复用回答、引用和候选，避免重复调用造成引用数量漂移；证据变化后自动失效。规划与总结温度固定为 0。
- 全量自动测试增至 196 项并全部通过；本轮只修改检索编排、只读结果展示和文档，没有重提取或改写科学数据。
- 最终数据库快照：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-librarian-recall-stable-2026-07-29-v1.sqlite`，SHA-256 为 `c9d63be31ad660294e52a7d093d37d7c4bbbfb22b3c2e5138005070f6f3b5038`。

## 2026-07-28

### 图书管理员工作台与 DeepSeek 协议稳定化

- 将图书管理员置于搜索页主入口，精确检索保留为进阶模式；Agent 固定检索整个证据库，不再显示“全部文章/部分文章”范围控件。
- 新增浏览器本地对话记录：可新建、检索、恢复和删除历史会话；恢复历史不调用 DeepSeek，新问题只携带最近有限对话以控制用量。
- Agent 结果改为横向“条目 / 表格 / 图片 / 结论”标签，一次只展示一种类型；最终只返回回答实际引用的证据，减少无关卡片。
- 新增加载阶段、真实耗时和进度条；动画改用用户本机 Codex 宠物的 6 帧工作姿态。精确检索和科学数据不依赖该动画资源。
- 修复 DeepSeek 偶发把 DSML 工具协议作为正文返回的问题：后端解析并执行合法 DSML 调用，历史不再回传内部协议；每轮至少执行一次新检索，禁止沿用无绑定证据的旧 `[R编号]`。
- 工具循环结束后改用独立的受约束 JSON 通道生成中文总结；页面同时拦截旧历史中的协议文本和孤立引用，并安全渲染标题、加粗、列表、表格和引用徽标。
- 真实浏览器回归：异常历史中的 DSML 已隐藏；新查询返回正常中文答案，无协议文本，并绑定实际图片/结论证据。Codex 宠物加载动画可见且无空白帧。
- 193 项自动测试、SQLite v12 健康检查、目标论文 403/403 原文高亮、CSV/Excel、Search V2 基准和用户需求自检全部通过；未修改任何科学数据、图表记录或校对历史。
- 最终数据库快照：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-librarian-workspace-stable-2026-07-28-v1.sqlite`，SHA-256 为 `b1c9703f46577e6caac1246ca79a89f02ac4817a5ff93cbfae52b35680d264ee`；稳定标签与 bundle 使用 `librarian-workspace-stable-2026-07-28` 命名。

### 高速检索与图书管理员稳定版

- 根据导师“自然语言整句搜不到、关键词搜索偏慢”的反馈，新增 Search V2 可重建索引；现有六列、定性结论、图表、证据和校对历史保持权威且未被重写。
- 搜索页默认改为“问图书管理员”，用户可直接描述研究问题；精确检索降为进阶入口但完整保留四类模式、筛选、详情、原文证据和导出。
- 图书管理员使用项目 DeepSeek API 和只读工具调用，只返回数据条目、原始表格、论文图片、实验结论四类现有对象，并以 `[R编号]` 连接真实结果。
- 代表性查询从约 0.98–1.18 秒降至约 0.018–0.064 秒；中文整句与关键词写法能够得到相同核心检索词。索引首次全量构建后，普通数据修改只刷新变化论文。
- 完成真实 DeepSeek 联调：模型执行 6 次受控检索，正确指出库内没有高熵合金中子辐照直接数据，并将离子辐照结果明确标注为参考。
- 本地编辑端和公网只读端继续共用同一页面、数据库与图书管理员接口；公网仅通过权限层禁止上传、校对和重新抽取。
- 新增 `evidence-search-reindex`、`evidence-search-benchmark`、Agent 注册表、工具注册表、调用上限和公网速率限制，为后续 Agent 留出稳定扩展接口。
- 预改造标签、数据库快照、Git bundle 和界面截图已在正式修改前生成。详细人类工程日志见 `docs/logs/HUMAN_ENGINEERING_LOG_2026-07-28.md`，Agent 实施日志见 `docs/logs/AGENT_IMPLEMENTATION_LOG_2026-07-28.md`。
- 最终回归为 191/191 项测试通过；SQLite v12 完整性、外键、发布关系、图表文件、原文高亮、CSV/Excel、编辑端与只读端权限均通过。
- 最终数据库快照：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-librarian-search-stable-2026-07-28-v1.sqlite`，SHA-256 为 `c9d63be31ad660294e52a7d093d37d7c4bbbfb22b3c2e5138005070f6f3b5038`。

## 2026-07-27

### 全项目健康检查与稳定发布收口

- 冻结所有后台服务后完成 SQLite、固定语料、目标论文、编辑端、只读端、代码、启动脚本、依赖、Git 对象和凭据边界体检；187 项自动测试通过，本地编辑端与只读端真实检索通过，只读写请求返回 403，调用前后数据库 SHA-256 一致。
- 修复批量质量任务中断后状态残留：`Ctrl+C` 会将当前论文和批次写为 `interrupted`；新增 `evidence-reconcile-runs --older-than-hours 6`，用于结束硬中断留下的抽取/质量/处理运行元数据。该命令不修改六列数据、图表、证据或校对历史。
- 成功的 DeepSeek/质量运行现在清除旧 `error_message`，维护命令同步清理历史“已完成但残留错误”记录；本次收口修正 2 条已完成运行的旧错误文字。
- 修复目标审计的口径错误：单篇工作流通过只代表自动功能可用；项目最终目标还必须达到至少 30 篇真实 PDF 的数据、证据、搜索与图表验收。当前真实状态为固定 50 篇 PDF 身份 50/50，通过完整数据门 17 篇、图表就绪 30 篇、待提取 33 篇，因此初始语料目标尚未完成。
- 当前数据库健康快照：60 篇论文、60 个文档、6,501 条原始记录、5,048 处可报告数值来源、3,142 个独立事实、937 条定性结论和 291 个图表（59 表、232 图）；完整性、外键、必需索引、发布关系和图表文件均通过。
- 目标 DOI `10.1016/j.jnucmat.2018.08.031` 当前为 231 个事实、403/403 处自动证据可高亮、4 表和 10 图；`温度`、`硬度`、`钨`、作者检索与 CSV/Excel 通过。
- 新增 `MAINTENANCE_WORKFLOW.md` 与 `STAGE_AUDIT_2026-07-27.md`，重写当前稳定版和教师展示说明，更新 `AGENT.md` 的 50 篇固定集、运行收口和发布规范。网页发布身份更新为 `2026.07.27-project-health-stable.1`。
- GitHub 预审确认当前仓库约 379 MB、Git 对象约 149 MB，且包含生产 SQLite 与论文图表截图；本轮不擅自上传。推荐先选择“私有完整开发仓库”或“公开代码 + 脱敏演示数据”后再配置远端。
- 最终 SQLite 快照：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-project-health-stable-2026-07-27.sqlite`，SHA-256 为 `02ea16c62239690bc9db890d3ee52e4634ffff55d13948105d6392b044d2527c`；稳定标签为 `evidence-demo-2026-07-27-project-health-stable-1`，完整 bundle 为 `/Users/USER/Zotero/auto-research-backups/auto-research-project-health-stable-2026-07-27.bundle`。

## 2026-07-22

### 50 篇真实性测试集与识别/检索规则基线

- 将活动测试集扩充为 50 篇真实本地 PDF：保留旧 35 篇中通过“内容有效 + 论文身份一致”双门禁的 30 篇，剔除 5 个下载/验证占位文件，并从本地 Zotero 补入 20 篇经 DOI 或题目核验的论文。固定清单为 `config/evidence_test_set_50.json`，同时保存 JSON/Markdown 清单，选择器只使用 DOI 或题目。
- 50/50 文件均可打开、具有可提取正文且身份核验通过；其中 13 篇实验研究、6 篇计算研究、1 篇综述来自本轮新增语料，用于专门验证“实验测量、计算输出和综述引用不可混淆”的规则。
- 新增登记脚本在写入数据库前强制检查 PDF 格式、占位页、文字层、文件指纹和 DOI/题目一致性；不再因为 Zotero 中存在附件记录就默认它是真实论文。
- 将对抗式质量批处理和完整重提取的默认配置统一切换到 50 篇清单；全部新文章已建立文档记录和可恢复任务，正式 DeepSeek 处理仍按逐篇状态文件执行。
- 搜索规则仅在“具体意义、文章语境、题目和作者”等自然语言字段保留拼写容错；数值、单位、DOI、原文片段和聚合搜索文本改为精确/别名匹配，继续支持“钨/W/tungsten”等元素别名，降低短词和长证据产生的误命中。
- 证据定位增加同页版面窗口缓存；固定集验收查询直接限定到当前论文，不再先对全库排序后过滤。185 项自动测试通过，SQLite v11 健康检查通过；当前数据库为 57 篇、5,198 条原始记录、2,752 个独立数值事实、819 条定性结论和 273 个图表文件。
- 首轮 50 篇审计确认语料完整性通过，但功能完成门尚未通过：17 篇数据就绪、30 篇图表就绪、33 篇仍待抽取。该状态是后续 DeepSeek 对抗式处理的真实起点，不作为 50 篇完成版发布。

### DeepSeek 长文质量流程可靠性修复

- 50 篇首次真实批处理暴露出并发放大问题：外层两个对抗分支内部又分别并发三个专项请求，峰值可达六个并发，轻量连通测试虽成功，长文请求仍会发生连接失败或限流。发现连续失败后立即中止批次，未删除或覆盖原有稳定数据。
- 质量门改为严格的两路并行：完整性分支 A 与精确性分支 B 继续同时运行，满足对抗式独立抽取要求；每个分支内部的分块、专项和核验按顺序执行，避免隐藏的六路突发请求。
- 生产客户端由两次立即重试升级为四次指数退避，连接超时与长响应读取超时分离；状态页公开超时和尝试次数但不暴露密钥。错误记录现在保留 `Timeout`/`ConnectionError` 等安全类型，便于区分限流、网络与 JSON 问题。
- 对 DOI `10.1557/s43578-020-00071-8` 完成两页真实端到端复测：双路和第三审共评估 38 个候选，20 个双路通过、18 个第三审通过、0 个残留低分项；30 个条目及 8 个图片语义对象自动发布，证明抽取、复核、搜索入库和旧截图复用链路恢复正常。

### 文献身份与研究模式识别规则升级

- 将“PDF 能打开”升级为“文件真实性 + 文献身份一致”双门禁：继续检查文件指纹、占位页与正文有效性，并新增 PDF 首几页 DOI、题目词元覆盖和 PDF 元数据题目比对。只要 PDF 与登记论文不一致，即使文件格式正常也不能进入测试集通过状态。
- 将旧的单层实验关键词分类扩展为五种研究模式：实验研究、实验与计算联合研究、计算模拟/理论研究、综述/报告、类型不明确。`irradiation`、`ion`、`TEM` 等领域词不再单独证明论文是实验研究。
- 明确识别第一性原理/DFT、分子动力学、多尺度建模、Phy-X 与 SRIM 程序计算；纯计算论文的 DeepSeek 候选若标为直接测量，会在本地证据验证前被拒绝。混合论文继续逐条区分 measured、derived 和 calculated。
- 新增“散射/束流测量实验”类型，避免把 Rutherford 散射截面实验误归为辐照损伤实验。现有 Andersen 1980 论文识别为散射/束流实验；BISON 建模、U3Si2 第一性原理和 Phy-X/SRIM 屏蔽研究识别为计算研究。
- 抽取提示包、DeepSeek 全文抽取和网页研究类型卡统一读取同一识别结果；计算论文优先检索模型、边界条件、收敛设置和计算输出，不再使用只面向实验论文的固定提示。
- 现有 35 篇审计新增 `pdf_identity_match`、研究模式、模式置信度和评分证据。30 个真实 PDF 全部通过 DOI/题目身份匹配；原有 5 个下载/验证占位文件仍如实失败，没有被新规则放宽。

### DeepSeek Pro 统一证据工作区与图注识别修复

- 重构搜索详情交互：AI 不再作为独立的全局左侧抽屉。用户打开数值、表格或图片后才出现统一证据工作区，左侧为当前对象的对话，右侧为该对象的数值、语境、原文证据或完整图表详情。
- 本地编辑端与只读分享端共用同一搜索和详情组件。只读端允许无数据库写入的 `/api/context-chat`，但上传、校对、修正、重新抽取和快照等写操作继续返回 403；真实调用前后数据库 SHA-256 保持一致。
- DeepSeek 抽取、分析和证据对话的内部默认模型统一为 `deepseek-v4-pro`；对话请求显式走 Pro 抽取通道，避免旧环境中的 Flash 分析配置意外接管。
- 保留默认问题“说明这个数据本身的含义，并总结该数据在文章中的具体含义”，用户可直接发送、修改或清空。真实本地端与只读端调用均返回中文解释、PDF 页引用、证据说明和边界条件。
- 修复上传论文 `10.1088/1361-648x/ac519a` 漏掉 Figure 3：旧规则看到图注正文以 `(a)` 开头就误判为正文引用。修复后 Figure 3 完整包含两个子图、坐标轴、图例和原始图注，已进入图片搜索并由 DeepSeek Pro 生成中文标题、解释和标签。
- 将单点修复提升为通用图注规则：支持 `Figure 3. (a)…`、`Figure 3(a)…`、图号单独一行而图注续行；先拼接跨行图注，再剥离子图标记判断剩余语言。`Fig. 3a`、`Fig. 3(a) shows…`、`Table 1 lists…` 和图表目录仍会被排除。
- 页面增加轻量动效、证据工作区入场、检索光带和响应式双栏布局；尊重 `prefers-reduced-motion`，不让动效影响科研内容阅读。
- 完整自动测试 174 项通过；数据库健康检查为 37 篇、5,198 条原始记录、2,752 个独立事实、819 条定性结论和 273 个图表文件。论文 53 为 5 个表格、8 幅图片，Figure 3 可用 `U3Si2 Xe Cs 溶解能` 检索。
- 35 篇冻结语料审计仍如实报告 30 个有效 PDF、17 篇数据就绪、30 篇图表就绪；5 个无效 PDF 与 13 篇待抽取使全语料完成门不通过，因此本次仅发布维护检查点，不虚假提升基础稳定版身份。
- Git 检查点：`evidence-demo-2026-07-22-pro-evidence-studio-checkpoint-1`；完整 bundle：`/Users/USER/Zotero/auto-research-backups/auto-research-pro-evidence-studio-2026-07-22.bundle`；SQLite 快照：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-pro-evidence-studio-2026-07-22.sqlite`。

## 2026-07-19

### 条目级 DeepSeek 证据对话

- 在数据条目、原始表格和论文图片的搜索结果中增加“AI 解读”；只有用户主动点选某个对象后，页面左侧才展开对话区。默认问题为“说明这个数据本身的含义，并总结该数据在文章中的具体含义”，可直接发送、修改或清空。
- 对话上下文由当前结构化条目、论文题目/DOI、原始 PDF 定位页及按问题检索到的少量相关页组成；不发送整份数据库或其他论文。每次回答要求区分原文陈述与上下文解释，并返回引用页、证据说明和适用边界。
- 多轮对话只保留当前浏览器会话内最近的有限历史；刷新页面后清空。该功能不写入六列数据、图表元数据、审核状态或版本历史，也不从图片推测曲线数据点。
- AI 对话仅在本地编辑端显示。只读分享端隐藏入口，并由后端拒绝该 POST 请求，避免公网访问者消耗项目 DeepSeek 额度或触发 PDF 内容传输。
- 新增 PDF 页文本缓存、问题与历史长度上限、扫描件无文字层提示，以及未配置、网络不可用、模型响应异常的分级错误信息；继续复用项目钥匙串中的 DeepSeek 配置。
- 新增条目/图表上下文测试、无数据库写入测试、只读路由和网页契约测试；172 项自动测试通过。真实浏览器已验证数据与表格入口、左侧 480 px 对话面板、默认提示词和只读端隔离。
- 使用条目 #1174 完成一次真实只读烟雾测试：`deepseek-v4-flash` 返回 213 字中文回答，引用 PDF 第 1–3 页；调用前后 SQLite 文件哈希一致。

## 2026-07-18

### 科研排版、多文章检索与无人工门槛维护版

- 数值搜索卡片采用 Times New Roman / STIX / Cambria Math 字体栈，科学计数法、幂指数和正负号使用语义化上标；单位字号提升到与数值一致。显示层转换经过 HTML 转义，数据库中的原始值和单位未被改写。
- 搜索范围从单篇校对导航中拆分出来：默认检索全部数据库，也可按题目、DOI、一作或通讯作者勾选多篇文章。选定范围统一约束数值事实、实验结论、原始表格、论文图片及 CSV/Excel 导出；只读端使用不含本地路径和 Zotero key 的安全论文目录。
- 图表搜索卡片按“材料/对象 + 研究重点”两级展示既有元数据；新论文的 DeepSeek 图表标题提示改为“研究对象/材料 + 关键条件或对比 + 物理量/图表类型”，优先写明材料与对比双方。此次未重新抽取既有论文或批量改写图表记录。
- 用户流程不再以人工核验作为发布门槛：双路一致与第三次复核通过的候选自动进入搜索，剩余低分候选自动隔离；校对页保留为可选纠错工具，历史 `manual_review` 状态仅作兼容性审计。
- 更新用户界面措辞、自检、目标审计和交接报告，清除“必须逐条人工放行”的旧产品假设。新增多文章范围、只读目录安全、科学排版、图表标题合约和自动质量门回归测试。
- 170 项 Python 测试全部通过；数据库健康检查通过，4,754 条原始记录、2,484 个独立事实、770 条实验结论和 243 个图表文件均可读；目标论文 219/219 处自动数据证据可高亮定位。维护过程中未调用 DeepSeek、未重新抽取文章。

### 恢复图表中文语义编目并接入对抗式质量门

- 按 `AGENT.md` 和项目原有图表编目实现恢复中文工作语言：`label` 继续保存不可变的 Figure/Table 原文编号，`display_name` 为基于图注与相邻正文的中文短标题，`caption` 保留出版商原文，`context_explanation` 单独保存中文解释。
- 修复对抗式质量管线绕过既有中文提示词与清洗器的问题；双路图表语义、第三次复核和单独图表重建现在共用同一个提示与校验规则，英文编号标题、纯英文解释及空泛标签不能再自动发布。
- 恢复并强化智能标签：每个图表至少两个专属检索标签，排除“材料、方法、原文图片、原文表格”及其英文占位词；W、TEM、SRIM、dpa 等公式、元素符号和通用缩写保持不翻译。
- 修正图表完整度评分边界：中文标题、中文解释和专属标签为必填；物理量、变量、材料、条件和方法仅在图注或上下文明确支持时填写，缺失不能通过猜测补齐，也不再被错误视为必然低质量。
- 新增 `evidence-quality-visuals <文章>`，对既有稳定截图只重建可检索语义；两路 DeepSeek 并行生成，分歧项由第三位 DeepSeek 给出修正版，截图、哈希、原始图注、PDF 定位和人工版本均不重新生成或覆盖。
- 对固定十篇测试集的全部 91 个图表完成真实 DeepSeek 专项重建：91/91 有中文标题、91/91 有中文上下文解释、91/91 有至少两个专属标签，0 个标题退化为仅 `Figure/Table` 编号；“空洞肿胀、He气泡、辐照条件、位错”等中文检索实测可命中。
- 修复前数据库备份：`/Users/USER/Zotero/auto-research-backups/experimental_evidence-before-chinese-visual-repair-2026-07-18.sqlite`；SQLite 完整性、外键、243 个图表文件和质量发布关系检查通过。

### 阶段性完整性检测与展示资料校准

- 依据 `AGENT.md`、稳定版文档、项目日志和长期项目边界逐项核验代码、SQLite、十篇/全库语料、图表文件、编辑端、只读端、搜索、导出、原文证据、启动脚本、安全头和 Git 可恢复性；详细结果保存于 `STAGE_AUDIT_2026-07-18.md`。
- 166 项自动测试、Python/前端/启动脚本语法、SQLite 完整性与外键、质量运行产物、243 个图表文件及冻结哈希、十篇功能回归、编辑端和只读端真实页面均通过。
- 修正网页仍展示 `2026.07.16` 旧发布身份的问题，使其与 `STABLE_RELEASE.md` 的 `2026.07.18-chinese-visual-semantics.1` 一致；同步更新单元测试。
- 重写过期的 `TEACHER_DEMO.md`：当前为 35 篇、4,754 条原始历史、2,484 个独立事实、243 个图表；重点论文为 225 个事实和 339/339 处可定位数值证据。
- 核实十篇功能回归为 10/10，但双路 DeepSeek 全文质量门实际完成 8 篇，2 篇因网络失败；修正稳定版中此前“10 篇成功、失败 0”的过度表述。
- 全库审计当前为 17 篇数据就绪、13 篇有效 PDF 待抽取、5 篇无效占位文件；243 个图表中 135 个具有中文标题/解释，剩余 108 个仍待语义补齐；当前事实层人工审核进度为 0/2,484。上述均列为规划缺口，不伪装为验收通过。

## 2026-07-16

### DeepSeek 双路对抗式抽取与自动发布质量门

- 新增两个互不共享候选的 DeepSeek 并行分支：完整性审计分支重点防漏，精确性审计分支重点防单位、样品、条件和定性/数值混淆；两者均保留已有分块抽取、覆盖缺口、原文证据门和独立核验。
- 新增候选级五维评分：双路一致性、事实支持、字段完整度、证据位置和综合分。默认 85 分以上且双路同时支持的候选标记为 `dual_pass` 并自动发布。
- 低分或单路候选自动进入第三次 DeepSeek 原文页复核；通过者标记为 `third_pass` 并发布，仍低分、无返回或服务失败者标记为 `manual_review`，不进入搜索。
- 校对页新增“质量待审”页签，可并排查看最佳候选、另一分支、原文位置、五维得分和拦截原因；人工采用后标记 `manual_approved` 并发布，不采用则保留为 `rejected`。已决定候选禁止再次覆盖决定。
- 数值、实验结论、表格和图片搜索统一显示质量来源与得分，并支持筛选双路通过、第三次通过、人工通过和历史稳定数据；CSV/Excel 导出继承当前质量筛选。
- 旧的网页 DeepSeek 入口、文章工作流和 `evidence-deepseek-extract --commit` 均改为调用质量门，消除单路自动提交旁路；单路命令不带 `--commit` 时只作诊断预览。
- 数据库结构升级到 v11，新增质量运行和候选审计表；迁移前备份保存于 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-before-quality-gate-2026-07-16.sqlite`。迁移后仍为 35 篇、3,168 条历史记录和 243 个本地图表，质量表初始为空。
- 真实 DeepSeek 在数据库副本上完成一页烟雾测试：11 个候选中 8 个双路通过、2 个第三次复核通过、1 个 84 分结论进入人工审核；7 条数值与 1 个图片语义对象发布到副本，正式库保持 0 次质量运行。
- 验证通过 161 项自动测试、Python 编译、前端语法、SQLite 完整性与外键检查；十篇固定回归集 10/10 通过，仍为 1,313 个独立事实、1,609 处数值证据和 91 个图表。
- 修复旧试点标签造成的假告警：首版清单中的 `HEA` 与 `RHEA` 分别归并到既定 HEA/CCA 和 W 基难熔合金两半，30 篇试点重新通过 15+15 平衡验收。

### 撤销云端图表增强并恢复本地稳定方案

- 根据实际效果评估，MinerU 云端图表增强未达到本项目的质量要求；本次完整反向撤销相关三次实现提交，恢复到引入云端增强前的本地图表证据架构。
- 活动数据库恢复为结构 v10，不保留云端解析运行、候选、来源版本或搜索覆盖；界面、接口、命令行、健康检查和测试中对应的云端功能均已移除。
- 删除当前云端图片缓存与配置入口，并移除 macOS 钥匙串中的 `auto-research-mineru` 项目凭据；搜索、校对、只读分享和原文跳转重新只使用现有 243 个本地稳定图表资产。
- 本地稳定边界保持不变：PyMuPDF 从真实 PDF 裁剪高分辨率图表，DeepSeek 只读取图注及相邻文字进行语义编目，不读取图像像素、不生成曲线数据点。
- 撤销前数据库已保存到 `/Users/USER/Zotero/auto-research-backups/experimental_evidence-before-cloud-rollback-2026-07-16.sqlite`，仅用于恢复与审计；被否决的云端版本也只保留在旧 Git 标签和旧 bundle 历史中，不影响当前运行。
- 发布身份更新为 `2026.07.16-local-visual-stable.1`；158 项自动测试、前端与启动脚本语法、Python 编译、Git 对象检查和 SQLite 健康检查全部通过。冻结清单中的 243 个图表身份逐项一致，243 个图片文件哈希零变化；十篇回归集 10/10 通过，共 1,313 个独立事实、1,609 处数值证据和 91 个图表。

## 2026-07-15

### 本地迁移与一键启动修复

- 活动项目已从 iCloud Drive 迁移到 `/Users/USER/Zotero/auto-research`；Git 完整历史、SQLite 数据库、DeepSeek 产物、图表证据和网页资源保持一致。旧 iCloud 目录仅作迁移备份，不再用于运行或开发。
- 定位启动失败的直接原因：Python 在初始化时读取 iCloud 中未本地化的缓存文件，服务长时间未监听 8765 端口，启动器同时持续输出连接错误。
- 本地编辑端和只读公网端统一使用 `~/Library/Caches/AutoResearchEvidence`保存 Python 缓存与启动日志；启动等待不再显示连续的 `curl` 错误，且可在服务提前退出时显示真实日志。
- Zotero 根目录中的两个一键入口已指向本地项目：`打开本地编辑工作台.command` 和 `创建导师公网链接.command`。
- 迁移验收通过 157 项自动测试、SQLite 完整性与外键检查、前端语法、启动脚本语法和 Git 对象检查；本地编辑端已用根目录一键入口启动，只读端的搜索和原文高亮接口亦通过，访问后数据库指纹不变。
- 发布身份更新为 `2026.07.15-local-stable.1`；数据库结构仍为 v10，本次迁移不改写科学数据。

## 2026-07-14

### 图表证据重构与全库质量重提取

- 图表证据将不可变的原文编号、简短中文名称、原始图注和中文证据解释分为独立字段；校对页可修改检索名称与解释，但不改写原始图注。
- 新增 DeepSeek 图表编目：仅把 PDF 文字层中的图注与相邻正文交给模型，生成简短中文名称、物理量、材料、条件、方法、解释和 2–7 个专属标签；模型不读取图像像素，不从曲线猜测数值。
- 修复图注识别与裁剪：拒绝把 `Fig. 8(b)` 等正文引用识别成图，合并出版商拆成多个文本块的完整图注，分离同页左右相邻图，支持“图在前一页、图注在后一页”，并使表格在下一幅图之前停止。
- 表格裁剪新增真实表格边界优先级；排除带页码引导点的表格/图片目录页和 `Table 1 lists...` 等正文引用。搜索卡片改为完整宽高比缩放，避免原文件正确但网页预览只显示表头。
- 已对用户报告的图表回归样例进行高分辨率重建与目视检查：Figure 6 不再混入相邻 Figure 7，Figure 8 恢复为真实原子结构图，Figure 9 不再误收 Figure 8 与正文，Figure 1/2 同页分离；Table 2、ODS-NiCoFeCr Table 1、SiC Table 1/Table 7 和涂层成分 Table 1 均包含核心数据且无整页正文污染。
- 数据库结构升级为 v10；重建 PDF 截图时保留已有 DeepSeek/人工元数据和全部校对版本。图表搜索提高简短名称与文中解释权重，卡片不再用“见原文”等空泛占位语填充缺失字段。
- 新增 35 篇固定语料的可断点重提取器：使用 DOI/题目解析论文，在 DeepSeek 调用前拦截占位或损坏 PDF，对已有数据只去重追加待校对候选，逐篇写入可恢复 JSON 状态。正式质量运行使用 4 页分块，保留方法、结果、实验类型专项、覆盖缺口和独立证据核验全部层级。
- 按用户调整后的范围完成首批 10 篇内容有效 PDF 全文重提取：10 篇成功、失败 0；另有 1 篇下载占位 PDF 被跳过且不计入 10 篇。十篇回归审计为 1,313 个独立事实、1,609 处数值证据、91 个图表，六列、搜索和原文定位均通过。
- 全库健康结果为 3,168 条原始六列记录、2,588 条可报告数值来源、2,129 个独立物理事实、476 条定性结论、580 条隔离旧文字值和 243 个图表文件；SQLite 完整性、外键、索引和运行产物检查全部通过。
- 搜索的短英文模糊阈值收紧，避免 `less/than/half` 等普通词宽松匹配大量无关结果；中文元素名与符号（如“钨”/`W`）继续保持等价检索。
- 发布 `2026.07.14-stable.4`；对应 Git 标签和 bundle 为 `evidence-demo-2026-07-14-visual-evidence-stable-4` 与 `auto-research-20260714-visual-evidence-stable-4-full.bundle`。

## 2026-07-13

### 分层校准核验集

- 新增人工核验批次策略 `calibration`：不再只连续选取相似的高风险数据，而是用确定性覆盖算法从待审核记录中选择证据来源、原文位置、数值形态、候选角色、物理类别、页面和核验优先级尽可能不同的样本。
- 校准标签只用于提高前几批人工反馈的代表性，不写入六列数据，不改变审核状态，也不把启发式候选角色冒充成已确认的 measured/derived/calculated/qualitative 结论。
- 校对页面新增“下载分层校准集”，命令行新增 `evidence-review-batch <文章> --strategy calibration`；原有按核验风险排序的待审核清单和全部待审核下载继续保留。
- 真实目标论文生成的首批 20 条校准集覆盖正文、表格和图片关联，5 种数值形态、4 种候选角色、6 类物理语义及 PDF 第 2–9 页；原有 114 条数据及其 2 条已审核状态未发生变化。

### 条目、表格、图片三模式检索

- 全库搜索页新增 `条目搜索`、`表格搜索`、`图片搜索` 三种模式；本地编辑端与只读分享端继续共用同一套前端、SQLite 数据库和图片资源，不产生第二份展示数据。
- 数据库结构升级到版本 8，新增可视证据对象及其与六列数据的关系表。每个对象保存图/表编号、标题、PDF 页码、裁剪坐标、原 PDF 指纹、检索标签、物理量、变量、材料、条件、方法和原文语境。
- 目标论文从最终出版 PDF 第 2–9 页建立 4 个完整表格和 10 个图片对象，均保存为三倍分辨率的原文裁剪图；原有 114 条六列数据、2 条已审核和 112 条待审核未被改写。
- 条目搜索会把同一原表格关联的数据组成一个结果组，默认展示前三条，其余数据可展开；这是纯展示折叠，完整结果数、导出内容、审核状态和单条身份保持不变。
- 表格与图片详情采用左侧大幅原文截图、右侧证据信息的弹窗布局，并可双向跳转：从条目打开原表/原图，也可从图表查看相关数据条目。
- 表格来源、正文来源、正文结合图片和仅图片证据被明确区分；系统不通过 OCR 重画原表，也不从曲线像素猜测精确数值。
- 新增 `evidence-index-visuals` 命令及只读可访问的图表搜索、元数据和图片接口；数据库健康检查同步验证结构版本、索引和全部图片文件。
- 浏览器实测确认 Table 3 可按“纳米硬度”检索、完整截图正常加载、12 条相关数据默认显示 3 条并折叠其余 9 条；Figure 8 可按“位错环密度 随剂量”排到首位并正常展示原图和关联数据。

### 同值表格身份修复与第 26 次非覆盖扫描

- 覆盖缺口清单新增材料/环境解释和短原文片段，并明确“数值相同但材料、元素、表格行列或名义/实测角色不同”仍是不同数据；避免多个 `19.8 at%` 或名义值等于实测值时被误判为已覆盖。
- 第 26 次 DeepSeek 非覆盖扫描成功完成：357 条原始候选、205 条核验候选、19 条拒绝、133 条重复；单运行覆盖 105/114（92.1%），其中材料成分覆盖 37/38（97.4%）。整轮候选一致率 51.2%，因此没有直接取代较稳定的第 23 次主运行。
- 集成预览新增语义型 `composition_table` 选择器，并支持按补充运行分别指定焦点。最终组合为：运行 23 作主运行；运行 25 只取 `coverage_gap_audit + qualitative_results`；运行 26 只取 `composition_table`。
- 最佳按运行聚焦集成为 196 条候选，覆盖 110/114（96.5%）；材料成分、辐照条件、初始组织、硬度、计算/热力学参数和显微观察均达到 100%。剩余 4 条为 3 mm TEM 圆片、±10 灰度容差、30 µm 压痕间距和图标签中的完整剂量节点。
- 覆盖缺口提示新增通用原文数量锚点清单，可从 PDF 逐行捕获数字、误差、单位、英文数字和倍数表达。真实 PDF 检查已捕获 `Three-mm`、`±10 in grey scale` 和 `Indent spacing was 30 mm`；其中最后一项的 PDF 文字层把微米符号解析为 `mm`，必须保留原文并由人工确认，不能静默改写。

### 长运行锁恢复与 DeepSeek 网络故障分类

- 第 27 次验证在第 4 分块进度写入时触发 SQLite `locking protocol`；数据库完整性、外键和目标论文 114 条校对数据均无损，运行记录已明确标为失败且没有完成产物。
- SQLite 连接新增 30 秒忙等待；AI 运行创建、进度、完成和失败状态写入新增有界指数退避。回滚失败不再覆盖原始异常，避免长扫描与网页/健康查询并发时遗留虚假的 `running` 状态。
- 第 28 次验证遇到本机 DNS 无法解析 `api.deepseek.com`，重试后安全标记失败；锁保护正常生效，没有再次出现数据库写入异常。
- 新增 `DeepSeekUnavailableError`：DNS、超时、429 和 5xx 不再被误认为“输出太密需要拆页”，只有模型 JSON 格式问题才触发逐页/逐焦点降级，防止服务离线时放大请求次数。
- 运行 27/28 作为失败审计保留，不纳入集成结果；本轮所有成功报告仍为非覆盖预览，六列表保持 114 条、已审核 2 条、待审核 112 条。

## 2026-07-12

### 项目维护与运行时加固

- DeepSeek 客户端现在会对一次网络超时、连接错误、HTTP 429 或服务端 5xx 执行一次安全重试；连续失败后只返回经过清理的错误信息，不输出响应正文或项目密钥。
- `DEEPSEEK_TIMEOUT_SECONDS` 增加容错与边界限制：无效值回退到 180 秒，有效范围限制为 10–1800 秒，避免环境文件误填导致网页状态接口无法启动。
- 只读分享服务启动时完全禁用文献索引，不再触发任何可能写入 SQLite 的 PDF 扫描；实测启动只读服务并访问模式、全库搜索和原文证据接口后，数据库 SHA-256 保持不变。
- ngrok 密钥改为通过子进程环境变量传递，不再放入命令行参数；项目自检同步验证脚本中不存在 `--authtoken`，降低密钥出现在系统进程列表中的风险。
- 数据库健康检查新增 SQLite 完整性、超过 6 小时未结束的 AI 运行、已完成运行的 JSON 产物缺失检查；历史失败运行仍保留为审计记录，不被误判为活动故障。
- 网页将运行摘要明确标为“最近一次 DeepSeek 运行”，并提示预览结果不会自动替代校对表，避免把波动较大的最新预览误解为已保存数据。
- 维护验收：114 项自动测试全部通过；Python 编译、前端语法、全部 macOS 启动脚本语法、依赖一致性、Git 对象完整性和差异格式检查均通过。真实数据库共 1314 条当前记录，无外键异常、重复稳定键、过期运行或缺失运行产物；目标论文保持 114 条数据、2 条已审核、112 条待审核，原文高亮覆盖 114/114。

### DeepSeek 自动抽取可重复基准

- 新增 `evidence-benchmark <文章 DOI/题目> --run-id <运行编号>`：直接读取已保存的 DeepSeek 产物和当前六列基线，不再次调用模型，也不修改校对数据。
- 将旧的“同页、同数值、同单位即可命中”替换为确定性一对一匹配；重复值必须再由原文片段、表图定位、具体意义或实验对象提供身份线索，降低同页不同实验条件被错误合并的风险。
- 报告同时给出严格匹配、部分匹配、候选一致率、基准覆盖率、证据门通过率、各类数据覆盖及两侧待人工判断清单；未完成人工审核前明确标记为临时基准，不表述为科学准确率。
- 为目标论文 DeepSeek 第 8 次运行生成可离线回放的 JSON 与中文 Markdown 报告：原产物 125 条候选经当前证据门排除 4 条聚合候选，再合并 20 条重复表述，得到 101 条当前规则候选；其中一对一匹配 79 条（严格 58、部分 21），候选一致率由原始比较的 64.0% 提升到 78.2%，114 条基线覆盖率为 69.3%。硬度和计算/热力学参数均覆盖 100%，显微观察与趋势匹配 2/6，下一轮需补强观察起始剂量、饱和阈值和定性尺寸抽取。
- 修复 PDF 正文与参考文献混排页误跳过问题：目标论文第 9 页前半的硬化模型、SRIM 损伤深度和压痕条件会继续进入抽取，只有从页首即以参考文献为主的页面才跳过。
- 抽取提示新增“复合定性结论必须拆成一结论一行”，并明确提取观察起始/饱和阈值、定性尺寸词和存在/缺失结论；本地证据门会拒绝把“高密度位错环、未见空洞”合并成一条。
- 新增回归测试，覆盖标量与二维尺寸不可误配、定性结论极性、同页重复温度按原文身份分配、中英文重复合并、不同元素同值不误合并、正文/参考文献混排页保留，以及从真实运行产物生成报告的完整流程。

### 目标论文第 22 次非覆盖重扫与精度收敛

- 使用项目专用 DeepSeek 配置对目标 DOI 执行第 22 次 `preview` 重扫；处理修复后的 PDF 第 1–9 页，第 10 页纯参考文献跳过，原有 114 条校对数据未被覆盖。
- 原运行生成 662 条原始候选，经当时证据核验和去重得到 315 条预览候选，基线覆盖率达到 78.1%，并覆盖全部辐照条件、初始组织、硬度、计算/热力学参数以及 6/6 条显微观察与趋势；同时候选一致率仅 28.2%，证明额外抽取通道带来明显重复和字段拆分噪声。
- 按本轮收紧后的确定性规则离线回放：315 条候选中证据门保留 282 条、去重后 183 条，一对一匹配 88 条，候选一致率提高到 48.1%，基线覆盖率保持 77.2%。未匹配项仍可能是真实新增数据，不能直接视为错误。
- 实验类型现在保留完整诊断列表，但只有达到相对分数门槛或题目直接命中的类型进入抽取重点；多个有效类型合并为一个定向通道。目标论文抽取通道由 9 个收敛为 3 个，排除低分的腐蚀、磁性、光谱和热学偶然词，减少 API 调用和重复候选。
- 证据门新增三项字段完整性约束：误差必须与中心值同条保存；原文给出 `中心值±误差` 时不得只保留中心值；表格 `名义值（实测值）` 必须拆成两条物理意义明确的数据。
- 去重新增材料范围兼容、变量赋值规范化和单位上标等价处理；允许同一全局条件在“全部材料/未明确材料”表述间合并，但仍禁止合并不同具体材料、不同元素或不同实验类型的数据。

### 三通道第 23 次确认扫描

- 使用收敛后的三个互补通道再次执行目标论文非覆盖扫描：总耗时约 9 分钟，原始候选由第 22 次的 662 降至 310，最终预览候选由 315 降至 147；拒绝 4 条、合并重复 159 条，原有 114 条校对数据保持不变。
- 运行内一对一匹配 98 条，候选一致率 66.7%，基线覆盖率 86.0%；当前离线证据门进一步排除被截断的二维像素标量后，保留 146 条候选、匹配 99 条，候选一致率 67.8%、基线覆盖率 86.8%。
- 辐照条件、初始组织、硬度和计算/热力学参数均达到 100% 基线覆盖，材料成分达到 89.5%，显微观察与趋势为 4/6。尚未覆盖项集中在少数样品制备参数、剂量节点列表、名义/实测成分同值歧义以及有序化/无析出物定性结论。
- 候选新增 `extraction_pass` 与 `extraction_focus`，便于量化每个通道的新增覆盖和重复贡献。
- 新增第四个“覆盖缺口复查”步骤：前三遍结束后把已抽候选清单与原文一起交给 DeepSeek，只寻找遗漏的几何尺寸、时间、间距/数量、完整向量、阈值和定性存在/缺失结论。该步骤失败时记录待办，不使已有有效候选整轮失败。
- 证据门新增二维/多维数值截断检查，并区分 `6.3×10^15` 科学计数法与 `0.2×0.2` 二维尺寸；定性值末尾重复写出的 `nanometers/nm` 会在字段拆分和基准比对中正确处理。

### 第 24 次缺口通道验证与聚焦集成预览

- 第 24 次非覆盖运行启用覆盖缺口复查，总计 232 条原始候选、109 条最终预览、13 条拒绝、110 条重复；单次基线覆盖率仅 60.5%，低于第 23 次，证明 DeepSeek 单次运行存在显著波动，不能自动以“最新运行”替代“最佳运行”。
- 缺口通道自身保留 4 条候选，其中补回 `20 s` 电抛时间和每试样至少 10 个压痕等第 23 次遗漏，因此该通道有增量价值，但不应连同第 24 次全部低覆盖候选盲目合并。
- 新增 `evidence-ensemble-preview`：保留指定最佳主运行，只从补充运行中选取 `coverage_gap_audit` 候选，再重新执行本地证据门、去重和一对一基准比对；不调用 DeepSeek、不修改六列表。
- 目标论文集成预览采用主运行 23 + 运行 24 的 4 条缺口候选，共 149 条候选、匹配 101 条，候选一致率 67.8%、基线覆盖率 88.6%，较主运行 23 增加 2 条覆盖而仅增加 3 条净候选。
- 基准匹配从分数贪心改为最大覆盖的一对一增广路径算法，避免灵活候选抢占唯一基准后造成虚假漏报；匹配仍按证据分数确定性排序。

### 第 25 次逐表格缺口审计与精简集成

- 覆盖缺口提示改为逐句、逐表格行列审计，明确关注英文数字/连字符写法、最小/最大条件、容差、二维数值、剂量序列、倍数关系，以及 `Nominal (Measured)` 相同数值和 `n.m./bal.` 单元格；这些规则不包含目标基线的具体答案。
- 第 25 次 DeepSeek 非覆盖扫描处理目标 PDF 第 1–9 页，共 279 条原始候选、141 条最终预览候选、18 条拒绝、120 条重复；单次覆盖率 83.3%、候选一致率 67.4%，没有自动替代第 23 次主运行，也没有写入六列表。
- 第 25 次缺口通道新增 6 条候选，补回二维像素尺寸、至少 5 小时干燥、每试样至少 10 个压痕和 20 秒抛光时间。主运行 23 + 缺口通道集成后为 151 条候选、覆盖 104/114（91.2%）、候选一致率 68.9%。
- 基准比较新增英文数字与阿拉伯数字等价识别；论文原始值 `five times` 保持不变，但可与开发基线 `~5` 正确比较，不再把真实倍数关系误报为漏抽。
- `evidence-ensemble-preview` 支持重复指定安全焦点别名。加入缺口通道与结果通道中的定性证据后，最终精简集成为 158 条候选、覆盖 106/114（93.0%）、候选一致率 67.1%，显微观察与趋势达到 6/6；相比加入整个结果通道少 10 条候选而覆盖不变。
- 当前仍未覆盖 8 条，集中在 3 mm TEM 圆片、灰度容差、压痕间距、完整剂量节点和 4 个名义/实测成分表单元。它们已作为下一轮结构化表格抽取重点，不通过放宽证据门或猜测补齐。
- 整轮实验保留运行 25 原始产物、单运行报告和多种聚焦集成报告；数据库仍为目标论文 114 条、已审核 2 条、待审核 112 条，集成报告明确记录 `database_rows_changed=0`。

## 2026-07-11

### 只读证据链与校对表格密度优化

- 明确只读分享版不是第二套网页：本地编辑版与只读版共用同一份 HTML、JavaScript、CSS 和 SQLite 数据库；只读服务器仅通过后端权限和前端显示模式收敛为“数据搜索”。
- 移除网页服务器启动时的旧 demo 填充调用；今后打开本地版或只读版不会更新论文时间戳、重新写入目标文章或触发 DeepSeek。启动服务、读取 `/api/ui-mode` 再停止后，数据库 SHA-256 前后完全一致。
- 修复只读搜索结果的“原文证据”按钮：不再预先调用只对编辑版开放的单条详情接口，而是直接携带当前搜索结果打开公开的证据定位接口。
- 只读浏览器实测通过完整证据链：高亮句子放大图、整页高亮图和原文 PDF 地址均正常生成；页面控制台无错误。
- 校对表格数据字号提升至 14 px，文本框根据内容自动增高并限制最大高度，减少题目和实验条件被截断的情况。
- 操作区由窄列纵向堆叠改为三列紧凑布局；实测首条重点核验数据的操作区高度约 96 px，整行约 113 px，避免按钮把数据行撑出大面积留白。
- 自检新增 `readonly_source_direct` 与 `dense_review_rows` 契约，防止后续重新引入只读端私有接口依赖或低密度按钮布局。
- 验证：前端语法与 Python 语法检查通过，54 个证据工作流测试全部通过；目标论文自检和数据库健康检查均通过，仍保持 114 条数据、2 条已审核、112 条待审核。

## 2026-07-10

### 可逆的“存在歧义 / 不采用”核验决定

- 自动抽取行新增“存在歧义”和“不采用”两类正式核验决定，提交时必须选择原因；决定以新版本追加，原始抽取、证据页码和历史记录均不被覆盖。
- 两类决定均可恢复为“待审核”，恢复同样只追加版本，不删除历史，便于后续重新判断。
- 普通搜索、CSV 和 Excel 导出会排除当前被标记为“存在歧义”或“不采用”的候选；尚未人工确认的自动候选仍可搜索，但结果会明确显示“待审核”。
- 人工学习通道新增负样本：不采用项用于约束 DeepSeek 避免同类候选，歧义项用于提示模型在关系不明确时进入待办而不是猜测。
- 校对进度卡新增歧义和不采用计数，文章状态、学习报告和核验队列在决定或恢复后同步刷新。
- 数据库结构版本升级为 7，迁移保留全部既有版本；历史遗留的孤立版本进入只读隔离表，不再破坏活动数据外键完整性，也不会进入检索结果。新增接口、前端决定弹窗和回归测试覆盖排除、恢复与负样本提示。
- 实际数据库迁移后目标文章仍为 114 条、已确认 1 条、已修正 1 条；33 条孤立历史版本已隔离保存，活动库外键检查为零异常。
- 浏览器实测确认原因弹窗、取消不写入、六项进度统计和搜索结果核验状态正常，控制台无错误；54 个证据工作流测试与项目自检全部通过。

### 本地编辑工作台快捷入口

- 新增 `/Users/USER/Zotero/打开本地编辑工作台.command`，双击后启动或打开 `http://127.0.0.1:8765` 本地可编辑工作台。
- 更新 `scripts/start_evidence_ui.command` 输出文案，明确该入口是本地可编辑模式；公网分享应使用只读链接入口。

### ngrok 免费账号公网分享路径

- 新增 `scripts/start_readonly_ngrok.command`：双击或终端运行后，会启动 `8766` 只读网页服务并通过 ngrok 输出可发给导师的 HTTPS 链接。
- 新增便捷入口 `/Users/USER/Zotero/创建导师公网链接.command`，以后可直接双击创建公网只读链接。
- 只读分享版收敛为“搜索专用应用”：前端隐藏校对、上传、人工补录、修正历史和文章切换；后端只开放搜索、导出、原文证据和静态资源接口。
- 脚本只读取 `NGROK_AUTHTOKEN` 环境变量或本机 `.env.ngrok` 文件；当前支持 `/Users/USER/Zotero/.env.ngrok` 作为默认便捷配置位置，避免把 ngrok token 写入版本库。
- 若已有 ngrok 正在服务同一个只读端口，脚本会直接显示现有公网链接，避免重复启动隧道。
- 脚本在启动 ngrok 前会检查 `/api/ui-mode` 必须返回 `read_only: true`，防止误把可编辑工作台暴露到公网。
- README 与 AGENT 文档改为推荐 ngrok 免费账号路线；Cloudflare/localtunnel 保留为备用但不再作为首选。
- GitHub Pages 已记录为后续“静态只读快照”方案：适合长期展示和下载，但不能运行 DeepSeek、PDF 上传或 SQLite 写入。
- 自检新增 `public_readonly_ngrok_share`，后续若脚本误指向编辑端口或移除只读检查，会在验收中失败。

### 只读标识与数据库健康检查

- 页面顶部只读徽标统一显示为“只读模式”，不再出现“导师只读模式”等角色化命名。
- 本地编辑模式接口返回 `read_only=false`，前端会隐藏只读徽标；只读分享模式接口返回 `read_only=true`，前端才显示该徽标。
- 六列当前数据查询改为通过 `v_current_six_column_data` 视图读取，并新增面向文章和版本表的索引，降低后续全库检索和校对页读取的耦合。
- 新增 `evidence-db-health` 命令，用于检查当前视图、索引、六列必填字段、稳定键重复和人工核验进度。

### 文章处理状态总览

- `/api/papers` 现在直接返回每篇文章的六列工作流状态：未扫描、已扫描无入库数据、待审核和已完成。
- 校对页文章下拉框会显示论文题目及处理状态，例如“待审核 112/114”，帮助后续多篇文章轮流抽取和人工核验。
- 当前文章摘要条新增“处理状态”字段，文章选择区新增全库状态总览，便于判断下一篇应该扫描、补抽取还是继续审核。
- 自检网页契约新增 `paper_status_overview`，确保后续 UI 修改不会丢失文章队列状态提示。

### 核验后即时刷新学习反馈

- 新增前端 `refreshReviewFeedback()`：确认、修正或人工补录后，会重新读取文章队列状态、当前文章学习报告、全库学习报告、证据审计和抽取状态。
- 校对页的“待审核 n/m”、学习样本摘要、当前文章状态和修正历史会在每次保存后即时更新，便于边审核边观察系统学习到了什么。
- 自检网页契约新增 `review_feedback_refresh`，防止后续改动破坏“人工核验 → 学习反馈 → 下一轮抽取提示”的闭环。

### 证据质量优先核验队列

- 对目标文章 114 条自动数据增加只读的核验优先级，不改变固定六列内容，也不把“优先核验”解释为“数据已判错”。
- 排序依据仅使用可审计信号：原文高亮强弱、是否依赖图/曲线或趋势、是否为近似/范围/定性表述，以及是否含计算或推导说明。
- 校对页默认按核验优先级排序，新增“只看重点项”筛选和行内优先级标识；当前目标文章识别出 5 条“优先核验”、5 条“重点核验”和 104 条“常规核验”，其中 10 条重点项尚待人工审核。
- “下载待审核清单”和“下载全部待审核”改为使用相同优先级顺序，并在每条记录中写明排序原因，便于先处理最需要物理判断的数据。
- 浏览器实测确认本地编辑模式不再显示“只读模式”徽标；文章选择区仍只在校对页出现，10 条重点项筛选与原文高亮入口均可用。
- 验证：前端语法检查、Python 编译检查和 52 个证据工作流单元测试全部通过。

### 面向研究者的前端体验优化

- 将页面视觉方向统一为“材料实验室数据核验工作台”：冷灰蓝承载结构，束流橙仅用于证据、当前步骤和待判断项，减少无功能装饰。
- 页面主标题会随当前功能切换为“校对实验数据、搜索实验数据、导入实验文献、补录遗漏数据、查看修正记录”，并提供一行对应操作说明。
- 将文章选择区压缩为题目下拉框和“切换文章”主操作；低频的自动提取、DeepSeek 补抽取、导出、实验类型和诊断状态收进默认折叠的“提取、导出与文章状态”。
- 新增“专注校对”模式：隐藏侧栏、页头和文章工具，只保留筛选、六列编辑表与右侧不可变原始版本；按 Escape 或页面按钮可退出，模式切换不写数据库。
- 当前筛选结果会自动选中第一条并展示右侧原始版本，避免首屏出现空白证据区；该选择不等于确认或保存。
- 桌面浏览器 1280×720 实测中，数据表顶部由约 1175 px 提前到约 581 px；专注模式下有效校对高度约 604 px。
- 390 px 窄屏下取消原文面板固定浮层，改为排列在编辑表下方；页面无横向整体溢出。
- 浏览器复测覆盖文章工具展开、首条数据自动选中、搜索页标题切换、专注模式进入/按钮退出/Escape 退出，未发现控制台错误；目标文章自检通过，52 个证据工作流单元测试全部通过。

### 人工校对学习提示预览

- 新增学习报告模块，将人工确认、修正和人工补录样本汇总为 DeepSeek 后续抽取可使用的学习提示。
- DeepSeek 抽取器与网页预览共用同一份提示生成逻辑，避免“页面看到的学习内容”和“模型实际收到的提示”不一致。
- 修正历史页新增学习报告卡片，显示当前文章/全库样本数、进入提示的样本数、安全边界，以及将加入下一次抽取的提示片段。
- 新增 `/api/current-paper/learning-report`、`/api/learning-report` 及 Markdown 下载接口；这些接口只读，不调用 DeepSeek，也不修改数据库。
- 学习提示明确声明：样本只用于字段边界和中文表述偏好，不能作为当前论文数据证据。

### 只读公网分享模式

- 新增 `evidence-serve --read-only` 启动模式：使用同一套网页和同一个 evidence 数据库，但后端会拒绝所有 POST 写入请求。
- 只读模式默认进入“搜索数据”页面，隐藏上传文献、人工补录、切换文章、自动提取/核验、DeepSeek 抽取和生成快照等写入入口。
- 顶部新增“只读模式”标识，避免误以为这是可编辑工作台；本地编辑模式不显示该标识。
- README 和 AGENT 文档补充推荐公网化路径：本机只读端口 `8766` + Cloudflare Quick Tunnel 临时链接。
- 当前网络下 Cloudflare Tunnel 边缘连接被阻断，已记录 `localtunnel` 作为零配置备用路径：`npx --yes localtunnel --port 8766 --local-host 127.0.0.1`。
- 搜索页每条结果新增“原文证据”按钮，可直接打开高亮后的 PDF 证据；可编辑模式下仍保留“去校对”按钮。
- 自检网页契约新增 `readonly_mode` 和 `search_source_evidence_button`，确保后续 UI 修改不会破坏只读浏览动线。

### 人工核验按原文页码排序

- 校对页新增“显示顺序”选择器：抽取顺序、原文页码升序、原文页码降序、未审核优先并按页码。
- 排序仅影响前端显示，不改数据库、不改变原始抽取版本，也不触发 DeepSeek。
- 这个入口用于目标文章 114 条数据的人工核验：研究者可以按 PDF 页码顺序核对 Methods/Results 中的上下文。
- 自检网页契约新增 `review_source_sort`，防止后续 UI 调整时丢失按原文顺序核验的能力。

### 原始目标审计报告

- 新增命令 `evidence-goal-audit <文章 DOI/题目>`，把用户原始目标拆成逐条可验证要求。
- 报告区分 `ready_for_human_review` 和 `goal_complete`：当前目标文章已可开始人工核验，但由于仍有未审核自动抽取数据，最终自动化目标不能标记完成。
- 已生成目标文章审计报告：`data/evidence/goal_audits/2_irradiation-effects-in-high-entropy-alloys-and-316h-stainless-steel-at-3_goal_audit.md`。

### 网页端待审核清单下载

- 校对页新增“下载待审核清单”入口，直接下载当前文章下一批未审核数据的 Markdown 清单。
- 校对页新增“下载全部待审核”入口，按当前剩余未审核数量导出完整 Markdown 清单，便于一次性离线核对目标文章全部候选数据。
- 新增只读接口 `/api/current-paper/review-batch.md?paper_id=<id>&limit=20`；该接口不调用 DeepSeek、不修改数据库、不生成额外行。
- 批次清单生成逻辑拆分为 `review_batch_payload`，命令行留档和网页下载共用同一份内容。

### 校对页审核进度卡片

- 校对页新增显眼的人工核验进度卡片，显示已审核比例、待审核数、已确认、已修正和人工补录数。
- 进度卡片提示下一步操作：“下一条未审核”或“下载待审核清单”，帮助目标文章 114 条数据持续分批核验。
- 自检网页契约新增 `review_progress_card`，防止后续 UI 调整时丢失人工核验入口。

### 行内原文证据按钮

- 校对表每条自动抽取数据新增“原文证据”按钮，可直接打开对应 PDF 高亮句子和整页位置。
- 人工补录行没有自动原文定位，该按钮自动禁用，避免误解。
- 自检网页契约新增 `row_source_button`，确保行内核验证据入口不被后续 UI 修改遗漏。

## 2026-07-09

### 本地网页一键启动

- 新增 macOS 双击启动脚本 `scripts/start_evidence_ui.command`。
- 若 `http://127.0.0.1:8765` 已有服务运行，脚本只打开浏览器；否则会启动 `evidence-serve` 并自动打开浏览器。
- README 已补充双击启动和终端启动两种方式，降低后续人工核验时的操作门槛。

### 实验类型识别通用化

- 新增本地只读实验类型识别模块：在调用 DeepSeek 前，先根据论文题目和本地 PDF 前几页文本识别主要实验类型。
- 当前支持的实验类型包括：辐照/离子束/中子实验、力学性能实验、显微结构/成分表征、热学/热分析实验、电学/输运实验、光谱/能谱实验、电化学/腐蚀实验、材料制备/工艺实验和磁学实验。
- DeepSeek 抽取提示词从“辐照材料论文”改为“材料、物理、化学或工程论文中的科学实验数据”，并会根据识别结果追加对应实验类型的抽取重点。
- 自检命令新增 `experiment_type_detection` 检查；验收摘要现在要求“文章标识 → 真实 PDF → 实验类型识别 → 六列数据 → 原文高亮定位”全部通过。
- 新增命令 `evidence-classify-experiment <文章 DOI/题目>`，用于不调用 DeepSeek 的实验类型诊断。
- 校对页新增“实验类型识别”卡片，直接展示主实验类型、置信度和次级类型，避免系统能力只藏在后台。
- 备用抽取包 `evidence-prompt` 已从“只抽辐照实验”改为“按识别到的实验类型抽取科学实验数据”，并写入 `experiment_profile` 与 `extraction_foci`。
- 验证结果：目标文章被识别为“辐照/离子束/中子实验”，同时保留力学性能、显微表征和制备工艺等次级类型；非辐照热学实验测试样例被识别为“热学/热分析实验”。

### 全库搜索导出范围修正

- 修正搜索页空关键词导出范围：`/api/six-export.csv` 和 `/api/six-export.xlsx` 现在与空关键词搜索保持一致，导出整个六列数据库，而不是当前校对文章。
- 增加回归测试，确保空搜索和空导出都能覆盖其它已登记文章的数据。

### 人工核验进度可见性

- 新增六列数据审核进度统计：总数、已审核、待审核、已确认、已修正、人工补录和审核比例。
- 自检报告、文章工作流结果和网页 API 均可返回该进度；校对页面行数提示显示确认/修正/人工补录明细，方便逐篇推进人工核验。
- 增加回归测试，覆盖确认、修正、人工补录和未审核行的统计关系。

### 未审核数据逐条导航

- 校对页面新增“下一条未审核”按钮，用于在目标文章的未审核自动抽取行之间逐条跳转。
- 若当前筛选条件没有显示未审核行，按钮会切换到“只看未审核”；若存在未确认编辑，会先要求确认，避免误丢临时修改。
- 自检网页契约新增 `next_unreviewed_queue` 检查，确保后续改 UI 时不会破坏人工核验动线。

### 连续审核操作

- 每条数据新增“确认并下一条”操作：保存当前行的确认/修正版本后，自动跳转到下一条未审核数据。
- 普通“确认当前内容”仍保留，只保存当前行；连续审核按钮用于目标文章剩余数据的批量人工核验。
- 自检网页契约新增 `confirm_and_next` 检查，确保确认保存与未审核队列导航同时存在。

### 校对页快捷键

- 校对页面新增键盘快捷键提示和处理逻辑：`Ctrl/⌘+Enter` 确认当前行，`Shift+Ctrl/⌘+Enter` 确认并下一条，`Alt+N` 跳到下一条未审核，`Alt+S` 打开当前选中行的原文高亮证据。
- 快捷键只在“校对数据”页面生效，便于连续审核目标文章数据而不影响搜索、上传或人工补录页面。
- 自检网页契约新增 `review_keyboard_shortcuts` 检查。

### 目标文章核验交接摘要

- 新增命令 `evidence-review-handoff <文章 DOI/题目>`，基于只读自检结果生成 Markdown 核验交接摘要，不调用 DeepSeek、不修改数据库。
- 交接摘要包含目标文章、PDF 路径、六列数据数量、原文定位覆盖、审核进度、推荐核验流程、快捷键、学习样本导出位置和复查命令。
- 已为目标文章生成交接文档：`data/evidence/review_handoffs/2_irradiation-effects-in-high-entropy-alloys-and-316h-stainless-st.md`。

### 待审核批次清单

- 新增命令 `evidence-review-batch <文章 DOI/题目> --limit N`，导出下一批未审核自动抽取数据的 Markdown 核验清单。
- 清单包含每条数据的 `item_id`、六列内容、证据页码/定位、原文片段和本地证据接口，便于按 20 条一组推进人工核验。
- 已为目标文章生成首批 20 条清单：`data/evidence/review_batches/2_irradiation-effects-in-high-entropy-alloys-and-316h-stainless-st_next20.md`。

### 用户界面与检索优化

- 将文章选择、当前文章信息和自动抽取操作限定在“校对数据”页面显示，避免在搜索、上传、人工补录和历史页面误触。
- 将 DeepSeek 操作按钮改为更明确的语义：
  - `自动提取/核验`：执行当前文章的自动流程。
  - `DeepSeek 补充抽取（不覆盖）`：对已有数据的文章只生成候选补充结果，不覆盖已校对表。
- 增加 DeepSeek/自动处理阶段式进度条，显示 PDF 读取、分块抽取、证据核验、结果生成等阶段。
- 将“保存当前文章数据快照”改为“生成当前文章 CSV 备份”，明确它是项目目录内的备份/导出动作，不是正常保存数据的必要步骤。
- 增加当前文章 CSV/Excel 下载入口；搜索页增加 CSV/Excel 两种格式导出。
- 精简页面提示语，删除或改写容易造成理解负担的口语化说明。
- 前端显示逐步减少对 Zotero 本地代码的依赖，优先使用题目、DOI、作者和年份。

### 搜索能力

- 搜索继续默认覆盖整个六列数据库，不依赖当前选择文章。
- 搜索权重保持“具体意义”高于“数据在文中的解释”。
- 新增常用元素中文名、元素符号、英文名的同义扩展，例如 `钨`、`W`、`tungsten` 可互相命中。
- 将第一作者和通讯作者纳入搜索字段。

### 数据与安全边界

- 新增 `papers.corresponding_author` 字段；旧数据库通过迁移自动补列。
- 不修改 Zotero 原始数据库或 Zotero PDF 文件。
- DeepSeek API key 仍只允许来自环境变量或项目专用 macOS Keychain，不写入 Git 文件。
- 网页、工作流和命令行统一重复扫描保护：已有六列数据或已完成 DeepSeek 运行的文章，默认不会再次调用 DeepSeek；确需重扫时必须在网页确认或在命令行显式加 `--force-rescan`。

### 验证要求

- 前端脚本需通过 `node --check`。
- Python 模块需通过语法检查。
- 单元测试需覆盖全库搜索、作者搜索、元素名/符号搜索和导出能力。
- 提交后继续创建 Git tag 和 bundle 备份。

### 数据状态 checkpoint

- 保存当前本地 evidence 数据库状态：34 篇文章、905 条六列数据、19 次 AI 抽取运行。
- 目标文章 `Irradiation effects in high entropy alloys and 316H stainless steel at 300 °C` 已补入第一作者和通讯作者：Wei-Ying Chen。
- 保存新增 DeepSeek 运行记录 `paper_001_run_0018.json` 与 `paper_001_run_0019.json`，以及当前文章 CSV 备份 `HRXA6ESH_2026-07-09T041113.636590Z0000.csv`。
- 本 checkpoint 前已扫描运行产物，未发现真实 API key。

### 文章选择器优化

- 命令行和内部工作流的文章选择器支持 DOI、完整题目、唯一题目片段、paper id，以及旧的本地/Zotero key。
- 题目匹配会进行简单标准化；例如用户输入 `300 C` 可匹配数据库题目中的 `300 °C`。
- 若题目片段匹配多篇文章，系统会拒绝自动选择并提示改用 DOI 或完整题目。
- README/AGENT 文档中的推荐操作方式改为 DOI/题目优先，逐步降低对设备相关 Zotero key 的依赖。

### 人工补录优化

- “人工补录”页面新增独立文章选择器，可按论文题目/年份/DOI/已有数据量选择所属文章。
- 人工补录保存时只写入所选文章，不切换“校对数据”页面的当前文章，也不会触发 DeepSeek。
- 保存后自动刷新全库学习样本与文章数据量；如果补录的是当前校对文章，会同步刷新当前表格和证据审计。
- 将后端状态文案中的“DeepSeek 补充抽取预览”统一为“DeepSeek 补充抽取（不覆盖）”，减少用户对预览/保存关系的误解。
- 验证：前端脚本语法检查通过；66 个 Python 单元测试全部通过；本地网页服务可读取 34 篇已登记文章。

### 自动化验收自检

- 新增只读命令 `evidence-self-check <paper-selector>`，用于验证“指定文章标识 → 已有六列数据 → 可证据定位 → 可搜索/导出 → 可进入学习通道”的完整状态。
- 自检命令不调用 DeepSeek、不切换当前文章、不保存快照、不修改数据库行；缺数据时返回失败并提示具体检查项。
- 本地目标文章验收结果：题目选择器可解析到 paper_id=2；本地 PDF 存在；已有 114 条六列数据；114/114 条自动数据可回到 PDF 高亮定位；109 条为句子/片段级强定位；`温度`、`硬度`、`Wei-Ying Chen` 搜索均可命中；CSV/Excel 生成检查通过。
- 自检新增 `web_ui_contract` 检查，覆盖左侧六列表编辑、右侧不可变原始版本、确认后保存、未确认修改提醒、人工补录、全库搜索、原文高亮入口，以及文章选择区仅在“校对数据”页面显示。
- 自检新增 `requirements` 用户需求级摘要，将底层检查映射到六个验收项：文章标识到抽取数据、六个必需字段、可编辑校对并保留原始、人工补录无伪原始、自由关键词搜索/导出、人工审核学习闭环。

## 2026-07-13 教师展示稳定版

### 展示前验收

- 完成 Python 全量测试：124 项全部通过。
- 完成数据库健康检查：SQLite 完整性和外键检查通过，无遗留运行中的抽取任务；全库 1,314 条六列数据可读。
- 完成重点文章自检：114 条数据全部可加载，114/114 可生成原文定位，109 条具有句子或片段级强定位；CSV 与 Excel 导出通过。
- 使用真实浏览器验收本地编辑端：文章选择、114 条校对数据、审核进度、原文证据高亮均正常，编辑端不显示“只读模式”。
- 使用真实浏览器验收只读端：仅显示全库搜索；搜索 `钨` 返回 100 条相关结果并匹配 `W`；原文证据高亮与片段正常加载。
- 通过 HTTP 验收只读端搜索结果导出：CSV 与 Excel 均返回正确文件类型和附件名称。

### 稳定性处理

- SQLite 连接增加等待超时和繁忙等待；抽取审计写入在短暂锁冲突时自动重试，避免锁错误掩盖原始异常。
- 将 DeepSeek 网络不可用与模型返回格式错误分开处理；网络中断时快速结束并记录失败，不再触发无意义的拆分重试。
- 失败的 run 27（SQLite locking protocol）与 run 28（DeepSeek DNS 故障）仅保留审计记录，未写入候选产物，也未改变重点文章 114 条已保存数据。
- 新增 `TEACHER_DEMO.md`，固定展示顺序、启动入口、当前数据量和审核边界。

### 搜索工作台与视觉优化

- 将全库搜索重构为“数据条目、原始表格、论文图片”三种明确的信息对象，三种模式继续共用同一数据库和同一套编辑端/只读端前端。
- 数据条目新增核验状态、证据来源和结果排序筛选；查询、筛选条件与 CSV/Excel 导出保持一致。
- 增加输入停顿后自动搜索、示例搜索、最近搜索、清除/重置、`/` 聚焦搜索框、加载骨架与无结果引导。
- 搜索结果重新建立“报告值 → 具体意义 → 实验条件与文章语境 → 原文证据 → 论文标识与操作”的阅读层次，并直接展示证据短句和关键词高亮。
- 同一原表的数据继续默认折叠；完整表格和论文图片仍以原文高分辨率截图展示，不从像素猜测曲线精确点。
- 视觉风格统一为深海军蓝、纸张白与束流橙，增加克制的科学索引装饰、悬浮反馈、粘性结果工具栏和窄屏适配。
- 为大结果集增加完整数量提示：页面最多渲染前 100 条以保持响应速度，CSV/Excel 导出全部匹配记录，并在界面明确说明。
- 真实浏览器验证桌面端、390 px 手机端和只读端均无前端错误；只读端仅显示搜索入口，原文证据接口和高亮图片可正常访问。
- 服务端对浏览器切页或关闭造成的正常断连静默处理，不再将 `BrokenPipeError` 误记为应用故障。
- 完成 130 项 Python 单元测试，全部通过。

### 网页内分层校准核验

- 将原有“下载分层校准集”扩展为可直接在校对页面启动的 20 条代表性样本模式，同时保留 Markdown 下载入口。
- 校准样本由本地确定性算法从待审核数据中选择，覆盖正文、表格、图片关联、不同数值形态、候选角色、物理类别和 PDF 页面；这些维度只用于抽样，不替代人工判断。
- 校准模式只显示本轮样本，“确认并下一条”和 `Alt+N` 均停留在本轮范围内；进度卡实时显示剩余数量。
- 退出校准模式不会写入未确认修改，也不会修改六列原始版本；浏览器只读操作验证数据库文件哈希保持不变。
- 新增本地 JSON 接口 `/api/current-paper/review-batch` 供页面读取样本 ID；只读分享端继续拒绝该接口。

### 五篇真实 PDF 测试集

- 将五篇固定验证论文写入 `config/evidence_test_set_5.json`，避免每次测试随机抽到不同文章；五篇均解析到已登记论文和真实本地 PDF。
- 新增 `evidence-test-set-audit`，统一核验 PDF 可打开性、文件指纹、六列完整性、原文高亮、实验类型、文章内检索和 20 条分层校准样本。
- 本轮审计通过：5/5 篇 PDF 指纹匹配，合计 478 条六列数据，478/478 条可生成原文高亮，共生成 100 条校准样本。
- 人类可读与 JSON 报告保存在 `data/evidence/test_sets/five-paper-v1_audit.md` 和 `five-paper-v1_audit.json`。

### 文章快速导航

- 将“切换文章”改为显式的文章导航器：支持题目、第一作者、通讯作者和 DOI 搜索，并可按研究对象、实验方法、处理状态缩小范围。
- 预设导航标签包括聚变堆材料、高熵/中熵合金、钨与难熔合金、辐照实验、辐照模拟/计算、显微/缺陷表征、力学性能和氢同位素行为；标签允许一篇论文同时属于多个范围。
- 增加“全部文章 / 五篇测试集 v1”快捷范围、实时结果数量、清除筛选和当前候选论文的作者/标签摘要。
- 筛选只改变候选列表，不切换当前文章、不读取新数据、不调用 DeepSeek；仍需用户显式点击“切换并读取”。作者元数据缺失时明确提示，不从文件名猜测。
- 对实验/模拟边界增加回归：`simulated fusion environment` 的重离子物理实验仍归为“辐照实验”，只有 Phy-X/SRIM 等明确计算方法才归入“辐照模拟/计算”。
- 完成 134 项 Python 单元测试，数据库健康检查和重点论文自检均通过；编辑端保持 34 篇论文、1,314 条六列数据，只读端继续禁止访问文章列表和编辑接口。

### 目标论文校准批次续审

- 根据端到端目标审计，当前系统功能要求均已满足，目标论文仍有 112/114 条候选等待用户人工核验；这一步保留为真实人工反馈，不由程序代替确认。
- 分层校准批次现在按文章保存在本机浏览器中。暂离校准、刷新页面或切换文章后，可继续相同的 20 个 item_id，不会重新抽样并混入下一批。
- 本地保存内容仅包含 paper_id、item_id 和保存时间，不包含六列正文、PDF 内容、API key 或其他凭据；续审不会自动确认、修改或写入数据库。
- 完成批次后自动清除本地续审标记。实际浏览器验证了“开始 20 条 → 暂离 → 刷新 → 继续同一批 20 条”，数据库人工审核进度保持 2/114 不变。

### 人工审核误操作恢复

- 将“恢复待审核”从不采用/存在歧义扩展到已确认和已修正记录，恢复前显示明确确认提示。
- 恢复操作继续采用追加版本：保留自动抽取原始版本、确认/修正后的字段和全部历史；当前状态回到待审核，并暂时退出学习样本，直到用户再次审核。
- 修复通过“审核状态”下拉框离开分层校准时页面内存清空批次的问题；按钮暂离、状态筛选、刷新和切换文章现在均保留同一批 item_id。
- 临时数据库测试覆盖确认与修正两条路径：恢复后版本号递增，修正字段仍保留，原始字段不变，学习样本撤回，审核进度回到待审核。
- 真实浏览器只读式验收：已确认和已修正记录各显示一个“恢复待审核”入口；未点击入口，目标论文仍为 2/114 已审核，保存的 20 条校准批次仍可继续。

### 可选核验备注与学习理由

- 在右侧不可变原始版本下增加“核验备注”，用于记录为什么确认或怎样修正；该备注不增加第七列，不进入科学数据搜索。
- 备注在点击确认前只存在于当前页面，并纳入未确认修改保护；确认后写入该版本的 `edit_note`，与原始抽取和六列当前值分别保存。
- 将确认/修正样本的 `edit_note` 作为有长度限制的 `reviewer_rationale` 加入 DeepSeek 学习提示，同时继续禁止把历史样本的数值、材料或结论当作当前 PDF 的证据。
- 临时数据库测试证明备注同时进入版本历史、学习样本和提示预览；真实页面测试证明输入时标记未确认、键盘清空后恢复干净，未点击确认时数据库审核进度保持 2/114。

### 五篇测试集图表、文章导航与校对性能收口

- 将五篇固定测试论文的图表索引补齐为 54 个原文视觉资产：48 张图片、6 个表格；5/5 篇均有可用截图。所有资产由本地真实 PDF 通过 `pdf_layout` 版面定位生成高分辨率截图，不修改 Zotero PDF。
- 明确模型边界：五篇论文的候选语义抽取记录均来自 DeepSeek；当前 DeepSeek 适配器只提交 PDF 文本，不提交图片像素。图表截图由本地程序生成，曲线点不会被自动猜测。
- 五篇测试集验收报告新增逐篇图/表数量、截图生成方式和 `visual_assets_ready` 检查；当前合计 478 条候选、478/478 条原文定位、100 条分层校准样本，整体验收通过。
- “切换文章”区域增加题目/作者/DOI 总搜索、独立一作/通讯作者筛选、处理状态、大类标签和快捷范围。大类包括聚变堆、辐照实验、辐照模拟、高熵/中熵合金、钨与难熔合金、显微/缺陷表征和力学性能；另提供全部文章、五篇测试集、最近访问。
- 文章大类为非排他导航标签，一篇论文可同时命中多个范围。筛选只改变候选列表；不会切换文章、重新扫描、写数据库或调用 DeepSeek。下拉列表按测试集和主要研究类型分组，并显示题目、作者、年份、DOI 与待审核数量。
- 六列 `具体数值` 建立可检索数值约束：必须包含数值，或是 `bal.`、`n/a`、破折号等明确表格标记。纯文字的材料名、方法名、设施名和趋势描述不再出现在数据条目搜索中，也不能作为新候选导入或直接确认；历史记录不删除，保留在校对页供人工改为数值或标记“不采用”。
- 将原文证据审计从逐条确认的同步刷新中移除，改为文章加载后后台核验。确认、修正、歧义、不采用和恢复待审核不再等待约 3 秒的全篇证据重算。
- 大文章校对表首屏最多渲染 80 条，每次“显示更多”再增加 80 条；下一条未审核和分层校准仍可自动定位目标。真实浏览器验证 409 条文章首屏为 80/409，展开为 160/409，刷新后恢复 80/409。
- 真实浏览器验证：`辐照实验` 筛出 32/34 篇；作者 `El-Atwani` 精确筛出 1/34 篇；筛选前后当前文章不变。全量 137 项单元测试、数据库健康检查、目标论文自检和五篇测试集审计均通过。

### 文章集合预设与非数值记录隔离修正

- 修复文章筛选条件意外叠加：此前“五篇测试集”会继续叠加作者、处理状态和研究方向，可能显示 0 篇；“全部”又仅代表全部研究方向，容易与“全部文章”混淆。
- 将“全部文章 / 五篇测试集 / 最近访问”改为文章集合预设。点击任一集合会清除题目、作者、研究方向和处理状态筛选；真实页面验证五篇测试集固定显示 5/34，全部文章固定显示 34/34。
- 将研究方向中的“全部”改名为“全部方向”，并在文章集合旁明确标注“点击后清除其他筛选”，避免用户误解筛选层级。
- 建立统一的可报告数值规则：数值、范围、不等式、科学计数法、合金成分式和明确表格标记可保留；纯文字以及把数值埋在叙述句中的内容不再属于六列当前实验数据。
- 搜索、校对、分层校准、当前文章导出、文章数据计数、证据审计和五篇测试审计统一使用 `list_reportable_current_data`。原始版本仍完整保存在 SQLite 和修正历史中，不删除证据。
- 全库原始 1,314 条记录中，当前可报告数据为 1,132 条，182 条历史文字/叙述记录被隔离。当前文章 paper 29 从原始 409 条收敛为 366 条；示例 `less than half of the lattice swelling in pure W` 不再出现在校对、校准或搜索中。
- 五篇固定测试集按统一规则重新审计：当前可报告数据 411 条、原文定位 411/411、图表资产 54 个、分层校准 100 条，整体仍通过。
- 完成 138 项 Python 全量测试、数据库健康检查、目标论文自检和真实浏览器回归，全部通过。

## 2026-07-14

### 物理事实聚类与定性结论分流

- 在不修改任何原始数据行的前提下，新增可逆的“物理事实”视图：只在论文、数值/单位、物理量、材料、实验条件和语义角色兼容时聚合重复提及；不同元素、不同剂量/温度、辐照前后和名义/实测值仍保持独立。
- 全库 1,132 条可报告数值来源记录整理为 937 个独立物理事实，合并 195 条重复提及。聚类使用更保守的完全链接规则，避免 A 与 B、B 与 C 相似时错误将条件不兼容的 A 与 C 合并。每个事实保留成员 item_id、所有原文页码/定位/短句和多证据入口；数据库原始 1,314 条记录及版本历史未删除。
- 校对表、分层校准、人工进度、文章导航计数、全库搜索、CSV/Excel 导出和五篇测试集统一使用独立事实口径。对一个事实的确认或决策会为其每条来源追加版本，可随时恢复待审核。
- 搜索页扩展为四种证据对象：数据条目、原始表格、论文图片和实验结论。纯文字的科学观察、趋势、比较和未观察结果进入独立的实验结论搜索；方法、仪器、设施、材料名和单独条件不会冒充结论。当前收录 120 个独立定性结论。
- DeepSeek 抽取合约现在显式分开 `data`、`findings` 和 `pending_tasks`；数值候选与定性结论分别通过原文证据门禁，仍以待人工确认状态入库。页面进度和最近运行摘要会分别显示数值与定性结果。
- 五篇真实 PDF 测试集按独立事实重新验收：386 个物理事实、411 处数值证据全部可定位、54 个原文图表、100 条分层校准样本，5/5 通过。
- 完成 146 项 Python 全量测试、SQLite 完整性/外键/索引检查、目标论文自检和真实浏览器回归，全部通过。浏览器已确认 34/34 文章导航、重复事实/多证据标识、四种搜索模式及定性结论的 PDF 句子级高亮均正常。

### 阶段稳定版 2026.07.14-stable.1

- 新增明确的稳定版身份与 `STABLE_RELEASE.md`，集中记录数据快照、已验收工作流、启动/恢复方式和“程序稳定不等于科学结论已人工确认”的边界。
- 将编辑端启动分成核心数据与辅助信息两层：上传历史、处理队列、DeepSeek 状态、实验类型或学习报告中任意一项暂时失败时，论文选择、已保存数据和校对表仍可读取，顶部显示正式降级提示。
- 实验结论检索补齐 CSV 和 Excel 导出，导出与当前关键词同步，并保留聚类成员与全部原文证据位置。
- 编辑端与只读端的所有响应统一增加内容类型防嗅探、无来源引用、同源嵌入和禁用摄像头/麦克风/定位等基础安全头，不影响 PDF 和原文图像查看。
- 完成 148 项 Python 全量测试、全库健康检查、目标论文自检、五篇真实 PDF 审计、编辑端真实浏览器验收和只读端路由验收，全部通过；稳定化期间未调用 DeepSeek，也未写入科学数据。

### 图表校对稳定版 2026.07.14-stable.2

- 校对页新增“数据条目 / 原始表格 / 论文图片”三类校对对象。表格和图片以当前文章为范围，直接展示高分辨率原文截图、原始图注、页码及可检索元数据；数据条目右侧可直接跳到关联图表。
- 新增追加式 `visual_asset_reviews` 版本表。图表可确认、修正、标记存在歧义、不采用或恢复待审核；只修改物理量、变量、材料、条件、方法、解释和标签等检索元数据，原截图、原图注和历史决定均保留。
- DeepSeek 自动处理开始前先执行本地图表建档，因此模型网络错误或后续保存失败时，已生成截图仍可进入校对。进度与完成摘要分别报告表格和图片数量；当前 DeepSeek 仍只读取 PDF 文字层，不读取图像像素，也不猜测曲线点。
- 图表检测扩展到“整页栅格图 + OCR 文字层”的老论文：当 PDF 没有单独图片对象时，使用图注及所在栏目的页面区域生成待核验截图。实际上传的 Andersen 1980 论文已恢复 107 条数值记录并建立 Figure 2–6 共 5 幅高分辨率截图，11 条数据与图建立关联；该论文没有原始表格，表格计数为 0。
- 修复上传页面与数据库规则冲突：DOI 继续为可选字段。无 DOI 论文以题目和已验证 PDF 指纹追溯，数值记录、定性结论、校对和健康检查不再因 DOI 为空整批失败。
- DeepSeek 处理队列现在同步记录运行中、完成或失败状态；单条定性结论保存错误只进入拒绝清单，不再使已核验数值和图表整批失败。历史失败仍保留用于审计。
- 数据库结构升级到 v9；完成 151 项 Python 全量测试、前端语法、Python 编译和本地接口回归。

### 35 篇全库回归稳定版 2026.07.14-stable.3

- 将固定测试集从 5 篇扩展为当前登记的全部 35 篇，配置位于 `config/evidence_test_set_35.json`。34 篇通过 DOI、1 篇无 DOI 老论文通过完整题目解析；测试集合不依赖跨设备不稳定的 Zotero key。
- 校对页“文章集合”同步改为“35 篇全库测试集”，接口与真实页面均返回 35/35 个唯一 paper_id。测试集配置、命令行审计、网页选择器和静态契约使用同一来源，避免集合数量漂移。
- 全库审计区分文献/PDF 完整性、结构化数据、原文定位、数据搜索和图表证据，不再用单一“通过/失败”掩盖缺失阶段。没有数据的论文明确标记为 `pending_extraction`，不会生成占位数据。
- 当前真实结果：10 篇已有结构化数据，全部 10 篇通过六列完整性、证据高亮和 DOI/题目逐篇搜索；20 篇真实 PDF 尚待 DeepSeek 抽取；5 篇关联文件实际是下载跳转或人机验证占位页，必须先重新关联真实 PDF。
- 修复 PDF 指纹误报：主论文表中的历史指纹可与 `documents` 表正式登记指纹共同核验。新增内容门禁，即使文件可被 PDF 阅读器打开，只要正文是 `Preparing to download`、`HHS Vulnerability Disclosure` 或 `Gauging your humanity` 等占位内容，也不会被认作真实论文。
- 将本地图表证据从 70 个扩展至 223 个，现为 37 个表格、186 幅图片，覆盖 30/35 篇。图表检测补充矢量图页的图注区域截图；仍不调用 DeepSeek 读取像素，不自动猜测曲线点。
- 完成 152 项 Python 全量测试、前端语法、Python 编译、SQLite 完整性/外键/图表文件检查、目标论文 110/110 证据定位、全库文章接口和“钨/W”别名搜索回归。


## 2026-09-19 · #28 核心发布链路目录状态

发现原子发布及审核成功后，文献目录仍消费旧六列人工确认状态；定性/表图单独存在时可误报未扫描。新增只读提取状态投影，以 completed 质量记录与全部 manual_review 候选为依据；不读写搜索索引，不改变科研记录或旧人工核验含义。生产界面优先显示明确的提取状态，旧数据保留兼容回退。

真实合成 PDF 和已验证结果包串联发布、审核批准/拒绝、索引故障、同请求重试、重开数据库与发布幂等；定性/表格单独存在及真实服务 DTO 到生产 JavaScript 渲染均覆盖。目标 17 项通过；统一检查共享 1417、Mac 369 项通过，另补真实受保护 HTTP 审核→目录→检索及重复提交回归。未调用模型、未改变安装 App；核心/交付验收先于 #26/#27 优化。


## 2026-09-19 · #30 更新入口工作区保护

修复安全更新脚本固定读取开发目录数据库的问题，复用 App 工作区选择与只读校验；SQLite 在线备份使用权限 0600 的唯一暂存文件，完整性检查/哈希后无覆盖登记，失败只移除本次暂存。无效选择不回退到另一库，首次构建不初始化科研库。

统一检查共享 1417 项、Mac 374 项通过。17 项工作区/更新目标测试通过，包括真实 SQLite WAL、重复备份、故障清理、来源未变化、默认目录和无效选择。更新指南与入口区分候选构建、安装及完整数据恢复归档；没有操作生产备份或已安装 App。


## 2026-09-19 · #32 取消与本机发布恢复边界

真实 PDF/SQLite 故障回归复现：原子发布已提交、checkpoint.complete 失败后取消任务，重开恢复被变成 cancelled，丢失完成收据入口。复用既有 finalizing 阶段作为持久边界：写入失败不发布；进入后只允许恢复同一幂等本机提交，迟到取消不撤销已保存事实；之前的取消仍停止发布。收费执行与零模型恢复共用边界，任务目录明确提示本机发布，避免重启收费；旧密文格式保留，历史已取消任务不自动重写。

74 项目标测试通过，覆盖真实数据库重开、迟到取消、完成收据故障、边界写入失败、模型调用计数不增加、先取消零发布和未知调用结果保护。用户再次确认 #33 图表缺失仍未解决，已登记为核心阻断；最新额度保留线为 30%。未调用收费模型，未修改生产数据或安装版。

- Issue #32 完整统一检查：共享 1421 项、Mac 374 项（245 subtests）通过；仍仅为源码验收，未更新安装版。


## 2026-09-20 — #33 恢复 AI 图表自动核验（第一部分）

用户重新明确产品不允许人工审核功能；AGENT 原有规定也已将 manual_review 定义为自动隔离而非必需队列。纠正此前实现方向。原子提取链曾在截图保存后硬编码人工审核，漏接既有两路语义核验。

现从同一不可变 PDF 发现图表，把两路图注/邻文语义请求纳入冻结任务与额度，使用既有对抗比较及第三路复核；通过者在同一事务内绑定来源几何、保存截图/中文语义、发布并更新检索。无来源匹配、无有效语义或未过阈值不能发布。图片像素不交给文本模型解读，不产生曲线点。新 planner v3 为图表预留最多四次、32000 token 调用上限，仍由准备动作显示总预算与收据核算；未执行收费测试。

真实合成 PDF 覆盖自动通过、第三路通过、未通过隔离三条上传→核验→发布→图表检索→数据库重开路径。统一检查：共享 1423，Mac 374（245 subtests）通过。历史 SQLite manual_review 枚举与旧收据计数字段为格式兼容暂保留，新领域结果明确为 ai_unresolved。

此为源码第一部分；人工 UI/API 删除、既有文章不重提正文的图表补全、冻结候选与实际文章验收尚未完成，#33 保持打开。


## 2026-09-20 — #33 移除产品人工审核（第二部分）

删除文献人工队列、候选批准/拒绝/纠正、图表结构人工粘贴与确认控制器，以及旧审核标签页恢复。保留图表只读展示和已有核验结构导出。生产 HTTP 在鉴权之后统一将新旧审核入口返回 410；旧接口、过期标签页和注入的旧服务都不能恢复人工决定流程。启动与构建验收契约改为确认人工入口不存在，目录及收据展示 AI 自动结果。历史 SQLite 状态和既有科学版本没有重写。

移除与退役功能对应的界面验收，保留跨标签迟到响应、目录刷新、来源隔离及导出测试；增加旧接口无法写库的真实受保护 HTTP 回归。共享全套 1418 通过；Mac 旧契约失败已定位为要求旧人工入口与允许审核写入，按退役规则更新并做目标验证。最终全套结果以该 PR 的 GitHub 检查为准。尚未更新 build55，#33 继续开放。
